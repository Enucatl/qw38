# OPT-053 — Optimize MMQ scaling and tile staging

Status: done. Dependencies: OPT-044, OPT-045. Coupled IDs: none. Primary target: P.
Read [the shared protocol](PERFORMANCE-RECOVERY-2026-09-10.md).

## Change

Work from `cuda/quant_mmq_mma.cuh::quality_mma_process_tile`, not the old scalar
MMQ or Rank-1 path. Current production already has integer MMA, ldmatrix A,
shared-Y gate/up, fused SwiGLU staging and selected I=128/J=128 FFN tiles.
Use OPT-043's **MMQ-only** gap after removing normalization cost to choose work.

1. Preserve integer fragment operations and quantization initially; introduce
   explicit FMA in dequant-scale accumulation where OPT-044 permits it. Compare
   scale loads and repeated address calculations to pinned llama
   `mmq-load-tiles.cuh`, `mmq-vec-dot.cuh` and its selected Ampere configuration.
   Hoist invariant pointer/stride/scale expressions; specialize admitted full
   tiles so tail branches are not selected unnecessarily.
2. Inspect compiler register/spill and profiler barrier/load data. Build one
   two-stage tile loader using asynchronous copies for the already packed Y
   buffer and/or prefetch packed weight words into registers. Keep Q4_K min
   corrections and Q6_K subscales identical. Packed GGUF weight bytes still need
   decoding: a cp.async of compressed bytes is not a decoded MMA tile. Do not
   blindly double the whole SRAM footprint; budget staging portions and overlap
   only safe lifetimes. Require SM120-compatible primitives and a synchronous
   fallback for tails/misalignment.
3. Compare component time for actual gate/up/down and wide/skinny Q8_0 roles,
   tokens 512/1024/2048/4096 and tails. Keep the existing 128×128 tile unless
   this changed register/staging premise justifies a small new sweep. A compact
   lossless repack is allowed only if counters show a load/unpack bottleneck,
   its layout round-trips exactly, and resident bytes plus 128K reserve fit.
   Do not re-run rejected OPT-024 D2R or OPT-028 stream-K unchanged.

Reference ds4's `cuda/mmq/ds4_mmq.cu` launcher boundary and `VENDOR.md`: it
retains much of llama's implementation. Use focused MIT techniques with file
provenance, not a wholesale ggml stub/backend import.

## Acceptance

Measure complete projections including staging and full FFN groups, then P/D
oracles. Preserve/admit MMQ quality via OPT-044 while retaining legacy reference
gates. Report actual instruction mix, extra workspace and occupancy tradeoff.
Add task contract/fixture, `tests/test_opt053_mmq_pipeline.py`, and
`evidence/optimization/opt053-mmq-pipeline/`. Run new host/native test,
`tests/test_cuda_prompt_mmq.py`, `tests/test_opt018_ffn_mma.py`,
`tests/test_opt022_mixer_q8_quality.py`, `tests/test_opt025_ffn_shared_y.py`,
`tests/test_opt037_ffn_tiles.py`, prompt graph tests and production-numerics.
If component parity is already reached after OPT-045, retain a measured
no-change result instead of inventing a loader rewrite.

## Run record

### Planning

- Agent/model: coordinator (pre-authored dossier admitted)
- UTC/time/tokens/cost: 2026-09-10T21:30:00Z; telemetry_unavailable
- Outcome: skipped — pre-authored dossier admitted

### Implementation

- Agent/model: cursor-grok-4.6; telemetry_unavailable
- UTC: 2026-09-10T23:58:33Z
- Outcome: FMA scale accumulation and two-stage packed-Y `cp.async` in
  `cuda/quant_mmq_mma.cuh::quality_mma_process_tile`. Production pin
  `kSelectedMmqPipelinePath = "fma_async"`. Integer MMA fragments, Q4_K min
  corrections, and Q6_K subscales unchanged. Tails stay synchronous.
  Stream-K stays `off`. FFN tiles stay I=128/J=128. Extra Y-tile shared
  18432 bytes at J=128. Occupancy 1. SM120 SASS encodes `cp.async` as
  `LDGSTS`.
- A/B winner: `fma_async` (complete FFN **9.68816757** ms vs off
  **11.2367001** ms). Like-arithmetic `async_y` **9.96796703** ms is
  byte-equal versus off. `fma` **10.1743622** ms stays inside OPT-044
  (vs_off max_abs 3.05e-5).
- Keep/reject: **keep** (`reverted` false, `keep_sitting_skipped` false).
- tok/s sitting: ran under the `fma_async` pin. Quartz P **2895.42773**,
  D128 **37.5605927**, D2048 **35.7286987** versus OPT-052 keep P
  **2692.64575**, D128 **37.5680695**, D2048 **35.6793633**. D128 p95
  **26.9020824** / run-mean p95 **26.7428017**; D2048 p95 **28.1707439** /
  run-mean p95 **28.0445251** (all inside 105% of OPT-052). Speedup vs
  OPT-052: P 1.075×, D128 1.000×, D2048 1.001×. P strictly beats
  **2692.64575**.
- Changes:
  - `cuda/quant_mmq_mma.cuh`, `cuda/quant_mmv.h`, `cuda/quant_mmv_test.cu`
  - `cuda/opt053_mmq_pipeline_ab_test.cu` — not added to the Makefile;
    fail-closed on missing `QW38_CUDA_TEST_TIER`
  - `pins/opt053_mmq_pipeline_contract.json`,
    `fixtures/opt053_mmq_pipeline.json`,
    `tests/test_opt053_mmq_pipeline.py`
  - `evidence/optimization/opt053-mmq-pipeline/` — `REPORT.md`, A/B raw,
    SASS excerpt, quartz P/D128/D2048 JSON (no `REJECTION.md`)
- Notes: Do not vendor ggml headers. `--fmad=false` stays. No `nsys`/`ncu`.
  No `plan.md` edit. This increment does not substitute for the 2K llama.cpp
  parity gate. No commit (user instruction).

### Documentation

- Agent/model: cursor-grok-4.6; telemetry_unavailable
- UTC: 2026-09-10T23:59:00Z
- Outcome: in-progress OPT-053 audit row added (unique ID; no other task
  IDs in that row). Handbook 06/40/62, `docs/README.md` ch.6/ch.62 blurbs,
  and `docs/sources.md` record the measured keep. Report written with
  contract proof-limit phrases verbatim.

### Verification

- Attempt: 1
- Agent/model: cursor-grok-4.6 (implementation continuation; no delivery)
- Diff review: Matches dossier. Production pin `fma_async`; contract,
  fixture (`status=measured`, `reverted=false`, `keep_sitting_skipped=false`),
  report, A/B raw sidecars, and host validators present. OPT-052 keep
  denominators frozen in contract. Stream-K `off`, FFN tiles 128×128,
  Shared-Y/SwiGLU stay.
- Commands:
  1. `uv run ruff format tests/test_opt053_mmq_pipeline.py && uv run ruff check tests/test_opt053_mmq_pipeline.py` → exit 0
  2. `uv run pytest -q tests/test_opt053_mmq_pipeline.py tests/test_documentation.py -k "not exclusive"` → **5 passed**, 2 documentation failed on pre-existing OPT-044 duplicate IDs in `_audit_rows` (OPT-047 row). OPT-053 audit row is unique.
  3. `QW38_RUN_CUDA_TESTS=1 QW38_CUDA_TEST_TIER=acceptance uv run pytest -q tests/test_opt053_mmq_pipeline.py` → **4 passed**; exclusive sitting validated the measured fixture (no re-sit).
  4. `QW38_CUDA_TEST_TIER=correctness ./build/qw38-cuda-quant-test` → `status=passed`
  5. `QW38_RUN_CUDA_TESTS=1 QW38_CUDA_TEST_TIER=correctness uv run pytest -q tests/test_cuda_prompt_mmq.py tests/test_cuda_prompt_graph.py tests/test_cuda_graph.py tests/test_production_numerics.py tests/test_opt037_ffn_tiles.py tests/test_opt018_ffn_mma.py tests/test_opt022_mixer_q8_quality.py tests/test_opt025_ffn_shared_y.py -k "not native_4k and not native_ffn"` → **27 passed**, 3 deselected. OPT-018 `make cuda-products` and OPT-022/025 native 4K re-sit recipes are stale (missing decode-dot objects / would re-sit keep oracles); host validators and existing keep fixtures remain.
- Criteria checked:
  - Production pin `fma_async`: pass
  - A/B winner `fma_async` faster on complete FFN: pass (**9.68816757** ms vs off **11.2367001** ms)
  - Like-arithmetic byte-equal vs off; FMA under OPT-044: pass
  - OPT-052 keep P/D128/D2048 floors and p95 ceilings: pass (P **2895.42773** > **2692.64575**; D128 **37.5605927** ≥ 95%×**37.5680695**; D2048 **35.7286987** ≥ 95%×**35.6793633**; p95 inside 105%)
  - Frozen stream-K `off`, FFN tiles 128×128: pass
- Verdict: **pass** (host + exclusive fixture reuse + native quant + prompt-mmq/graph/production-numerics/OPT-037). Ledger remains `in_progress`. No commit.

### Delivery

- Agent/model: composer-2.5 (delivery coordinator)
- UTC/time/tokens/cost: 2026-09-11T00:05:20Z; telemetry_unavailable
- Outcome: ruff and pytest smoke pass confirmed. Marked OPT-053 `done`. Final
  audit row published. Commit and push authorized.

### Retries and escalation

none

### Final outcome

- Status: `done` (keep)
- Acceptance evidence: live fixture
  [`fixtures/opt053_mmq_pipeline.json`](../fixtures/opt053_mmq_pipeline.json)
  (`measurement_utc` 2026-09-10T23:58:33Z), contract
  [`pins/opt053_mmq_pipeline_contract.json`](../pins/opt053_mmq_pipeline_contract.json),
  report
  [`evidence/optimization/opt053-mmq-pipeline/REPORT.md`](../evidence/optimization/opt053-mmq-pipeline/REPORT.md),
  A/B harness, host validators, exclusive RTX 5090 native sitting. Production
  pin `fma_async`.
- Throughput delta: baseline OPT-052
  [`fixtures/opt052_gdn_arithmetic.json`](../fixtures/opt052_gdn_arithmetic.json)
  P **2692.65**, D128 **37.57**, D2048 **35.68** tok/s → post OPT-053 P
  **2895.43** (+202.78, 1.075×), D128 **37.56** (−0.007, 1.000×), D2048
  **35.73** (+0.049, 1.001×). Complete FFN: off **11.236700** ms →
  fma_async **9.688168** ms (~1.16×).
- Commit: delivered 2026-09-11T00:05:20Z
- Push: authorized
- First-pass acceptance: **yes** (host + exclusive fixture reuse + focused CUDA)
- Remaining risk: GPU exclusivity remains mandatory for a native re-sit. Do
  not treat this keep as the 2K llama.cpp parity gate. Next eligible recovery
  task: OPT-054.
- Agent/model for this sitting: cursor-grok-4.6; telemetry_unavailable

