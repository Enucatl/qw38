# OPT-077 — Parallelize the one-token GDN state reduction

Status: done. Dependencies: OPT-070, OPT-071, OPT-073. Coupled IDs: none.

## Run record

### Planning (2026-09-11T16:45:00Z)

skipped — pre-authored dossier admitted (coordinator)
Priority: P1 after Q4 admission; target: 48 decode GDN cores/token.
Read [the post-069 protocol](PERFORMANCE-RECOVERY-POST-069.md).
Quality risk: high, recurrent arithmetic; state precision/layout stay FP32.
Instrumentation: captured committed state and complete GDN replay are added.

### Implementation (2026-09-11T18:24:04Z)

- Agent/model: implementation (Cursor Grok 4.6). No commit during this stage.
  Ledger left `in_progress`. `plan.md` unchanged. Production pin stays
  `sequential`. `claims_throughput: false`.
- Added one-token tiled recurrence beside `prepare_recurrence_window`.
  Convolution stays a separate launch. Q/K norms once per key head in shared
  memory. 2D key/value tile, four warps/CTA, shared tree reduction, one write
  per candidate cell. Layouts `tile16` and `tile32`. Grid
  `value_heads x ceil(value_width/value_tile)`. FP32 row-major state; no
  transpose, no prompt-path change.
- Native `build/qw38-cuda-opt077-gdn-decode-test`: W8/W32 × 1/4 updates,
  production 16×48×128, sequential vs tiles vs sampled FP64, grid proof,
  committed isolation, attrs/cuobjdump. seq_abs ~1e-9; nonfinite 0;
  local_bytes 0; occupancy 9; 56 regs.
- Feedback 1+3 rotating 48-layer complete GDN, capture
  `24f5204d4326c4b6dc8e8ac8e7a838ec264ef540f0dfa64b5073fd36c1343d46`:
  sequential 13.697 ms, tile16 13.161 ms, tile32 11.740 ms. Survivor `tile32`.
- Files changed: decode path/recurrence, gdn_step, scheduler capture,
  replay `decode-gdn`, probe `gdn-ab`, native test, Makefile, pins, tools,
  tests, fixture, evidence, this dossier.
- Commands:

```
uv run pytest -q tests/test_opt077_gdn_decode.py
```

**pass** — 9 passed in 0.04s (host); re-run after GPU sitting.

```
uv run python tools/run_optimization_task.py --task OPT-077 --mode feedback --phase gdn
```

**pass** — exit 0; `elapsed_s=21.863`; deadline 300s; product=24; three
configs × (1+3) rotating complete GDN plus one D2048+32 pair; survivor
`tile32`; run
`build/optimization-runs/OPT-077/feedback/20260911T182300Z-3848f1a3`.

```
uv run python tools/run_optimization_task.py --task OPT-077 --mode acceptance --phase quality-state
```

**pass** — exit 0; identity-cached OPT-073; run
`build/optimization-runs/OPT-077/acceptance/20260911T182325Z-f40ebdd4`.

```
uv run python tools/run_optimization_task.py --task OPT-077 --mode acceptance --phase gdn
```

**pass** — exit 0; `elapsed_s=31.165`; deadline 300s; product=26; sequential vs
tile32 3+10 rotating GDN plus five engine pairs; run
`build/optimization-runs/OPT-077/acceptance/20260911T182333Z-cfd548f5`.
Acceptance n=10: sequential 12.516 ms vs tile32 11.894 ms; paired CI
-0.164 .. 1.409 ms (not entirely positive; mean 0.623 ms). Engine five
D2048+32 pairs: 878.416 vs 866.012 ms; e2e non-regression true.
Verdict: **retain_sequential** / `uncertainty`. `production_kept=false`.
tok/s delta vs sequential baseline: **0**.

### Verification (2026-09-11T18:25:30Z)

- Agent/model: verification (Cursor Grok 4.6, same sitting as implementation
  per user implementation+verification request). `telemetry_unavailable`.
- Independent review: diff stays on OPT-077 decode GDN recurrence, tests,
  pins, fixture, evidence, Makefile, replay `--gdn-decode`, probe `gdn-ab`.
  Production `kSelectedGdnDecodePath` remains `sequential`. Prompt fused path
  unchanged. `plan.md` untouched.
- Traced acceptance claims to executed artifacts: native stdout sequential
  match ~1e-9 and finite checks; replay launch variants and 48 GDN layers;
  paired Student-t on ten rotating rounds with CI crossing zero; five engine
  pairs with recapture; OPT-073 identity-cached quality-v3. Fixture/report
  match those numbers.
- Commands re-checked: host pytest 9 passed; feedback/quality-state/gdn runner
  exits 0 as recorded above. `uv run ruff check` on the new Python files
  passed.
- Verdict: **PASS**. Measured retain_sequential is the admitted outcome
  (component interval not entirely positive). First-pass acceptance.

### Evidence spot-check

[`evidence/optimization/opt077-gdn-decode/REPORT.md`](evidence/optimization/opt077-gdn-decode/REPORT.md):
measured sitting; sequential vs tile16 vs tile32; survivor `tile32` with
mean complete GDN saving 0.623 ms but CI -0.164..1.409 ms; quality-v3
absolute fail, engine NR pass; verdict **retain_sequential** due to
`component_interval_not_positive`. tok/s delta **0** vs sequential baseline
(`claims_throughput=false`).

### Verdict

**PASS** — host contract, native numeric, complete rotating GDN with tiled
dispatch proof, identity-cached quality, and documented
**retain_sequential** decision meet OPT-077 acceptance. No production pin
change; no tok/s claim (`claims_throughput=false`).

### Delivery (2026-09-11T18:26:00Z)

Delivery authorized with this sitting. Marked OPT-077 `done` in ledger.
Coupled IDs: none.

### Final outcome (2026-09-11T18:26:00Z)

Screened sequential `prepare_recurrence_window` control and two value-tile
candidates (`tile16`, `tile32`) on repaired OPT-071 decode-GDN captures.
Native sequential/FP64 match ~1e-9; local_bytes 0. Complete GDN acceptance
n=10: sequential 12.516 ms vs tile32 11.894 ms (CI crosses zero). Engine
D2048+32 non-regression holds. Quality-v3 absolute fail, engine NR pass.
Verdict: **retain_sequential** / `uncertainty`; shipping stays `sequential`.
Host contract and 9 pytest cases passed; implementation feedback/acceptance
phases passed within 300 s. Coupled IDs: none. First-pass delivery.
Retries: 0. No production pin change.
**tok/s delta vs sequential baseline: 0** (`claims_throughput=false`).

## Evidence and implementation

1. Confirm OPT-071 still observes `launch_gdn_prepare_tiled` selecting
   `kSequentialWindows` for one token. Current recurrence launches 48 CTAs and
   each output lane loops twice over 128 key elements, with serial lane-0 Q/K
   norms. OPT-052's prompt preprocessing does not replace this decode path.
   Entry requires >=0.10 ms/token plausible saving from corrected core time;
   record no-go if dispatch or attribution disproves the opportunity.
2. Add one-token optimized recurrence beside `prepare_recurrence_window`.
   Keep convolution separate, precompute Q/K norms once per key head, and
   distribute the key reduction among threads while loading contiguous value
   columns of the existing row-major FP32 state. Use a 2D key/value tile with
   register state shards and warp/shared reduction; write each candidate cell
   once. Bound to two layouts: value tiles 16 and 32 columns, four warps/CTA.
   Grid is value_heads x ceil(value_width/value_tile), not 48 single CTAs.
3. Retain FP32 recurrence, decay semantics, grouped/tiled value-head mapping,
   gated output norm and public state layout. Pinned llama's warp-sharded
   `gated_delta_net_cuda` motivates parallelism, but its transposed state is
   not a drop-in layout. No persistent transpose, lower-precision state, scan
   rewrite or prompt-path change in this task. If conversion proves necessary,
   reject this bounded design and document the separate storage problem.
4. Replay captured log_decay/beta/convolution input, gate/norm and committed
   states at D128/D2048. Reset each candidate state from the same snapshot
   outside timing. Complete cost includes convolution, normalization, recurrence,
   gated output and any required preparation. Core-only speed is diagnostic.
5. Calibrate changed recurrent reduction against independently evaluated FP64
   sampled state/output and actual pinned llama GPU recurrence on the same
   inputs, following OPT-059's predeclared policy. Full finite state checks,
   restored same-path graph/eager equality and atomic rollback are mandatory.
   Run both quality spans and short/long recurrence before any install.

## Tests and commands

New target `build/qw38-cuda-opt077-gdn-decode-test`, tests
`tests/test_opt077_gdn_decode.py`; extend repaired component replay with one
explicit `decode-gdn` family. No generic stateful benchmark framework.
Tiny full references: key/value widths 8 and 32, one/four updates. Production
reference: 16 selected state/output coordinates x 4 updates per held-out
layer, full reduction width, with separately cached same-input GPU reference.
Feedback: two capture positions, control + two layouts, 1+3 complete rotating
rounds over 48 layer states. Quality and state tests are separate named phases.

```sh
uv run pytest -q tests/test_opt077_gdn_decode.py
uv run python tools/run_optimization_task.py --task OPT-077 --mode feedback --phase gdn
uv run python tools/run_optimization_task.py --task OPT-077 --mode acceptance --phase quality-state
uv run python tools/run_optimization_task.py --task OPT-077 --mode acceptance --phase gdn
```

Common 3+10 component pairs and five D2048+32 pairs decide keep. Stop on
recurrent drift, state/cancellation failure, resource spill loss or insufficient
complete saving. Do not relax recurrence <=0.02 or reinterpret the old prompt
fusion rejection as a decode measurement. Long release belongs to OPT-080.
