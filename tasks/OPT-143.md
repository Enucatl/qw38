# OPT-143 — Evaluate one production-matched short-decode attention optimization

Status: pending. Dependencies: OPT-135, OPT-141. Coupled IDs: none.
Priority: P1. Plan impact: none; existing release and quality gates retained.

## Outcome

Evaluate one change addressing the evidenced short-decode attn_core excess, especially D2048. The reported +47.51 ms is over twelve middle-window evals, not per eval. Treat low occupancy/memory latency as a hypothesis until authenticated production counters support it.

## Eligibility and candidate freeze

Read OPT-107/108/111/129/130/137 and OPT-139–141. Require matched positive family excess and a source-grounded mechanism for the production dispatch. Missing required evidence blocks the experiment. Measured absence of opportunity permits no_opportunity with evidence. Do not automatically lower OPT-137's MMA threshold or revive rejected OPT-130.

Freeze exactly one candidate, affected position range, source functions, expected savings estimate and disconfirming check before timing. Prefer existing attention implementations and launch organization where evidence supports reuse; otherwise make the smallest change at the responsible shared kernel. No layout/partition sweep. Candidate design must be concrete at execution planning, not inferred from warp_query counters belonging to a different path.

## Scope and acceptance

Limit production work to attention kernels/dispatch and necessary graph/workspace integration. Preserve canonical BF16 KV, checkpoint format, quality budgets and public behavior. Use the latest accepted parent, retaining OPT-137 long-context behavior.

Default target: D2048 decode_only; guards: its complete_request, D128/D8192/D32768 decode_only and complete_request, and P4096 prefill. If evidence instead supports only another short region, freeze the appropriate target and reciprocal guard before any samples. Use OPT-135 target_guard_v2 unchanged.

Screen complete attention across rotating real layers (one warmup, three paired rounds), charging preparation/conversion/reduction/output costs. Only a survivor runs actual OPT-058 candidate quality, affected state/graph/checkpoint and 128K reserve checks, followed by three warmups and ten AB/BA whole-engine pairs per required workload. Verify launch identities and all affected dispatch transitions, including 1024, 4096/4097 and 8192 when relevant. Changed arithmetic requires measured candidate NLL; no borrowed parent quality.

Deliver evidence/optimization/opt143-short-attention/REPORT.md, frozen contract, raw gates and keep/reject/inconclusive verdict. Retain parent on no-keep; incomplete evidence is blocked. Report per-workload shipping deltas separately from component speedups. OPT-142 is not required to measure a controlled improvement, but unresolved residual forbids a complete whole-wall attribution claim.

## Execution requirements

Read [TASK-TESTING-STRATEGY.md](TASK-TESTING-STRATEGY.md) and the dependency dossiers before implementation. Reuse existing runners, parsers and gates; add only necessary task registration and focused checks. Freeze an enumerated GPU matrix and per-child/aggregate deadlines before execution, use the shared GPU lock and reuse builds. Do not rerun until significant or weaken acceptance to fit a timeout. Missing hardware evidence is incomplete.

Record exact runnable commands, raw evidence paths, identity and independent verification in this dossier at execution. New artifact paths above are planned deliverables, not existing evidence. This task is drafted only; no implementation or measurement has run.

