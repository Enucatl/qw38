# OPT-143 — Evaluate one production-matched short-decode attention optimization

Status: done. Dependencies: OPT-135, OPT-141. Coupled IDs: none.
Priority: P1. Plan impact: none; existing release and quality gates retained.

## Outcome

Evaluate one change addressing the evidenced short-decode attn_core excess, especially D2048. The reported +47.51 ms is over twelve middle-window evals, not per eval. Treat low occupancy/memory latency as a hypothesis until authenticated production counters support it.

## Eligibility and candidate freeze

Read OPT-107/108/111/129/130/137 and OPT-139–141. Require matched positive family excess and a source-grounded mechanism for the production dispatch. Missing required evidence blocks the experiment. Measured absence of opportunity permits no_opportunity with evidence. Do not automatically lower OPT-137's MMA threshold or revive rejected OPT-130.

Freeze exactly one candidate, affected position range, source functions, expected savings estimate and disconfirming check before timing. Prefer existing attention implementations and launch organization where evidence supports reuse; otherwise make the smallest change at the responsible shared kernel. No layout/partition sweep. Candidate design must be concrete at execution planning, not inferred from warp_query counters belonging to a different path.

## Scope and acceptance

Limit production work to attention kernels/dispatch and necessary graph/workspace integration. Preserve canonical BF16 KV, checkpoint format, quality budgets and public behavior. Use the latest accepted parent, retaining OPT-137 long-context behavior.

Default target: D2048 decode_only; guards: its complete_request, D128/D8192/D32768 decode_only and complete_request, and P4096 prefill. If evidence instead supports only another short region, freeze the appropriate target and reciprocal guard before any samples. Use OPT-135 target_guard_v2 unchanged.

Screen complete attention across rotating real layers (one warmup, three paired rounds), charging preparation/conversion/reduction/output costs. Only a survivor runs actual OPT-058 candidate quality, affected state/graph/checkpoint and 128K reserve checks, followed by three warmups and ten AB/BA whole-engine pairs per required workload. Verify launch identities and all affected dispatch transitions, including 1024, 4096/4097 and 8192 when relevant. Changed arithmetic requires measured candidate NLL; no borrowed parent quality.

Deliver evidence/optimization/opt143-short-attention/REPORT.md, frozen contract, raw gates and keep/reject/inconclusive verdict. Retain parent on no-keep; incomplete evidence is blocked. Report per-workload shipping deltas separately from component speedups. OPT-142 is not required to measure a controlled improvement, but unresolved residual forbids a complete whole-wall attribution claim.

## Execution requirements

Read [TASK-TESTING-STRATEGY.md](TASK-TESTING-STRATEGY.md) and the dependency dossiers before implementation. Reuse existing runners, parsers and gates; add only necessary task registration and focused checks. Freeze an enumerated GPU matrix and per-child/aggregate deadlines before execution, use the shared GPU lock and reuse builds. Do not rerun until significant or weaken acceptance to fit a timeout. Missing hardware evidence is incomplete.

Record exact runnable commands, raw evidence paths, identity and independent verification in this dossier at execution. New artifact paths above are planned deliverables, not existing evidence. This task is drafted only; no implementation or measurement has run.

## Run record

### Planning (2026-09-15T00:35:00Z)
skipped — pre-authored dossier admitted (candidate frozen at implementation from OPT-139–141 evidence)

### Implementation (cursor-grok-4.6-high, 2026-09-15T01:02:20Z)

KEEP/REJECT: **no_opportunity** (measured). Production pins not flipped. Did not commit.
Parent retained: `post124_plus_opt127_decode_segments8_plus_opt137_mma` (`kSelectedOpt137DenseMma = true`, MMA threshold 8192, hybrid_crossover @1024, verified_max 4096, n_parts 16).
Shipping decode attention remains `dense_bf16_tile_f16_mma_decode_v1`. Shipping delta **0**. Candidate measured delta N/A. Quality N/A (no arithmetic change; OPT-058 not invoked; `candidate_nll_not_measured=false`).
`claims_throughput=false`.

Read OPT-139 identity/counters, OPT-141 matched llama comparisons, OPT-138 family-gaps/next-experiments, OPT-137 parent stack, OPT-135 `target_guard_v2`. Freeze ran against those artifacts; it did not invent a kernel.

**Freeze (measured, not pre-decided)**
- D2048 `attn_core` middle-window excess **+47.510 ms** (n=3, one-sided low 47.383 ms) over twelve evals, not per eval. D128 also significant (+4.297 ms).
- Production identity: D2048 `vec128_online` / `vec128_online_decode_attention`; D128 `warp_query`. Warp-query as D2048 evidence rejected (`kernel_selector_mismatch`).
- OPT-141 D2048 attn_core comparable (`flash_attn_ext_vec` vs `vec128_online`); typed slots admitted; `supported_mechanism=null` on both engines.
- Admission: `throughput_alone_cannot_establish_mechanism`. No named source/SASS observations. Occupancy 15.62% vs llama 10.8% and DRAM throughput 2.33% vs 29.8% are **not** a freeze.
- Forbidden: did not lower OPT-137 MMA threshold; did not revive rejected OPT-130 occupancy `n_parts=8`.
- Candidate: **null**. Verdict: **no_opportunity**. No CUDA production change.

Quality was wired in `pins/opt143_iteration_contract.json` (`require_candidate_nll=true`, `--phase quality` → OPT-058 `--quality --quality-config --q4-decode`) so a frozen arithmetic candidate would run NLL. Because freeze produced no candidate, quality short-circuited with `nll_required=false` rather than stubbing `candidate_nll_not_measured`.

Frozen GPU matrix: RTX sitting GPU, image `qw38-cuda:13.0.2`, GPU lock `build/optimization-runs/qw38-gpu.lock`, child 300s, long 1200s, aggregate 7200s ceiling. `make cuda-opt143-diagnostics` rc 0 under lock. No new native attention binary (no candidate).

**Files created**
- `tools/opt143_short_attention.py`
- `tests/test_opt143_short_attention.py`
- `pins/opt143_short_attention_contract.json`
- `pins/opt143_iteration_contract.json`
- `pins/opt143_short_attention_provenance.json`
- `fixtures/opt143_short_attention.json`
- `evidence/optimization/opt143-short-attention/REPORT.md`
- `evidence/optimization/opt143-short-attention/freeze.json`
- `evidence/optimization/opt143-short-attention/answers.json`

**Files modified**
- `Makefile` (`cuda-opt143-diagnostics` + PHONY; quality/checkpoint/memory-fit)
- `tools/run_optimization_task.py` (`QW38_OPT143_*` result prefixes)

```
uv run ruff format tools/opt143_short_attention.py tests/test_opt143_short_attention.py tools/run_optimization_task.py
uv run ruff check tools/opt143_short_attention.py tests/test_opt143_short_attention.py tools/run_optimization_task.py
```
outcome: formatted; All checks passed.

```
uv run pytest tests/test_opt143_short_attention.py tests/test_opt137_long_attention.py tests/test_opt139_counter_identity.py tests/test_opt141_matched_llama_counters.py -q
```
outcome: 41 passed.

```
uv run python tools/opt143_short_attention.py --phase preflight --mode acceptance --run-dir build/optimization-runs/opt143
```
outcome: `{"phase": "preflight", "ok": true, "verdict": "no_opportunity"}` at 2026-09-15T01:02:16Z. GPU available, make rc 0, production pins parent, freeze as above.

```
uv run python tools/opt143_short_attention.py --phase correctness --mode acceptance --run-dir build/optimization-runs/opt143
uv run python tools/opt143_short_attention.py --phase screen --mode acceptance --run-dir build/optimization-runs/opt143
uv run python tools/opt143_short_attention.py --phase quality --mode acceptance --run-dir build/optimization-runs/opt143
uv run python tools/opt143_short_attention.py --phase state-memory --mode acceptance --run-dir build/optimization-runs/opt143
uv run python tools/opt143_short_attention.py --phase performance --mode acceptance --run-dir build/optimization-runs/opt143
uv run python tools/opt143_short_attention.py --phase mechanism --mode acceptance --run-dir build/optimization-runs/opt143
uv run python tools/opt143_short_attention.py --phase report --mode acceptance --run-dir build/optimization-runs/opt143
```
outcome: all `ok: true`. correctness/screen/quality/state-memory/performance skipped with `no_candidate_no_arithmetic_change`. mechanism recorded null mechanism. report `verdict=no_opportunity`.

Raw gates: `build/optimization-runs/opt143/*.json` copied to `evidence/optimization/opt143-short-attention/raw/`. Report: `evidence/optimization/opt143-short-attention/REPORT.md`.

Blockers: none. Independent verification pending. No commit (user requested).

### Documentation (2026-09-15T01:05:00Z)

Reviewed implementation (`tools/opt143_short_attention.py`), pins
(`opt143_short_attention_contract.json`, `opt143_iteration_contract.json`,
`opt143_short_attention_provenance.json`), fixture
(`fixtures/opt143_short_attention.json`), freeze/report artifacts under
`evidence/optimization/opt143-short-attention/`, and raw phase sidecars in
`evidence/optimization/opt143-short-attention/raw/`.

**Pins/fixtures (consistent, unverified):**

- `pins/opt143_short_attention_contract.json`: parent
  `post124_plus_opt127_decode_segments8_plus_opt137_mma`; `candidate=null`;
  D2048 target `decode_only` under `target_guard_v2`; guards
  D128/D8192/D32768 decode plus P4096 prefill; `require_candidate_nll=true`
  with proof-limit clause that quality is required only when arithmetic changes.
- `pins/opt143_iteration_contract.json`: acceptance tier includes quality with
  OPT-058 baseline in `make_targets`; quality workload wired to
  `opt143_short_attention.py --phase quality`.
- `fixtures/opt143_short_attention.json`: verdict `no_opportunity`;
  `production_kept=false`; `candidate=null`; quality/state-memory/performance
  skipped with `no_candidate_no_arithmetic_change`; `nll_required=false`;
  `candidate_nll_not_measured=false`; `opt058_invoked=false`.

**REPORT.md finalized** at
`evidence/optimization/opt143-short-attention/REPORT.md`:

- Frozen candidate `None` with admission `throughput_alone_cannot_establish_mechanism`.
- OPT-138 family excess (+47.51 ms over twelve middle-window evals; D128 +4.30 ms).
- OPT-139 production identity (`vec128_online_decode_attention` at D2048;
  warp_query rejected as D2048 evidence).
- OPT-141 matched llama comparison (`flash_attn_ext_vec`; `supported_mechanism=null`).
- Quality short-circuit documented (`nll_not_required_no_arithmetic_change`).
- Performance evidence checklist with independent verification stamp.

**Verifier focus:**

1. Confirm measured `no_opportunity` is evidence-backed (no source/SASS mechanism;
   occupancy/DRAM alone cannot freeze).
2. Confirm OPT-137 MMA threshold not lowered and OPT-130 not revived.
3. Confirm quality gate: `candidate_nll_not_measured` must be `false` when NLL not
   required (no stub skip).
4. Confirm parent retained; shipping delta 0; no production kernel/selector change.
5. Confirm `claims_throughput=false` for the no-candidate path.

Did not commit.

### Verification (2026-09-15T01:05:00Z)

**Verdict: PASS**

Independent verification against dossier scope, ledger acceptance (`One
source-supported short-decode attention candidate yields verified keep or measured
no-keep under target_guard_v2`), and keep/reject quality gate. No semantic fixes
applied.

#### Gates

```
uv run pytest tests/test_opt143_short_attention.py tests/test_opt137_long_attention.py tests/test_opt139_counter_identity.py tests/test_opt141_matched_llama_counters.py -q
```

outcome: **41 passed** in 0.11s.

```
uv run ruff check tools/opt143_short_attention.py tests/test_opt143_short_attention.py tools/run_optimization_task.py
```

outcome: All checks passed.

#### Acceptance evidence

| Criterion | Evidence | Result |
|-----------|----------|--------|
| Measured no_opportunity with evidence | `freeze.json` verdict `no_opportunity`; reasons include `throughput_alone_cannot_establish_mechanism`, `opt141_llama_mechanism_null`, `warp_query_rejected_as_d2048_evidence` | PASS |
| Positive family excess authenticated | OPT-138 D2048 +47.51 ms (n=3); D128 +4.30 ms | PASS |
| Production identity at D2048 | `vec128_online_decode_attention`; warp_query rejected | PASS |
| No OPT-137 threshold change / OPT-130 revival | freeze `opt137_mma_threshold=8192`, `opt130_not_revived=true` | PASS |
| Quality gate when NLL not required | fixture quality `nll_required=false`, `candidate_nll_not_measured=false`, `skip_reason=no_candidate_no_arithmetic_change` | PASS |
| Parent retained on no-keep | `production_kept=false`; shipping_decode_attention unchanged | PASS |
| Shipping delta 0 | report.json `shipping_delta=0`; `candidate_measured_delta=null` | PASS |
| Required artifacts present | REPORT.md, freeze.json, answers.json, raw gates, fixture, pins, tool, tests | PASS |
| No production kernel change | candidate null; all acceptance phases skipped or mechanism null | PASS |

Verdict `no_opportunity`. Parent retained. Quality N/A (no arithmetic change).
First-pass acceptance: yes.

### Final outcome

- Status: `done`
- Verdict: `no_opportunity` (measured; parent retained)
- Acceptance evidence:
  [`fixtures/opt143_short_attention.json`](../fixtures/opt143_short_attention.json);
  [`evidence/optimization/opt143-short-attention/REPORT.md`](../evidence/optimization/opt143-short-attention/REPORT.md);
  verification record above (2026-09-15T01:05:00Z)
- Candidate measured delta: **N/A**; shipping delta: **0**; quality result:
  **nll_not_required_no_arithmetic_change**
- D2048 `attn_core` family excess +47.51 ms over twelve middle-window evals;
  production path `vec128_online_decode_attention`; no source-grounded mechanism
  for short-decode attn_core; OPT-141 llama `supported_mechanism=null`;
  occupancy/DRAM differences alone cannot freeze a candidate
- OPT-137 MMA retained at >=8192; OPT-130 occupancy path not reopened;
  `claims_throughput=false`; no production kernel or selector change
- First-pass acceptance: yes (2026-09-15T01:05:00Z)

