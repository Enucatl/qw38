# OPT-072 — Reevaluate every correctness-based optimization rejection

Status: done. Dependencies: OPT-059, OPT-069. Coupled IDs: none.
Priority: P0. Target: recover faster implementations excluded by unsuitable
tests while preserving actual correctness. No production pin change here.
Read [the post-069 protocol](PERFORMANCE-RECOVERY-POST-069.md).

## Run record

### Planning (2026-09-11T15:32:00Z)

skipped — pre-authored dossier admitted (coordinator)

## Implementation

1. Inventory OPT-001 through OPT-069 dossiers, raw reports and candidate
   variants inside kept tasks. Include numeric rejection, missing admission,
   exact scalar association, invalid prompt/scorer, missing authority, stale
   object/selector, and genuine layout/nonfinite/state failures. Record original
   test/reference, metric, envelope, source/candidate, latest equivalent path,
   current disposition and concrete next owner. Include OPT-018 historical
   association work, OPT-042/046 Q4, OPT-062/063 admission, and skipped half-scale
   staging. Do not assume that every historical variant needs reinstatement.
2. Classify each entry: already superseded by an admitted equivalent;
   wrong-reference or unsupported numerical rule; incomplete evidence; real
   correctness bug; performance-only rejection; or mixed causes. Confirm the
   actual failure, not just the task's overall done/rejected label. OPT-064/066
   evidence repair belongs to OPT-070; OPT-068's numerical checks passed and
   its measured performance loss is not rescued by tolerance changes.
3. For each remaining correctness-based candidate, reevaluate its smallest
   original failing input against strict output, independently decoded FP64
   original input, FP64 exact staged input, and pinned GPU authority if covered.
   Use current typed staging and source; no historical 30-sample oracle for
   error reproduction. Mark missing production references as unadmitted and
   route Q4 through OPT-074/075. Do not present a partial audit as completed
   reevaluation: every entry needs reproduced evidence, a proven superseding
   equivalence, or a specific unresolved dependency and follow-up.
4. Explicitly compare the original rule with OPT-059's frozen reference-derived
   v2 rule. No candidate-fitted epsilon, no copying synthetic M17/K256 errors
   to real production K. Report whether relaxation changes eligibility and
   which faster implementation it would admit; keep performance unmeasured
   unless valid current complete-cost evidence exists.
5. Audit Q4 Q8_1 consumers as a behavior defect separately from policy. Test
   sums +/-2047, +/-2049, +/-4064; distinguish half(sum(q)), half(sum(x)), and
   exact int32 sums of quantized bytes. A sum(x) producer must not feed a
   sum(q) consumer. Permit a focused typed consumer repair only if necessary
   for the diagnostic; preserve current production Q8/Q6 staging semantics.
6. For an independent legacy numerical candidate not covered by OPT-075–079,
   enumerate its case and one control/candidate screen in this task's contract
   before executing it. Use current full-family replay, at most one candidate
   per phase. Qualifying results are screened-in follow-ups, not automatic
   production keeps. Performance-only losers get a reviewed no-repeat reason.

## Bounded tests and commands

Create `pins/opt072_rejection_review_contract.json` and a machine-readable
review register; reference historical files without modifying them. Host tests
check coverage/disposition and that nonfinite/layout errors cannot be admitted
by numerical headroom. Native reuse: `build/qw38-cuda-opt059-numerics-test` and
existing focused candidate targets, listed exactly per review case.

One `--phase case-ID` is at most two implementations, two relevant shapes,
16 sampled rows x 4 vectors per shape, one execution for correctness; optional
screen is 1+3 whole-family rounds. Dry-run must expand a finite case-ID list
with actual loop counts. The register audit itself has no GPU work.

```sh
uv run pytest -q tests/test_opt072_rejection_review.py
uv run python tools/run_optimization_task.py --task OPT-072 --mode feedback --phase inventory
uv run python tools/run_optimization_task.py --task OPT-072 --mode feedback --phase q4-original-failure
uv run python tools/run_optimization_task.py --task OPT-072 --mode acceptance --phase review
```

Acceptance is review completeness, not a speed claim. Report each old rejection
as still valid, evidence-invalid, numerically eligible, already superseded or
blocked with an owner. Stop GPU work immediately on a real correctness defect;
keep its failing reproducer and route a repair. An actionable entry with no
reproducer/reference/owner prevents a complete verdict. Quality risk is limited
to proposed admission; hard semantic errors are never relaxed.

### Implementation (2026-09-11T15:44:57Z)

- Agent/model: implementation (Cursor Grok 4.6). No commit. Ledger left
  `in_progress`. `plan.md` unchanged. No production pin change.
- Inventory: 78 register entries covering OPT-001–OPT-069. Q4 integer paths
  are numerically eligible vs staged FP64 but blocked on OPT-074/075. OPT-068
  remains a performance-only no-repeat. OPT-064/066 evidence repair stays
  OPT-070. Half-scale Q8_1 is enumerated (`half-scale-q8-1`) and not executed.
- Files changed:
  - `pins/opt072_rejection_review_contract.json`
  - `pins/opt072_iteration_contract.json`
  - `fixtures/opt072_rejection_review.json`
  - `tests/test_opt072_rejection_review.py`
  - `tools/opt072_rejection_review.py`
  - `tools/run_optimization_task.py` (host-only skip GPU/compile)
  - `cuda/q4k_decode_path.cuh` (typed Q8_1 pairing constants)
  - `evidence/optimization/opt072-rejection-review/REPORT.md`
  - `tasks/OPT-072.md`
- Commands:

```
uv run pytest -q tests/test_opt072_rejection_review.py
```

**pass** — 7 passed in 0.02s.

```
uv run python tools/run_optimization_task.py --task OPT-072 --mode feedback --phase inventory
```

**pass** — exit 0; `elapsed_s=0.0419`; deadline 300s; `gpu_work=false`;
`loop_product_total=69`; run
`build/optimization-runs/OPT-072/feedback/20260911T154449Z-fae51e47`.

```
uv run python tools/run_optimization_task.py --task OPT-072 --mode feedback --phase q4-original-failure
```

**pass** — exit 0; `elapsed_s=0.0452`; deadline 300s; cases=2 candidates=2
product=4; run
`build/optimization-runs/OPT-072/feedback/20260911T154452Z-008ceca6`.

```
uv run python tools/run_optimization_task.py --task OPT-072 --mode acceptance --phase review
```

**pass** — exit 0; `elapsed_s=0.0429`; deadline 300s; review completeness;
run `build/optimization-runs/OPT-072/acceptance/20260911T154457Z-10579c0f`.

## Verification (2026-09-11T15:46:12Z)

Independent integration verifier. No commit. Ledger left `in_progress`.
`plan.md` unchanged.

### Diff vs dossier / ledger / plan

| Requirement | Status |
|---|---|
| `pins/opt072_rejection_review_contract.json` | present (untracked) |
| `fixtures/opt072_rejection_review.json` register | present (untracked), 78 entries |
| `tests/test_opt072_rejection_review.py` | present (untracked), 7 tests |
| `tools/opt072_rejection_review.py` | present (untracked) |
| `pins/opt072_iteration_contract.json` host-only | present (untracked) |
| `tools/run_optimization_task.py` GPU/compile skip | modified |
| `cuda/q4k_decode_path.cuh` Q8_1 pairing constants | modified (+8 lines) |
| `evidence/optimization/opt072-rejection-review/REPORT.md` | present (untracked) |
| `implementation_ledger.md` OPT-072 `in_progress` | modified (not `done`) |
| `plan.md` unchanged | confirmed (no diff) |
| No production pin change | confirmed |
| No dossier directory | N/A |

Fixture matches `build_register()` (78 IDs identical). `validate_register` and
`validate_q8_1_audit` both succeed. All 69 tasks covered; every entry has
`disposition`, `next_owner`, and `reevaluation`. Dispositions:
`kept` 48, `no_repeat` 13, `blocked` 9, `already_superseded` 3,
`numerically_eligible` 3, `still_valid` 2. `evidence_invalid` disposition is
defined but unused (0 rows; `incomplete_evidence` rows map to `blocked` or
`numerically_eligible`).

### Commands

```
uv run ruff format .
```

**PASS** — 157 files left unchanged; no retest needed.

```
uv run ruff check tests/test_opt072_rejection_review.py tools/opt072_rejection_review.py tools/run_optimization_task.py
```

**PASS** — All checks passed.

```
uv run ruff check pins/opt072_rejection_review_contract.json pins/opt072_iteration_contract.json fixtures/opt072_rejection_review.json cuda/q4k_decode_path.cuh
```

**FAIL** — `invalid-syntax` on `.cuh` and JSON (not Python; expected).

```
uv run pytest -q tests/test_opt072_rejection_review.py
```

**PASS** — 7 passed in 0.02s.

```
uv run python tools/run_optimization_task.py --task OPT-072 --mode feedback --phase inventory
```

**PASS** — exit 0; `elapsed_s=0.0435`; `gpu_work=false`; `loop_product_total=69`;
run `build/optimization-runs/OPT-072/feedback/20260911T154612Z-8fb0e185`.

```
uv run python tools/run_optimization_task.py --task OPT-072 --mode feedback --phase q4-original-failure
```

**PASS** — exit 0; `elapsed_s=0.0452`; cases=2 candidates=2 product=4;
run `build/optimization-runs/OPT-072/feedback/20260911T154616Z-c2fec07c`.

```
uv run python tools/run_optimization_task.py --task OPT-072 --mode acceptance --phase review
```

**PASS** — exit 0; `elapsed_s=0.0483`; review completeness;
run `build/optimization-runs/OPT-072/acceptance/20260911T154616Z-64a01c43`.

### Issues (non-blocking)

1. `test_q4_q8_1_sum_cases_distinguish_sum_q_sum_x_and_int32` nests `abs(total)
   == 2049` assertions inside the `2047` branch (dead code); 2049 coverage relies
   on `validate_q8_1_audit()` at the end of the test.
2. `half-scale-q8-1` workload args invoke `--phase review` (no dedicated phase);
   screen is enumerated-only per contract — consistent with task intent but not
   independently runnable via `--phase half-scale-q8-1`.
3. All deliverable artifacts except four tracked edits remain untracked.

### Verdict

**PASS** — host contract, register completeness, Q8_1 audit, and optimization
runner phases meet OPT-072 acceptance for review completeness. Ledger correctly
left `in_progress`; no production pin change.

### Final outcome (2026-09-11T15:47:11Z)

78-entry rejection review register covering OPT-001–OPT-069 with disposition,
next owner, and reevaluation for every historical numerical/test rejection.
Q4 integer paths numerically eligible vs staged FP64 but blocked on OPT-074/075;
OPT-068 performance-only no-repeat; OPT-064/066 evidence repair routed to OPT-070;
Q8_1 consumer audit with typed pairing constants in `cuda/q4k_decode_path.cuh`.
Host contract, inventory, q4-original-failure, and review phases passed within
300 s; `gpu_work=false`; 7 pytest cases. Coupled IDs: none. First-pass delivery.
Retries: 0. No production pin change. No tok/s claim (`claims_throughput=false`).

