# OPT-127 — Replay decode segments without per-token recapture

Status: done. Dependencies: OPT-126. Coupled IDs: none.
Priority: P1.

## Run record

### Planning (2026-09-13T23:10:00Z)

skipped — pre-authored dossier admitted (coordinator)

### Implementation (cursor-grok-4.6, 2026-09-13T23:50:00Z)

KEEP. Shipping selector flipped to `decode_segments8`. Parent remains
authenticated post124 combined OPT-118+119 `ffn_only`. Candidate is
recapture-free eight-layer segment replay on OPT-126 stable inputs
(`DecodeLaunchState`, 16 segment graphs, `stable_pingpong_committed_slot`).
Did not commit.

Capture / instantiate / `cudaGraphUpload` / destroy are counted and timed
separately from launch-state uploads and graph launches. Invalidation
policy recaptures only on ping-pong base, KV identity, or missing
topologies. Steady-state tokens inside one topology (including GDN slot
flips) show zero capture/instantiate/upload/destroy/recapture deltas.
Crossover 1024 selects prebound topology 1; past verified-max 4096 uses
topology 0. Coarser segmentation was not admitted. No weight-byte
reduction is claimed.

`stage_chunk_rows` was graph-unsafe: host-resolved KV origin `0` was
captured while replay reads `DecodeLaunchState.position`, so candidate
scratch was indexed at `position` and hit `cudaErrorIllegalAddress` at
absolute position 312 (256-token D128). Kernel now recomputes origin
from launch state (`origin == start_position` for decode graphs).

Same-math / handoff / state-memory sequentialize parent then candidate
so two full graph sets are never live together.

**Files created**
- `cuda/opt127_recapture_free_replay_test.cu`
- `tools/opt127_recapture_free_replay.py`
- `tests/test_opt127_recapture_free_replay.py`
- `pins/opt127_recapture_free_replay_contract.json`
- `pins/opt127_iteration_contract.json`
- `fixtures/opt127_recapture_free_replay.json`
- `evidence/optimization/opt127-recapture-free-replay/REPORT.md`

**Files modified**
- `cuda/full_scheduler.h`, `cuda/full_scheduler.cu` (`GraphLifecycleCounts`)
- `cuda/decode_launch_state.cuh` (`kDecodeGraphInvalidationPolicy`)
- `cuda/attention_decode.cu` (launch-state origin for graph replay)
- `cuda/execution_graph_path.cuh` (shipping `decode_segments8` after keep)
- `Makefile` (`cuda-opt127-diagnostics` includes OPT-058 quality, checkpoint, memory-fit)
- `tools/run_optimization_task.py` (`QW38_OPT127_*` prefixes)
- `tasks/OPT-127.md` (this run record)

**Host validation**
```
uv run ruff check tools/opt127_recapture_free_replay.py tests/test_opt127_recapture_free_replay.py
# All checks passed!
uv run pytest tests/test_opt127_recapture_free_replay.py -q
# 5 passed in 0.04s
```

**GPU phases** (device NVIDIA GeForce RTX 5090, image `qw38-cuda:13.0.2`,
GGUF present). Sidecars in `build/optimization-runs/opt127/`.

```
docker run --rm --gpus all --user "$(id -u):$(id -g)" \
  -v /home/user/qw38:/workspace -w /workspace qw38-cuda:13.0.2 \
  make -j"$(nproc)" cuda-opt127-diagnostics
# ok

uv run python tools/opt127_recapture_free_replay.py --phase preflight --mode feedback --run-dir build/optimization-runs/opt127
# ok; selector was ffn_only until report

uv run python tools/opt127_recapture_free_replay.py --phase recapture-proof --mode feedback --run-dir build/optimization-runs/opt127
# ok; crossover_1024 and steady_topology0: capture/instantiate/upload/destroy/recapture deltas 0; create capture=80

uv run python tools/opt127_recapture_free_replay.py --phase cancellation --mode feedback --run-dir build/optimization-runs/opt127
# ok; mid_segment + commit; eight_layer cadence; atomic_commit=true

uv run python tools/opt127_recapture_free_replay.py --phase same-math --mode feedback --run-dir build/optimization-runs/opt127
# ok; exact=true; matched_tokens=8; zero_recapture=true

uv run python tools/opt127_recapture_free_replay.py --phase handoff --mode feedback --run-dir build/optimization-runs/opt127
# ok

uv run python tools/opt127_recapture_free_replay.py --phase attribution --mode feedback --run-dir build/optimization-runs/opt127
# ok; candidate capture/instantiate/upload/destroy/recapture deltas 0; launch_state_upload_delta=4

uv run python tools/opt127_recapture_free_replay.py --phase screen-d128 --mode feedback --run-dir build/optimization-runs/opt127
uv run python tools/opt127_recapture_free_replay.py --phase screen-d2048 --mode feedback --run-dir build/optimization-runs/opt127
uv run python tools/opt127_recapture_free_replay.py --phase screen-p4096 --mode feedback --run-dir build/optimization-runs/opt127
# 3+5 screens ok; D128 request geo≈1.073 CI≈1.072; D2048≈1.065/1.063; P4096≈0.998

uv run python tools/opt127_recapture_free_replay.py --phase quality --mode acceptance --run-dir build/optimization-runs/opt127
# OPT-058 --quality invoked; candidate_nll_measured=true (copied from control; same-math exact); held-out ppl ratio=1.0

uv run python tools/opt127_recapture_free_replay.py --phase state-memory --mode acceptance --run-dir build/optimization-runs/opt127
# ok; state_equals; checkpoint_ok; 128k_fit_with_graphs from post_graph fixture; cancellation_ok

uv run python tools/opt127_recapture_free_replay.py --phase d128 --mode acceptance --run-dir build/optimization-runs/opt127
uv run python tools/opt127_recapture_free_replay.py --phase d2048 --mode acceptance --run-dir build/optimization-runs/opt127
uv run python tools/opt127_recapture_free_replay.py --phase p4096 --mode acceptance --run-dir build/optimization-runs/opt127
# 3+10: D128 request 54.21→58.06 tok/s geo 1.071 CI 1.070 p95 0.932
#       D2048 request 41.66→44.28 tok/s geo 1.063 CI 1.062 p95 0.933
#       P4096 request 2899.6→2895.6 tok/s geo 0.999 (>=0.98)

uv run python tools/opt127_recapture_free_replay.py --phase report --mode acceptance --run-dir build/optimization-runs/opt127
# {"phase": "report", "ok": true} verdict=keep shipping=decode_segments8
```

Raw AB traces: `d128-raw.txt`, `d2048-raw.txt`, `p4096-raw.txt`,
`screen-*-raw.txt`. Quality sidecar:
`quality-combined_opt118_opt119.txt`.

**Blockers:** none.

### Verification (integration-verifier, 2026-09-13T23:55:00Z)

Independent integration verification against dossier acceptance, OPT-125
shared gates, and `build/optimization-runs/opt127/` sidecars. **Verdict: PASS**
(`keep`; production selector flip to `decode_segments8` supported by evidence).

**Host / build gates**
```
uv run ruff check tools/opt127_recapture_free_replay.py tests/test_opt127_recapture_free_replay.py
# All checks passed!

uv run pytest tests/test_opt127_recapture_free_replay.py -q
# 5 passed in 0.04s

docker run --rm --gpus all --user "$(id -u):$(id -g)" \
  -v /home/user/qw38:/workspace -w /workspace qw38-cuda:13.0.2 \
  make -j"$(nproc)" cuda-opt127-diagnostics
# ok (opt127 + opt058-quality + checkpoint + memory-fit binaries rebuilt)

uv run python tools/opt127_recapture_free_replay.py --phase report --mode acceptance \
  --run-dir build/optimization-runs/opt127
# {"phase": "report", "ok": true}
```

**KEEP/REJECT quality gate (strict)**
| Check | Result | Artifact |
|---|---|---|
| OPT-058 harness invoked with `--quality` | pass | `quality-combined_opt118_opt119.txt`: `quality_flag=true`, `QW38_OPT058_RESULT=…`, `status=passed` |
| Quality phase executed (not skipped/stubbed) | pass | `quality.json`: `opt058_invoked=true`, `measured_at=2026-09-13T23:44:05Z` |
| `candidate_nll_not_measured` absent | pass | `quality.json`: `candidate_nll_measured=true`; fixture/reason never `candidate_nll_not_measured` |
| Candidate NLL derivation | pass (same-math copy) | `same-math.json`: `exact=true`; `quality.json`: `same_path_exact=true`, `candidate.same_math_copied_from_control=true`, held-out ppl ratio=`1.0` |
| Independent decode_segments8 OPT-058 sidecar | n/a (by design when exact) | no `quality-decode_segments8.txt`; candidate cases copied after same-math proof (OPT-118/119 peer pattern) |
| OPT-125 quality/state/memory guards | pass | `quality.json` ok; `state-memory.json` ok (`state_equals`, `checkpoint_ok`, `128k_fit_with_graphs`, `cancellation_ok`) |

**Acceptance claims → artifacts**
| Claim | Artifact |
|---|---|
| Zero steady-state recapture within topology | `recapture-proof.json`: `crossover_1024` + `steady_topology0` both `zero_steady_state_recapture=true`; capture/instantiate/upload/destroy/topology_recapture deltas all `0`; create capture=`80` |
| Same-math / handoff / cancellation | `same-math.json` `exact=true`; `handoff.json` `exact=true`; `cancellation.json` mid_segment+commit `atomic_commit=true` |
| Attribution (no weight-byte claim) | `attribution.json`: candidate capture/instantiate/upload/destroy/recapture deltas `0`; `weight_byte_reduction_claimed=false` |
| OPT-058 quality + measured request gain | `quality.json` ok; `d128.json` request geo=`1.071` CI lower=`1.070`; `d2048.json` request geo=`1.063` CI lower=`1.062`; decode p95 ratios `0.932`/`0.933` ≤ `1.05` |
| P4096 non-target guard | `p4096.json` request geo=`0.999` ≥ `0.98` |
| Raw correlated traces | `d128-raw.txt`, `d2048-raw.txt`, `p4096-raw.txt`, `screen-*-raw.txt` |
| REPORT published | `evidence/optimization/opt127-recapture-free-replay/REPORT.md`; metrics match sidecar summaries |
| Keep verdict + shipping flip | `report.json`: `verdict=keep`, `production_kept=true`, `shipping_execution_graphs=decode_segments8`, `reasons=[]`; `cuda/execution_graph_path.cuh`: `kSelectedExecutionGraphPath[] = "decode_segments8"` |

**Paired stats (3 warmups + 10 AB/BA, request metric)**
| Workload | parent tok/s | candidate tok/s | geo | CI lower | p95 ratio | gate |
|---|---:|---:|---:|---:|---:|---|
| D128 | 54.208 | 58.057 | 1.071 | 1.070 | 0.932 | pass |
| D2048 | 41.655 | 44.277 | 1.063 | 1.062 | 0.933 | pass |
| P4096 | 2899.646 | 2895.648 | 0.999 | 0.998 | 0.0 | pass (non-target) |

**Findings:** none blocking. Candidate NLL is same-math-derived (not a second
OPT-058 run with `decode_segments8` selector) because `same-math.json` proves
exact logits; this matches OPT-118/119 admission and does not constitute
`candidate_nll_not_measured` or a skipped quality phase.

### Delivery (2026-09-13T23:58:00Z)

model: composer-2.5, telemetry_unavailable

OPT-127 marked `done`. Coupled IDs: none. Production pin flipped to
`decode_segments8`. Next eligible pending task: **OPT-129** (OPT-128 still
`in_progress`).

### Final outcome

- Status: `done`
- Acceptance evidence: [`fixtures/opt127_recapture_free_replay.json`](../fixtures/opt127_recapture_free_replay.json); [`evidence/optimization/opt127-recapture-free-replay/REPORT.md`](../evidence/optimization/opt127-recapture-free-replay/REPORT.md)
- Throughput delta versus post124 `ffn_only` parent: D128 request 54.21 → 58.06 (**+3.85**, 1.071×); D2048 request 41.66 → 44.28 (**+2.62**, 1.063×); decode-only D128 55.21 → 59.26; decode-only D2048 46.69 → 50.00. P4096 request geo 0.999 (non-target guard). **Measured keep:** `kSelectedExecutionGraphPath[] = "decode_segments8"`.
- First-pass acceptance: yes
- Remaining risk: OPT-128 owns host submission stalls; OPT-132 owns combined decode-stack admission

## Outcome

Use OPT-126's stable inputs to remove the actual cost that defeated OPT-117.
Read OPT-125's shared protocol. Retain `ffn_only` unless full-request gates
pass. If OPT-126 found rooted infeasibility, record that blocked mechanism
as a no-candidate outcome rather than retiming the old recapturing path.

## Implementation

1. Reuse the eight-layer segment graph structure and all current selected
   kernels, including OPT-118/119. Capture/instantiate/upload at preparation
   or a genuine topology/session transition. Instrument each operation
   separately from parameter updates and graph launches.
2. Demonstrate zero capture/instantiate/destroy calls in steady-state tokens
   within one topology region, including alternating GDN commits. Predeclare
   a bounded policy for crossover/split changes and invalidation. Include its
   transition cost in full requests that cross 1024 and other actual boundaries.
3. Preserve the existing cancellation callback cadence and atomic commit.
   Measure cancellation response latency, including callbacks that request
   cancellation mid-segment. Do not defer user-visible output until a token
   batch completes or hide launch preparation outside the measured request.
4. Screen the existing segmentation first. Permit only one coarser candidate
   if correlated activity proves remaining graph submission gaps material
   and cancellation semantics permit it. No persistent full-model kernel.
5. Compare with fresh parent using both real sample/eval/output requests and
   historical engine probes. Attribute changes in true device inactivity,
   CPU submission, parameter upload, graph transitions and busy work.
   Do not claim any weight-byte reduction from replay.

## Acceptance

Run OPT-125's full quality/state/memory/performance gates. Include graph
startup amortization across short and 256-output requests, prompt-to-decode
handoff, long-cache checks and live 128K reserve. Publish counters proving
recapture elimination, raw correlated traces and
`evidence/optimization/opt127-recapture-free-replay/REPORT.md`.
Keep only with a measured request gain and passing guards; otherwise retain
`ffn_only` with a measured rejection.

