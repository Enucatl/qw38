# OPT-115 — Audit complete-request memory traffic and performance bounds

Status: done. Dependencies: OPT-113, OPT-114. Coupled IDs: none.
Priority: P0. Diagnostic foundation and shared protocol for OPT-115–124.

## Run record

### Planning (2026-09-13T12:00:00Z)

skipped — pre-authored dossier admitted (coordinator)

### Implementation (cursor-grok-4.6-high, 2026-09-13T12:41:02Z)

Diagnostic audit only. No production kernel, selector, or dispatch change.
`claims_throughput=false`, `claims_performance_improvement=false`. Did not
commit. model `cursor-grok-4.6-high`. telemetry_unavailable.

**Files created**
- `pins/opt115_pipeline_traffic_contract.json`
- `pins/opt115_iteration_contract.json`
- `tools/opt115_pipeline_traffic.py`
- `cuda/opt115_pipeline_traffic_test.cu`
- `fixtures/opt115_pipeline_traffic.json`
- `tests/test_opt115_pipeline_traffic.py`
- `evidence/optimization/opt115-pipeline-traffic/REPORT.md`

**Files modified**
- `Makefile` (`cuda-opt115-diagnostics`, `build/qw38-cuda-opt115-pipeline-traffic-test`; links `OPT110_LLAMA_OBJECT` so post113 `llama_q4k_mmvq` FFN graphs can capture)
- `tools/run_optimization_task.py` (`QW38_OPT115_NATIVE_COUNTS=`, `QW38_OPT115_PIPELINE_TRAFFIC_RESULT=`)
- `tasks/OPT-115.md` (this run record)
- `implementation_ledger.md` (status `in_progress`; coordinator already set this)

**Host validation**
```
uv run ruff format tools/opt115_pipeline_traffic.py tests/test_opt115_pipeline_traffic.py
# 1 file reformatted, 1 file left unchanged
uv run ruff check tools/opt115_pipeline_traffic.py tests/test_opt115_pipeline_traffic.py
# All checks passed!
uv run pytest tests/test_opt115_pipeline_traffic.py -q
# 10 passed in 0.12s; re-run after GPU/report 10 passed in 0.07s
```

**GPU phases** (device NVIDIA GeForce RTX 5090, image `qw38-cuda:13.0.2`,
GGUF present). Sidecars in `build/optimization-runs/opt115/`. Unrelated
`python` PID 1512418 held 868 MiB; this dossier does not authorize stopping
it. Stale empty `build/optimization-runs/qw38-gpu.lock` dated 2026-09-12
was not held. A/A remained `repeatable`. D131040+32 in a 131072-capacity
session fitted despite the extra resident process.

First native link without `OPT110_LLAMA_OBJECT` failed
`internal: cannot capture CUDA scheduler FFN graphs` because post113
`llama_q4k_mmvq` FFN capture requires the OPT-110 adapter static hooks.
Relinked the diagnostic binary only; production `cuda/full_scheduler.cu`
unchanged. Probe then succeeded (`opt110_engine_hooks_ready=true`).

```
docker run --rm --gpus all --user "$(id -u):$(id -g)" -v /home/user/qw38:/workspace -w /workspace qw38-cuda:13.0.2 make build/qw38-cuda-opt115-pipeline-traffic-test
# ok (links OPT110_LLAMA_OBJECT)

docker run --rm --gpus all --user "$(id -u):$(id -g)" -e QW38_CUDA_TEST_TIER=screen -v /home/user/qw38:/workspace -w /workspace qw38-cuda:13.0.2 ./build/qw38-cuda-opt115-pipeline-traffic-test --workload request-trace --prefix 128 --warmups 0 --samples 1 --tokens 1 models/Qwen3.8-27B-Q4_K_M.gguf
# ok; A/B ~57.13/58.24 tok/s; execution_graphs=ffn_only

uv run python tools/opt115_pipeline_traffic.py --phase request-trace --mode feedback --run-dir build/optimization-runs/opt115
# ok 2026-09-13T12:27:07Z–12:31:39Z

uv run python tools/opt115_pipeline_traffic.py --phase long-context --mode feedback --run-dir build/optimization-runs/opt115
# ok 12:31:39Z–12:40:55Z; D8192/D32768/D131040 all measured, populated_cache=true

uv run python tools/opt115_pipeline_traffic.py --phase traffic-timeline --mode feedback --run-dir build/optimization-runs/opt115
# ok ~7s; D128 coverage 0.9658, D2048 coverage 0.9747

uv run python tools/opt115_pipeline_traffic.py --phase ranking --mode feedback --run-dir build/optimization-runs/opt115
# ok (host)

uv run python tools/opt115_pipeline_traffic.py --phase report --mode acceptance --run-dir build/optimization-runs/opt115
# {"task": "OPT-115", "phase": "report", "ok": true}
```

**A/A (fresh same-binary post113; 3 warmups + 10 interleaved AB/BA)**
| Workload | A tok/s | B tok/s | geo ratio | Verdict |
|---|---:|---:|---:|---|
| P4096 | 2985.6727 | 2986.1139 | 1.0001 | repeatable |
| D128 | 57.3949 | 57.3951 | 1.0000 | repeatable |
| D2048 | 55.6682 | 55.6640 | 0.9999 | repeatable |

Combined `aa_verdict`: **repeatable**. OPT-113 tok/s numbers remain historical.
Session loop TTFT 1402.31 ms, p50 20.63 ms, p95 20.68 ms.

**Long-context (131072 capacity, 32 decode tokens, populated cache)**
- D8192: 41.420 tok/s, TTFT 3159.92 ms
- D32768: 22.118 tok/s, TTFT 21911.78 ms
- D131040: 7.723 tok/s, TTFT 242093.44 ms

These probes are separately labeled; they are not D128/D2048 wins.

**Timeline / bounds / ranking (no throughput claim)**
- D128: GPU busy union 7.89 ms, GPU idle 9.97 ms, coverage 0.966
- D2048: GPU busy union 8.26 ms, GPU idle 10.03 ms, coverage 0.975
- D128 weight peak bound 10.18 ms at listed 1792 GB/s; independent sums are not a bound
- Ranked go/no-go: OPT-121 (weights 10.18 ms), OPT-117 (launch/idle 9.97 ms; revalidates OPT-114 ~11 ms on post113), OPT-120 (128K KV 4.79 ms, not a D128 win), OPT-122 go_conditional (GDN 0.089 ms), OPT-119 (activations), OPT-118 (PCIe/logits)

**Blockers**
- None for the required GPU phases. Sitting was not exclusive (868 MiB unrelated process). Measured sustainable bandwidth was not counter-sampled (`None`); peak 1792 GB/s remains an optimistic listed bound.

## Outcome

Explain the remaining time and bytes in the actual production pipeline and
rank experiments by removable request latency. Separate device GDDR traffic,
L2 reuse, host/device transfers, allocation capacity and one-time loading.
Deliver conditional bounds for this workload, not a proof that llama.cpp is
optimal. This task changes diagnostics only; candidate implementation belongs
to OPT-117–122.

## Starting evidence

- [OPT-113 fixture](../fixtures/opt113_coupled_stack_gate.json) and
  [contract](../pins/opt113_coupled_stack_gate_contract.json): production
  `post113_selected` is Q4 `llama_q4k_mmvq`, prompt attention `opt111_base`,
  decode attention `hybrid_crossover@1024`, GDN `sequential`, MMQ `fma_async`,
  graphs `ffn_only`. P4096/D128/D2048 are 3064.77/57.297/55.647 tok/s versus
  pinned llama 3227.71/68.614/67.272. These are historical observations, not
  reusable paired controls. Use the complete selector record, not this summary.
- The reconciled [128K ledger](../fixtures/cuda_memory_fit_post_graph.json)
  passes: resident model 18,973,870,432 B, KV 8,589,934,592 B, GDN state/rings
  158,859,264 B, workspace 1,831,836,288 B, reserve 1,610,612,736 B.
  OPT-113's earlier accounting failure was repaired; do not reopen it as OOM.
- [Tensor inventory](../pins/tensor_inventory.json) totals Q4_K 10,341,089,280 B,
  Q8_0 7,155,384,320 B and Q6_K 1,455,820,800 B. These are stored bytes;
  embedding lookup does not read the entire embedding tensor per token.
- [OPT-114](OPT-114.md) reported about 11 ms/token removable launch overhead
  on its older control and failed segment capture. Revalidate both the
  attribution and applicability to post113; wall minus leaf-event sums alone
  cannot identify causal removable overhead.
- [OPT-109](OPT-109.md) won recurrence-only but lost complete GDN. Decode
  [scheduler](../cuda/full_scheduler.cu) already swaps GDN candidate/committed
  pointers and scatters one KV row per attention layer. Do not assume it copies
  the full 8 GiB cache on every token.

## Investigation

1. Authenticate one fresh post113 control and pinned llama
   `cc83d7b4824f73cfdda4dfbb47ee39804f71b328`: binary, source/dirty state,
   GGUF hash, complete selectors, actual launched kernels, capacities, compiler
   flags, graphs, GPU clocks/power and timing boundaries. Reproduce A/A before
   interpreting A/B. Use an exclusive, serialized GPU sitting under the
   existing operational procedure; this dossier does not authorize stopping
   unrelated services. If exclusivity cannot be obtained, report the blocker.
2. Trace P4096, D128 and D2048 plus an ordinary `Session::sync` → sample → eval
   → output loop. Compare benchmark and CLI/server cancellation-poll behavior.
   Include prefill-to-first-token latency, 256 emitted decode tokens, p50/p95
   inter-token latency and total request wall time. Add separately labeled
   D8192/D32768 and bounded D131040+32 probes in a 131072-capacity session.
   Capacity alone is not evidence of reading a populated long cache.
3. Map weight reads, activation/quantization buffers, KV append/read,
   recurrent old/new state, carry/checkpoint copies, output-head/logits,
   token transfers, host sampling, allocations, barriers and graph updates to
   their producers/consumers and lifetimes. Record bytes/request, bytes/token,
   call counts and scope. Identify repeated quantize/dequantize/layout passes,
   FP32/BF16 intermediates and any materialized attention scores.
4. Use available timeline and targeted memory counters on complete execution.
   Distinguish GPU busy time, GPU idle, CPU work overlapped with GPU work,
   dependency waits, synchronization and launch submission. Report event
   unions and unclassified time; avoid adding overlapping intervals. Validate
   counter coverage and profiler perturbation against uninstrumented wall
   timing. If counters are unavailable, label byte estimates and bound their
   uncertainty; do not fabricate hardware measurements.
5. Reconcile tensor-derived compulsory reads with measured DRAM/L2 traffic.
   Compute time bounds using both hardware peak and measured sustainable
   bandwidth for relevant access patterns, with explicit cache assumptions.
   NVIDIA lists 1792 GB/s peak for the RTX 5090 in its
   [Blackwell architecture paper](https://images.nvidia.com/aem-dam/Solutions/geforce/blackwell/nvidia-rtx-blackwell-gpu-architecture.pdf).
   Peak is an optimistic bound, not observed application bandwidth. Separate
   fixed-format and quality-gated lower-bit byte models. Include unpack,
   reductions, compute and serial dependency constraints; use max/critical-path
   reasoning when costs overlap, not a sum of independent peak bounds.
6. Inspect pinned llama and local sibling sources for each ranked mechanism.
   Planning inspected llama HEAD `1945e092030f8668ff93382799502d01490e564d`
   and ds4 HEAD `c238077a87186381bf626cc531bccffe1fef79e7`; neither is silently
   the pinned benchmark authority. Use `../llama.cpp/src/models/qwen35.cpp`,
   its CUDA graph/attention/MMQ code, `../ds4/ds4_cuda.cu` and
   `../ds4/cuda/mmq/`. Record source revision, function, physical dtype,
   actual consumers and transferability. DeepSeek compression/indexer/MoE
   structure is not Qwen's 48-GDN/16-attention architecture.
7. Publish per-task removable-byte/time bounds and uncertainties for OPT-117–122.
   Investigate important unclassified spans before declaring no opportunity.
   Below-resolution opportunities may be deferred with evidence; a missing
   metric or broad family residual is not a measured zero. No kernel retuning
   or new optimization in this task.

## Shared execution and keep protocol for OPT-117–123

- Read this dossier and OPT-116. Reuse existing optimization/quality harnesses;
  create only task-specific instrumentation, contracts and checks needed for
  trustworthy evidence. Freeze candidate list, primary workload, timing
  boundary, quality contract, sample count and stop rules before results.
- OPT-117–119 cover scheduler/intermediate traffic; OPT-120 owns KV encoding,
  OPT-121 weights, OPT-122 recurrent-state encoding. Preserve exact token IDs,
  template, model identity, full causal context and public session semantics.
  No layer/token dropping, speculative decoding, batching, new model or
  generic framework. Precision experiments are authorized by the 2026-09-13
  request; document any admitted architecture-policy exception with a dated
  note during implementation, preserving the original plan and gate records.
- Compare each mechanism against fresh post113 and its actual parent stack.
  Declare the parent explicitly. Graph/ownership work can be a parent for later
  slices only if kept; a rejected prerequisite means use its retained control.
  Freeze combined quality against post113, never a ratcheting sequence of
  already-degraded candidates.
- Use microbenchmarks to debug correctness or explain results. A correct
  plausible pipeline candidate must get a bounded full-engine screen; a leaf
  kernel loss alone cannot veto a hypothesized enclosing win. Equally, no
  isolated/component win admits a keep. Rooted infeasibility, failed quality,
  or a quantified negligible upper bound can stop a candidate before timing.
- Default screening: 3 warmups and 5 paired AB/BA complete-workload runs.
  Final candidate acceptance: 3 warmups and 10 paired AB/BA runs for each
  declared target and P4096/D128/D2048, 256 decode outputs for D128/D2048.
  Freeze any resource-driven long-context count reduction before candidates.
  Report raw wall samples, geometric throughput ratios, paired log-ratio
  one-sided 95% Student-t bounds, p50/p95 and all failures. No extra sampling
  to turn an inconclusive result into a pass. OPT-123 also retains the original
  benchmark protocols for historical outcome fields.
- For a new keep require target throughput ratio lower bound >1.00 versus
  its parent, an improvement larger than measured A/A noise, and point ratios
  >=0.98 on every non-target P/D workload with decode p95 <=1.05 times parent.
  Predeclare whether the target is decode, prefill or long-context requests;
  long-context gains must not be presented as D128/D2048 wins. A memory-only
  improvement is a separately labeled result, not a throughput keep.
- Full OPT-116 candidate quality precedes long acceptance timing. Required
  state tests cover graph/eager same-path results, prompt-to-decode handoff,
  chunk boundaries, cancellation before commit, failure/frontier isolation,
  divergent-prefix replay, repeated save/restore and sampler continuity.
  Packed formats require explicit versioned compatibility and exact same-format
  round trips. Never expand and requantize on restore if it changes the saved
  state. Test retained paths as well as new ones.
- Reconcile persistent and transient peak bytes, repacks, graphs and 128K fit
  with the existing 1.5 GiB reserve. Preserve the source GGUF; replacement
  device encodings must account for preparation peak and duplicate lifetimes.
  Reject candidates that need an unreported dense shadow or raise the cap.
- Deliver separate `kernel_correctness`, `model_quality`, `state_memory`,
  `performance`, `production_kept` and historical release verdicts. A completed
  rejection or evidence-backed no-opportunity disposition satisfies a research
  task. Missing required evidence is inconclusive/blocked, never a pass.
  Retain reproducible negative results and remove rejected production dispatch.

## Acceptance and artifacts

Deliver task-owned machine-readable raw evidence/summary and
`evidence/optimization/opt115-pipeline-traffic/REPORT.md`, linked from this
dossier and ledger on completion. Include a bytes/lifetime table, complete
request timeline, pinned-source comparison, bounds with assumptions and ranked
OPT-117–122 go/no-go findings. Verify counts, units, interval coverage and
reproducibility with focused host checks. No throughput or optimality claim.

### Verification

model: composer-2.5, telemetry_unavailable

#### Commands (exact outcomes)

| # | Command | Exit | Outcome |
|---|---------|------|---------|
| 1 | `uv run ruff format .` | 0 | `256 files left unchanged` — no formatting edits |
| 2 | `uv run ruff check .` | 1 | **FAIL** — 2 pre-existing errors outside OPT-115 diff: `F841` unused `shipping_q4` in `tests/test_opt089_q4_promotion.py:157`; `F401` unused import `source_paths` in `tools/opt088_batch_gate.py:49`. Both present in `HEAD`; OPT-115 files clean (scoped check below). |
| 2b | `uv run ruff check tools/opt115_pipeline_traffic.py tests/test_opt115_pipeline_traffic.py` | 0 | `All checks passed!` |
| 3 | `uv run pytest -q tests/test_opt115_pipeline_traffic.py` | 0 | `10 passed in 0.06s` |
| 4 | `make cuda-opt115-diagnostics` (host) | 2 | `make: nvcc: No such file or directory` — host lacks CUDA toolkit |
| 4b | `docker run … qw38-cuda:13.0.2 make cuda-opt115-diagnostics` | 0 | Target up-to-date; binary present at `build/qw38-cuda-opt115-pipeline-traffic-test` (19 088 672 B, built 2026-09-13T12:25Z) |
| 5 | `uv run python tools/opt115_pipeline_traffic.py --phase report --mode acceptance --run-dir build/optimization-runs/opt115` | 0 | `{"task": "OPT-115", "phase": "report", "ok": true}` |
| 6 | `uv run python -c "… validate_fixture(fx) …"` | 0 | `validate_fixture: OK`; claims false; `aa_verdict=repeatable`; D128 coverage 0.9658; D2048 0.9747; ranked OPT-117–122 present |

Formatting changes: **none** (ruff format left all files unchanged; tests not re-run for formatting).

#### Acceptance claim trace

| Dossier claim | Evidence | Verification |
|---|---|---|
| Machine-readable raw evidence + summary | `fixtures/opt115_pipeline_traffic.json` (170 507 B); raw sidecars in `build/optimization-runs/opt115/` (aa-*, long-*, request-trace, traffic-timeline, ranking, report) | `validate_fixture` OK; `report` acceptance `ok: true`; fixture has all `REQUIRED_FIXTURE_KEYS` |
| `evidence/optimization/opt115-pipeline-traffic/REPORT.md` | File exists (130 lines) | Content matches fixture: sitting identity, A/A repeatable, long-context probes, bytes table, timeline, bounds, ranking |
| Bytes/lifetime table | Fixture `traffic_map.d128.phases` + REPORT §Bytes/lifetime | `test_compulsory_bytes_counts_units_and_kv_scaling`: units=`bytes`, 8 phases, GDN 158 859 264 B, KV scales with prefix; REPORT weights 18 247 717 696 B/token peak 10.18 ms |
| Complete request timeline | Fixture `timeline.d128` / `timeline.d2048` | `test_interval_coverage_uses_unions_not_overlap_sums` + fixture: `overlap_not_summed=true`, D128 coverage 0.9658, D2048 0.9747; GPU busy/idle unions in REPORT |
| Pinned-source comparison | Fixture `source_comparison` | `test_source_comparison_and_ranking`: pinned llama `cc83d7b…`, inspection HEAD not pin, ds4 mechanisms indexed |
| Bounds with assumptions | Fixture `bandwidth_bounds` | `test_bandwidth_bounds_use_peak_not_sum`: 1792 GB/s peak, `independent_sum_not_a_bound=true`, `measured_sustainable_bandwidth_gb_s=null`; REPORT assumptions listed |
| Ranked OPT-117–122 go/no-go | Fixture `ranked_opportunities.ranked` | `test_source_comparison_and_ranking`: OPT-121 go (10.18 ms) … OPT-118 go; matches REPORT table order/verdicts |
| Focused host checks (counts, units, coverage, reproducibility) | `tests/test_opt115_pipeline_traffic.py` (10 tests) | All 10 passed: contracts, loop products, post113 auth, A/A repeatable, bytes/KV scaling, bounds, timeline unions, source/ranking, reproducibility hooks, scaffold validation |
| `claims_throughput=false` | Contract, fixture, REPORT | `test_contracts_makefile_and_native_hooks` + `test_validate_scaffold_fixture_when_present` |
| `claims_performance_improvement=false` | Contract, fixture, REPORT | Same tests; fixture root and ranked block both false |
| No production kernel/selector changes | `git diff` scope | Only `Makefile` (diagnostic target), `tools/run_optimization_task.py` (parse hooks), dossier/ledger. No changes to `cuda/full_scheduler.cu`, `cuda/q4k_decode_path.cuh`, `cuda/execution_graph_path.cuh`. `test_reproducibility_hooks_and_required_keys` asserts `llama_q4k_mmvq` / `ffn_only` selectors unchanged |

#### Diff boundary check

- **In scope delivered:** diagnostic binary `cuda/opt115_pipeline_traffic_test.cu`, runner `tools/opt115_pipeline_traffic.py`, contracts, fixture, tests, REPORT, Makefile diagnostic target, native-parse hooks.
- **Out of scope respected:** no production selector/dispatch edits; `production_kept=true`; `instrumentation_only=true` in iteration contract.
- **Ledger:** status remains `in_progress` (not marked done by verifier).

#### Verdict: **PASS**

All dossier acceptance artifacts are present, internally consistent, and validated by focused host tests plus `validate_fixture` / report acceptance. GPU diagnostic binary builds and target resolves under `qw38-cuda:13.0.2`. Repo-wide `ruff check .` fails on two **pre-existing** lint issues in OPT-088/089 files already in `HEAD`; they are outside the OPT-115 diff and do not affect OPT-115 file hygiene (scoped ruff clean). Host `make cuda-opt115-diagnostics` blocked by missing host `nvcc`; docker-equivalent gate passes with up-to-date binary.

### Final outcome (2026-09-13T12:43:43Z)

`diagnostic_delivered`: complete-request memory-traffic audit and conditional
performance bounds on fresh post113 control. Same-binary A/A is **repeatable**
(P4096 ~2986, D128 ~57.39, D2048 ~55.67 tok/s — calibration only).
Timeline unions cover **96.6%** (D128) and **97.5%** (D2048) of decode wall;
weight peak bound **10.18 ms** at listed 1792 GB/s (independent sums not a
bound). Long-context D8192/D32768/D131040 probes measured separately. Ranked
OPT-117–122 go/no-go published (OPT-121 weights 10.18 ms, OPT-117 launch/idle
9.97 ms, OPT-120 128K KV 4.79 ms not a D128 win). `claims_throughput=false`;
`claims_performance_improvement=false`; production selectors unchanged
(`llama_q4k_mmvq`, `ffn_only`). No tok/s delta to report.
