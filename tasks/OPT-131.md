# OPT-131 — Fuse one measured residual decode launch chain

Status: done. Dependencies: OPT-128, OPT-130. Coupled IDs: none.
Priority: P1.

## Run record

### Planning (2026-09-14T01:09:00Z)

skipped — pre-authored dossier admitted (coordinator)

### Implementation (cursor-grok-4.6-high, 2026-09-14T01:24:28Z)

NO-OPPORTUNITY. Parent is OPT-127 kept `decode_segments8` plus OPT-128
no-opportunity and OPT-130 reject (`hybrid_crossover@1024`, n_parts=16,
verified_max=4096). Profiled remaining adjacent producer/consumer chains in
`cuda/scheduler_primitives.cu`, `cuda/gdn_step.cu`, and `cuda/full_scheduler.cu`
on the graph parent versus eager. OPT-119 mixer/FFN norm→Q8 is retained and
not recreated. GDN chains stay unfrozen on OPT-109 isolated win / enclosing
loss. Last residual→logits_norm crosses the eight-layer cancellation poll.
Top remaining legal chain `mixer_residual_add_to_ffn_norm` has traffic upper
bound 0.026 ms/token at 100 GB/s versus freeze threshold 0.20 ms/token
(2% request materiality 0.327 ms/token). decode_segments8 already removed
3.17 ms/token of eager launch gap. Admitted candidates `[]`. No production
edit. Did not commit. model `cursor-grok-4.6-high`. telemetry_unavailable.

**Files created**
- `pins/opt131_decode_chain_contract.json`
- `pins/opt131_iteration_contract.json`
- `tools/opt131_decode_chain.py`
- `cuda/opt131_decode_chain_test.cu`
- `tests/test_opt131_decode_chain.py`
- `fixtures/opt131_decode_chain.json`
- `evidence/optimization/opt131-decode-chain/REPORT.md`

**Files modified**
- `Makefile` (`cuda-opt131-diagnostics`, `build/qw38-cuda-opt131-decode-chain-test`)
- `tools/run_optimization_task.py` (`QW38_OPT131_*` prefixes)
- `tasks/OPT-131.md` (this run record)

**Host validation**
```
uv run ruff format tools/opt131_decode_chain.py tests/test_opt131_decode_chain.py
# 1 file reformatted, 1 left unchanged
uv run ruff check tools/opt131_decode_chain.py tests/test_opt131_decode_chain.py tools/run_optimization_task.py
# All checks passed!
uv run pytest tests/test_opt131_decode_chain.py -q
# 5 passed in 0.03s
```

**GPU phases** (device NVIDIA GeForce RTX 5090, image `qw38-cuda:13.0.2`,
GGUF present). Sidecars in `build/optimization-runs/opt131/`.

```
docker run --rm --gpus all --user "$(id -u):$(id -g)" \
  -v /home/user/qw38:/workspace -w /workspace qw38-cuda:13.0.2 \
  make -j"$(nproc)" cuda-opt131-diagnostics
# ok (opt131 + opt058-quality + checkpoint + memory-fit)

uv run python tools/opt131_decode_chain.py --phase preflight --mode feedback --run-dir build/optimization-runs/opt131
# ok; parent OPT-127 kept decode_segments8; OPT-128 no-opportunity; OPT-130 reject

uv run python tools/opt131_decode_chain.py --phase identity --mode feedback --run-dir build/optimization-runs/opt131
# ok; 16 segment graphs; 1954 nodes; hybrid_crossover 1024/4096/n_parts=16; Q8 fusion on

uv run python tools/opt131_decode_chain.py --phase profile --mode feedback --run-dir build/optimization-runs/opt131
# ok; graph 16.370 ms/token; eager 19.544 ms/token; launch gap 3.174 ms/token

uv run python tools/opt131_decode_chain.py --phase occupancy --mode feedback --run-dir build/optimization-runs/opt131
# ok; occupancy_available=false (anonymous-namespace kernels); isolated events recorded

uv run python tools/opt131_decode_chain.py --phase lifetime --mode feedback --run-dir build/optimization-runs/opt131
# ok; greedy_valid, repeat logits, eval changes, restore, cancel isolated, subsequent request; lazy D2H 4 B

uv run python tools/opt131_decode_chain.py --phase cancellation --mode feedback --run-dir build/optimization-runs/opt131
# ok; mid_segment + commit; eight_layer cadence; atomic_commit=true

uv run python tools/opt131_decode_chain.py --phase equivalence --mode feedback --run-dir build/optimization-runs/opt131
# ok; graph_matches_eager=true; unchanged fallback

uv run python tools/opt131_decode_chain.py --phase rank --mode feedback --run-dir build/optimization-runs/opt131
uv run python tools/opt131_decode_chain.py --phase freeze --mode feedback --run-dir build/optimization-runs/opt131
# freeze admitted=[]; verdict=no_material_opportunity; threshold=0.20 ms/token; top bound=0.026 ms/token

uv run python tools/opt131_decode_chain.py --phase screen-d128 --mode feedback --run-dir build/optimization-runs/opt131
uv run python tools/opt131_decode_chain.py --phase screen-d2048 --mode feedback --run-dir build/optimization-runs/opt131
uv run python tools/opt131_decode_chain.py --phase screen-p4096 --mode feedback --run-dir build/optimization-runs/opt131
# skipped — no candidate admitted

uv run python tools/opt131_decode_chain.py --phase quality --mode acceptance --run-dir build/optimization-runs/opt131
# skipped; required=false; candidate_nll_measured=false (no complete-request A/B)

uv run python tools/opt131_decode_chain.py --phase state-memory --mode acceptance --run-dir build/optimization-runs/opt131
# ok; lifetime+cancel+128k_fit; checkpoint skipped_no_candidate

uv run python tools/opt131_decode_chain.py --phase d128 --mode acceptance --run-dir build/optimization-runs/opt131
uv run python tools/opt131_decode_chain.py --phase d2048 --mode acceptance --run-dir build/optimization-runs/opt131
uv run python tools/opt131_decode_chain.py --phase p4096 --mode acceptance --run-dir build/optimization-runs/opt131
# skipped — no candidate admitted

uv run python tools/opt131_decode_chain.py --phase report --mode acceptance --run-dir build/optimization-runs/opt131
# {"phase": "report", "ok": true} verdict=no_material_opportunity
```

**Measured critical-path (4-token decode, prefix 128)**

| Arm | wall ms/token | notes |
|---|---:|---|
| parent graph decode_segments8 | 16.370 | 16 segment graphs, 1954 nodes |
| eager PointwisePath::kFused | 19.544 | leaf timings recorded |
| launch gap already removed | 3.174 | OPT-127 graphs; not fusion savings |

Top remaining legal chain `mixer_residual_add_to_ffn_norm`: traffic upper
**0.026 ms/token** (64×20 KiB write+read at 100 GB/s). Freeze threshold
**0.20 ms/token**. 2% request materiality **0.327 ms/token**. OPT-058 quality
and 3+10 A/B were not required after the quantified bound.

Lazy greedy D2H remains **4 bytes**. Graph vs eager greedy tokens match.
Cancellation mid-segment and commit preserve frontier.

**Blockers:** none. Occupancy of anonymous-namespace production kernels is
unavailable; isolated CUDA-event times of the public launch APIs were
recorded. Nsight Systems still absent; graphs vs eager walls plus traffic
bounds are sufficient here.

### Verification (integration-verifier, 2026-09-14T01:25:56Z)

Independent integration verification against dossier acceptance, OPT-125
shared gates, and aggregated fixture artifacts. **Verdict: PASS**
(`no_material_opportunity`; parent `decode_segments8` unchanged; no production
fusion pin added).

**Host / build gates**
```
uv run ruff check tools/opt131_decode_chain.py tests/test_opt131_decode_chain.py tools/run_optimization_task.py
# All checks passed!

uv run pytest tests/test_opt131_decode_chain.py -q
# 5 passed in 0.03s

uv run python tools/opt131_decode_chain.py --phase report --mode acceptance --run-dir build/optimization-runs/opt131
# {"phase": "report", "ok": true}
```

**NO-OPPORTUNITY acceptance claims → artifacts**
| Claim | Artifact |
|---|---|
| Parent OPT-127 kept `decode_segments8` | `identity`: `parent=decode_segments8`, 16 segment graphs, 1954 nodes; `cuda/execution_graph_path.cuh`: `kSelectedExecutionGraphPath[] = "decode_segments8"` |
| OPT-128/OPT-130 parent stack preserved | `identity`: `poll_without_device_sync=false`, `defer_elapsed_event_sync=false`; `attention_decode_path.cuh`: `kSelectedDecodeAttentionCrossoverThreshold = 1024`, `kSelectedVec128NParts = 16`; OPT-119 Q8 fusion retained in `full_scheduler.h` |
| Profiled graph vs eager; launch gap already removed | `profile`: graph `16.370` ms/token; eager `19.544` ms/token; `launch_gap_ms_per_token=3.174`; lazy D2H `4` bytes in `lifetime` |
| Lifetime / cancellation / equivalence | `lifetime` ok; `cancellation` mid_segment+commit `atomic_commit=true`; `equivalence`: `graph_matches_eager=true`, `unchanged_fallback=true` |
| Ranked chains; OPT-109 GDN prior enforced | `rank`: top chain `mixer_residual_add_to_ffn_norm` bound `0.026` ms/token; GDN chain `freeze_legal=false` (OPT-109 enclosing loss) |
| Freeze threshold not met; no candidate admitted | `freeze`: `admitted_candidates=[]`, `verdict=no_material_opportunity`, threshold `0.2` ms/token; top bound `0.026` ms/token |
| OPT-058 quality skipped (no admitted candidate) | `quality`: `required=false`, `reason=no_decode_chain_candidate_admitted_to_complete_request_ab` |
| OPT-125 state/memory guards | `state_memory` ok (`lifetime_ok`, `cancellation_ok`, `128k_fit_with_graphs`, `checkpoint_ok`/`skipped_no_candidate`) |
| Baseline unchanged | `performance`/`report`: `tok_s_delta.speedup=0.0`, `baseline_unchanged=true`, `shipping_decode_chain_fusion=none` |
| REPORT published | `evidence/optimization/opt131-decode-chain/REPORT.md`; metrics match `fixtures/opt131_decode_chain.json`/`rank`/`freeze` summaries |

### Delivery (2026-09-14T01:27:00Z)

model: composer-2.5, telemetry_unavailable

OPT-131 marked `done`. Coupled IDs: none. Production selector unchanged
`decode_segments8`; no decode-chain fusion pin added. Next eligible pending
task: **OPT-132** (already `in_progress`).

### Final outcome

- Status: `done`
- Verdict: `no_material_opportunity`
- Acceptance evidence: [`fixtures/opt131_decode_chain.json`](../fixtures/opt131_decode_chain.json); [`evidence/optimization/opt131-decode-chain/REPORT.md`](../evidence/optimization/opt131-decode-chain/REPORT.md)
- Throughput delta versus sitting `decode_segments8` baseline: D128/D2048/P4096 tok/s delta **0.0**; speedup **0.0**; `baseline_unchanged=true`. Top remaining chain traffic upper bound **0.026 ms/token** (freeze threshold **0.20 ms/token**); launch gap **3.17 ms/token** already removed by OPT-127 graphs.
- First-pass acceptance: yes
- Remaining risk: OPT-132 owns combined decode-stack admission and corrected headroom

## Outcome

Address a remaining short-kernel dependency chain only if true activity traces
show material request cost after graph/host and attention work. Read OPT-125.
Freeze a parent containing admitted dependencies; no assumed additive gains.

## Implementation

1. Profile the retained parent and identify one adjacent producer/consumer
   chain in `cuda/scheduler_primitives.cu`, `cuda/gdn_step.cu` or
   `cuda/full_scheduler.cu`. Rank avoided launches and intermediate traffic
   on the critical path. Reuse OPT-119's existing norm→Q8 fusion; do not
   recreate it. OPT-109's isolated GDN win and enclosing loss are required
   prior evidence if a GDN chain is chosen.
2. Freeze at most two implementations of that single chain. Preserve
   intermediate rounding and operation order needed by the quality contract;
   explicitly test any fused rounding difference. No weight/KV/state precision
   change, recurrence rewrite, new general fusion framework or full-layer
   persistent kernel.
3. Prove buffer ownership, aliasing and candidate/committed state separation.
   Respect graph dynamic inputs and cancellation boundaries; do not fuse
   across a required cancellation or commit boundary.
4. Screen component, enclosing branch and complete requests. Measure register
   pressure/occupancy where available; saved launches may be offset by slower
   kernels. If graphs already removed the causal gap or the quantified bound
   is negligible, complete with no-material-opportunity and no production edit.

## Acceptance

Apply OPT-125's quality/state/memory/request gates; add focused equivalence
checks for the changed chain and unchanged fallback. Deliver
`evidence/optimization/opt131-decode-chain/REPORT.md` with causal upper
bound, candidate data and retained decision. A kernel-only speedup is not
a keep.

