# OPT-085 — Re-evaluate Q4 decode candidates

Status: done. Dependencies: OPT-075, OPT-076, OPT-082, OPT-084. Coupled IDs: none.

## Run record

### Planning (2026-09-11T21:46:00Z)

skipped — pre-authored dossier admitted (coordinator)
Priority: P0. Target: at most one Q4 production path under the new ladder.
Read [the post-080 protocol](PERFORMANCE-RECOVERY-POST-080.md).
Quality risk: changed Q4 association/reduction; staging typedness stays fixed.
Instrumentation: actual eager/graph dispatch; repaired OPT-071 replay.

### Implementation (2026-09-11T22:05:00Z)

No commit. Agent/model: cursor-grok-4.6 (`telemetry_unavailable`).
`opt074_coverage_unadmitted` is not a keep blocker. At most one Q4 path
selected: retain packed. `claims_throughput: false`. Speedup versus the
packed baseline is **0**.

Created: `tools/opt085_q4_reevaluation.py`,
`pins/opt085_iteration_contract.json`,
`pins/opt085_q4_reevaluation_contract.json`,
`tests/test_opt085_q4_reevaluation.py`,
`fixtures/opt085_q4_reevaluation.json`,
`evidence/optimization/opt085-q4-reevaluation/REPORT.md`.

Changed: `Makefile` (`cuda-opt085-diagnostics` → OPT-082 parity test,
component replay, engine probe, OPT-058 quality diagnostic),
`tools/run_optimization_task.py` (`QW38_OPT085_*` observation prefixes).

Historical OPT-075/076 reports unmodified. Capture identity shared with
those sittings (`5fdf3368e3a1898547bf5ecbe61333b4eaecdc9e4b70d97cdffce2edf7d20920`).

Independent verdicts (this sitting):

| Config | kernel_parity_pass | model_quality_pass | performance_pass | production_kept |
|---|---|---|---|---|
| packed_paired_staged | true | true | false | true (retained) |
| integer_q8_paired | true | false (incomplete candidate NLL) | false | false |
| late_w4 | true | false (incomplete candidate NLL) | true | false |

Kernel parity: OPT-082 Q4 ident catalog (26 cases) this sitting; packed
association pass with documented same-math miss
`Q4_K_fused_vs_independent_M3_N1_K256_random_same`; integer_q8 and
integer_q8_late association 6/6. Typed `sum_q` vs `sum_x` rejected.

Quality: identity-cached OPT-084 `--quality` freeze. Packed vs shipping
baseline zero-delta pass. Candidate-specific teacher-forced NLL was not
measured, so candidate `model_quality_pass` is incomplete (not pass). A
keep requires all three gates.

Performance (acceptance n=10, this sitting): complete 64-layer rotating
FFN packed 16.394 ms vs `late_w4` 9.815 ms; paired CI 6.555 .. 6.602 ms
(positive). D2048+32 five pairs: 873.359 vs 641.469 ms, E2E non-regression.
Feedback screen (1+3): packed 16.332 / integer_q8_paired 11.641 /
late_w4 9.814 ms. Historical ranking identity remains valid; keep still
blocked on incomplete candidate quality.

Commands and outcomes:

```
uv run pytest -q tests/test_opt085_q4_reevaluation.py
```

15 passed.

```
uv run python tools/run_optimization_task.py --task OPT-085 --mode feedback --phase parity
```

passed (`status=passed`, product=26, elapsed_s≈3.29, run_dir
`build/optimization-runs/OPT-085/feedback/20260911T220007Z-9249a999`).

```
uv run python tools/run_optimization_task.py --task OPT-085 --mode feedback --phase q4
```

passed (`status=passed`, product=12, elapsed_s≈13.14, run_dir
`build/optimization-runs/OPT-085/feedback/20260911T220018Z-06c2ff73`).

```
uv run python tools/run_optimization_task.py --task OPT-085 --mode acceptance --phase quality
```

passed (`status=passed`, product=7, elapsed_s≈0.51, run_dir
`build/optimization-runs/OPT-085/acceptance/20260911T220044Z-094358e1`).

```
uv run python tools/run_optimization_task.py --task OPT-085 --mode acceptance --phase q4
```

passed (`status=passed`, product=26, elapsed_s≈22.18, run_dir
`build/optimization-runs/OPT-085/acceptance/20260911T220049Z-c62312cd`).

**Verdict: packed retained.** `late_w4` wins component/E2E performance after
kernel parity, but is not `production_kept` because candidate quality is
incomplete. Tok/s delta versus packed baseline: **0** (shipping unchanged).

## Outcome

Reopen OPT-075 cooperative integer Q4, OPT-076 late-reduction Q4, and current
packed control. First gate is OPT-082 kernel parity. Do **not** require
OPT-074 production-family admission or Quartz-vs-llama production projections.
`opt074_coverage_unadmitted` is not a blocker. Issue independent kernel-parity,
quality, performance, and keep/revert verdicts. Select at most one Q4
production path.

Historical complete-FFN signals (identity must be reconfirmed if used for
ranking): packed ~16.43 ms vs `integer_q8_paired` ~11.77 ms; packed ~16.42 ms
vs `late_w4` ~9.82 ms.

## Implementation

1. Configurations, no new fusion/tile/compiler/half-scale grid:
   - control: shipping `packed` / `paired_staged`;
   - OPT-075 survivor: `integer_q8` / `paired_integer`;
   - OPT-076 survivor: `integer_q8_late` / `paired_integer` `late_w4`.
   Four warps/row and FP32-scale Q8Block stay fixed. Half-scale Q8_1 remains
   forbidden.
2. Ladder A/B: structural correctness (hard invariants, typed staging,
   `sum_q` vs `sum_x` pairing). Then OPT-082 Q4_K CPU-dequant association plus
   same-math checks (paired vs separate, fused vs unfused, eager vs graph).
   Candidates that fail kernel parity are rejected before quality.
3. Survivors run the OPT-083/084 quality suite with `--quality`. Compare
   against the OPT-084 shipping Quartz baseline (primary) and pinned llama
   (inspectable). A known baseline defect does not reject a candidate that
   does not worsen it. A meaningful NLL/PPL/recurrence/continuation
   regression rejects even if parity passed.
4. Then existing component/E2E methodology: complete 64-layer rotating FFN
   on repaired OPT-071 captures; 3+10 AB/BA; five D2048+32 pairs; separate
   graph capture per configuration; prove actual launches. Existing OPT-075/076
   timings may rank only if source/selector/capture identity remains valid;
   a keep requires this task's specified fresh confirmation.
5. Select at most one Q4 path. If no candidate beats packed on performance
   after passing A–C, retain packed. Reports must include:
   `kernel_parity_pass`, `model_quality_pass`, `performance_pass`,
   `production_kept` as independent booleans. Do not use OPT-074 unadmitted
   as a reason.

## Acceptance

- Each configuration has the four independent verdicts.
- Packed remains the control; a keep requires parity + quality + measured
  complete-FFN saving with a positive paired CI and E2E non-regression.
- Historical OPT-075/076 reports are unmodified.
- `claims_throughput` is true only if `production_kept` and the measured
  tok/s/component evidence is this sitting's.

## Tests and commands

```sh
uv run pytest -q tests/test_opt085_q4_reevaluation.py
uv run python tools/run_optimization_task.py --task OPT-085 --mode feedback --phase parity
uv run python tools/run_optimization_task.py --task OPT-085 --mode feedback --phase q4
uv run python tools/run_optimization_task.py --task OPT-085 --mode acceptance --phase quality
uv run python tools/run_optimization_task.py --task OPT-085 --mode acceptance --phase q4
```

Feedback parity reuses OPT-082 Q4 cases for the three idents. Feedback q4:
three configs × (1+3) complete rotating FFN, one D2048+32 pair for the
screen survivor. Acceptance quality: identity-cached OPT-084 suite on the
survivor(s). Acceptance q4: 3+10 FFN pairs and five engine pairs. Stop on
structural fail, parity fail, quality regression, negative complete saving,
or fallback-measured-as-candidate. Do not call OPT-074 admission.

## Non-goals

No Q8/MMQ decision (OPT-086). No half-scale Q8_1. No new Q4 kernel. No
OPT-074 rerun. No OPT-088 release claim.

## Verification (2026-09-11T22:04:10Z)

- Attempt: 1
- Agent/model: composer-2.5 (`telemetry_unavailable`)

```
uv run ruff format .
```

196 files left unchanged.

```
uv run pytest -q tests/test_opt085_q4_reevaluation.py
```

15 passed in 0.04s.

```
uv run python tools/run_optimization_task.py --task OPT-085 --mode feedback --phase parity
```

passed (`status=passed`, product=26, `claims_throughput=false`, elapsed_s≈1.22,
run_dir `build/optimization-runs/OPT-085/feedback/20260911T220319Z-f1c80478`).

```
uv run python tools/run_optimization_task.py --task OPT-085 --mode feedback --phase q4
```

passed (`status=passed`, product=12, `claims_throughput=false`, elapsed_s≈13.10,
run_dir `build/optimization-runs/OPT-085/feedback/20260911T220322Z-93af6f98`).

```
uv run python tools/run_optimization_task.py --task OPT-085 --mode acceptance --phase quality
```

passed (`status=passed`, product=7, `claims_throughput=false`, elapsed_s≈0.52,
run_dir `build/optimization-runs/OPT-085/acceptance/20260911T220336Z-342aafd2`).

```
uv run python tools/run_optimization_task.py --task OPT-085 --mode acceptance --phase q4
```

passed (`status=passed`, product=26, `claims_throughput=false`, elapsed_s≈22.14,
run_dir `build/optimization-runs/OPT-085/acceptance/20260911T220339Z-8b625096`).
First attempt hit a transient GPU lock when run in parallel with quality;
immediate retry passed.

**Verdict: PASS**

### Delivery (2026-09-11T22:04:10Z)

- Agent/model: composer-2.5 (`telemetry_unavailable`)
- Marked OPT-085 `done`. Coupled IDs: none. `plan.md` unchanged. OPT-016 and
  OPT-056 stay blocked. Next eligible pending by dependency order: OPT-086.
- `claims_throughput: false`. Tok/s delta versus packed baseline: **0**.

### Final outcome (2026-09-11T22:04:10Z)

Independent kernel-parity, quality, performance, and keep/revert verdicts for
packed vs `integer_q8_paired` vs `late_w4`. **Packed retained**
(`production_kept=true` for `packed_paired_staged` only). `late_w4` wins
component/E2E performance but is not `production_kept` because candidate
quality is incomplete. `integer_q8_paired` rejected on incomplete candidate
quality. Verdict: **packed retained**.
