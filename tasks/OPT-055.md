# OPT-055 — Capture measured remaining launch overhead

Status: in_progress. Dependencies: OPT-054. Coupled IDs: none. Supersedes pending OPT-031.
Primary target: D2048, plus P only where profiling justifies prompt capture.
Read [the shared protocol](PERFORMANCE-RECOVERY-2026-09-10.md).

## Scope and decision

Revisit OPT-043 launch/wait attribution after the compute tasks. Old OPT-031
would capture exact-4096 mixer/GDN beside FFN graphs without first knowing the
remaining benefit. This task covers that work at the **selected** internal batch
size and additionally addresses decode coverage. If CPU submission gaps are
below measurement noise, record a no-change result with evidence; do not build
graphs for an assumed speedup. Compare public Session/server cancellation-enabled
execution as well as the bare kernel oracle, since a non-null poll callback
currently permits per-layer `cudaDeviceSynchronize`.

## Implementation if justified

Extend `SchedulerGraphs` in `cuda/full_scheduler.{h,cu}` with stable-address
decode layer segments containing mixer/projections/core/FFN, then benchmark
larger segments up to a full token. Put changing token/position/frontier values
in a preallocated device parameter block. For context-dependent grid sizes use
small bounded position buckets or node parameter updates; do not allocate a
graph for every context position or process all 128K KV for a short token.

Keep D2H logits, successful state publication, and cancellation decisions outside
the captured candidate computation. Select bounded segments when full-token
capture violates existing cancellation latency. Avoid CUDA synchronization at
every layer when polling can occur at a safe already-completed segment boundary;
do not remove cancellation or publish partially completed state. Preserve
errors, timeouts and explicit stream/event dependencies.

For prompt, capture stable mixer/GDN work only at OPT-054's selected physical
batch size; keep candidate KV indexing/position updates correct. Start with
reuse of selected-shape FFN graphs. No generic graph framework. References:
pinned llama `ggml/src/ggml-cuda/ggml-cuda.cu` graph machinery and ds4
`ds4_cuda.cu` `g_decode_graph_stream` capture,
launch and invalidation logic. Dynamic model policies in ds4 are out of scope.

## Acceptance

Require graph/eager equality within the same arithmetic path; prompt/decode
cross-path quality is already covered by OPT-044. Test token changes, cache
frontier growth, reset/restore, partial tails, graph invalidation, cancellation
at each admitted boundary and injected failure before publication. Count graph
nodes/launches and measured idle/waits; include parameter uploads, graph update
and D2H costs in timing. Exclude initial capture only where the frozen protocol
already creates graphs before timing; report first-request setup separately.
Update allocation ledger and verify 128K post-graph reserve.
Add task contract/fixture, `tests/test_opt055_execution_graphs.py`, and
`evidence/optimization/opt055-execution-graphs/`. Run new host/native test,
`tests/test_cuda_graph.py`, `tests/test_cuda_prompt_graph.py`,
`tests/test_cuda_atomic_eval.py`, `tests/test_cuda_prefix_sync.py`,
`tests/test_cuda_checkpoint.py`, `tests/test_cuda_memory_fit.py`,
`tests/test_server.py`, and shared P/D acceptance.

## Run record

### Planning

- Agent/model: coordinator (pre-authored dossier admitted)
- UTC/time/tokens/cost: 2026-09-10T22:30:00Z; telemetry_unavailable
- Outcome: skipped — pre-authored dossier admitted

### Implementation

- Agent/model: cursor-grok-4.6 (implementation; Task slug cursor-grok-4.6-high unavailable)
- UTC/time/tokens/cost: 2026-09-11T01:21:59Z; telemetry_unavailable
- Changes: `SchedulerGraphs` node counts, host `GraphLaunchParams` (token/position/frontier/2048-row KV bucket), empty decode-segment and prompt-mixer slots, pin `kSelectedExecutionGraphPath` `ffn_only`; exclusive A/B of remaining `other_idle` on D128/D2048 and 4096-row prompt with poll versus null; contract/fixture/host test/evidence.
- Commands:
  - exclusive A/B sitting: winner `ffn_only`; below_noise true; D128 graphs idle 0.0855464935 ms / 25.5044994 ms wall; D2048 idle 0.105142593 ms / 27.6636486 ms; prompt idle 0 ms / 1422.6759 ms; graph/eager equal; poll-8 cancel frontier 0; 384 decode + 448 prompt FFN nodes; 16,777,216 graph bytes
  - copied OPT-054 P 2895.42773, D128 37.5605927, D2048 35.7286987; speedup 0
- Outcome: measured no-change; layer-segment mixer/core graphs stay unpopulated.

### Documentation

- Agent/model: cursor-grok-4.6 (same session; composer-2.5 Task unavailable)
- UTC/time/tokens/cost: 2026-09-11T01:21:59Z; telemetry_unavailable
- Changes: handbook live numbers in `docs/53-stable-address-cuda-graphs.md`, `docs/06-system-optimization.md`, `docs/62-cuda-full-prefill.md`, `docs/54-post-graph-128k-memory.md`, `docs/sources.md`, `docs/README.md`, `docs/65-documentation-audit.md`; report/fixture/contract written.
- Outcome: proof-limit phrases present in REPORT; unique-ID hygiene retained

### Verification

- Agent/model: coordinator (delivery verification)
- UTC/time/tokens/cost: 2026-09-11T01:30:00Z; telemetry_unavailable
- Commands:
  - `uv run ruff format .` + `uv run ruff check .` → pass
  - `uv run pytest -q tests/test_opt055_execution_graphs.py tests/test_documentation.py` → 7 passed, 1 skipped
- Verdict: OPT-055 measured no-change accepted; delivery authorized

### Delivery

- Agent/model: coordinator (delivery)
- UTC/time/tokens/cost: 2026-09-11T01:30:00Z; telemetry_unavailable
- Commands:
  - `uv run ruff format .` + `uv run ruff check .` → pass
  - `uv run pytest -q tests/test_opt055_execution_graphs.py tests/test_documentation.py` → 7 passed, 1 skipped
- Outcome: ruff and pytest smoke pass confirmed. Marked OPT-055 `done`. Final audit row published. Commit and push authorized.

### Retries and escalation

workspace-mismatch on first two native A/B attempts (cancel/tail/decode poll used graphs bound to another workspace); repaired to share the bound workspace. Third sitting passed.

### Final outcome

- Status: `done` (measured no-change)
- Acceptance evidence: live fixture
  [`fixtures/opt055_execution_graphs.json`](../fixtures/opt055_execution_graphs.json)
  (`measurement_utc` 2026-09-11T01:21:59Z), contract
  [`pins/opt055_execution_graphs_contract.json`](../pins/opt055_execution_graphs_contract.json),
  report
  [`evidence/optimization/opt055-execution-graphs/REPORT.md`](../evidence/optimization/opt055-execution-graphs/REPORT.md)
- Throughput delta: baseline OPT-054
  [`fixtures/opt054_prefill_microbatch.json`](../fixtures/opt054_prefill_microbatch.json)
  P **2895.43**, D128 **37.56**, D2048 **35.73** tok/s → post OPT-055 P
  **2895.43** (+0, 1.000×), D128 **37.56** (0, 1.000×), D2048 **35.73**
  (0, 1.000×). Speedup **0** (measured no-change; remaining idle below noise).
- Commit: delivered 2026-09-11T01:30:00Z
- Push: authorized
- First-pass acceptance: **yes** (ruff + pytest smoke + measured fixture reuse)
- Remaining risk: GPU exclusivity remains mandatory for a native re-sit. Do
  not treat this no-change as the 2K llama.cpp parity gate. Next eligible recovery
  task: OPT-056.
- Agent/model for this sitting: coordinator; telemetry_unavailable
