# OPT-075 — Admit or reject the existing cooperative Q4 FFN

Status: done. Dependencies: OPT-070, OPT-073, OPT-074. Coupled IDs: none.

## Run record

### Planning (2026-09-11T16:25:00Z)

skipped — pre-authored dossier admitted (coordinator)
Priority: P1, first decode performance decision. Target: complete Q4 FFN.
Read [the post-069 protocol](PERFORMANCE-RECOVERY-POST-069.md).
Quality risk: changed reduction/association; Q8Block FP32 staging stays fixed.
Instrumentation: existing corrected replay plus actual eager/graph dispatch.

### Implementation (2026-09-11T17:26:05Z)

- Agent/model: implementation (Cursor Grok 4.6). No commit. Ledger left
  `in_progress`. `plan.md` unchanged. Production pins stay `packed` /
  `paired_staged`. `claims_throughput: false`. Historical 1.26×/1.40× remain
  hypotheses.
- Screened three configs, four warps/row, FP32-scale Q8Block: packed/
  paired_staged control; integer_q8 + shared_stage (separate gate/up);
  integer_q8 + paired_integer. OPT-062/063 kernels reused; no new fusion,
  tile, compiler, or half-scale grid.
- OPT-074 GPU-calibrated Q4 ceilings used; legacy 0.0003 recorded separately.
  This sitting original/staged abs 7.629e-06 / 0.0: legacy **pass**, versioned
  production **pass**. OPT-074 Q4 families remain v2-unadmitted
  (`held_out_exceeds_frozen_calibration_ceiling`). That unfinished freeze is
  **not** a numerical rejection.
- Complete 64-layer rotating FFN (norm, one shared gate/up stage, SwiGLU,
  down, residual) on OPT-071 capture
  `5fdf3368e3a1898547bf5ecbe61333b4eaecdc9e4b70d97cdffce2edf7d20920`.
  Feedback means: packed 16.353 ms, integer_q8_separate 12.775 ms,
  integer_q8_paired 11.666 ms. Survivor vs packed control:
  integer_q8_paired. Acceptance n=10: packed 16.433 ms vs paired 11.773 ms;
  paired CI (control−candidate) 4.618 .. 4.703 ms (positive). Engine five
  D2048+32 pairs: 875.710 vs 717.444 ms; e2e non-regression true.
- Dispatch proven by launch variants (not frozen selector strings): packed
  `q4k_gate_up_swiglu_prequant` / `quant_mmv_packed`; separate
  `q4k_coop_mmv_prequant_q8` / `q4k_coop_mmv_q8`; paired
  `q4k_coop_gate_up_swiglu_prequant_q8` / `q4k_coop_mmv_q8`. 64/64 calls, two
  staging ops, Q8/Q6 selectors preserved, staging invalidation kept. Graphs
  recaptured after the override (`q4-ab`).
- OPT-073 identity-cached quality: v3 absolute **fail** (visible), engine
  non-regression **pass**. Verdict: **retain_packed** /
  `missing_evidence` (OPT-074 Q4 unadmitted). `production_kept=false`.
  tok/s delta vs packed baseline: **0** (shipping unchanged).
- Files changed:
  - `cuda/q4k_decode_path.cuh`
  - `cuda/ffn_decode_path.cuh`
  - `cuda/optimization_component_replay.cu`
  - `cuda/optimization_component_replay.h`
  - `cuda/optimization_engine_probe.cu`
  - `Makefile`
  - `tools/run_optimization_task.py`
  - `pins/opt075_q4_production_admission_contract.json`
  - `pins/opt075_iteration_contract.json`
  - `fixtures/opt075_q4_production_admission.json`
  - `tests/test_opt075_q4_production_admission.py`
  - `tools/opt075_q4_production_admission.py`
  - `evidence/optimization/opt075-q4-production-admission/REPORT.md`
  - `tasks/OPT-075.md`
- Commands:

```
uv run pytest -q tests/test_opt075_q4_production_admission.py
```

**pass** — 12 passed in 0.03s (host); 12 passed in 0.02s after GPU sitting.

```
uv run python tools/run_optimization_task.py --task OPT-075 --mode feedback --phase q4
```

**pass** — exit 0; `elapsed_s=47.048`; deadline 300s; product=12; three
configs × (1+3) rotating complete FFN plus one D2048+32 pair; survivor
`integer_q8_paired`; run
`build/optimization-runs/OPT-075/feedback/20260911T172514Z-9e99f0c0`.

```
uv run python tools/run_optimization_task.py --task OPT-075 --mode acceptance --phase quality
```

**pass** — exit 0; `elapsed_s=0.548`; deadline 300s; product=1;
identity-cached OPT-073; quality-v3 absolute fail, engine NR pass; run
`build/optimization-runs/OPT-075/acceptance/20260911T172615Z-ba726d83`.

```
uv run python tools/run_optimization_task.py --task OPT-075 --mode acceptance --phase q4
```

**pass** — exit 0; `elapsed_s=22.654`; deadline 300s; product=26; 3+10
complete FFN pairs and five D2048+32 pairs; retain packed; run
`build/optimization-runs/OPT-075/acceptance/20260911T172622Z-57dff084`.

## Implementation

1. Reuse OPT-062/063 kernels. Screen exactly three configurations: shipping
   packed/paired_staged; integer_q8 with separate gate/up; integer_q8 with
   paired_integer gate/up/SwiGLU. Fix four warps/row and FP32-scale Q8Block.
   No new fusion, tile, compiler or half-scale grid. OPT-042/046 historical
   implementations are reviewed through their current equivalents, not rebuilt
   as redundant production variants.
2. Validate original and staged FP64 errors and total error against OPT-074's
   actual GPU-calibrated budgets on calibration then untouched held-out cases.
   Record strict legacy pass/fail separately. The old 0.000305175781 versus
   0.000300 comparison may remain failed while the versioned production rule
   passes; explain that decision explicitly instead of editing the old result.
3. Measure real complete FFN with all 64 layer weights/inputs, norm, one shared
   gate/up stage, SwiGLU, separate down stage, down and residual. Use the repaired
   OPT-071 capture/cache behavior. Choose one integer survivor against the
   current packed production control, not only against another candidate.
4. Prove every gate/up/down projection uses the intended path in eager and
   captured execution, one pair per layer and two staging operations per FFN.
   Preserve trace fallback, current Q8/Q6 selectors and staging invalidation.
   Recapture graphs after selecting the configuration; the old packed graph
   must not silently remain in the timed candidate.
5. Run complete candidate model quality under OPT-073's explicit internal
   policy with legacy absolute/OPT-056 results still visible. A functional
   baseline defect cannot excuse new output/state errors. Only after quality,
   complete component acceptance and short E2E guards pass may the selected
   integer configuration become production. Otherwise retain packed and record
   whether the reason is precision, missing evidence, performance or uncertainty.

## Tests and commands

Reuse `build/qw38-cuda-component-replay`, existing OPT-062/063 diagnostics and
`build/qw38-cuda-optimization-engine-probe`. Add behavioral host/evidence tests
in `tests/test_opt075_q4_production_admission.py`; avoid frozen selector-string
assertions. Tiny cases: M17/K256 and M33/K512 with zero, cancellation and tail
guards. Production references: 16 rows x 4 captures per gate/up/down case,
decoded once and shared across the three candidates.

Feedback: three configurations x (1+3) complete rotating rounds, then at most
one D2048+32 control/survivor pair. Acceptance is the common 3+10 complete
FFN pairs and five D2048+32 pairs. Full quality is a separate identity-cached
phase once per survivor; no long P/D protocol here.

```sh
uv run pytest -q tests/test_opt075_q4_production_admission.py
uv run python tools/run_optimization_task.py --task OPT-075 --mode feedback --phase q4
uv run python tools/run_optimization_task.py --task OPT-075 --mode acceptance --phase quality
uv run python tools/run_optimization_task.py --task OPT-075 --mode acceptance --phase q4
```

Stop on missing real GPU admission, nonfinite/layout/state error, held-out
quality failure, negative complete saving or inconclusive acceptance. Do not
call another unfinished reference freeze a numerical rejection. Deliver a
real production decision with uncertainty. Historical 1.26x/1.40x screens
are hypotheses, not expected or newly delivered tok/s.

## Verification (2026-09-11T17:28:23Z)

Independent integration verifier. `plan.md` unchanged.

### Diff vs dossier / ledger / plan

| Requirement | Status |
|---|---|
| `tools/opt075_q4_production_admission.py` | present (untracked) |
| `tests/test_opt075_q4_production_admission.py` | present (untracked), 12 tests |
| `pins/opt075_q4_production_admission_contract.json` | present (untracked) |
| `pins/opt075_iteration_contract.json` | present (untracked) |
| `fixtures/opt075_q4_production_admission.json` | present (untracked) |
| `evidence/optimization/opt075-q4-production-admission/REPORT.md` | present (untracked) |
| Scoped Q4 FFN production overrides | modified — `q4k_decode_path.cuh`, `ffn_decode_path.cuh`, replay/engine TUs |
| `tools/run_optimization_task.py` | modified — OPT-075 task registration |
| `Makefile` (`cuda-opt075-diagnostics`) | modified |
| Three configs screened (packed, integer_q8 separate, integer_q8 paired) | confirmed in REPORT |
| Complete 64-layer rotating FFN with dispatch proof | confirmed — 64/64 calls, two staging ops |
| Verdict **retain_packed** (`missing_evidence`) | confirmed — `production_kept=false` |
| Shipping unchanged (`packed` / `paired_staged`) | confirmed |
| No production pin change | confirmed |
| `plan.md` unchanged | confirmed (no diff) |

### Commands

```
uv run ruff check .
uv run ruff format --check .
```

**PASS** — all checks passed; 165 files already formatted.

```
uv run pytest -q tests/test_opt075_q4_production_admission.py
```

**PASS** — 12 passed in 0.03s.

Implementation-phase runner commands (feedback q4, acceptance quality, acceptance
q4) recorded above under Implementation; all passed within 300 s deadlines.

### Evidence spot-check

[`evidence/optimization/opt075-q4-production-admission/REPORT.md`](evidence/optimization/opt075-q4-production-admission/REPORT.md):
measured sitting; three configs screened; survivor `integer_q8_paired` with
positive complete FFN saving (4.66 ms mean); quality-v3 absolute fail, engine NR
pass; verdict **retain_packed** due to `opt074_coverage_unadmitted_missing_evidence`;
`shipping_unchanged=true`. tok/s delta **0** vs packed baseline
(`claims_throughput=false`).

### Verdict

**PASS** — host contract, OPT-074 GPU budgets, complete rotating FFN with
all-leg dispatch proof, identity-cached quality, and documented **retain_packed**
decision meet OPT-075 acceptance. No production pin change; no tok/s claim
(`claims_throughput=false`).

### Delivery (2026-09-11T17:28:23Z)

Delivery agent. Commit and push authorized. Marked OPT-075 `done` in ledger;
coupled IDs: none.

### Final outcome (2026-09-11T17:28:23Z)

Screened packed/paired_staged control, integer_q8 separate gate/up, and
integer_q8 paired_integer on repaired OPT-071 captures. Numeric legacy and
versioned production rules pass; OPT-074 Q4 families remain v2-unadmitted.
Complete FFN acceptance n=10: packed 16.433 ms vs paired 11.773 ms (positive
CI). Engine D2048+32 non-regression holds. Quality-v3 absolute fail, engine NR
pass. Verdict: **retain_packed** / `missing_evidence`; shipping stays
`packed` / `paired_staged`. Host contract and 12 pytest cases passed;
implementation feedback/acceptance phases passed within 300 s. Coupled IDs:
none. First-pass delivery. Retries: 0. No production pin change.
**tok/s delta vs packed baseline: 0** (`claims_throughput=false`).
