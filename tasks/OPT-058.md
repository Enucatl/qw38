# OPT-058 — Establish a valid finite scheduler and quality baseline

Status: done. Dependencies: OPT-057. Coupled IDs: none.

## Run record

### Planning (2026-09-11T05:28:00Z)

skipped — pre-authored dossier admitted (coordinator)
Primary target: remove ambiguous correctness blockers before numerical tuning.
Read [the batch protocol](PERFORMANCE-RECOVERY-2026-09-11.md).

### Implementation (2026-09-11T06:00:00Z)

Did not commit. Did not change `plan.md`. No throughput claim. No unrelated kernel tuning.

**Scheduler (finite).** Eager / FFN-graph / traced tokens `[42, 3649]` are all finite and byte-identical per token. Selectors: `q4=packed`, `q8=dp4a_q8_1`, `ffn=paired_staged`, `graph=ffn_only`, `rms=parallel_fma`. Traced path uses `execute_token_traced_bundle` (filter `*`) and records five taps in one traversal (layers 0/3/63 residual, `final_norm`, logits). Reproduced nonfinite: `SchedulerGraphs::create` capture left stale Q8 decode staging; eager reuse of that workspace yielded 248320 NaN logits. Fix: `SchedulerWorkspace::invalidate_q8_decode_staging()` after capture, with eager-after-capture regression. OPT-046 integer-pin all-NaN remains `unreproduced_on_current_packed_production`.

**Functional.** Token 271 decodes to `\n\n` (`0a0a`). Original eight prompts: Quartz and llama both greedily emit 271 first (prompt/scorer defect). V2 no-thinking chat template: 7/8 exact A–D on both engines; `task_arithmetic` is A (token 32) on both, expected B. Rendering matches `src/template.cpp` `enable_thinking=false`. Teacher-forced NLL is a separate operation.

**NLL.** Held-out llama reference frozen at `pins/production_quality_v2_llama_reference.json` (OPT-044 span 16385–17409). Quartz vs llama PPL ratios: wikitext 1.00007656, held-out 0.99978640; recurrence incremental NLL −0.00831433. QLT-001 1.05 and OPT-056 eight-task fail preserved. quality-v2 suite **fail** because arithmetic does not pass pinned llama; prerequisite blocked on that item.

#### Command outcomes

```
uv run pytest -q tests/test_opt058_quality_baseline.py tests/test_quality.py
```
exit 0; **16 passed**. Earlier sitting before fixture/report: 15 passed, 1 skipped. With `tests/test_optimization_loop.py` included: 31 passed.

```
uv run python tools/run_optimization_task.py --task OPT-058 --mode feedback --phase scheduler
```
exit 0; `status=passed`; `elapsed_s=10.64` then later cache-hit sittings; run `build/optimization-runs/OPT-058/feedback/20260911T054640Z-a483c712`; all six mode×token logits finite; SHA-256 token 42 `f0a6d8fb50d240af6783a42fd75fe327962485d8e2e8e55175df5f83b08db8af`, token 3649 `1351ade64e734b149abe059c464c8acddd909a1aa462254e5faf8fc819caf694`.

```
uv run python tools/run_optimization_task.py --task OPT-058 --mode feedback --phase functional
```
First sitting failed (`20260911T054653Z-495f1422`): llama `cudaMalloc` OOM while Quartz still resident; `mma_quality_q4_attrs` also split JSON. After releasing Quartz before llama, stop at 248046/248044, JSON-escape decoded text, and capture llama stdout to a file: exit 0; `20260911T054836Z-7c7f0cb7` then clean `20260911T060026Z-5274d90c`; `elapsed_s≈9.53`; `status=passed` (generation finite; parser still `wrong_choice` on arithmetic). Smoke `20260911T060025Z-7e5cfae0` also passed (`elapsed_s=0.85`).

```
uv run python tools/run_optimization_task.py --task OPT-058 --mode acceptance --phase quality-baseline
```
exit 0; `status=passed`; `elapsed_s=78.00`; deadline plan 7200 s; run `build/optimization-runs/OPT-058/acceptance/20260911T055652Z-2c969791`; Quartz mean NLL wikitext 1.5252005926497396, held-out 1.7878782710057632, recurrence_short 1.771902140891233, recurrence_long 1.8354702048193376.

Host `run_llama_quality_reference.py` without Docker failed (`exit 1`): `libcudart.so.13` missing. Oracle in `qw38-llama-authority:cuda-13.0.2` against `pins/production_quality_v2_nll.bundle` exit 0 in 49 s (2304 `step` lines). `--from-log` freeze wrote `pins/production_quality_v2_llama_reference.json`.

Original eight-prompt diagnosis (native `--prompt-set original`): exit 0; both engines first token 271 on all eight cases. Evidence under `evidence/optimization/opt058-quality-baseline/`.

**Blocker.** `task_arithmetic` fails on pinned llama and Quartz together (A vs expected B). Do not loosen the parser or mask logits. Scheduler finite and held-out NLL complete.

### Verification (2026-09-11T06:04:00Z)

Independent integration verifier. Did not commit. Reviewed diff against dossier acceptance, `implementation_ledger.md` OPT-058 row, and `plan.md` (unchanged; no throughput claim).

**Deliverables present:** `pins/opt058_quality_baseline_contract.json`, `pins/production_quality_v2_inputs.json`, `pins/production_quality_v2_llama_reference.json` (held-out `held_out_wikitext_1024` frozen), `fixtures/opt058_quality_baseline.json`, `evidence/optimization/opt058-quality-baseline/REPORT.md`, scheduler Q8-staging fix in `cuda/full_scheduler.cu`, native `build/qw38-cuda-opt058-quality-baseline-test`, `tests/test_opt058_quality_baseline.py`.

**Acceptance path:** prerequisite blocked on `task_arithmetic` (pinned llama and Quartz both emit A/token 32; expected B). Exact defect documented in REPORT, fixture `blocker`, and dossier. NLL/held-out relative scores complete within v2 bounds; quality-v2 suite `fail` is expected and does not invalidate the blocked-prerequisite outcome.

#### Command outcomes

```
uv run ruff format .
```
exit 0; `137 files left unchanged`.

```
uv run ruff check .
```
exit 0; `All checks passed!`.

```
uv run pytest -q tests/test_opt058_quality_baseline.py tests/test_quality.py
```
exit 0; **16 passed** in 0.04s.

```
uv run python tools/run_optimization_task.py --task OPT-058 --mode feedback --phase scheduler
```
exit 0; `status=passed`; `elapsed_s=3.41`; run `build/optimization-runs/OPT-058/feedback/20260911T060208Z-fecfe505`; six mode×token logits finite (cache hit on compile).

```
uv run python tools/run_optimization_task.py --task OPT-058 --mode feedback --phase functional
```
exit 0; `status=passed`; `elapsed_s=8.73`; run `build/optimization-runs/OPT-058/feedback/20260911T060213Z-97921cb6`; generation finite; parser `wrong_choice` on `task_arithmetic` unchanged.

```
uv run python tools/run_optimization_task.py --task OPT-058 --mode acceptance --phase quality-baseline
```
exit 0; `status=passed`; `elapsed_s=67.23`; deadline 7200 s; run `build/optimization-runs/OPT-058/acceptance/20260911T060223Z-8e5a462e`; Quartz NLL wikitext 1.5252005926497396, held-out 1.7878782710057632.

**Verdict: PASS** — finite scheduler baseline established, v2 prompts and held-out llama reference frozen, relative NLL scores complete; functional prerequisite honestly blocked on `task_arithmetic` with exact unresolved defect (not incomplete work).

### Delivery (2026-09-11T06:04:25Z)

- Agent/model: composer-2.5 (delivery)
- UTC/time/tokens/cost: 2026-09-11T06:04:25Z; telemetry_unavailable
- Commands:
  - verifier PASS confirmed from Run record → Verification (2026-09-11T06:04:00Z)
- Outcome: Marked OPT-058 `done`. Final ledger history and audit row published.
  Commit and push authorized.

### Final outcome

- Status: `done` (finite scheduler + v2 quality baseline; no throughput claim)
- Acceptance evidence: fixture
  [`fixtures/opt058_quality_baseline.json`](../fixtures/opt058_quality_baseline.json),
  contracts
  [`pins/opt058_quality_baseline_contract.json`](../pins/opt058_quality_baseline_contract.json)
  and
  [`pins/opt058_iteration_contract.json`](../pins/opt058_iteration_contract.json),
  held-out reference
  [`pins/production_quality_v2_llama_reference.json`](../pins/production_quality_v2_llama_reference.json),
  report
  [`evidence/optimization/opt058-quality-baseline/REPORT.md`](../evidence/optimization/opt058-quality-baseline/REPORT.md)
- Throughput delta: none (`claims_throughput=false`)
- Functional blocker: `task_arithmetic` — Quartz and pinned llama both emit A
  (token 32); expected B; documented in fixture `blocker` and REPORT
- Scheduler: six mode×token logits finite; SHA-256 token 42
  `f0a6d8fb50d240af6783a42fd75fe327962485d8e2e8e55175df5f83b08db8af`, token 3649
  `1351ade64e734b149abe059c464c8acddd909a1aa462254e5faf8fc819caf694`
- NLL: wikitext 1.5252005926497396, held-out 1.7878782710057632 (frozen llama
  reference); PPL ratios within v2 bounds
- Commit: pending delivery 2026-09-11T06:04:25Z
- Push: authorized to `origin/main`
- First-pass acceptance: **yes** (ruff + pytest + live scheduler/functional/quality-baseline)
- Remaining risk: `task_arithmetic` blocks full quality-v2 pass until prompt/model
  defect is resolved; do not loosen parser or mask logits. Next eligible recovery
  task: OPT-059.
- Agent/model for this sitting: composer-2.5; telemetry_unavailable

## Evidence and entry points

OPT-046 reports NaNs with retained packed MMV as well as its rejected candidate.
`cuda/full_scheduler_test.cu` executes a token separately for each trace filter.
OPT-056's eight functional checks all emit token 271, but the inputs from
`tools/freeze_quality_inputs.py` are bare completion instructions and
`tools/run_llama_quality_reference.py::CASES` excludes them. The held-out llama
NLL is also absent. These are three separate questions; do not hide one by
loosening a numerical bound.

Read `cuda/full_scheduler.cu`, `cuda/full_scheduler_test.cu`,
`src/diagnostic_trace.h`, `src/template.cpp`, `tools/qw38_quality.py`,
`tools/run_llama_quality_reference.py`, `tools/llama_authority/quality_oracle.cpp`,
and `evidence/optimization/opt046-q4-decode/REJECTION.md` before editing.

## Implementation

1. Build current release and traced scheduler objects through OPT-057. Record
   all effective selectors, binary/flag identities and outputs for tokens
   `[42, 3649]`. In a focused baseline diagnostic, compare eager, FFN graph,
   and traced eager execution, resetting to identical state. Capture all needed
   residual taps during one token traversal, not one traversal per filter.
2. On the first nonfinite, narrow to the first failing layer and boundary:
   input norm, mixer projections, GDN/attention output, residual, FFN norm,
   gate/up, activated, down, final residual. Dump finite counts, first index
   and input hashes. Check activation staging cache invalidation, normalized
   buffer reuse, trace callback lifetimes and workspace initialization. Compare
   to a fresh untraced run. Use one affected layer with saved inputs for diagnosis;
   no repeated 64-layer scalar oracle. Fix the minimal reproduced cause; do not
   assume the old rejection still describes current production.
3. If all current modes are finite, retain that reproduction and classify the
   old failure as historical or unreproduced. Do not invent a fix. Preserve the
   old fixture. Make current nonfinite failures hard errors in the new probe,
   with a behavioral regression for any diagnosed code defect.
4. Run the exact eight original functional token sequences on pinned llama
   and Quartz. Decode token 271 with the pinned tokenizer; record the next
   token, top-two logits and up to 16 free-running tokens per case. This is
   diagnosis, not a new expected answer. If llama also emits formatting text,
   establish the prompt/scorer defect rather than attributing it to CUDA dots.
5. Freeze `pins/production_quality_v2_inputs.json` using the actual supported
   no-thinking chat template (`src/template.cpp` and its fixtures), with an
   explicit assistant answer boundary and the same eight questions/answers.
   Generate at most 16 tokens, strip only leading/trailing whitespace, and
   accept exactly one of A/B/C/D matching the fixed answer. Reject extra words,
   empty output, a second answer or answer-changing parser shortcuts. Do not
   mask logits to the four choices. Run pinned llama first; if it cannot pass
   a case, report invalid baseline and investigate rendering before tuning.
6. Keep teacher-forced NLL and free-running functional evaluation as different
   operations. Generalize the existing llama bundle writer to an explicit case
   list so it can score the eight original probes and both 1024-target spans.
   Do not alter the old seven-case reference in place. Freeze missing held-out
   llama scores on the OPT-044 offsets and hashes. Reuse the model across cases;
   prefill long contexts in chunks and score the exact target positions.
7. Add a v2 verdict that requires every named case and its reference. Missing
   held-out authority is `incomplete`, never passed by omission or finite-only
   checks. Keep PPL <=1.01 and recurrence incremental NLL <=0.02. Preserve old
   QLT-001 and OPT-056 verdicts alongside v2. Register this baseline in OPT-057
   so later candidates can reuse immutable reference outputs.

## Fast tests and bounded execution

Add `tests/test_opt058_quality_baseline.py`: finite versus nonfinite injected
tap, missing authority/case, empty/extra-text answer, valid whitespace, and
strict separation of teacher forcing from generated answers. Use tiny fake
logit arrays for parser/verdict tests. Run existing template tests only if its
implementation changes; normally this task uses its current behavior.

Native target: `build/qw38-cuda-opt058-quality-baseline-test`.
Smoke has no model: 4 parser/tap cases. Screen phase `scheduler` uses 2 tokens
× 3 execution modes, one model load, full finite checks and five saved taps.
Phase `functional` is 8 cases × 2 engines × at most 16 output tokens (256 output
tokens maximum). Phase `quality-baseline` is explicit one-time acceptance:
2 spans × 1024 scored targets plus the existing short/long recurrence pair,
one reference result per engine. No repetitions or throughput claims.

```sh
uv run pytest -q tests/test_opt058_quality_baseline.py tests/test_quality.py
uv run python tools/run_optimization_task.py --task OPT-058 --mode feedback --phase scheduler
uv run python tools/run_optimization_task.py --task OPT-058 --mode feedback --phase functional
uv run python tools/run_optimization_task.py --task OPT-058 --mode acceptance --phase quality-baseline
```

Each feedback phase has the shared 300-second deadline. Stop at the first
nonfinite and use the saved layer capture. The one-time complete quality freeze
is separately timed; do not run it on every edit. If baseline is invalid, retain
failure evidence and continue independent instrumentation, not arithmetic promotion.

## Acceptance

Deliver `pins/opt058_quality_baseline_contract.json`, v2 quality inputs and
references, `fixtures/opt058_quality_baseline.json`, and
`evidence/optimization/opt058-quality-baseline/REPORT.md`. Establish a finite
current baseline, valid functional prompts with a passing pinned authority,
and complete relative quality scores, or leave this prerequisite blocked with
the exact unresolved defect. A report alone does not admit nonfinite production.
No claimed speedup and no unrelated kernel tuning.
