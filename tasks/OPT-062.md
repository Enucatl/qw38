# OPT-062 — Put admitted cooperative Q4 dots on every decode FFN leg

Status: done. Dependencies: OPT-059, OPT-061. Coupled IDs: none.

## Change

Revisit the existing OPT-046 implementation using real-input v2 quality and
streaming timing. Do not begin by writing another dot kernel. Current
`paired_staged` gate/up and `launch_quant_mmv_prequant` bypass the Q4 selector;
simply changing `kSelectedQ4DecodePath` is incomplete.

Read `cuda/q4k_decode_dots.{cu,cuh}`, `cuda/q4k_decode_path.cuh`,
`cuda/quant_mmv.cu::launch_quant_mmv_prequant`,
`cuda/full_scheduler.cu::execute_ffn`, `cuda/ffn_decode_path.cuh`, and the
OPT-046 rejection/OPT-049 keep. Preserve both historical controls.

1. Define only three complete-FFN configurations: current packed paired-staged
   control; cooperative integer Q8Block shared-stage gate/up plus integer down;
   and a half-scale staging candidate only if OPT-059 admits its exact semantics.
   Start at 4 warps/row, the measured OPT-046 winner. No warps×staging×fusion
   Cartesian sweep. If the half-scale layout is not admitted, skip it explicitly.
2. For the Q8Block candidate, quantize normalized BF16 once, call
   `launch_q4k_coop_mmv_prequant` for gate and up into existing FP32 buffers,
   run existing BF16 SwiGLU, then use cooperative down through `matrix_vector`.
   This intentionally uses separate gate/up calls for the first admission.
   Compare complete FFN against the current paired control, not OPT-045's
   obsolete separate baseline. OPT-063 owns integer paired fusion.
3. Make prequantized dispatch explicit about staging type. A `Q8Block*` function
   may dispatch only compatible Q8Block kernels; it must not reinterpret that
   pointer as Q8_1 because a global selector changed. If half-scale wins, stage
   its distinct layout and call its typed/internal launcher. Account for buffer
   alignment, bytes and invalidation when workspace storage is reused by Q8/Q6.
4. Add diagnostic dispatch records for actual gate, up and down launch variants
   and staging count. Record those during graph capture as well as eager work.
   Test that selecting integer reaches all three legs with one gate/up stage
   and a separate down stage. A constant saying integer is not sufficient proof.
5. Keep the Q4 scale/min layout exact. Independently test all eight group indices,
   high/low nibbles and negative activation sums. For half-scale candidates,
   use the OPT-059 format ID, not the existing misleading llama-style name.
   A failure against FP64 staged math is a kernel defect; the old CUD-001
   reduction-order miss alone is not the production gate.
6. Wire the accepted complete configuration into current selectors/admission,
   recapture FFN graphs, and confirm selected/effective settings agree. Keep
   strict reference tests selecting the strict launcher explicitly. Update
   historical pin assertions only when they erroneously inspect current source;
   leave old measured fixtures and rejection verdicts unchanged.

## Fast tests

Target `build/qw38-cuda-opt062-q4-admission-test`; pytest
`tests/test_opt062_q4_admission.py`. Reuse OPT-061's replay and OPT-059's
references. Do not invoke OPT-046's old evidence-writing pytest sitting.

Smoke: Q4 M17/K256, control+Q8Block candidate, four vectors (zero,
alternating, positive/negative group sums), full output. Correctness: the same
plus M19/K512 and production K5120/K17408,16 sampled output rows × 4 actual
activations each; at most 128 production FP64 dots, all candidates share refs.
The complete FFN probe runs in screen, uses saved actual layer inputs and compares activated
BF16/down/residual, not only separate dot outputs. Include graph/eager dispatch
coverage and a reused-buffer invalidation test.

Screen: at most 3 configurations × 3 paired samples after 1 warmup, one rotating
complete-FFN set; both real gate/up and down shapes are naturally included.
Stop immediately on nonfinites, layout errors or v2 quality failures. Promote
only one candidate to acceptance. Add a short D2048 probe only after a component
screen win; do not run P/D128/D2048 for every staging variant.

```sh
uv run pytest -q tests/test_opt062_q4_admission.py tests/test_opt059_numerics.py
uv run python tools/run_optimization_task.py --task OPT-062 --mode feedback
uv run python tools/run_optimization_task.py --task OPT-062 --mode acceptance
```

## Acceptance and rejection

Use new-batch component acceptance and target/cross-workload guards. Any default
change requires the complete v2 quality checks on the combined current pins,
including both NLL spans and functional answers. Report error versus original
and staged FP64, complete FFN time, selected calls, graph identity and scratch
bytes. Preserve original CUD-001 strict-reference passing results separately.

Deliver contract/fixture and `evidence/optimization/opt062-q4-admission/REPORT.md`.
If no candidate beats the current paired FFN with quality, retain current
production and record rejection. Do not count the old 3.5× measurement as this
task's win. An unexpected numerical defect requires a bounded fix and rerun,
not an expanded tolerance. No prompt MMQ, Q8/Q6 or global compiler change.

## Run record

### Planning (2026-09-11T08:22:00Z)

skipped — pre-authored dossier admitted (coordinator)
Primary target: D2048, complete FFN. Read
[the batch protocol](PERFORMANCE-RECOVERY-2026-09-11.md).

### Implementation (2026-09-11T08:45:33Z)

Cursor Grok 4.6 implementation agent. No commit.

Wired cooperative integer Q8Block onto all three decode FFN legs: `execute_ffn`
bypasses `paired_staged` packed fusion when `q4_decode_uses_integer_q8block()`,
quantizes BF16 once, calls typed `launch_q4k_coop_mmv_prequant_q8` for gate and
up, existing BF16 SwiGLU, then cooperative down via `matrix_vector`.
`launch_quant_mmv_prequant(Q8Block*)` never reinterprets as Q8_1. OPT-061 replay
follows the same integer branch. Dispatch records cover eager and graph capture.
Half-scale Q8_1 skipped (OPT-059 `v2_admitted=false`). Production pin stays
`packed` / `paired_staged`.

First feedback failed smoke: staged FP64 abs `0.000398784876` on ±4 group
vectors for **both** packed and integer while vs-original was 0. Bounded fix:
host Q8 reconstruction now does the GPU float mul before promoting to double
(not an expanded 3e-4 tolerance). Rerun passed with staged and integer-vs-packed
abs 0.

Commands:

- `uv run pytest -q tests/test_opt062_q4_admission.py tests/test_opt059_numerics.py` — **pass** (19 tests)
- `uv run python tools/run_optimization_task.py --task OPT-062 --mode feedback` — **pass** (`ok`, 3.35 s, [feedback run](../build/optimization-runs/OPT-062/feedback/20260911T084506Z-b0ae8a9d))
- `uv run python tools/run_optimization_task.py --task OPT-062 --mode acceptance` — **pass** (`ok`, 4.38 s, screen 2.70 s, [acceptance run](../build/optimization-runs/OPT-062/acceptance/20260911T084528Z-e8dda304))

Screen (rotating 64-layer complete FFN, 1 warmup + 3 samples): packed
**15.7531624** ms, integer_q8 **12.4565439** ms. Integer dispatch:
gate/up `q4k_coop_mmv_prequant_q8`, down `q4k_coop_mmv_q8`, stages 1+1,
graph capture identical. Residual/down vs packed max_abs **7.62939453e-06**,
activated prefix 0, graph/eager 0, nonfinite 0. Scratch Q8 19584 bytes.

Keep/reject: **not installed**. Component screen winner is integer_q8 (~1.26×
complete FFN). Production remains packed because OPT-059 `integer_dp4a_q8` is
testing-admitted only and this sitting did not run combined v2 NLL / functional
answers. OPT-046 3.5× is not counted. D2048 probe not run
(`historical_oracles=false`). OPT-063 still owns integer paired fusion.

Blockers / follow-up: installing the pin needs v2 quality on the combined
current pins (ledger “batch quality/non-regression”). No implementation
blocker for the wiring itself.

### Verification (2026-09-11T08:49:10Z)

Independent verifier (composer-2.5). Model: composer-2.5;
`telemetry_unavailable`. Verdict: **PASS** (wired, packed retained).

Commands:

- `uv run ruff format .` — **pass** (145 files unchanged)
- `uv run ruff check .` — **pass**
- `uv run pytest -q tests/test_opt062_q4_admission.py tests/test_opt059_numerics.py` — **pass** (19 tests, 0.04 s)
- `uv run python tools/run_optimization_task.py --task OPT-062 --mode feedback` — **pass** (`ok`, 1.65 s, [feedback run](../build/optimization-runs/OPT-062/feedback/20260911T084905Z-7c69b80a))
- `uv run python tools/run_optimization_task.py --task OPT-062 --mode acceptance` — **pass** (`ok`, 4.34 s, screen 2.69 s, [acceptance run](../build/optimization-runs/OPT-062/acceptance/20260911T084909Z-e97840bd))

Screen (rotating 64-layer complete FFN, 1 warmup + 3 samples): packed
**15.7643099** ms, integer_q8 **12.4760742** ms (~1.26× component). Integer
dispatch: gate/up `q4k_coop_mmv_prequant_q8`, down `q4k_coop_mmv_q8`, stages
1+1, graph capture identical. Residual/down vs packed max_abs **7.62939453e-06**,
activated prefix 0, graph/eager 0, nonfinite 0. Scratch Q8 19584 bytes.
Production pin stays `packed` / `paired_staged` (`installed=false`).

Throughput: **no tok/s** (`claims_throughput=false`; component complete-FFN ms
only; no D2048 probe). Speedup delta vs baseline **0** (production unchanged).
