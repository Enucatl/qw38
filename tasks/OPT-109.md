# OPT-109 — Keep transposed GDN state for the session lifetime

Status: done. Dependencies: OPT-101, OPT-106, OPT-114. Coupled IDs: none.
Primary target: D128/D2048 GDN core and state traffic.

## Run record

### Planning (2026-09-12T22:55:00Z)

skipped — pre-authored dossier admitted (coordinator)

### Implementation (cursor-grok-4.6, 2026-09-12T23:22:46Z)

- Agent/model: implementation (Cursor Grok 4.6). No commit. Ledger left
  `in_progress`. Production pin stays `sequential`. `claims_throughput: false`.
- Legal path `persistent_transposed`: resident col-major FP32 GDN recurrent
  state for the session lifetime. Decode recurrence consumes/produces
  col-major with **zero timed relayout**. Canonical checkpoint bytes stay
  row-major. Prompt-boundary conversion is once per synchronized prompt.
  Two CUDA-graph address variants (AB/BA ping-pong), not copy-back.
- Pilot recurrence-only on identical buffers **won** (3+10): sequential
  **0.02592 ms** vs persistent **0.01102 ms**; mean_diff **+0.01489 ms**;
  95% CI low **+0.01153**; `timed_relayout=0`. Stop-rule did not fire.
- Complete 48-layer GDN enclosing (3+10, capture
  `8b954514af28526225acbdc344b48dcc98485daa454a1835f8f7c43f021433e6`)
  **lost**: sequential **13.190 ms** vs persistent **13.993 ms**; mean_diff
  **−0.803 ms**; 95% CI **[−1.053, −0.553]**; `saving_ge_0_10_ms=false`;
  `timed_relayout_launches=0`; `decode_conversions=0`. Dispatch
  `prepare_recurrence_decode_transposed` `grid=(48,32)`. Kernel-only was
  faster (~1.63 vs ~0.67 ms) but enclosing wall lost.
- Session design therefore did not survive. D128/D2048 five-pair and P4096
  GPU sittings were **not** run (task: engine pairs only for a surviving
  session design). Quality (OPT-073 reuse), state/lifetime/checkpoint/graph,
  and 128K memory ledger passed. OPT-077/094/101 fixtures untouched.
- Verdict: **reject / retain sequential**. `production_kept=false`.
  tok/s delta vs sitting sequential baseline: **0**.

**Files created**
- `pins/opt109_persistent_gdn_state_contract.json`
- `pins/opt109_iteration_contract.json`
- `tools/opt109_persistent_gdn_state.py`
- `cuda/opt109_persistent_gdn_state_test.cu`
- `tests/test_opt109_persistent_gdn_state.py`
- `fixtures/opt109_persistent_gdn_state.json`
- `evidence/optimization/opt109-persistent-gdn-state/REPORT.md`
- `evidence/optimization/opt109-persistent-gdn-state/REJECTION.md`

**Files modified**
- `Makefile` (`cuda-opt109-diagnostics`, native test binary)
- `cuda/gdn_decode_path.cuh` (path, conversion counters, session live layout)
- `cuda/gdn_decode_recurrence.cuh` (resident launcher; OPT-101 records timed relayout)
- `cuda/gdn_step.h` / `cuda/gdn_step.cu` (dispatch, convert/copy helpers)
- `cuda/full_scheduler.cu` (commit flag, prompt-boundary convert, reset)
- `cuda/checkpoint.cu` (save/restore canonical row-major)
- `cuda/optimization_component_replay.cu` / `cuda/optimization_engine_probe.cu`
- `tools/run_optimization_task.py` (OPT-109 result prefixes)
- `tasks/OPT-109.md` (this run record)

**Host validation**
```
uv run ruff format tools/opt109_persistent_gdn_state.py tests/test_opt109_persistent_gdn_state.py
uv run ruff check tools/opt109_persistent_gdn_state.py tests/test_opt109_persistent_gdn_state.py
# All checks passed!
uv run pytest -q tests/test_opt109_persistent_gdn_state.py
...........                                                              [100%]
11 passed in 0.04s
```

**Native / GPU**
```
docker ... make build/qw38-cuda-opt109-persistent-gdn-state-test
# parity/state/pilot native: pass; layout roundtrip; seq_abs ~1e-9; graphs AB/BA
# pilot 3+10 positive; complete GDN 3+10 negative enclosing (see above)
```

Fixture: `status=rejected`, `evidence_complete=true`,
`shipping_gdn_decode=sequential`, `production_kept=false`.

**Blockers:** none. Complete GDN enclosing lost; sequential retained. Do not
retry OPT-077/094/101 layouts.

### Documentation (composer-2.5, 2026-09-12T23:23:00Z)

Reviewed `evidence/optimization/opt109-persistent-gdn-state/REPORT.md` and
`REJECTION.md` against dossier acceptance after rejected delivery. Report was
incomplete on claim labels, sitting identity, eligibility, parity scope,
formatted pilot and complete GDN tables, skipped phases after complete GDN loss,
and cross-links to fixture/rejection. Updated report prose; expanded rejection
with independent verdicts and explicit complete-GDN-negative-CI stop reason
(pilot won, enclosing lost); added OPT-109 freeze paragraph to
`docs/06-system-optimization.md` (mirroring OPT-106/114) and audit row to
`docs/65-documentation-audit.md`. Verified `report_path` and fixture references
are consistent across `pins/opt109_persistent_gdn_state_contract.json`,
`pins/opt109_iteration_contract.json`, and
`fixtures/opt109_persistent_gdn_state.json`. Did not commit. No production code
changes.

**Files changed**
- `evidence/optimization/opt109-persistent-gdn-state/REPORT.md`
- `evidence/optimization/opt109-persistent-gdn-state/REJECTION.md`
- `docs/06-system-optimization.md`
- `docs/65-documentation-audit.md`
- `tasks/OPT-109.md` (this run record)

### Verification (2026-09-12T23:25:00Z)

**Verdict: pass** — measured rejection and production retention are supported by
artifacts; host checks green.

**Measured rejection (complete 48-layer GDN negative CI)**
- `fixtures/opt109_persistent_gdn_state.json`: `status=rejected`,
  `production_kept=false`, `evidence_complete=true`, `tok_s_delta=0.0`.
- Pilot recurrence-only (3+10): `positive=true`, control **0.02592 ms** vs
  candidate **0.01102 ms**, mean_diff **+0.01489 ms**, 95% CI low **+0.01153**,
  `timed_relayout=0`.
- Complete GDN (3+10, capture
  `8b954514af28526225acbdc344b48dcc98485daa454a1835f8f7c43f021433e6`):
  `positive=false`, control **13.190 ms** vs candidate **13.993 ms**,
  mean_diff **−0.803 ms**, 95% CI **[−1.053, −0.553]**,
  `saving_ge_0_10_ms=false`, `timed_relayout_launches=0`,
  `decode_conversions=0`.
- D128/D2048/P4096 engine sittings skipped (`complete_gdn_lost`).
- `evidence/optimization/opt109-persistent-gdn-state/REPORT.md` and
  `REJECTION.md` match fixture numbers; verdict **rejected**.

**Production unchanged (sequential row-major)**
- `cuda/gdn_decode_path.cuh`: `kSelectedGdnDecodePath[] = "sequential"`.
- Fixture: `shipping_gdn_decode=sequential`, `pins_applied.gdn_pin=sequential`,
  `claims_throughput=false`.
- OPT-077/094/101 historical fixtures untouched (`shipping_gdn_decode=sequential`).

**Fixture → artifacts**
- `fixtures/opt109_persistent_gdn_state.json` → `report_path` →
  `evidence/optimization/opt109-persistent-gdn-state/REPORT.md` +
  `REJECTION.md`.
- Contracts: `pins/opt109_persistent_gdn_state_contract.json`,
  `pins/opt109_iteration_contract.json` (`diagnostics_make_target=
  cuda-opt109-diagnostics`).
- Runner: `tools/opt109_persistent_gdn_state.py`; native:
  `cuda/opt109_persistent_gdn_state_test.cu` →
  `build/qw38-cuda-opt109-persistent-gdn-state-test`.
- Phase payloads embedded in fixture: `parity`, `pilot`, `gdn`, `quality`,
  `state`, `memory` (d128/d2048/prefill-guard skipped).

**Host validation**
```
uv run ruff format .
# 246 files left unchanged
uv run pytest -q tests/test_opt109_persistent_gdn_state.py
# 11 passed in 0.03s
```

**Native build**
```
make cuda-opt109-diagnostics
# host: nvcc not found (expected without CUDA toolchain)
docker run --rm --gpus all --user "$(id -u):$(id -g)" \
  -v "$(pwd):/workspace" qw38-cuda:13.0.2 make cuda-opt109-diagnostics
# exit 0
```

### Delivery (2026-09-12T23:24:41Z)

Delivery authorized after independent verification pass. Marked OPT-109 `done`.
Coupled IDs: none. **Measured rejection:** pilot recurrence-only won (3+10:
+0.015 ms; CI low +0.012 ms) but complete 48-layer GDN enclosing lost (−0.803
ms; CI entirely negative). Production unchanged (sequential row-major GDN
decode retained). D128/D2048/P4096 skipped after `complete_gdn_lost`.

### Final outcome (2026-09-12T23:24:41Z)

`measured_reject`: resident col-major GDN session layout failed the complete
48-layer enclosing gate despite a winning recurrence-only pilot. Parity,
quality, state/lifetime/checkpoint/graph, and 128K memory passed; engine pairs
and P4096 were not run. Shipping GDN decode path unchanged (`sequential`).

**tok/s delta vs OPT-114 baseline:** **0** (rejection; production unchanged).

Evidence: [`fixtures/opt109_persistent_gdn_state.json`](fixtures/opt109_persistent_gdn_state.json);
[`evidence/optimization/opt109-persistent-gdn-state/REPORT.md`](evidence/optimization/opt109-persistent-gdn-state/REPORT.md);
[`evidence/optimization/opt109-persistent-gdn-state/REJECTION.md`](evidence/optimization/opt109-persistent-gdn-state/REJECTION.md).

**Commit:** delivered 2026-09-12T23:24:41Z
**Push:** origin/main (authorized)

## Outcome

Evaluate the topology llama actually uses: persistent col-major FP32 device
state from one token to the next, with canonical row-major conversion only at
external checkpoint boundaries. Retain the selected sequential row-major path
as control.

## Why this differs from OPT-101

OPT-101 implemented the warp-column recurrence but its launcher performs a
source-to-candidate transpose, inverse preparation, recurrence, and
candidate-to-source transpose on every layer invocation. It therefore paid two
full 786,432-float state traversals per layer in addition to the recurrence,
then rejected the complete path (13.339 vs 12.418 ms over 48 layers). Pinned
llama keeps `M[col][row]` as the live device representation; it does not pay
those per-token relayouts. Persistent layout lifetime, not the recurrence
kernel alone, is the new mechanism. The OPT-101 timings include enclosing
work, so use recurrence-only and complete timings separately.

## Implementation

1. First run a resident-layout pilot: feed the OPT-101 recurrence a
   pre-existing col-major state and compare it with the current recurrence on
   identical state/query buffers. Stop the task if the recurrence itself loses;
   do not redesign session ownership to rescue a losing kernel.
2. Make both committed and tentative GDN state buffers col-major for the entire
   live session only if the pilot wins. The decode recurrence consumes and
   produces col-major state directly. No row-to-column or column-to-row kernel
   may appear inside a timed layer or token.
3. Preserve atomicity with col-major ping-pong buffers or an equivalent
   recoverable journal. Commit by swapping/advancing ownership only after all
   layers succeed; cancellation and errors leave committed state unchanged.
4. Have prompt GDN emit the persistent layout directly when practical. If a
   prompt-boundary conversion is required, execute it once per synchronized
   prompt, outside per-layer decode timing, and include it in P4096 wall time.
5. Convert to canonical row-major only when serializing a checkpoint or serving
   an explicit diagnostic view; convert back only on restore. Record conversion
   count and bytes for prefill, decode, save, and restore.
6. Reuse the OPT-101 warp-column recurrence, but prepare q/k inverses once per
   key head and keep them out of the 48 value-head inner work. Screen exactly
   `persistent_transposed` against `sequential_row_major`; no tile sweep.
7. Verify that state addresses/layout identities remain stable under CUDA graph
   capture. If pointer swapping invalidates a graph, use graph node parameter
   update or two pre-captured address variants rather than copying state back.

## Verification and evidence

- Add contracts, fixture, native state-layout/lifetime test, component replay,
  host validator, runner entry, and
  `evidence/optimization/opt109-persistent-gdn-state/REPORT.md`.
- Test token counts 0, 1, 2, 32, prompt-to-decode transition, reset, divergent
  prefix replay, checkpoint save/restore, failure after a middle layer, and
  cancellation before publication. Canonical checkpoint bytes must round-trip.
- Run independent FP64 recurrence samples, current full-model quality, and the
  128K memory ledger. Report recurrence-only, conversion-only, and complete
  48-layer GDN time without summing overlapping intervals.
- Performance acceptance uses 3+10 resident-recurrence pairs for the pilot,
  then 3+10 complete GDN pairs and five uninstrumented D128/D2048 engine pairs
  for a surviving session design, plus P4096 and checkpoint-time guards.

## Acceptance and stop rule

- The pilot must beat the current recurrence on identical buffers. A surviving
  session design must contain zero relayout launches in timed decode and save
  at least 0.10 ms/token over complete 48-layer GDN with a positive 95% paired
  interval.
- D128 and D2048 point throughput must improve with existing p95/non-regression
  bounds. P4096, quality, state, checkpoint, cancellation, graph, and memory
  gates must pass.
- If persistent lifetime still loses, retain sequential row-major and do not
  retry OPT-077/094/101 layouts.

## Non-goals

No reduced-precision GDN state, changed recurrence equation, per-token
checkpoint conversion, new scan algorithm, or reinterpretation of OPT-101's
measured rejection.
