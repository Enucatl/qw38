# OPT-140 — Add a production-boundary prefill attention replay

Status: done. Dependencies: OPT-139. Coupled IDs: none.
Priority: P1. Plan impact: none; existing release and quality gates retained.

## Outcome

Make P4096 attn_core measurable with its actual production inputs and enclosing work. The supplied batch's prefill attention counters used decode-attention; OPT-138 correctly refused that replay for production comparison.

## Scope

Read OPT-111, OPT-138, cuda/opt111_llama_prompt_attention.cuh, the prompt scheduler path and existing component replay. Extend cuda/optimization_component_replay.cu/.h and the existing diagnostic driver with prompt-attention (or an equivalent explicit production-boundary mode). Reuse current implementations and captures.

Capture actual Q/K/V, physical strides, causal masks, visible lengths and selectors for the 16 attention layers. Match P4096 at capacity131072, empty initial state, prefix reuse disabled and final-token output policy. Include every preparation, BF16/F16 conversion, staging, core, merge and epilogue assigned to attn_core by production accounting, with explicit ownership of excluded work. Record actual prompt chunking and rotate across applicable layers. Do not equate a single repeatedly cached tile with complete prompt cost.

Wire phase-specific replay selection into timing and NCU collection. Retain exact target launch identity from OPT-139; distinguish the complete family replay from the kernel selected for counters. Produce a boundary manifest relating replay operations and outputs to production trace leaves.

## Verification and completion

Check production/replay outputs and KV effects under existing tolerances, causal tails and chunk boundaries; reject decode replay substitution. Run one warmup plus three alternating rotating replay rounds and bounded targeted NCU for the production prefill kernel. Profiled runs are not throughput samples. Respect shared GPU locking and explicitly frozen phase deadlines.

Deliver evidence/optimization/opt140-prefill-attention-replay/REPORT.md, boundary/output comparisons, replay samples and structured counters. Done requires a matched P4096 replay and evidence-backed mechanism or explicit unknown. No attention optimization or new quality tolerance is included.

## Execution requirements

Read [TASK-TESTING-STRATEGY.md](TASK-TESTING-STRATEGY.md) and the dependency dossiers before implementation. Reuse existing runners, parsers and gates; add only necessary task registration and focused checks. Freeze an enumerated GPU matrix and per-child/aggregate deadlines before execution, use the shared GPU lock and reuse builds. Do not rerun until significant or weaken acceptance to fit a timeout. Missing hardware evidence is incomplete.

Record exact runnable commands, raw evidence paths, identity and independent verification in this dossier at execution. New artifact paths above are planned deliverables, not existing evidence. This task is drafted only; no implementation or measurement has run.

## Run record

### Planning (2026-09-14T23:37:00Z)
skipped — pre-authored dossier admitted

### Implementation (2026-09-15T00:06:00Z)

Frozen matrix: RTX 5090, image `qw38-cuda:13.0.2`, child 300s, prompt-attention 1200s, aggregate 7200s, GPU lock `build/optimization-runs/qw38-gpu.lock`. No attention optimization. Existing OPT-111 abs tol 2e-3 reused; no new quality tolerance.

```
uv run ruff format tools/opt140_prefill_attention_replay.py tools/opt138_remaining_gap_profile.py tools/opt139_counter_identity.py tools/run_optimization_task.py tests/test_opt140_prefill_attention_replay.py tests/test_opt138_remaining_gap_profile.py tests/test_opt139_counter_identity.py
uv run ruff check tools/opt140_prefill_attention_replay.py tools/opt138_remaining_gap_profile.py tools/opt139_counter_identity.py tools/run_optimization_task.py tests/test_opt140_prefill_attention_replay.py tests/test_opt138_remaining_gap_profile.py tests/test_opt139_counter_identity.py
```

outcome: All checks passed.

```
uv run pytest tests/test_opt140_prefill_attention_replay.py tests/test_opt138_remaining_gap_profile.py tests/test_opt139_counter_identity.py -q
```

outcome: 35 passed.

```
docker run --rm --gpus all --user $(id -u):$(id -g) -e QW38_CUDA_TEST_TIER=smoke -v $PWD:/workspace -w /workspace qw38-cuda:13.0.2 make cuda-opt140-diagnostics
```

outcome: rc 0 under GPU lock (full_scheduler.trace.cuda.o + qw38-cuda-component-replay rebuilt).

```
uv run python tools/opt140_prefill_attention_replay.py --phase preflight --mode acceptance --run-dir build/optimization-runs/opt140
```

outcome: `{"phase": "preflight", "ok": true}`.

```
uv run python tools/opt140_prefill_attention_replay.py --phase replay --mode acceptance --run-dir build/optimization-runs/opt140
```

outcome: `{"phase": "replay", "ok": true}`. Capture recapture then reuse. 16/16 layers equal under existing abs 2e-3 (observed max_abs=0). 1 warmup + 3 rotating rounds, cache_mode=rotating. Dispatch `opt111_base` / `fattn_mma_pipeline_opt111_base`. decode-attention substitution rejected. Capture key `698a857387d5b5779f4921723f6592d72b248b3b4fd74f55d0c3116909cfcd11`.

```
uv run python tools/opt140_prefill_attention_replay.py --phase counters --mode acceptance --run-dir build/optimization-runs/opt140
```

outcome first pass: NCU `--kernel-name regex:fattn_mma_pipeline_opt111_base` profiled nothing (CUDA symbol is `fattn_mma_pipeline_kernel`). Second pass: `regex:fattn_mma_pipeline_kernel --launch-count 1 --replay-mode application` wrote `evidence/optimization/opt140-prefill-attention-replay/raw/p4096-attn_core.ncu-rep`. CSV export parsed; identity_ok=true; typed DRAM/L2/SM/tensor/occupancy slots filled; registers/spills null; `supported_mechanism=null` (`throughput_alone_cannot_establish_mechanism`); candidate null. Profiled run is not a throughput sample.

```
uv run python tools/opt140_prefill_attention_replay.py --phase report --mode acceptance --run-dir build/optimization-runs/opt140
```

outcome: `{"phase": "report", "ok": true, "status": "measured"}`. Report: `evidence/optimization/opt140-prefill-attention-replay/REPORT.md`. Fixture: `fixtures/opt140_prefill_attention_replay.json`. Boundary: `evidence/optimization/opt140-prefill-attention-replay/boundary-manifest.json`.

GPU after: RTX 5090 idle (34 MiB / 32607 MiB, 0%). No blockers. Not committed.

### Documentation (2026-09-15T00:06:00Z)

Reviewed implementation (`tools/opt140_prefill_attention_replay.py`),
OPT-139 wiring (`counter_record_from_ncu_blob`, `production_prefill_attn_identity`,
`identity_match`), pins, fixture, and acceptance run artifacts under
`build/optimization-runs/opt140/` and
`evidence/optimization/opt140-prefill-attention-replay/`.

**Pins/fixtures (consistent, unverified):**

- `pins/opt140_prefill_attention_replay_contract.json` matches implementation
  constants: selector `decode_segments8`, parent
  `post124_plus_opt127_decode_segments8_plus_opt137_mma`, P4096 capacity 131072,
  `replay_family=prompt-attention`, expected kernel `fattn_mma_pipeline_opt111_base`,
  `decode_attention_substitution=false`, existing abs tol 2e-3, 1 warmup + 3
  rotating samples, timeouts 300/1200/7200 s.
- `pins/opt140_iteration_contract.json` acceptance tier sequence
  `preflight → replay → counters → report`;
  `performance_admission.instrumentation_only=true`.
- `fixtures/opt140_prefill_attention_replay.json` status `measured`; required keys
  present; `claims_throughput=false`; `supported_mechanism=null`; `candidate=null`;
  `answers.matched_p4096_replay=true`; `answers.decode_attention_rejected=true`.

**REPORT.md completed** at
`evidence/optimization/opt140-prefill-attention-replay/REPORT.md`:

- Boundary manifest section with production leaves, exclusions, and complete-family
  vs counter-kernel distinction (`boundary-manifest.json`).
- Replay parity: 16/16 layers equal under abs 2e-3 (max_abs=0), causal tails and
  chunk boundaries, 1 warmup + 3 rotating rounds, capture key recorded.
- NCU typed counter table for P4096 prefill `fattn_mma_pipeline_opt111_base` with
  units, null slots, stall proof limits; raw `.ncu-rep`/`.ncu.csv`/`.ncu.txt` linked.
- Decode-attention substitution rejection documented (preflight
  `decode_attention_rejected`, fixture `decode_attention_substitution=false`).
- Mechanism admission explicitly unknown (`throughput_alone_cannot_establish_mechanism`).
- Performance evidence checklist section (diagnostics; shipping delta N/A).

**Host tests (unverified rerun by documentation agent):** prior implementation pass
reported 35 passed (`test_opt140_prefill_attention_replay.py`,
`test_opt138_remaining_gap_profile.py`, `test_opt139_counter_identity.py`).

**Ledger:** draft evidence column updated in `implementation_ledger.md` (verification
pending).

**Verifier focus:**

1. Confirm raw artifacts exist and CSV rows map to typed slots in
   `counter-evidence.json`.
2. Confirm P4096 replay dispatch `opt111_base` / `fattn_mma_pipeline_opt111_base`
   matches boundary manifest and NCU launch identity.
3. Confirm 16-layer parity, KV effects, and causal-tail checks under existing abs tol.
4. Confirm decode-attention replay is rejected and `decode_attention_substitution`
   stays false throughout.
5. Confirm no production selector/kernel change and `claims_throughput=false`
   throughout.
6. Label all causal mechanism conclusions **unverified** until independent verifier
   pass.

Did not commit.

### Verification (2026-09-15T00:06:52Z)

**Verdict: PASS**

Independent verification against dossier scope, ledger acceptance (`Real P4096 attention replay matches production inputs, complete family boundary and outputs; decode substitution is rejected`), and task completion criteria. No semantic fixes applied.

#### Gates

```
uv run pytest tests/test_opt140_prefill_attention_replay.py tests/test_opt138_remaining_gap_profile.py tests/test_opt139_counter_identity.py -q
```

outcome: **35 passed** in 0.09s.

```
uv run ruff check tools/opt140_prefill_attention_replay.py tools/opt138_remaining_gap_profile.py tools/opt139_counter_identity.py tools/run_optimization_task.py tests/test_opt140_prefill_attention_replay.py tests/test_opt138_remaining_gap_profile.py tests/test_opt139_counter_identity.py
```

outcome: All checks passed.

#### Acceptance evidence

| Criterion | Evidence | Result |
|-----------|----------|--------|
| P4096 production inputs (capacity 131072, empty state, prefix reuse off, final-token policy) | `replay-evidence.json` parsed + `prompt-attention.stdout.txt` dispatch line | PASS |
| 16-layer output/KV parity under existing abs tol 2e-3 | `parity.equal=true`, `max_abs=0`, `kv_max_abs=0`, 16/16 `layer_parity` | PASS |
| Complete family boundary (split/stage/convert/core/merge/epilogue) | `boundary-manifest.json` `complete_family_replay=true`, `production_leaves` / `excluded` | PASS |
| Counter kernel distinct from complete family | `counter_kernel_is_complete_family=false`, kernel=`fattn_mma_pipeline_opt111_base` | PASS |
| Decode-attention substitution rejected | `decode_attention_substitution=false` in replay/boundary/fixture; host tests `test_opt139_prefill_identity_and_decode_rejection` / `test_phase_specific_replay_family` | PASS |
| 1 warmup + 3 rotating rounds | `warmup_count=1`, `sample_count=3`, `cache_mode=rotating` | PASS |
| OPT-139 launch identity retained | `identity.ok=true`, `expected_kernel=fattn_mma_pipeline_opt111_base`, `mismatches=[]` | PASS |
| Bounded NCU counters (not throughput sample) | `counter-evidence.json` typed DRAM/L2/SM/tensor/occupancy; `claims_throughput=false`; `supported_mechanism=null` (`throughput_alone_cannot_establish_mechanism`) | PASS |
| Required artifacts present | `REPORT.md`, `boundary-manifest.json`, `replay-evidence.json`, `counter-evidence.json`, `fixtures/opt140_prefill_attention_replay.json`, `raw/p4096-attn_core.ncu-rep` (4.3 MB), replay stdout | PASS |

Capture key `698a857387d5b5779f4921723f6592d72b248b3b4fd74f55d0c3116909cfcd11`. Status `measured`. Diagnostics only; no attention optimization; no new quality tolerance; production selector unchanged.

### Final outcome

- Status: `done`
- Verdict: diagnostics complete (not a keep/reject)
- Acceptance evidence:
  [`fixtures/opt140_prefill_attention_replay.json`](../fixtures/opt140_prefill_attention_replay.json);
  [`evidence/optimization/opt140-prefill-attention-replay/REPORT.md`](../evidence/optimization/opt140-prefill-attention-replay/REPORT.md);
  verification record above (2026-09-15T00:06:52Z)
- Candidate measured delta: **N/A**; shipping delta: **N/A**; quality result:
  **N/A**
- Mechanism: **explicitly unknown** (`throughput_alone_cannot_establish_mechanism`;
  no named source/SASS observations); `supported_mechanism=null`; `candidate=null`
- P4096 prompt-attention replay 16/16 layers equal under existing abs 2e-3
  (`max_abs=0`, `kv_max_abs=0`); complete family boundary with counter kernel
  distinct from complete family; decode-attention substitution rejected;
  OPT-139 launch identity retained (`fattn_mma_pipeline_opt111_base`)
- Typed bounded NCU counters for P4096 prefill `attn_core` with raw
  `.ncu-rep`/`.ncu.csv` artifacts; profiled run is not a throughput sample
- First-pass acceptance: yes (2026-09-15T00:06:52Z)
- Remaining risk: causal mechanism unknown without SASS/source proof; no
  production changes

