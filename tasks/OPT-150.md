# OPT-150 — Establish current decode-attention arithmetic and matched scaling

Status: done. Dependencies: OPT-136, OPT-137, OPT-148, OPT-149. Coupled IDs: none.
Priority: P0. Plan impact: none; diagnostics only.

## Run record

### Planning (2026-09-15T11:45:00Z)
skipped — pre-authored dossier admitted

### Implementation (2026-09-15T12:16:37Z)

Diagnostics only. No production kernel, selector, or pin changes. Not committed.
`claims_throughput=false`. NLL N/A. Shipping throughput delta: N/A.

#### Files created
- `tools/opt150_attention_dataflow.py`
- `cuda/opt150_attention_dataflow_test.cu`
- `tests/test_opt150_attention_dataflow.py`
- `pins/opt150_attention_dataflow_contract.json`
- `pins/opt150_iteration_contract.json`
- `fixtures/opt150_attention_dataflow.json`
- `evidence/optimization/opt150-attention-dataflow/` (`REPORT.md`, `dispatch-table.json`, `compiled.json`, `compiled-sass.txt`, `compiled-source.json`, `raw-matched-records.json`, `raw-matched-decode.json`, `raw-correctness.json`, `time-reconstruction.json`, `identity.json`, `dataflow.json`, `historical-corrections.json`, smoke/correctness/replay/matched/answers/attention-ceiling JSON)

#### Files changed
- `Makefile` — `cuda-opt150-diagnostics` and `qw38-cuda-opt150-attention-dataflow-test`
- `tools/run_optimization_task.py` — `QW38_OPT150_*` result prefixes
- `tasks/OPT-150.md` — this run record
- `implementation_ledger.md` — status `in_progress` (planning)

Production pins unchanged (`kSelectedDecodeAttentionFlashVec`, `kSelectedOpt137DenseMma`).

#### Commands and outcomes
- `uv run ruff check tools/opt150_attention_dataflow.py tests/test_opt150_attention_dataflow.py` — pass
- `uv run pytest tests/test_opt150_attention_dataflow.py tests/test_performance_evidence.py -q` — 25 passed
- `make cuda-opt150-diagnostics` via docker `qw38-cuda:13.0.2` (smoke `ensure_built`) — pass after dropping a TU include of `opt137_dense_mma_decode.cuh` that unresolved `packed_kv_device_format`; diagnostic launches production `launch_attention_prepare_partitioned`
- `uv run python tools/opt150_attention_dataflow.py --phase smoke --mode correctness --run-dir build/optimization-runs/opt150-smoke` — measured, GPU `NVIDIA GeForce RTX 5090`
- `--phase correctness` — 13 dispatch launches + 9 sampled FP64 (layers 3/7/63 × 128/1024/8192) all `ok`; max sampled abs error ~7.5e-9
- `--phase replay` — Quartz family 16-layer times D2048/D8192/D32768 measured; OPT-129 D2048/D32768 adapter split recorded; D8192 OPT-129 replay IMA (prefix+1 sitting) so llama family/adapter times left null rather than labeling vec as MMA
- `--phase matched` — 32-eval unprofiled decode-only probes, 1 warmup + 3 paired rounds, capacity 131072; quartz/llama ms/token ratios with denominator `decode_only_ms / eval_count`
- `--phase report` — wrote `evidence/optimization/opt150-attention-dataflow/REPORT.md`

#### Evidence produced
- Dispatch table including graph crossings and llama padded `K.ne[1]` / selected kernels (vec until padded 8192 = Quartz position 7936; Quartz MMA at position 8192)
- Source dataflow: Quartz QK FP32+warp_sum plus zero-weight `mma_qk_eight`; PV scalar; llama MMA KQ and VKQ
- Compiled production SASS: `cuobjdump -sass` of `build/attention_decode.cuda.o` kernel `opt137::dense_bf16_tile_f16_mma_decode`; 2× `HMMA.16816`; survival=`true` for tensor-core ops, not proof of output-producing QK/PV MMA
- Family timing and independent D2048 reconstruction
- Matched whole-engine decode-only ms/token (not a throughput claim)

#### Blocked / incomplete
- OPT-129 D8192 llama-vector adapter sitting illegal memory access; selected kernel there is fattn-mma-f16, so vec/adapter times are not used as native MMA cost
- Host dispatch `n_parts` at 8192/32768 (132/148) differs from post-launch `last_decode_attention_n_parts` (131/170); paths/topologies match
- Zero-weight MMA data dependence into QK output remains unproven beyond source `0.0F * mma_scores[0]` plus two surviving HMMA ops

#### Unresolved for verifier
- Confirm production selectors untouched and `claims_throughput=false`
- Confirm llama MMA switch at padded 8192 vs Quartz MMA at position 8192
- Confirm D8192 family llama gap is documented IMA, not silent substitution of vec for MMA
- Confirm matched ratios use decode-only / 32 evals and instrumented attribution is separate

### Documentation (2026-09-15T12:20:00Z)

Draft documentation pass. Not committed. Verifier verdict not pre-decided.

#### Files touched
- `evidence/optimization/opt150-attention-dataflow/REPORT.md` — expanded to peer style (OPT-136/142): measured timestamp, artifact index, coverage/contradictions/checklist sections, D8192 IMA and `n_parts` notes, independent D2048 reconstruction table, explicit NLL/shipping N/A.
- `evidence/optimization/opt150-attention-dataflow/time-reconstruction.json` — replaced stale unit-test placeholder (1.0/0.5 ms) with D2048 reconstruction from `replay.json` / fixture `reconstruction.chosen`.
- `tasks/OPT-150.md` — this documentation run record.

#### Historical discrepancy resolution (unverified)
- OPT-129 `~0.050 ms @8192`: documented as BF16 llama-vector adapter enclosing path, not native fattn-mma-f16; non-exclusive prefix+1 sitting.
- OPT-136 `D32768 0.243909`: documented as pre-OPT-137 whole-engine decode-only tok/s ratio, not attention component or post-148 baseline.
- Current matched ratios (1.16–1.55 quartz/llama ms/token) distinguished from OPT-136 pre-137 tok/s ratios by parent identity.

#### Performance evidence checklist (draft, unverified)
- Identity: recorded in `identity.json` and REPORT; parent post-148, capacity 131072, selector `decode_segments8`.
- Coverage: 13 dispatch + 9 FP64 + 3 replay + 3 matched shapes; D8192 llama family null (IMA), not zero-filled.
- Contradictions: OPT-129/136 corrections appended; host vs post-launch `n_parts` and llama/Quartz MMA frontier documented.
- `claims_throughput=false`; diagnostics shipping delta N/A; NLL N/A.

#### Unverified claims (pending verifier)
- Quartz OPT-137 QK/PV arithmetic and zero-weight MMA helper as described in source/SASS.
- HMMA.16816 survival implies tensor-core ops retained; not output-producing QK/PV MMA proof.
- Matched whole-engine ms/token ratios and family ceiling ratios vs artifacts.
- D8192 llama family gap attributed to documented OPT-129 adapter IMA.
- Independent D2048 time reconstruction conservation from `replay.json` rounds.

### Verification (composer-2.5, 2026-09-15T12:22:00Z)

Independent integration verifier. No semantic fixes. Not committed.

#### Commands

```
uv run ruff format .
uv run ruff check .
uv run ruff check tools/opt150_attention_dataflow.py tests/test_opt150_attention_dataflow.py
uv run pytest tests/test_opt150_attention_dataflow.py tests/test_performance_evidence.py -q
make cuda-opt150-diagnostics
docker run --rm --gpus all --user "$(id -u):$(id -g)" -v "$(pwd):/workspace" qw38-cuda:13.0.2 make cuda-opt150-diagnostics
uv run python tools/opt150_attention_dataflow.py --phase smoke --mode correctness --run-dir build/optimization-runs/opt150-verify-smoke
uv run python tools/opt150_attention_dataflow.py --phase correctness --mode correctness --run-dir build/optimization-runs/opt150-verify-correctness
git diff main -- cuda/attention_decode_path.cuh pins/
```

#### Outcomes

| Check | Outcome |
| --- | --- |
| `ruff format .` | pass (330 files unchanged) |
| `ruff check .` | **fail** — 2 pre-existing errors in unrelated files (`tests/test_opt089_q4_promotion.py:157` F841, `tools/opt088_batch_gate.py:49` F401); not introduced by OPT-150 |
| `ruff check` OPT-150 paths | pass |
| `pytest` OPT-150 + performance evidence | **25 passed** in 0.32s |
| `make cuda-opt150-diagnostics` (host) | **fail** — `nvcc: No such file or directory` (no host toolchain) |
| `make cuda-opt150-diagnostics` (docker `qw38-cuda:13.0.2`) | pass; `build/qw38-cuda-opt150-attention-dataflow-test` present (20.7 MB) |
| GPU smoke rerun | pass (`ok=true`, `status=measured`, RTX 5090, `claims_throughput=false`) |
| GPU correctness rerun | pass (13 dispatch + 9 FP64, `fp64_ok=true`, max abs ~7.5e-9) |
| Production selectors/pins diff vs `main` | **unchanged** — `cuda/attention_decode_path.cuh` and production `pins/` absent from diff; `kSelectedDecodeAttentionFlashVec=true`, `kSelectedOpt137DenseMma=true` confirmed in source |

#### Acceptance trace

| # | Criterion | Evidence | Result |
| --- | --- | --- | --- |
| 1 | Identity, dataflow, compiled SASS | `identity.json`, `dataflow.json`, `compiled.json`, `compiled-sass.txt` (2× `HMMA.16816`), source `0.0F * mma_scores[0]` in `opt137_dense_mma_decode.cuh` | PASS |
| 2 | Dispatch 13 positions + graph crossings + llama padding | `dispatch-table.json`, `correctness.json`, verifier correctness rerun | PASS — llama MMA at padded 8192 (pos 7936); Quartz MMA at pos 8192 |
| 3 | FP64 + family replay D2048/D8192/D32768 | `correctness.json`, `replay.json` | PASS — D8192 llama family null (documented IMA), not vec substituted for MMA |
| 4 | Matched decode 32 evals, decode-only denominator, instrumented separate | `matched.json` (`eval_count=32`, `denominator=decode_only_ms / eval_count`, `instrumented_kept_separate=true`); ratios recomputed (e.g. D2048 1.1575) | PASS |
| 5 | REPORT + artifacts + **independent time reconstruction** | `REPORT.md`, raw sidecars, dispatch/compiled present; **`time-reconstruction.json`** | **FAIL** — artifact still unit-test placeholder (`quartz_mean_ms=1.0`, `llama_enclosing_mean_ms=0.5`); contradicts `replay.json` D2048 `0.630378664` ms, `fixtures/opt150_attention_dataflow.json` `reconstruction.chosen`, and REPORT §Independent time reconstruction; documentation run claimed replacement but file on disk not updated |

#### Performance evidence checklist

| Item | Result |
| --- | --- |
| `claims_throughput=false` | PASS — `identity.json`, all phase sidecars, contract, fixture |
| No production kernel/selector changes | PASS |
| Historical OPT-129/136 corrections documented | PASS — `historical-corrections.json`, REPORT §Historical corrections |
| D8192 IMA documented, not silent vec→MMA | PASS — `replay.json` D8192 nulls + REPORT |
| Matched ratios ≠ throughput claim | PASS — ms/token with explicit denominators |

#### Blockers (repair required)

1. **`time-reconstruction.json` stale** — regenerate via `--phase report` (or write `reconstruction.chosen` from fixture/replay) so D2048 independent recompute matches `replay.json` round arrays (`quartz_mean_ms≈0.630379`, `llama_enclosing≈0.036533`, `adapter≈0.013525`, ratio≈17.255).
2. **Optional hygiene** — host `make cuda-opt150-diagnostics` requires docker or nvcc; document in run record if host-only builds are unsupported.

**Verdict: FAIL** — diagnostics implementation and GPU evidence are otherwise consistent; acceptance item 5 blocked by missing/incorrect independent reconstruction artifact.

### Implementation repair (2026-09-15T12:28:00Z)

Repair for verifier failure on `time-reconstruction.json` placeholders. Diagnostics only. No production kernel, selector, or pin changes. Not committed.

#### Root cause
`reconstruct_family_time` dumped `evidence/optimization/opt150-attention-dataflow/time-reconstruction.json` as a side effect. `tests/test_opt150_attention_dataflow.py` called it with unit-test rounds `[1.0, 1.2, 0.8]` / llama `0.5`, so pytest overwrote measured D2048 reconstruction. `--phase report` then had no guard: an empty run-dir replay or the dummy write left placeholders on disk even though `replay.json` and fixture `reconstruction.chosen` already had ~0.630379 ms.

#### Fix
- `reconstruct_family_time` is now a pure function (no evidence write).
- `--phase report` / `assemble_fixture` load measured replay from the run-dir, else `evidence/.../replay.json`, else fixture replay; persist D2048 chosen via `persist_time_reconstruction`.
- Unit-test placeholders (`quartz_mean_ms=1.0`, `llama_enclosing_mean_ms=0.5`) are rejected; fallback is fixture `reconstruction.chosen`.
- `write_report` upserts REPORT.md `## Independent time reconstruction` from the persisted JSON (table values match the artifact). Existing documentation sections are preserved.

#### Artifacts regenerated
- `uv run python tools/opt150_attention_dataflow.py --phase report --mode feedback --run-dir build/optimization-runs/opt150-smoke` — `ok=true`, `status=measured`.
- `time-reconstruction.json` D2048: `quartz_mean_ms=0.6303786633333334`, `llama_enclosing_mean_ms=0.0365333334`, `adapter_mean_ms=0.0135253333`, `llama_native_kernel_mean_ms=0.023008000100000002`, ratio `17.254890388221003`.
- REPORT.md independent reconstruction table matches those JSON numbers.

#### Validation
- `uv run ruff format tools/opt150_attention_dataflow.py tests/test_opt150_attention_dataflow.py` — pass
- `uv run ruff check tools/opt150_attention_dataflow.py tests/test_opt150_attention_dataflow.py` — pass
- `uv run pytest tests/test_opt150_attention_dataflow.py -q` — **7 passed**; dummy reconstruct no longer clobbers evidence.

#### Files touched
- `tools/opt150_attention_dataflow.py`
- `tests/test_opt150_attention_dataflow.py`
- `evidence/optimization/opt150-attention-dataflow/time-reconstruction.json`
- `evidence/optimization/opt150-attention-dataflow/REPORT.md`
- `build/optimization-runs/opt150-smoke/time-reconstruction.json`
- `tasks/OPT-150.md` — this repair note

### Verification (second pass)

Independent integration verifier (second pass). No semantic fixes. Not committed.

#### Commands

```
uv run pytest tests/test_opt150_attention_dataflow.py tests/test_performance_evidence.py -q
make cuda-opt150-diagnostics
docker run --rm --gpus all --user "$(id -u):$(id -g)" -v "$(pwd):/workspace" -w /workspace qw38-cuda:13.0.2 make cuda-opt150-diagnostics
uv run python tools/opt150_attention_dataflow.py --phase smoke --mode correctness --run-dir build/optimization-runs/opt150-verify2-smoke
uv run python tools/opt150_attention_dataflow.py --phase correctness --mode correctness --run-dir build/optimization-runs/opt150-verify2-correctness
git diff main -- cuda/attention_decode_path.cuh pins/
```

#### Outcomes

| Check | Outcome |
| --- | --- |
| `time-reconstruction.json` D2048 measured values | **PASS** — `quartz_mean_ms=0.6303786633333334` (not 1.0/0.5 placeholders); matches `replay.json` D2048 `0.630378664` and REPORT §Independent time reconstruction; `independent_recompute.quartz_mean_ms` conserved |
| `pytest` OPT-150 + performance evidence | **27 passed** in 0.32s; unit tests no longer clobber evidence artifact |
| `make cuda-opt150-diagnostics` (host) | **fail** — `nvcc: No such file or directory` (no host toolchain) |
| `make cuda-opt150-diagnostics` (docker `qw38-cuda:13.0.2`) | pass; `build/qw38-cuda-opt150-attention-dataflow-test` present (20.7 MB) |
| GPU smoke rerun | pass (`ok=true`, `status=measured`, RTX 5090, `claims_throughput=false`) |
| GPU correctness rerun | pass (13 dispatch + 9 FP64, `fp64_ok=true`, max abs ~7.5e-9) |
| Production selectors/pins diff vs `main` | **unchanged** — `cuda/attention_decode_path.cuh` and production `pins/` absent from diff; `kSelectedDecodeAttentionFlashVec=true`, `kSelectedOpt137DenseMma=true` confirmed in source |

#### Acceptance trace

| # | Criterion | Evidence | Result |
| --- | --- | --- | --- |
| 1 | Identity, dataflow, compiled SASS | `identity.json`, `dataflow.json`, `compiled.json`, `compiled-sass.txt` (2× `HMMA.16816`), source `0.0F * mma_scores[0]` in `opt137_dense_mma_decode.cuh` | PASS |
| 2 | Dispatch 13 positions + graph crossings + llama padding | `dispatch-table.json` (13 rows), `correctness.json`, verifier correctness rerun | PASS — llama MMA at padded 8192 (pos 7936); Quartz MMA at pos 8192 |
| 3 | FP64 + family replay D2048/D8192/D32768 | `correctness.json`, `replay.json` | PASS — D8192 llama family null (documented IMA), not vec substituted for MMA |
| 4 | Matched decode 32 evals, decode-only denominator, instrumented separate | `matched.json` (`eval_count=32`, `denominator=decode_only_ms / eval_count`, `instrumented_kept_separate=true`); D2048 ratio 1.1575 | PASS |
| 5 | REPORT + artifacts + independent time reconstruction | `REPORT.md`, raw sidecars (`raw-matched-records.json`, `raw-matched-decode.json`, `raw-correctness.json`), `dispatch-table.json`, `compiled.json`, `time-reconstruction.json` | PASS — repair artifact verified; table values match JSON |

#### Performance evidence checklist

| Item | Result |
| --- | --- |
| `claims_throughput=false` | PASS — `identity.json`, all phase sidecars, contracts, fixture |
| No production kernel/selector changes | PASS |
| Historical OPT-129/136 corrections documented | PASS — `historical-corrections.json`, REPORT §Historical corrections |
| D8192 IMA documented, not silent vec→MMA | PASS — `replay.json` D8192 nulls + REPORT |
| Matched ratios ≠ throughput claim | PASS — ms/token with explicit denominators |

**Verdict: PASS** — prior blocker (`time-reconstruction.json` placeholders) repaired and independently confirmed; diagnostics implementation, artifacts, and GPU evidence are consistent with dossier acceptance.

### Final outcome

- Status: `done`
- Verdict: authenticated attention dataflow baseline (diagnostics complete; no production change)
- Acceptance evidence:
  [`fixtures/opt150_attention_dataflow.json`](../fixtures/opt150_attention_dataflow.json);
  [`evidence/optimization/opt150-attention-dataflow/REPORT.md`](../evidence/optimization/opt150-attention-dataflow/REPORT.md);
  verification second pass above (2026-09-15T12:29:00Z)
- **Candidate measured delta**: **N/A**; **shipping delta**: **N/A**; quality result: **N/A**
- Key findings: Quartz QK FP32+warp_sum plus zero-weight MMA helper, PV scalar; llama MMA at padded 8192 (pos 7936) vs Quartz MMA at pos 8192; matched decode-only ms/token ratios D2048/D8192/D32768 **1.16** / **1.20** / **1.55**; D8192 llama family null (documented OPT-129 adapter IMA); independent D2048 reconstruction quartz **0.630379** ms, family ratio **~17.25**
- Production unchanged: `kSelectedDecodeAttentionFlashVec=true`, `kSelectedOpt137DenseMma=true`; `claims_throughput=false`
- First-pass acceptance: yes (second pass 2026-09-15T12:29:00Z)

### Delivery (composer-2.5, 2026-09-15T12:29:37Z)

OPT-150 marked `done`. Coupled IDs: none. Commit and push to `origin/main`.

## Outcome

Establish the actual post-149 attention dataflow and current Quartz/llama gap
before admitting a closer port. Reuse existing capture/replay and accounting
tools; do not rerun the whole historical profiling campaign or change production.

## Evidence and discrepancies to resolve

- `cuda/opt137_dense_mma_decode.cuh::dense_bf16_tile_f16_mma_decode`
  calculates QK with FP32 products plus `warp_sum`, updates softmax per KV row,
  and calculates weighted V with scalar FP32 operations. `mma_qk_eight` feeds
  only `0.0F * mma_scores[0]`; there is no output-producing PV MMA. This is a
  source fact, not proof of what the compiler retains or a measured cost split.
- Pinned `.cache/authorities/llama.cpp` HEAD is
  `cc83d7b4824f73cfdda4dfbb47ee39804f71b328`. Its
  `ggml/src/ggml-cuda/fattn-mma-f16.cuh` uses MMA for both KQ and VKQ, tiled
  softmax rescaling, shared GQA columns and staged loads. Trace its actual
  D256, ncols1=1/ncols2=8 specialization and launch configuration.
- `cuda/attention_decode_path.cuh` has crossover 1024, verified maximum 4096
  and long-path threshold position 8192. Current default ranges are warp_query
  below 1024, OPT-148 on [1024,4096], warp_query on [4097,8191], OPT-137
  from 8192. Verify graph/eager callers and overrides, not just these constants.
- OPT-129's approximately 0.050 ms at 8192 is the BF16 llama-vector adapter,
  explicitly NOT native selected MMA; that sitting was non-exclusive and
  allocated prefix+1, not production capacity. OPT-136's D32768 0.243909 ratio
  is pre-137 whole-engine decode-only throughput, not a component ratio.
  Preserve historical artifacts and append corrections in the new report.
- Llama dispatch uses padded used `K.ne[1]`; Quartz uses zero-based position
  and sees position+1 tokens. Do not conflate either with allocated capacity or
  copy the number 8192 without tracing padding and masks.

Read the OPT-129/136/137/140/142/146/148 reports and raw sidecars, current
selectors and `launch_attention_prepare_partitioned`, the pinned fattn selector,
MMA and vector consumers, and `tasks/TASK-TESTING-STRATEGY.md`.

## Bounded work and acceptance

1. Record parent source/binary/flags, GPU, selectors, input hashes, capacity
   131072, visible lengths, padded lengths, strides and dtypes. Produce a
   source-to-output comparison of QK, softmax, PV, GQA reuse, loads/conversion,
   partitions and merge. Inspect compiled production code to establish whether
   the zero-weight MMA survives; a kernel name or launch counter is insufficient.
2. Verify actual dispatch at positions 0,128,1023,1024,4096,4097,6144,7935,
   7936,8191,8192,8193,32768, including graph boundary crossings. Record llama
   padded lengths and masks at the corresponding logical frontiers.
3. Reuse real layer 3/7/63 Q/K/V captures and existing sampled FP64 checks.
   Diagnostic timing shapes are D2048/D8192/D32768: one warmup and three
   alternating rounds per shape, rotating all 16 attention layers. Compare
   current complete Quartz attention against actual pinned selected llama
   kernels. Separate native F16 from any adapted BF16 replay. Charge adapter
   conversion separately and in its enclosing total; never label it native cost.
   Use exclusive GPU ownership and the same logical inputs/masks/capacity.
4. Reuse the OPT-136/142 accounting repairs for bounded matched decode probes
   at these three shapes (32 evals each, one warmup and three paired rounds).
   Keep instrumented attribution separate from uninstrumented timing. Report
   component ms, whole-engine ms/token and ratios with explicit denominators.
   Estimate the attention-only speedup ceiling from same-sitting family time;
   do not promise a whole-engine multiplier from a leaf-kernel ratio.
5. Deliver `evidence/optimization/opt150-attention-dataflow/REPORT.md`, raw
   matched records, dispatch table, compiled-code evidence and one independent
   time reconstruction. Reuse existing tooling, adding only missing diagnostic
   coverage with a bounded runnable check. Follow shared tiers, one build,
   GPU lock and explicit phase deadlines. Missing required evidence is
   incomplete/blocked; unresolved causal percentages may remain unknown.

Done means an authenticated current baseline and concrete OPT-151/152 inputs.
No production keep, NLL run, pin change or throughput improvement claim.
