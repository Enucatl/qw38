# OPT-141 — Collect matched llama counters for the selected gap families

Status: done. Dependencies: OPT-139, OPT-140. Coupled IDs: none.
Priority: P1. Plan impact: none; existing release and quality gates retained.

## Outcome

Resolve llama_ncu_kernel_identity_unresolved for the four OPT-138 phase/family selections and compare like-for-like counter evidence.

## Scope

Reuse OPT-136/138 private llama diagnostics and the pinned authority; do not alter the production dependency. Map trace launch identities to exact source functions, shapes, enclosing fused work and NCU selectors for decode attn_core, decode residual_norm_quant, prefill attn_core and prefill prompt_mmq. Resolve D128 and D2048 separately where dispatch differs.

Collect the largest-duration matched kernel per selected family/shape with authenticated real inputs, supported metric names and recorded replay/cache behavior. Bound the collection matrix before running: one collection per identified kernel and one diagnosed retry. Use explicit launch/range filters; do not profile an entire engine and attribute its counters to one kernel.

Compare Quartz and llama only where work, units and normalization match. If fusion differs, show the enclosing group and state which individual counters cannot be compared directly. P4096 kernel-leaf identities may be used with a demonstrated source/launch mapping; do not fabricate CUPTI graph-node IDs.

## Verification and completion

Test missing/ambiguous identities and fused-boundary mismatch rejection. Independently inspect one decode and one prefill raw report against the structured records. Deliver evidence/optimization/opt141-matched-llama-counters/REPORT.md and provenance-linked comparisons. Completion requires matched records for all four selections; an unresolved required identity is blocked, not a successful skip. A measured kernel may still have an unknown mechanism.

Update next-experiment recommendations only for supported mechanisms with a concrete source-grounded change and disconfirming measurement. No production changes, generic bandwidth conclusion from occupancy, or benchmark claims from NCU duration.

## Execution requirements

Read [TASK-TESTING-STRATEGY.md](TASK-TESTING-STRATEGY.md) and the dependency dossiers before implementation. Reuse existing runners, parsers and gates; add only necessary task registration and focused checks. Freeze an enumerated GPU matrix and per-child/aggregate deadlines before execution, use the shared GPU lock and reuse builds. Do not rerun until significant or weaken acceptance to fit a timeout. Missing hardware evidence is incomplete.

Record exact runnable commands, raw evidence paths, identity and independent verification in this dossier at execution. New artifact paths above are planned deliverables, not existing evidence. This task is drafted only; no implementation or measurement has run.

## Run record

### Planning (2026-09-15T00:08:00Z)
skipped — pre-authored dossier admitted

### Implementation (2026-09-15T00:32:00Z)

Frozen matrix: RTX 5090, image `qw38-cuda:13.0.2`, child 300s, prefill 1200s, aggregate 7200s, GPU lock `build/optimization-runs/qw38-gpu.lock`. Five identified kernels (residual D128/D2048 share `quantize_q8_1`). Diagnostics only; no production kernel or selector change. NCU duration is not a tok/s or keep metric.

```
uv run ruff format tools/opt141_matched_llama_counters.py tests/test_opt141_matched_llama_counters.py tools/opt139_counter_identity.py tools/run_optimization_task.py
uv run ruff check tools/opt141_matched_llama_counters.py tests/test_opt141_matched_llama_counters.py tools/opt139_counter_identity.py tools/run_optimization_task.py
```

outcome: 4 files left unchanged. All checks passed.

```
uv run pytest tests/test_opt141_matched_llama_counters.py tests/test_opt139_counter_identity.py tests/test_opt140_prefill_attention_replay.py -q
```

outcome: 29 passed.

```
uv run python tools/opt141_matched_llama_counters.py --phase preflight --mode acceptance --run-dir build/optimization-runs/opt141
```

outcome: `{"phase": "preflight", "ok": true}` under GPU lock. `make cuda-opt141-diagnostics` rc 0. NCU available. Pinned llama `cc83d7b4824f73cfdda4dfbb47ee39804f71b328`. Authenticated current pins: `decode_segments8`, OPT-137 dense MMA, ffn-only prerequisite unused.

```
uv run python tools/opt141_matched_llama_counters.py --phase identity --mode acceptance --run-dir build/optimization-runs/opt141
```

outcome: all four OPT-138 selections `matched` (D128/D2048 rows where dispatch is recorded separately). Decode attn `flash_attn_ext_vec<256,1>` (n_kv 128 vs 2048); residual `quantize_q8_1`; prefill attn `flash_attn_ext_f16<256,256,8,8>` via OPT-140 prompt-attention mapping; prefill mmq `mul_mat_q<GGML_TYPE_Q4_K,128,false>`. Largest decode attn leaf `flash_attn_combine_results` is enclosing fused sibling, not the matched compute kernel. `llama_ncu_kernel_identity_unresolved=false`.

```
uv run python tools/opt141_matched_llama_counters.py --phase counters --mode acceptance --run-dir build/optimization-runs/opt141
```

outcome first pass: `--nvtx-include` profiled nothing (llama uses `nvtxRangePushA`, not Start/End); `mul_mat_q<` missed NCU short name `mul_mat_q`. Diagnosed retry: drop NVTX filter; regex `flash_attn_ext_vec`, `flash_attn_ext_f16`, `^quantize_q8_1$`, `^mul_mat_q$`; `--kernel-name regex:… --launch-count 1 --replay-mode application`. Second pass: six typed records (five collections + residual D2048 reuse of D128). Prefill attn CSV reused from the same sitting. No full metric sweep.

```
uv run python tools/opt141_matched_llama_counters.py --phase report --mode acceptance --run-dir build/optimization-runs/opt141
```

outcome: `{"phase": "report", "ok": true, "status": "measured"}`. Report: `evidence/optimization/opt141-matched-llama-counters/REPORT.md`. Fixture: `fixtures/opt141_matched_llama_counters.json` (`claims_throughput=false`, `candidate=null`, `supported_mechanism=null`). Comparisons: decode attn D128/D2048 comparable (units match); residual and prompt_mmq `quartz_counters_unusable`; prefill attn `fused_boundary_mismatch` (Quartz 16x2 vs llama 8x8).

Independent raw-report inspection via `parse_ncu_output` / `typed_slots_from_launch`: decode `decode-attn_core-d128.ncu.csv` one launch `flash_attn_ext_vec` dram_read=`1.11 Mbyte` dram_throughput=`4.96 %` l2=`212987 sector`; prefill `prefill-attn_core-p4096.ncu.csv` one launch `flash_attn_ext_f16` dram_read=`151.94 Mbyte` dram_throughput=`4.87 %` l2=`226109556 sector`. Both match the structured records. NCU duration unused as a keep metric.

GPU after: RTX 5090 idle (34 MiB / 32607 MiB, 0%). No production changes. Not committed.

### Documentation (2026-09-15T00:30:47Z)

Reviewed implementation (`tools/opt141_matched_llama_counters.py`),
OPT-139 wiring (`parse_ncu_output`, `typed_slots_from_launch`, `kernel_stem`,
`admit_supported_mechanism`), OPT-140 prefill attn identity reuse, pins, fixture,
and acceptance run artifacts under `build/optimization-runs/opt141/` and
`evidence/optimization/opt141-matched-llama-counters/`.

**Pins/fixtures (consistent, unverified):**

- `pins/opt141_matched_llama_counters_contract.json` matches implementation
  constants: selector `decode_segments8`, parent
  `post124_plus_opt127_decode_segments8_plus_opt137_mma`, four OPT-138 selections,
  decode attn `flash_attn_ext_vec`, residual `quantize_q8_1`, prefill attn
  `flash_attn_ext_f16` via `prompt-attention`, prefill mmq `mul_mat_q`, timeouts
  300/1200/7200 s, max 5 collections + 1 retry per kernel.
- `pins/opt141_iteration_contract.json` acceptance tier sequence
  `preflight → identity → counters → report`;
  `performance_admission.instrumentation_only=true`.
- `fixtures/opt141_matched_llama_counters.json` status `measured`; required keys
  present; `claims_throughput=false`; `supported_mechanism=null`; `candidate=null`;
  `answers.llama_ncu_kernel_identity_unresolved=false`; all four selections
  `matched`.

**REPORT.md finalized** at
`evidence/optimization/opt141-matched-llama-counters/REPORT.md`:

- Provenance-linked structured JSON and raw artifact table (`.ncu-rep`/`.ncu.csv`/`.ncu.txt`).
- Identity table for all six rows (D128/D2048 separate where dispatch differs).
- First-pass NVTX/`mul_mat_q<` filter miss and diagnosed retry documented.
- Typed llama counter tables for five identified kernels (residual D2048 reuses D128).
- Quartz vs llama comparisons with fusion-boundary rejection notes.
- Decode attn D128/D2048 comparable where units match; residual and prompt_mmq
  `quartz_counters_unusable`; prefill attn `fused_boundary_mismatch` (16×2 vs 8×8).
- Independent raw-report inspection section (decode D128 + prefill P4096 attn).
- Mechanism admission explicitly unknown (`throughput_alone_cannot_establish_mechanism`).
- Performance evidence checklist section (diagnostics; shipping delta N/A).

**Host tests (unverified rerun by documentation agent):** prior implementation pass
reported 29 passed (`test_opt141_matched_llama_counters.py`,
`test_opt139_counter_identity.py`, `test_opt140_prefill_attention_replay.py`).

**Verifier focus:**

1. Confirm all four selections `matched` with authenticated llama launch mappings
   in `identity.json` and typed counter records in `counter-evidence.json`.
2. Confirm raw CSV rows map to structured slots (independent decode + prefill attn
   inspection via `parse_ncu_output` / `typed_slots_from_launch`).
3. Confirm D128/D2048 decode attn and residual authenticated separately; prefill attn
   uses `flash_attn_ext_f16` not `flash_attn_ext_vec`.
4. Confirm fused-boundary mismatches reject leaf comparison where fusion differs.
5. Confirm no production selector/kernel change and `claims_throughput=false`
   throughout.
6. Label all causal mechanism conclusions **unverified** until independent verifier
   pass.

Did not commit.

### Verification (2026-09-15T00:31:30Z)

**Verdict: PASS**

Independent verification against dossier scope, ledger acceptance (`All four selected
phase/family comparisons have authenticated llama launch mappings and matched counter
evidence`), and task completion criteria. No semantic fixes applied.

#### Gates

```
uv run pytest tests/test_opt141_matched_llama_counters.py tests/test_opt139_counter_identity.py tests/test_opt140_prefill_attention_replay.py -q
```

outcome: **29 passed** in 0.08s.

```
uv run ruff check tools/opt141_matched_llama_counters.py tests/test_opt141_matched_llama_counters.py tools/opt139_counter_identity.py tools/run_optimization_task.py
```

outcome: All checks passed.

#### Acceptance evidence

| Criterion | Evidence | Result |
|-----------|----------|--------|
| Four OPT-138 selections matched | `identity.json` selections all `matched`; fixture `answers.selections` | PASS |
| Authenticated llama launch mappings | Six identity rows `identity.ok=true` with trace sqlite + source function + replay family | PASS |
| D128/D2048 authenticated separately | decode attn `n_kv` 128 vs 2048; residual shared dispatch noted | PASS |
| Matched counter evidence per selection | `counter-evidence.json`: decode attn ×2, residual ×2 (D2048 reuse), prefill attn ×1, prompt_mmq ×1; all `error=null` | PASS |
| Independent raw-report inspection | `decode-attn_core-d128.ncu.csv` and `prefill-attn_core-p4096.ncu.csv`: 1 launch each, stems and typed slots match structured records | PASS |
| Fused-boundary rejection where fusion differs | residual `fused_boundary_mismatch`; prefill attn tile 16×2 vs 8×8; comparisons mark incomparable | PASS |
| Decode attn largest leaf not matched compute | `flash_attn_combine_results` enclosing; matched `flash_attn_ext_vec` | PASS |
| `llama_ncu_kernel_identity_unresolved=false` | fixture `answers`, identity phase sidecar | PASS |
| Bounded NCU counters (not throughput sample) | typed DRAM/L2/SM/tensor/occupancy; `claims_throughput=false`; `supported_mechanism=null` | PASS |
| Required artifacts present | `REPORT.md`, `identity.json`, `counter-evidence.json`, `comparisons.json`, `fixtures/opt141_matched_llama_counters.json`, raw `.ncu-rep`/`.ncu.csv`/`.ncu.txt` for five kernels | PASS |
| No production change | `production_kept=true`; `production_selector_change=false` in contract | PASS |

Llama revision `cc83d7b4824f73cfdda4dfbb47ee39804f71b328`. Status `measured`.
Diagnostics only; no optimization shipped; NCU duration not a keep metric.

### Final outcome

- Status: `done`
- Verdict: diagnostics complete (not a keep/reject)
- Acceptance evidence:
  [`fixtures/opt141_matched_llama_counters.json`](../fixtures/opt141_matched_llama_counters.json);
  [`evidence/optimization/opt141-matched-llama-counters/REPORT.md`](../evidence/optimization/opt141-matched-llama-counters/REPORT.md);
  verification record above (2026-09-15T00:31:30Z)
- Candidate measured delta: **N/A**; shipping delta: **N/A**; quality result:
  **N/A**
- Mechanism: **explicitly unknown** (`throughput_alone_cannot_establish_mechanism`;
  no named source/SASS observations); `supported_mechanism=null`; `candidate=null`
- All four OPT-138 selections `matched` with authenticated llama launch mappings;
  `llama_ncu_kernel_identity_unresolved=false`; D128/D2048 decode attn and residual
  authenticated separately; prefill attn via OPT-140 `flash_attn_ext_f16` mapping
- Typed bounded NCU counters for five identified llama kernels (residual D2048 reuses
  D128 collection); raw `.ncu-rep`/`.ncu.csv`/`.ncu.txt` artifacts; profiled runs are
  not throughput samples
- Comparisons: decode attn D128/D2048 comparable where units match; residual and
  prompt_mmq `quartz_counters_unusable`; prefill attn `fused_boundary_mismatch`
  (Quartz 16×2 vs llama 8×8)
- First-pass acceptance: yes (2026-09-15T00:31:30Z)
- Remaining risk: causal mechanism unknown without SASS/source proof; no production
  changes

