# OPT-074 — Complete production-shape llama GPU numerical admission

Status: done. Dependencies: OPT-059, OPT-071, OPT-072. Coupled IDs: none.
Priority: P0. Target: missing authority evidence blocking cooperative Q4.
Read [the post-069 protocol](PERFORMANCE-RECOVERY-POST-069.md).
Quality risk: wrong authority dispatch/reference; no production pin changes.

## Run record

### Planning (2026-09-11T16:05:00Z)

skipped — pre-authored dossier admitted (coordinator)

### Implementation (2026-09-11T16:26:12Z)

- Agent/model: implementation (Cursor Grok 4.6). No commit. Ledger left
  `in_progress`. `plan.md` unchanged. No production pin or kernel keep.
  `claims_throughput: false`.
- Authority: pinned llama.cpp `cc83d7b4824f73cfdda4dfbb47ee39804f71b328`, GGUF
  SHA-256 `31629f53165ab6a7dad8c9847dcfd1fdf55829dac1e6e748f4a68581b0033d34`.
  Exporter binds real tensor names and captured BF16 (lossless to FP32, originals
  retained). Evidence path forbids `--row-limit`. Observed dispatch is `mmvq`,
  N=1, full M. CUDA synchronize before host reads. Q8_1 bytes from private
  adapter `quantize_q8_1_sum_x`, not `ggml_cpy` F32→Q8_1. Fused gate/up/SwiGLU
  exported where requested (`fused_gate_up_swiglu: true`).
- Frozen OPT-059 layers/tokens used. 48 GPU cases measured; 12 attention×Q8
  mixer cases explicit `absent_layer_kind` (not substituted). OPT-059 historical
  missing-evidence records left in place.
- v2 freeze from calibration llama-vs-FP64 error before any candidate read.
  Held-out did not enlarge those ceilings. Q6 vocab K5120 is v2-admitted.
  Q4 gate/up, Q4 down, and both Q8 mixer families remain unadmitted because
  held-out abs/RMS exceeded the frozen calibration ceilings. Missing GPU
  outputs were not used as a successful policy-only freeze.
- Files changed:
  - `tools/llama_authority/projection_export.cpp`
  - `tools/llama_authority/build_projection_export.sh`
  - `cuda/full_scheduler.h`
  - `cuda/full_scheduler.cu`
  - `cuda/opt043_activation_capture_test.cu`
  - `Makefile`
  - `pins/opt074_production_gpu_admission_contract.json`
  - `pins/opt074_iteration_contract.json`
  - `fixtures/opt074_production_gpu_admission.json`
  - `tests/test_opt074_production_gpu_admission.py`
  - `tools/opt074_production_gpu_admission.py`
  - `evidence/optimization/opt074-production-gpu-admission/REPORT.md`
  - `tasks/OPT-074.md`
- Commands:

```
uv run pytest -q tests/test_opt074_production_gpu_admission.py
```

**pass** — 11 passed in 0.03s.

```
uv run python tools/run_optimization_task.py --task OPT-074 --mode feedback --phase q4-gate-up
```

**pass** — exit 0; `elapsed_s=2.686`; deadline 300s; product=1; full-M N=1
`mmvq` on `blk.0.ffn_gate.weight` (M=17408); fused SwiGLU export true;
`historical_oracles=none`; run
`build/optimization-runs/OPT-074/feedback/20260911T162355Z-8408513f`.
First attempt failed host-side `libcudart.so.13`; exporter now runs in
`qw38-llama-authority:cuda-13.0.2`.

```
uv run python tools/run_optimization_task.py --task OPT-074 --mode feedback --phase q4-down
```

**pass** — exit 0; `elapsed_s=2.094`; deadline 300s; product=1; full-M N=1
`mmvq` on `blk.0.ffn_down.weight` (M=5120); run
`build/optimization-runs/OPT-074/feedback/20260911T162411Z-8f2a5dd1`.

```
uv run python tools/run_optimization_task.py --task OPT-074 --mode acceptance --phase q4
```

**pass** — exit 0; `elapsed_s=1.684` (identity-matched reference cache);
deadline 300s; product=24; all Q4 calib+held-out GPU outputs present;
v2 unadmitted (held-out exceeds frozen calib ceilings); run
`build/optimization-runs/OPT-074/acceptance/20260911T162605Z-c73365f9`.
Uncached measurement was
`build/optimization-runs/OPT-074/acceptance/20260911T162419Z-9222822a`
(`elapsed_s=33.292`).

```
uv run python tools/run_optimization_task.py --task OPT-074 --mode acceptance --phase q8-q6
```

**pass** — exit 0; `elapsed_s=12.192`; deadline 300s; product=36; Q8 mixer
attention layers explicit absent; Q6 vocab v2-admitted; Q8 mixer held-out
exceeds calib ceilings; run
`build/optimization-runs/OPT-074/acceptance/20260911T162612Z-10a8e04f`.

## Implementation

1. Extend existing `tools/llama_authority/projection_export.cpp` and its pinned
   private build. It already accepts GGUF/tensor/input; the previous sitting
   exported only synthetic Q4. Bind real tensor names and exact captured BF16
   values converted losslessly to FP32 for the authority, retaining originals.
   Model/authentication, producer revision and reference flags are recorded.
2. Calibrate gate and up K5120, down K17408 first; then Q8 mixer K5120/K6144
   and Q6 vocab K5120 in separate family phases. Preserve actual full M and
   N=1 dispatch, even though output/host-reference checks sample 16 rows.
   Current --row-limit changes M and may choose another CUDA kernel: do not
   use it as evidence of production reduction. Instrument actual dispatch and
   separately export fused gate/up/SwiGLU where the production authority fuses.
3. Use OPT-059's frozen layers 0/3/31/32 at tokens 128/512 for calibration and
   62/63 at 2048/4095 for held-out. Validate family presence against inventory;
   absent layer-kind combinations are explicit, never substituted silently.
   Each named layer/family phase checks at most 16 rows x 4 available captured
   vectors, full K. Decode host rows once and reuse them. Export only the
   relevant full GPU projection(s), not a fresh full model per sampled row.
4. Synchronize the CUDA backend before reading outputs. Independently verify
   original-input error, exact-staged-input error, signedness and scales.
   If needed, export Q8_1 bytes from the actual quantization launcher through
   the private adapter; do not repeat unsupported ggml_cpy F32-to-Q8_1.
   Name sum_q/sum_x/exact-integer-sum variants and identify consumers.
5. Freeze OPT-059 v2 ceilings from authority data before reading candidate
   errors. Preserve v1 and historical missing evidence. Update per-family
   admission records only for measured exact shape/staging/variant coverage;
   wildcard columns=0 is not production coverage. Held-out data validates,
   never enlarges the calibration ceiling. Q4 admission is not contingent on
   finishing unrelated Q6 calibration, but its required Q4 phases must finish.
6. Produce reference cache keys, row/token lists, actual launcher records,
   finite checks and a concise admission table consumable by OPT-075 onward.
   Missing production GPU outputs must yield a blocked deliverable, not
   another successful policy-only freeze with Q4 still unrepresented.

## Tests and commands

Host tests mutate model/tensor/K/M, producer/consumer tags, row lists and
authority revision; test wrong dispatch, missing rows and nonfinite references.
Reuse `build/qw38-cuda-opt059-numerics-test` and the existing pinned
`qw38-llama-projection-export` target. No new general backend.

Feedback selects one listed layer/family case, at most 64 FP64 dots per
projection. Full GPU M is affordable for a projection and preserves dispatch;
full host M references and repeated token-generation captures are forbidden.
Acceptance is one execution of each frozen reference case, not a timing sweep.
Split explicit layer/family phases at the 300-second boundary and cache outputs.

```sh
uv run pytest -q tests/test_opt074_production_gpu_admission.py
uv run python tools/run_optimization_task.py --task OPT-074 --mode feedback --phase q4-gate-up
uv run python tools/run_optimization_task.py --task OPT-074 --mode feedback --phase q4-down
uv run python tools/run_optimization_task.py --task OPT-074 --mode acceptance --phase q4
uv run python tools/run_optimization_task.py --task OPT-074 --mode acceptance --phase q8-q6
```

The grouped acceptance commands expand an explicitly listed finite case plan;
they resume identity-matched reference work, never recalculate a finished
reference because a candidate changed. Stop on unsupported dispatch, absent
captures or nonfinite authority; do not infer a budget from another shape.

## Verification (2026-09-11T16:28:16Z)

Independent integration verifier. No commit. Ledger left `in_progress`.
`plan.md` unchanged.

### Diff vs dossier / ledger / plan

| Requirement | Status |
|---|---|
| `tools/llama_authority/projection_export.cpp` | modified — real tensor names, BF16→FP32, `quantize_q8_1_sum_x`, fused SwiGLU, `cudaDeviceSynchronize`, `--row-limit` forbidden |
| `tools/llama_authority/build_projection_export.sh` | present (untracked) |
| `cuda/full_scheduler.h` / `cuda/full_scheduler.cu` | modified — activation capture hooks |
| `cuda/opt043_activation_capture_test.cu` | modified — BF16 capture export |
| `Makefile` (`cuda-opt074-diagnostics`) | modified |
| `pins/opt074_production_gpu_admission_contract.json` | present (untracked) |
| `pins/opt074_iteration_contract.json` | present (untracked) |
| `fixtures/opt074_production_gpu_admission.json` | present (untracked) |
| `tests/test_opt074_production_gpu_admission.py` | present (untracked), 11 tests |
| `tools/opt074_production_gpu_admission.py` | present (untracked) |
| `evidence/optimization/opt074-production-gpu-admission/REPORT.md` | present (untracked) |
| `implementation_ledger.md` OPT-074 `in_progress` | modified (not `done`) |
| `plan.md` unchanged | confirmed (no diff) |
| No production pin change | confirmed (new OPT-074 pins only; existing pins untouched) |
| Authority pinned llama `cc83d7b` + GGUF SHA | confirmed in contract |
| Full-M N=1 `mmvq`, no `--row-limit` evidence | confirmed in exporter and admission checks |
| OPT-059 historical missing evidence preserved | confirmed |
| Q6 vocab v2-admitted; Q4/Q8 mixer unadmitted | confirmed in REPORT admission table |

### Commands

```
uv run ruff format .
```

**PASS** — 161 files left unchanged; no retest needed.

```
uv run ruff check tests/test_opt074_production_gpu_admission.py tools/opt074_production_gpu_admission.py
```

**PASS** — All checks passed.

```
uv run pytest -q tests/test_opt074_production_gpu_admission.py
```

**PASS** — 11 passed in 0.03s.

Implementation-phase runner commands (q4-gate-up, q4-down, q4, q8-q6) recorded
above under Implementation; all passed within 300 s deadlines.

### Verdict

**PASS** — host contract, full-M N=1 llama GPU dispatch evidence, frozen OPT-059
v2 ceilings, held-out validation, and admission table meet OPT-074 acceptance.
Ledger correctly left `in_progress`; no production pin change; no tok/s claim
(`claims_throughput=false`).

### Delivery (2026-09-11T16:28:16Z)

Delivery agent. Commit and push authorized by explicit verifier/delivery stage.
Marked OPT-074 `done` in ledger; coupled IDs: none.

### Final outcome (2026-09-11T16:28:16Z)

Full-M N=1 llama GPU numerical admission on captured BF16 activations with
pinned authority `cc83d7b` and GGUF SHA `31629f53…`. 48 GPU cases measured; 12
attention×Q8 mixer cases explicit `absent_layer_kind`. v2 ceilings frozen from
calibration before candidate reads; held-out did not enlarge them. Q6 vocab
K5120 v2-admitted; Q4 gate/up, Q4 down, and both Q8 mixer families remain
unadmitted (held-out exceeds frozen calib ceilings). Host contract and 11 pytest
cases passed; implementation feedback/acceptance phases passed within 300 s.
Coupled IDs: none. First-pass delivery. Retries: 0. No production pin change.
No tok/s claim (`claims_throughput=false`).
