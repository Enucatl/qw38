# OPT-042 — Study integer decode MMV admissibility

## Control

- Primary ID: OPT-042
- Coupled IDs: none
- Dependencies: OPT-038 (explicit prerequisite), OPT-034 done
- Status: pending
- Ledger acceptance: Retain paired FP32-packed versus Q4_K integer-dot diagnostic timings and complete numeric results using unchanged FP32-scale Q8 staging on synthetic and real FFN inputs, report fixed CUD-001 eligibility and a promotion/rejection recommendation without changing production dispatch or keep denominators; `claims_performance_improvement: false`.

## Goal and boundaries

Diagnostic only. Scope Q4_K FFN gate/up/down decode MMV. Keep Quartz's exact
BF16-to-Q8Block staging, fixed CUD-001 max abs 3e-4/RMS 2e-4, zero nonfinites,
public APIs, production selector, graphs, and all old fixtures. No Q6_K/logits,
Q8 staging change, GLU fusion, fast math, tolerance change, or allocation.
Classification: CUDA/numeric research; plan impact none.

## Repository evidence

- `cuda/quant_mmv.cu:104-215` — packed production FP32 MMV.
- `cuda/full_scheduler.cu:712-760` — three decode FFN MMV calls.
- `.cache/authorities/llama.cpp/ggml/src/ggml-cuda/mmvq.cu` and `vecdotq.cuh` — integer Q4_K dots and scale/min decomposition.
- `pins/cuda_quant_contract.json` — staging and fixed decode envelope.

## Implementation decisions

Add a study executable, contract, fixture, report, raw sidecars, and host test.
Candidate keeps Q8Block staging, computes packed `__dp4a` sums for each Q4_K
32-value subgroup plus the Q8 sum, then applies the decoded scale/minimum in
FP32 and folds subgroups in ascending order. Baseline is `launch_quant_mmv`
with `packed` loads. Time both pre-staged MMV and complete quantize-plus-MMV.
Use synthetic probes and real layer 0/3/63 normalized FFN inputs; retain input
checksums and all numeric metrics. This study cannot install the candidate.

## Acceptance and validation

Shapes: 17x256, 257x512, 17408x5120 (weight 128), and 5120x17408 (weight 64).
Use three warmups and 30 alternating CUDA-event pairs, reset inputs outside
timing, and weighted mean `(128*gate_up + 64*down)/192`. Record max abs, RMS,
relative distribution, cosine, first failure, nonfinites, staging equality,
candidate/baseline/host comparisons separately. Classify `eligible_and_faster`,
`numeric_reject`, or `performance_reject`; all are successful measurement
outcomes, with `claims_performance_improvement=false` and no successor oracle.
Focus: `uv run pytest -q tests/test_opt042_mmv_integer_study.py tests/test_documentation.py`;
hardware adds `QW38_RUN_CUDA_TESTS=1`; repository checks are `uv run ruff check .`
and `uv run pytest -q`. Update MMV/full-optimization docs and audit. A future
production task, if justified, must rerun unchanged OPT-021/032 protocols.

## Run record

Planning proposed 2026-09-10; implementation and verification not run.
