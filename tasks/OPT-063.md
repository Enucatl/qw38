# OPT-063 — Fuse cooperative integer gate/up and SwiGLU

Status: done. Dependencies: OPT-062. Coupled IDs: none.

## Run record

### Planning (2026-09-11T08:35:00Z)

skipped — pre-authored dossier admitted (coordinator)
Primary target: D2048. Read [the batch protocol](PERFORMANCE-RECOVERY-2026-09-11.md).

## Preconditions and implementation

Proceed only if OPT-062 produced a numerically admitted cooperative Q4 variant.
If it rejected every integer variant for quality, close this task as a documented
no-change with that evidence; do not build a fusion around invalid arithmetic.
If only performance was rejected, the admitted arithmetic can still be tested
against current production as a combined fused candidate.

Read `cuda/q4k_decode_dots.cuh::vec_dot_q4k_group`/`q4k_coop_mmv`,
`cuda/quant_mmv.cu::quant_mmv_q4k_gate_up_swiglu`,
`cuda/full_scheduler.cu::execute_ffn`, and pinned
`mmvq.cu::mul_mat_vec_q`'s `has_fusion` handling.

1. Add a focused paired kernel in `cuda/q4k_decode_dots.cu` or a new small TU.
   Keep two original GGUF weight pointers; no concatenation or repack. Each CTA
   owns the same output row in gate and up, K split across4 warps initially.
   Load an activation pack once per K/group and feed two packed Q4 dot paths.
   Keep gate and up FP32 accumulators separate through K.
2. Start with the exact OPT-062 accepted dot/staging semantics, including scale
   and min corrections. Refactor only the shared pack decoding needed to reuse
   activation loads. The existing kernel performs group shuffles inside K;
   do not simultaneously invent a different reduction tree here. Reduce both
   accumulators with the same row-completion mapping as the unfused control.
3. After the complete row reduction, compute existing SwiGLU arithmetic
   `silu(gate)*up` and round once to BF16. Write `ffn_activated_` directly;
   gate/up FP32 output arrays are unnecessary in normal execution. Do not fuse
   the following32-value Q8 activation quantization across independently scheduled
   row CTAs; keep its existing separate stage before down.
4. Candidate list: admitted separate integer FFN control and paired 4-warps.
   Add paired 2-warps only if compiler/resource evidence shows a concrete4-warp
   problem; do not sweep1/2/4/8 again. Report registers, spills, shared memory
   and active blocks. Keep current production as an extra performance control
   if OPT-062 did not install the separate integer path.
5. Add an explicit paired-integer selector and its staging format to
   `ffn_decode_path.cuh`, wire `execute_ffn`, invalidate reused activation caches
   and recapture FFN graphs. Trace mode may use the unfused equivalent when
   separate gate/up taps are requested; label the fallback and test numerical
   equivalence instead of inventing fused intermediate buffers in production.

## Fast tests and commands

Target `build/qw38-cuda-opt063-integer-ffn-test`; pytest
`tests/test_opt063_integer_ffn.py`.

Smoke: M17/K256, two implementations, three activation patterns, all outputs.
Include tail CTA and large negative gate values. Correctness: M33/K512 plus
actual FFN M17408/K5120,16 sampled rows × 4 real vectors; validate gate/up
temporary diagnostic results, BF16 activation and down/residual on captured
layers 0/31/63. Reuse decoded/reference rows; no host full17408× 5120 GEMV.
Check full GPU finite counts, output guards, selected paired launch and
same-configuration graph/eager equality. Shared-memory barriers must be reached
by all CTA threads even on tail rows; run one tiny race/memory sanitizer case
only if a synchronization or bounds concern appears.

Screen:2 configurations ×(1 warmup+3 paired samples), one rotating complete
FFN set. Optional third candidate is a separate logged experiment, within the
same 300-second budget. A gate/up-only win cannot promote if full FFN regresses.

```sh
uv run pytest -q tests/test_opt063_integer_ffn.py
uv run python tools/run_optimization_task.py --task OPT-063 --mode feedback
uv run python tools/run_optimization_task.py --task OPT-063 --mode acceptance
```

## Acceptance

Use batch component/short D2048 acceptance and current arithmetic admission.
Run affected graph and production quality gates before a default changes.
Deliver contract/fixture and `evidence/optimization/opt063-integer-ffn/REPORT.md`.
Weight the combined gate/up launch64 times, down 64 times. Include staging and
SwiGLU in cost. Retain the accepted unfused implementation on loss; no prompt
kernel, alternative exp approximation, persistent repack or global flags here.

### Implementation (2026-09-11T09:09:08Z)

Cursor Grok 4.6 implementation agent. No commit.

Added Q8Block pack-load sharing in `q4k_decode_dots.cuh` and a paired 4-warp
kernel `q4k_coop_gate_up_swiglu` that keeps two GGUF pointers, separate FP32
accumulators, the OPT-062 group-shuffle reduction, and admitted
`silu(gate)*up` rounded once to BF16. Selector `paired_integer` plus
`integer_q8` wires `execute_ffn` and OPT-061 replay. Trace taps fall back to
unfused integer and label `q8_fp32_unfused_trace`. Production pins stay
`packed` / `paired_staged`. 4-warp has 40 registers, 0 spills, 1024 B shared,
12 active blocks; 2-warp was not launched.

Commands:

- `uv run pytest -q tests/test_opt063_integer_ffn.py` — **pass** (5 tests)
- `uv run python tools/run_optimization_task.py --task OPT-063 --mode feedback` — **pass** (`ok`, 29.88 s, [feedback run](../build/optimization-runs/OPT-063/feedback/20260911T090723Z-cdfa6112))
- `uv run python tools/run_optimization_task.py --task OPT-063 --mode acceptance` — **pass** (`ok`, 4.95 s, screen 2.98 s, [acceptance run](../build/optimization-runs/OPT-063/acceptance/20260911T090800Z-0da6d4a6))

Smoke/correctness: fused vs unfused vs staged FP64 abs **0** on M17/K256,
M33/K512, sampled M17408/K5120 (16 rows × 4 vectors). Graph/eager equal.
Complete FFN rotating: unfused **12.3049278** ms, fused **11.2725649** ms,
packed control **15.8205442** ms. Layers 0/31/63 residual/down/activated
max_abs **0**. Screen winner `paired_integer`; not installed (OPT-062 integer
is not production; OPT-059 `v2_admitted=false`). `claims_throughput=false`.

### Verification (2026-09-11T09:10:28Z)

Independent verifier (composer-2.5). Model: composer-2.5;
`telemetry_unavailable`. Verdict: **PASS** (wired, packed retained).

Commands:

- `uv run ruff format .` — **pass** (146 files unchanged)
- `uv run ruff check .` — **pass**
- `uv run pytest -q tests/test_opt063_integer_ffn.py` — **pass** (5 tests, 0.02 s)
- `uv run python tools/run_optimization_task.py --task OPT-063 --mode feedback` — **pass** (`ok`, 1.93 s, [feedback run](../build/optimization-runs/OPT-063/feedback/20260911T091010Z-4024fe4e))
- `uv run python tools/run_optimization_task.py --task OPT-063 --mode acceptance` — **pass** (`ok`, 4.88 s, screen 2.98 s, [acceptance run](../build/optimization-runs/OPT-063/acceptance/20260911T091014Z-b6ab1891))

Screen (rotating 64-layer complete FFN, 1 warmup + 3 samples): unfused integer
**12.3049278** ms, paired_integer **11.2725649** ms, packed control
**15.8205442** ms. Residual/down/activated prefix/graph-eager max_abs **0**,
nonfinite **0**. Production pin stays `packed` / `paired_staged`
(`paired_integer_installed=false`).

Throughput: **no tok/s** (`claims_throughput=false`; component complete-FFN ms
only; no D2048 probe). Speedup delta vs baseline **0** (production unchanged).

