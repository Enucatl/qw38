# OPT-070 — Revalidate installed Q8/MMQ keeps with actual acceptance

Status: done. Dependencies: OPT-069, OPT-071, OPT-073, OPT-074. Coupled IDs: none.
Priority: P0. Target: trustworthy OPT-064/066 admission and causal E2E deltas.
Read [the post-069 protocol](PERFORMANCE-RECOVERY-POST-069.md).

## Run record

### Planning (2026-09-11T16:15:00Z)

skipped — pre-authored dossier admitted (coordinator)

## Evidence and scope

Both keeps are installed. Their runner acceptance schedules only
smoke/correctness/screen; native Q8 acceptance still uses 1+3 rounds. OPT-064
times three layers per group with random activations and excludes attention
output. OPT-066 uses layer 0 and a mean-only keep. No new kernel/layout sweep.
Quality risk: low for the harness, existing arithmetic risks stay explicit.
Instrumentation: native observed counts, paired rounds and effective selectors.

## Implementation

1. Repair the runner's performance-admission validation using narrow explicit
   task contracts. Read actual native warmup/round/candidate/shape/tier counts;
   reject disagreement with the plan, missing pairs, reused sample IDs,
   unexecuted acceptance, and a screen-only `keep=true`. Instrumentation-only
   tasks may succeed without speed admission but cannot claim a keep.
2. Reuse repaired OPT-071 replay with cached real inputs. Q8 control `r1_w4`
   versus installed `r2_w2`, all 48 GDN input/output and 16 attention
   input/output projection groups in true layer order; include staging/norm
   costs at the declared complete boundary. Hold arithmetic and cache reset
   identical. Prompt control `fma_async` versus `fma_async_x`, tile 128x128,
   all 64 complete FFNs with staging, SwiGLU, down and residual.
3. Add scoped overrides in the production path if absent and record actual
   launches after separate graph capture. Do not toggle only the test TU's
   inline selector. Keep OPT-058 staging invalidation on both sides.
4. Use current source for both configurations, changing one factor at a time.
   Measure Q8 at D2048 with X-prefetch held fixed; measure X-prefetch at P4096
   with the resulting Q8 decision fixed. If cross-workload guards reveal an
   interaction, one separately declared four-configuration screen may diagnose
   it; no unbounded factorial sweep. Historical OPT-056 is context, not the A/B.
5. Check arithmetic against OPT-074 coverage and full selected OPT-073 quality;
   row grouping can change FP32 association even when staging is unchanged.
   Missing coverage is unresolved admission, not a numerically validated keep.
   For each keep separately report raw paired component and E2E estimates,
   intervals, proof boundary and retain/revert/inconclusive. Revert only when
   replacement/control admission passes; do not undo unrelated scheduler fixes.
   For inconclusive evidence leave the shipping state unchanged and label its
   admission unresolved rather than claiming a validated keep.
6. Generate OPT-070 evidence from this sitting. Reconcile OPT-069's stale
   unmeasured report with its measured fixture using retained data and label
   the reporting correction. Do not alter historical numerical samples.

## Bounded tests and commands

Host target: `tests/test_opt070_keep_revalidation.py`; fake subprocesses prove
screen-only acceptance and wrong native counts fail. Native reuse target:
`build/qw38-cuda-component-replay`; engine reuse target:
`build/qw38-cuda-optimization-engine-probe`. Each feedback invocation selects
one family, two configurations x (1+3) complete rounds, sampled reference
16 rows x 4 vectors per projection shape. Captures are setup, cached once.
Acceptance runs the common 3+10 component pairs then five target engine pairs;
split component/engine into declared phases if necessary, never replay all
work because report assembly failed. No historical P/D sitting.

```sh
uv run pytest -q tests/test_opt070_keep_revalidation.py tests/test_optimization_loop.py
uv run python tools/run_optimization_task.py --task OPT-070 --mode feedback --phase q8
uv run python tools/run_optimization_task.py --task OPT-070 --mode feedback --phase mmq
uv run python tools/run_optimization_task.py --task OPT-070 --mode acceptance --phase q8
uv run python tools/run_optimization_task.py --task OPT-070 --mode acceptance --phase mmq
```

Acceptance: both installed changes have a valid individual verdict, production
dispatch is proven, and malformed evidence cannot install a new keep. Stop a
family on correctness failure, missing capture identity or inconsistent native
counts. A real loss is a revert decision, not a request for another tile/layout.

## Implementation

### Runner and contracts

`tools/run_optimization_task.py` now reads native warmup/round/candidate/shape/tier
counts and rejects screen-only `keep=true`, wrong counts, reused sample IDs,
missing pairs, unexecuted acceptance, and instrumentation-only keep claims.
Per-mode `mode_overrides` let one family phase use 1+3 on feedback and 3+10 on
acceptance. Pins: `pins/opt070_iteration_contract.json`,
`pins/opt070_keep_revalidation_contract.json`.

### Native replay and engine

Complete rotating mixer now includes GDN and attention **output** projections,
RMS/Q8_1 staging, and OPT-058 invalidation. Scoped production overrides:
`apply_q8_layout_ident` / `Q8DecodeLayoutScope` and `set_mmq_async_x_override`.
Engine `q8-ab` / `mmq-ab` apply the override **before** a separate graph capture
per side. After-capture `set_mmq_pipeline_path_override("off")` is unchanged.
Makefile target `cuda-opt070-diagnostics`.

### This sitting

Host: `tools/opt070_keep_revalidation.py`,
`tests/test_opt070_keep_revalidation.py`,
`fixtures/opt070_keep_revalidation.json`,
`evidence/optimization/opt070-keep-revalidation/REPORT.md`.
OPT-069 `REPORT.md` / fixture rewritten from retained sidecars (reporting
correction); historical samples unchanged. P4096 Quartz 2914.65698 tok/s vs
llama 3142.517034.

| Command | Outcome |
|---|---|
| `uv run pytest -q tests/test_opt070_keep_revalidation.py tests/test_optimization_loop.py` | 25 passed |
| `uv run python tools/run_optimization_task.py --task OPT-070 --mode feedback --phase q8` | ok, 13.4 s |
| `uv run python tools/run_optimization_task.py --task OPT-070 --mode feedback --phase mmq` | ok, 13.9 s |
| `uv run python tools/run_optimization_task.py --task OPT-070 --mode acceptance --phase q8` | ok, 23.3 s |
| `uv run python tools/run_optimization_task.py --task OPT-070 --mode acceptance --phase mmq` | ok, 44.8 s |

Q8 D2048 complete rotating mixer (48 GDN + 16 attention groups, 64 layers):
acceptance n=10 control `r1_w4` 7.455 ms vs `r2_w2` 7.506 ms; paired CI
(control-candidate) −0.0726 .. −0.0303 ms. Engine D2048+32 five pairs 874.076 vs
876.572 ms; 95% UB 3.81 ms ≤ 2% of control (17.48 ms). Launches recorded
`r1_w4` rows=1 and `r2_w2` rows=2 after separate graph capture.

MMQ P4096 64 complete FFNs, tile 128×128: acceptance n=10 control `fma_async`
887.292 ms vs `fma_async_x` 838.523 ms; paired CI 47.59 .. 49.95 ms. Engine
P4096 five pairs 1437.039 vs 1388.133 ms; non-regression holds. Launches
recorded `q4_i128_j128_fma_async` vs `q4_i128_j128_fma_async_x`.

### Verdicts

Both keeps: **inconclusive** (`opt074_coverage_unadmitted`). Mixer Q8 and Q4
gate/up/down families are not v2-admitted; missing coverage is unresolved
admission, not a validated keep. Revert is forbidden without control admission.
Shipping stays `r2_w2` / `fma_async_x`. OPT-073 quality-v3 engine non-regression
passes; absolute arithmetic stays failed.

**tok/s delta versus the then-current baseline:** OPT-069 P4096 Quartz
2914.65698 tok/s (baseline unchanged). Absolute delta 0, speedup 0. Llama
3142.517034 (informational). D2048 Quartz 35.4498482 vs llama 67.3394867.

## Verification (2026-09-11T17:05:50Z)

Independent integration verifier. `plan.md` unchanged.

### Diff vs dossier / ledger / plan

| Requirement | Status |
|---|---|
| Runner fail-closed admission (`run_optimization_task.py`) | modified — native counts, screen-only keep rejection, mode overrides |
| `tools/opt070_keep_revalidation.py` | present (untracked) |
| `tests/test_opt070_keep_revalidation.py` | present (untracked), 7 tests |
| `pins/opt070_iteration_contract.json` | present (untracked) |
| `pins/opt070_keep_revalidation_contract.json` | present (untracked) |
| `fixtures/opt070_keep_revalidation.json` | present (untracked) |
| `evidence/optimization/opt070-keep-revalidation/REPORT.md` | present (untracked) |
| OPT-069 reporting correction | modified — `REPORT.md`, fixture, `opt069_batch_gate.py` |
| Scoped Q8/MMQ production overrides | modified — `q8_decode_path.cuh`, replay/engine TUs |
| Both keeps inconclusive (`opt074_coverage_unadmitted`) | confirmed in REPORT and fixture |
| Shipping unchanged (`r2_w2`, `fma_async_x`) | confirmed |
| No production pin change | confirmed |
| `plan.md` unchanged | confirmed (no diff) |

### Commands

```
uv run ruff format .
uv run ruff check .
```

**PASS** — 163 files left unchanged; all checks passed.

```
uv run pytest -q tests/test_opt070_keep_revalidation.py tests/test_optimization_loop.py
```

**PASS** — 25 passed in 0.14s.

### Evidence spot-check

[`evidence/optimization/opt070-keep-revalidation/REPORT.md`](evidence/optimization/opt070-keep-revalidation/REPORT.md):
measured sitting; OPT-069 reporting correction from retained sidecars; Q8 and MMQ
both **inconclusive** with `opt074_coverage_unadmitted`; production dispatch
launches and separate graph capture recorded; tok/s delta 0 vs OPT-069 baseline
2914.65698 tok/s (`shipping_unchanged=true` in fixture).

### Verdict

**PASS** — runner admission repair, paired complete rotating/E2E evidence, separate
inconclusive verdicts for OPT-064/066, and OPT-069 reporting correction meet
OPT-070 acceptance. No production pin change; no tok/s claim
(`claims_throughput=false`).

### Delivery (2026-09-11T17:05:50Z)

Delivery agent. Commit and push authorized. Marked OPT-070 `done` in ledger;
coupled IDs: none.

### Final outcome (2026-09-11T17:05:50Z)

Repaired optimization-runner acceptance enforcement and revalidated installed
Q8 `r2_w2` and MMQ `fma_async_x` keeps with actual paired complete rotating
mixer and short E2E evidence. Both keeps **inconclusive** (`opt074_coverage_unadmitted`);
shipping selectors unchanged. OPT-069 REPORT rewritten from retained sidecars
(reporting correction only). Host contract and 25 pytest cases passed; GPU
feedback/acceptance phases recorded in dossier. Coupled IDs: none. First-pass
delivery. Retries: 0. **tok/s delta vs OPT-069 baseline:** 0 (2914.65698 tok/s
unchanged; llama 3142.517034 informational).

