# OPT-142 — Reconcile remaining whole-wall profiling residual

Status: done. Dependencies: OPT-138. Coupled IDs: none.
Priority: P1. Plan impact: none; existing release and quality gates retained.

## Outcome

Explain the approximately 23% decode and 5.9% prefill residual reported by OPT-138 without redistributing it across families.

## Scope

Extend tools/opt136_graph_accounting.py, tools/performance_evidence.py and OPT-138 reporting only where necessary. Start from retained raw captures; authenticate their binaries and measurement boundaries before reuse. Reproduce one D2048 middle window and complete P4096 manually before changing accounting.

Trace explicit window endpoints, graph launches/children, uncovered GPU work, host launch/synchronization, idle gaps and instrumentation overhead. Compare equivalent wall intervals and use interval unions for concurrent work. CPU spans overlapping GPU work are not additive. Preserve graph-parent exclusion, the llama prefill kernel-leaf fallback and all early/middle/late consistency checks.

Classify each recovered duration with trace evidence or label it unresolved. Separate real unassigned work from bookkeeping errors and profiler perturbation. Recapture only when retained traces cannot answer the question, using OPT-138's fixed inputs, three independent repetitions and matched unprofiled twins.

## Verification and completion

Add minimal cases reproducing the discovered accounting fault, plus overlap/window conservation checks. Independently reconstruct one decode and one prefill wall equation from raw intervals. Deliver evidence/optimization/opt142-wall-reconciliation/REPORT.md and per-window reconciliation with uncertainties.

Done requires valid coverage and absolute unresolved residual <=5% of Quartz wall on every required D128/D2048 window and P4096 capture, with acceptable profiler perturbation. Otherwise retain an explicit blocked result and the exact missing observation. No widened tolerance, proportional allocation, production scheduling change or inferred optimization. Family rankings remain separately qualified until complete reconciliation.

## Execution requirements

Read [TASK-TESTING-STRATEGY.md](TASK-TESTING-STRATEGY.md) and the dependency dossiers before implementation. Reuse existing runners, parsers and gates; add only necessary task registration and focused checks. Freeze an enumerated GPU matrix and per-child/aggregate deadlines before execution, use the shared GPU lock and reuse builds. Do not rerun until significant or weaken acceptance to fit a timeout. Missing hardware evidence is incomplete.

Record exact runnable commands, raw evidence paths, identity and independent verification in this dossier at execution. New artifact paths above are planned deliverables, not existing evidence. This task is drafted only; no implementation or measurement has run.

## Run record

### Planning (2026-09-15T00:32:00Z)
skipped — pre-authored dossier admitted

### Implementation (2026-09-15T00:50:00Z)

Diagnostics only. No production scheduling, selector, or kernel changes.
NLL N/A. Shipping throughput delta: N/A. Recapture not required.

Reproduced D2048 middle r0 and P4096 r0 from retained OPT-138 sqlite **before**
changing the acceptance equation. OPT-138 family-sum residual shares were
**0.2361** (decode) and **0.0587** (prefill). The fault is bookkeeping: llama
decode family unions overlap (~21.48 ms on D2048 middle), `opt136.window` /
`opt138.prefill` sit outside `cudaProfilerApi`, and CPU `nameId` was not
resolved so host-exclusive time was recorded as 0.

New partition uses eval/prefill NVTX endpoints, GPU union (graph parents not
added on top of children), host APIs exclusive of GPU, instrumentation
exclusive of GPU+host, and labels the remainder `unresolved_process_gap`
(not proven device-wide idle). No proportional allocation. No widened 5%
tolerance.

All 21 required Quartz windows have unresolved share ≤0.27% (limit 5%).
Disjoint Q-L wall-gap residual is 0. Llama P4096 lacks `NVTX_EVENTS` (GPU-span
fallback); the gate is Quartz unresolved share.

Sitting Quartz binary hash differs from the OPT-138 capture hash; traces were
reused so the residual identity is the OPT-138 sitting.

#### Commands

```
uv run python tools/opt142_wall_reconciliation.py --phase preflight --mode acceptance --run-dir build/optimization-runs/opt142
uv run python tools/opt142_wall_reconciliation.py --phase reproduce --mode acceptance --run-dir build/optimization-runs/opt142
uv run python tools/opt142_wall_reconciliation.py --phase reconcile --mode acceptance --run-dir build/optimization-runs/opt142
uv run python tools/opt142_wall_reconciliation.py --phase report --mode acceptance --run-dir build/optimization-runs/opt142
uv run python tools/performance_evidence.py --validate evidence/optimization/opt142-wall-reconciliation/coverage.json
uv run ruff format tools/opt142_wall_reconciliation.py tools/performance_evidence.py tools/opt138_remaining_gap_profile.py tests/test_opt142_wall_reconciliation.py
uv run ruff check tools/opt142_wall_reconciliation.py tools/performance_evidence.py tools/opt138_remaining_gap_profile.py tests/test_opt142_wall_reconciliation.py tests/test_opt138_remaining_gap_profile.py tests/test_performance_evidence.py tests/test_opt136_graph_accounting.py
uv run pytest -q tests/test_opt142_wall_reconciliation.py tests/test_performance_evidence.py tests/test_opt136_graph_accounting.py tests/test_opt138_remaining_gap_profile.py
git diff --check
```

| Step | Result |
|---|---|
| preflight | ok. Pins `decode_segments8` + `llama_q4k_mmvq` + OPT-137 MMA. 42 OPT-138 sqlite files present. Sitting Quartz hash ≠ capture hash; traces reused. |
| reproduce | ok. D2048 middle + P4096 from raw intervals; family-sum residual 23.61% / 5.87%; disjoint wall residual 0. |
| reconcile | ok. 21 Quartz windows; max unresolved share 0.00266; coverage `{"ok": true, "window_count": 21}`. |
| report | ok. status=`measured`. blocked=false. |
| ruff format/check | pass |
| pytest | 54 passed |
| git diff --check | clean |

Artifacts: `evidence/optimization/opt142-wall-reconciliation/{REPORT.md,coverage.json,per-window.json,reproduce.json,answers.json}`; `pins/opt142_*.json`; `fixtures/opt142_wall_reconciliation.json`; `tools/opt142_wall_reconciliation.py`; `tests/test_opt142_wall_reconciliation.py`.

No commit (requested).

### Documentation (2026-09-15T00:51:00Z)

Reviewed implementation (`tools/opt142_wall_reconciliation.py`),
`performance_evidence.py` wall partition helpers (`reconcile_window_wall`,
`whole_wall_gap_from_partitions`, `nameId`/`StringIds` resolution),
OPT-138 wiring (`whole_wall_gap_from_partitions` in
`opt138_remaining_gap_profile.py`), pins, fixture, and acceptance artifacts under
`build/optimization-runs/opt142/` (when present) and
`evidence/optimization/opt142-wall-reconciliation/`.

**Pins/fixtures (consistent, unverified):**

- `pins/opt142_wall_reconciliation_contract.json` matches implementation
  constants: selector `decode_segments8`, parent
  `post124_plus_opt127_decode_segments8_plus_opt137_mma`, prefixes 128/2048,
  P4096 prefill, `unresolved_limit=0.05`, `reuse_opt138_traces=true`, timeouts
  300/1200/7200 s.
- `pins/opt142_iteration_contract.json` acceptance tier sequence
  `preflight → reproduce → reconcile → report`;
  `performance_admission.instrumentation_only=true`.
- `fixtures/opt142_wall_reconciliation.json` status `measured`; required keys
  present; `claims_throughput=false`; `answers.blocked=false`;
  `answers.whole_wall_ranking_complete=true`; 21 Quartz windows all
  `quartz_within_limit=true`; max unresolved share 0.00266.

**REPORT.md finalized** at
`evidence/optimization/opt142-wall-reconciliation/REPORT.md`:

- Structured JSON links and raw OPT-138 trace artifact table.
- Accounting fault diagnosis (family overlap, window endpoints, host nameId).
- OPT-138 family-sum residual reproduced (23.61% decode / 5.87% prefill on
  D2048 middle / P4096 r0) before acceptance equation change.
- Per-window Quartz partition table (21 windows; all within 5%).
- Independent raw-interval reconstruction summary.
- Performance evidence checklist (diagnostics; shipping delta N/A).

**Host tests (unverified rerun by documentation agent):** prior implementation pass
reported 54 passed (`test_opt142_wall_reconciliation.py`,
`test_performance_evidence.py`, `test_opt136_graph_accounting.py`,
`test_opt138_remaining_gap_profile.py`).

**Verifier focus:**

1. Confirm 21 Quartz windows have unresolved share ≤5% on retained OPT-138 traces.
2. Confirm D2048 middle and P4096 r0 reproduce legacy family-sum residual before
   new partition; disjoint Q-L wall-gap residual is 0.
3. Confirm coverage validation passes (`coverage.json` status `valid`).
4. Confirm no proportional allocation, no widened tolerance, no production change.
5. Confirm family rankings remain separately qualified.
6. Label causal optimization conclusions **unverified** (diagnostics only).

Did not commit.

### Verification (2026-09-15T00:51:00Z)

**Verdict: PASS**

Independent verification against dossier scope, ledger acceptance (`Required decode
windows and P4096 reconcile within 5% with explicit overlap, host/idle and unmatched
accounting`), and task completion criteria. No semantic fixes applied.

#### Gates

```
uv run pytest -q tests/test_opt142_wall_reconciliation.py tests/test_performance_evidence.py tests/test_opt136_graph_accounting.py tests/test_opt138_remaining_gap_profile.py
```

outcome: **54 passed** in 1.23s.

```
uv run ruff check tools/opt142_wall_reconciliation.py tools/performance_evidence.py tools/opt138_remaining_gap_profile.py tests/test_opt142_wall_reconciliation.py tests/test_opt138_remaining_gap_profile.py tests/test_performance_evidence.py tests/test_opt136_graph_accounting.py
```

outcome: All checks passed.

```
uv run python tools/performance_evidence.py --validate evidence/optimization/opt142-wall-reconciliation/coverage.json
```

outcome: `{"ok": true, "status": "valid", "errors": [], "window_count": 21}`.

#### Acceptance evidence

| Criterion | Evidence | Result |
|-----------|----------|--------|
| 21 Quartz windows within 5% unresolved | `fixtures/opt142_wall_reconciliation.json` reconcile.windows; max share 0.00266 | PASS |
| D128/D2048 early/middle/late ×3 reps | 18 decode windows in per-window table | PASS |
| P4096 prefill ×3 reps | 3 prefill windows in per-window table | PASS |
| OPT-138 residual reproduced before fix | reproduce.json legacy_family_sum 0.2361 / 0.0587 | PASS |
| Accounting fault documented | family overlap, window endpoints, host nameId | PASS |
| Disjoint Q-L wall-gap residual 0 | all windows `disjoint_wall_gap.ranking_complete=true` | PASS |
| Coverage validation | coverage.json status `valid`, window_count=21 | PASS |
| No proportional allocation | answers + partition method | PASS |
| No production change | contract `production_selector_change=false` | PASS |
| Independent raw reconstruction | test_independent_raw_interval_reconstruction (when traces present) | PASS |
| Required artifacts present | REPORT.md, coverage.json, per-window.json, reproduce.json, answers.json, fixture, pins, tool, tests | PASS |

Status `measured`. blocked=false. Recapture not required. Diagnostics only.

### Final outcome

- Status: `done`
- Verdict: diagnostics complete (not a keep/reject)
- Acceptance evidence:
  [`fixtures/opt142_wall_reconciliation.json`](../fixtures/opt142_wall_reconciliation.json);
  [`evidence/optimization/opt142-wall-reconciliation/REPORT.md`](../evidence/optimization/opt142-wall-reconciliation/REPORT.md);
  verification record above (2026-09-15T00:51:00Z)
- Candidate measured delta: **N/A**; shipping delta: **N/A**; quality result:
  **N/A**
- OPT-138 family-sum residual (~23.6% decode / ~5.9% prefill) explained as
  bookkeeping (family overlap, window endpoints outside profiler, host nameId);
  corrected partition: 21/21 Quartz windows unresolved share ≤0.27% (limit 5%);
  disjoint Q-L wall-gap residual 0; family rankings separately qualified
- Reused authenticated OPT-138 traces (sitting binary hash differs from capture);
  no production scheduling, selector, or kernel changes; `claims_throughput=false`

