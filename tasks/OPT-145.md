# OPT-145 — Evaluate one remaining normalization or prompt-MMQ optimization

Status: done. Dependencies: OPT-135, OPT-143, OPT-144. Coupled IDs: none.
Priority: P2. Plan impact: none; existing release and quality gates retained.

## Outcome

After attention work, refresh residual_norm_quant decode and prompt_mmq prefill evidence and evaluate at most one remaining candidate. Their original +6.66/+7.21 ms decode and +27.11 ms prefill excesses justify investigation, not two speculative implementation tasks.

## Selection and scope

Reauthenticate the accepted stack and reuse OPT-138/139/141 machinery to refresh positive excess and targeted counters for these two families. Compare each family's excess as a fraction of its matched whole-workload interval; never rank twelve decode evals against one prompt by raw milliseconds. Freeze the larger supported fraction as the target, with deterministic family-name tie break and a source-supported mechanism. If the winner lacks causal evidence, perform one bounded resolving measurement; do not substitute a favorite kernel.

For residual_norm_quant, map normalization, residual, quantization and downstream projections to the actual fused comparison boundary. The entire decode-mixer replay's stalls cannot identify an RMSNorm bottleneck. For prompt_mmq, separate quant_mmq_mma_quality_kernel from other prompt-ffn kernels and include quantization/staging/output costs. Read OPT-110/112/115/122/131 to avoid repeating shipped adapters or rejected fusion assumptions.

Freeze one concrete source-level candidate and falsifying experiment before screen timing. Restrict production changes to the selected family and necessary callers. No general fusion framework, new quantization format or simultaneous second-family optimization.

## Verification and completion

Use complete-family rotating real-input screen: one warmup plus three paired rounds. Freeze target/guard roles before sampling under OPT-135: winning decode region(s) or P4096 is the target; all other D128/D2048/D8192/D32768 decode_only/complete_request and P4096 metrics are guards, including complete_request for any decode target.

For survivors, run actual candidate OPT-058 quality, numerical/state tests appropriate to the change, graph/checkpoint and memory gates affected by workspace changes, then three warmups and ten AB/BA whole-engine pairs. Retain all losses and parent on no-keep.

Deliver evidence/optimization/opt145-secondary-family/REPORT.md with refreshed selection evidence, frozen candidate and keep/reject/inconclusive verdict. Measured no positive opportunity may close as no_opportunity; missing evidence is blocked. Record the unselected family's evidence for future prioritization without adding another automatic experiment.

## Execution requirements

Read [TASK-TESTING-STRATEGY.md](TASK-TESTING-STRATEGY.md) and the dependency dossiers before implementation. Reuse existing runners, parsers and gates; add only necessary task registration and focused checks. Freeze an enumerated GPU matrix and per-child/aggregate deadlines before execution, use the shared GPU lock and reuse builds. Do not rerun until significant or weaken acceptance to fit a timeout. Missing hardware evidence is incomplete.

Record exact runnable commands, raw evidence paths, identity and independent verification in this dossier at execution. New artifact paths above are planned deliverables, not existing evidence. This task is drafted only; no implementation or measurement has run.

## Run record

### Planning (2026-09-15T00:52:00Z)
skipped — pre-authored dossier admitted

### Implementation (cursor-grok-4.6-high, 2026-09-15T01:24:37Z)

KEEP/REJECT: **no_opportunity** (measured). Production pins not flipped. Did not commit.
Parent retained: `post124_plus_opt127_decode_segments8_plus_opt137_mma` (`kSelectedOpt137DenseMma = true`, MMA threshold 8192, hybrid_crossover @1024, `opt111_base` prompt attention, Q4 decode `llama_q4k_mmvq`, RMS `parallel_fma`). OPT-143/144 attention keep was not applied.
Shipping Q4 remains `llama_q4k_mmvq`. Prompt MMQ remains `fma_async_x`. Shipping delta **0**. Candidate measured delta N/A. Quality N/A (no arithmetic change; OPT-058 not invoked; `candidate_nll_not_measured=false`).
`claims_throughput=false`.

Read OPT-138 family-gaps/windows, OPT-139/141 identity and matched llama counters, OPT-110 shipped Q4 adapter, OPT-112 deferred residual→typed-Q8 fusion, OPT-115 audit protocol, OPT-122 packed-GDN no-opportunity, OPT-131 residual-chain no-opportunity, OPT-135 `target_guard_v2`, OPT-143/144 attention no_opportunity. Freeze ran against those artifacts; it did not invent a kernel.

**Selection (derived from measured OPT-138 windows; not raw ms)**
- `residual_norm_quant` D128 middle +7.209 ms / 201.599 ms window = **0.03576**; D2048 +6.662 ms / 246.212 ms = 0.02706. Twelve evals, not per eval.
- `prompt_mmq` P4096 complete +27.109 ms / 1347.757 ms window = **0.02011**.
- Rank by excess fraction of the matched whole-workload interval. Raw ms would prefer prompt_mmq; fraction ranking selects **residual_norm_quant**. Tie-break unused.
- Unselected `prompt_mmq` recorded in `selection.json` (`smaller_supported_excess_fraction`) for future prioritization; no automatic second experiment.
- Target frozen before sampling: D128 `decode_only` under `target_guard_v2`; complete_request is the additional target guard; D2048/D8192/D32768 decode and P4096 prefill are guards.

**Freeze (measured, not pre-decided)**
- Positive matched excess on the selected family: yes.
- Fused comparison boundary: Quartz `rms_norm_fp32_to_bf16_parallel` vs llama `rms_norm_f32` + `quantize_q8_1`. OPT-141 `fused_boundary_mismatch` (`llama_quantize_q8_1_vs_quartz_rms_plus_bf16_quant_fusion`); leaf counters incomparable (`quartz_counters_unusable`).
- One bounded resolving measurement: fused-boundary identity check. Decode-mixer replay stalls are **not** an RMSNorm bottleneck. Named source/SASS observations: false. `supported_mechanism=null`.
- Forbidden: did not re-port OPT-110; did not revive deferred OPT-112 fusion; did not revive OPT-131 chain fusion; did not revive OPT-122 packed GDN; did not lower OPT-137 MMA threshold; did not revive OPT-130.
- Candidate: **null**. Verdict: **no_opportunity**. No CUDA production change.

Quality was wired in `pins/opt145_iteration_contract.json` (`require_candidate_nll=true`, `--phase quality` → OPT-058 `--quality --quality-config --q4-decode`) so a frozen arithmetic candidate would run NLL. Because freeze produced no candidate, quality short-circuited with `nll_required=false` rather than stubbing `candidate_nll_not_measured`.

Frozen GPU matrix: RTX 5090, image `qw38-cuda:13.0.2`, GPU lock `build/optimization-runs/qw38-gpu.lock`, child 300s, long 1200s, aggregate 7200s ceiling. `make cuda-opt145-diagnostics` rc 0 under lock (quality/checkpoint/memory-fit/component-replay already current). No new native family binary (no candidate).

**Files created**
- `tools/opt145_secondary_family.py`
- `tests/test_opt145_secondary_family.py`
- `pins/opt145_secondary_family_contract.json`
- `pins/opt145_iteration_contract.json`
- `pins/opt145_secondary_family_provenance.json`
- `fixtures/opt145_secondary_family.json`
- `evidence/optimization/opt145-secondary-family/REPORT.md`
- `evidence/optimization/opt145-secondary-family/freeze.json`
- `evidence/optimization/opt145-secondary-family/selection.json`
- `evidence/optimization/opt145-secondary-family/answers.json`

**Files modified**
- `Makefile` (`cuda-opt145-diagnostics` + PHONY; quality/checkpoint/memory-fit/component-replay)
- `tools/run_optimization_task.py` (`QW38_OPT145_*` result prefixes)

```
uv run ruff format tools/opt145_secondary_family.py tests/test_opt145_secondary_family.py tools/run_optimization_task.py
uv run ruff check tools/opt145_secondary_family.py tests/test_opt145_secondary_family.py tools/run_optimization_task.py
```
outcome: formatted; All checks passed.

```
uv run pytest tests/test_opt145_secondary_family.py tests/test_opt143_short_attention.py tests/test_opt144_prefill_attention.py -q
```
outcome: 33 passed (host, before GPU/report). After fixture/report: `uv run pytest tests/test_opt145_secondary_family.py tests/test_opt141_matched_llama_counters.py tests/test_opt138_remaining_gap_profile.py -q` → 37 passed.

```
nvidia-smi -L
# GPU 0: NVIDIA GeForce RTX 5090

uv run python tools/opt145_secondary_family.py --phase preflight --mode acceptance --run-dir build/optimization-runs/opt145
# ok; selected residual_norm_quant fraction 0.03576 vs prompt_mmq 0.02011; verdict no_opportunity; make cuda-opt145-diagnostics rc 0 under GPU lock

uv run python tools/opt145_secondary_family.py --phase correctness --mode acceptance --run-dir build/optimization-runs/opt145
# skipped no_candidate_no_arithmetic_change; fused-boundary identity recorded

uv run python tools/opt145_secondary_family.py --phase screen --mode acceptance --run-dir build/optimization-runs/opt145
# skipped; complete-family screen not launched (no candidate)

uv run python tools/opt145_secondary_family.py --phase quality --mode acceptance --run-dir build/optimization-runs/opt145
# skipped; OPT-058 not invoked; candidate_nll_not_measured=false

uv run python tools/opt145_secondary_family.py --phase state-memory --mode acceptance --run-dir build/optimization-runs/opt145
# skipped; parent 128K/checkpoint admitted

uv run python tools/opt145_secondary_family.py --phase performance --mode acceptance --run-dir build/optimization-runs/opt145
# skipped; no target_guard_v2 AB/BA (no candidate)

uv run python tools/opt145_secondary_family.py --phase mechanism --mode acceptance --run-dir build/optimization-runs/opt145
# ok; resolving fused_boundary_mismatch; source/SASS false

uv run python tools/opt145_secondary_family.py --phase report --mode acceptance --run-dir build/optimization-runs/opt145
# {"task": "OPT-145", "phase": "report", "ok": true, "verdict": "no_opportunity", "selected_family": "residual_norm_quant"}
```

GPU: RTX 5090, image `qw38-cuda:13.0.2`. Sidecars in `build/optimization-runs/opt145/` and `evidence/optimization/opt145-secondary-family/raw/`. Did not commit. model `cursor-grok-4.6-high`. telemetry_unavailable.

### Documentation (composer-2.5, 2026-09-15T01:26:00Z)

Drafted [`evidence/optimization/opt145-secondary-family/REPORT.md`](../evidence/optimization/opt145-secondary-family/REPORT.md) from implementation artifacts: fraction-ranked family selection (`residual_norm_quant` 0.0358 vs `prompt_mmq` 0.0201), freeze verdict `no_opportunity`, OPT-141 fused-boundary mismatch (`rms_norm_fp32_to_bf16_parallel` vs `rms_norm_f32`+`quantize_q8_1`), null mechanism, parent retained. Quality section documents `nll_required=false` short-circuit (`no_candidate_no_arithmetic_change`); OPT-058 wiring present for arithmetic candidates. Unselected `prompt_mmq` recorded in `selection.json`. Performance evidence checklist items 1–6 and 8 populated; item 7 marked pending verifier. Shipping delta 0; candidate measured delta N/A. No production pin flip.

### Verification (2026-09-15T01:26:00Z)

**Verdict: PASS**

Independent verification against dossier scope, ledger acceptance (`Fresh evidence selects at most one normalization or prompt-MMQ candidate with verified keep or measured no-opportunity/no-keep`), and keep/reject quality gate. No semantic fixes applied.

#### Gates

```
uv run pytest tests/test_opt145_secondary_family.py tests/test_opt143_short_attention.py tests/test_opt144_prefill_attention.py tests/test_opt141_matched_llama_counters.py tests/test_opt138_remaining_gap_profile.py -q
```

outcome: **59 passed** in 0.14s.

```
uv run ruff check tools/opt145_secondary_family.py tests/test_opt145_secondary_family.py tools/run_optimization_task.py
```

outcome: All checks passed.

#### Acceptance evidence

| Criterion | Evidence | Result |
|-----------|----------|--------|
| Measured no_opportunity with evidence | `freeze.json` verdict `no_opportunity`; reasons include `fused_boundary_mismatch_quartz_rms_plus_bf16_quant`, `decode_mixer_stalls_cannot_identify_rmsnorm`, `quartz_counters_unusable`, `opt112_deferred_fusion_not_revived` | PASS |
| Fraction-ranked family selection | `selection.json` `residual_norm_quant` 0.03576 vs `prompt_mmq` 0.02011; raw-ms ranking rejected | PASS |
| Positive family excess authenticated | OPT-138 D128 +7.21 ms / 201.60 ms window; D2048 +6.66 ms / 246.21 ms window | PASS |
| Unselected family recorded | `prompt_mmq` in `selection.json` with `smaller_supported_excess_fraction`; no automatic second experiment | PASS |
| No OPT-110/112/131/122/130 revival | freeze reasons `opt110_not_re_ported`, `opt112_deferred_fusion_not_revived`, `opt131_chain_fusion_not_revived` | PASS |
| Quality gate when NLL not required | `raw/quality.json` `nll_required=false`, `candidate_nll_not_measured=false`, `skip_reason=no_candidate_no_arithmetic_change`; iteration contract wires OPT-058 for arithmetic candidates | PASS |
| Parent retained on no-keep | `production_kept=false`; shipping Q4 `llama_q4k_mmvq`, prompt MMQ `fma_async_x` unchanged | PASS |
| Shipping delta 0 | report `shipping_delta=0`; `candidate_measured_delta=null` | PASS |
| Required artifacts present | REPORT.md, freeze.json, selection.json, answers.json, raw gates, fixture, pins, tool, tests | PASS |
| No production kernel change | candidate null; correctness/screen/performance skipped; mechanism null | PASS |

Verdict `no_opportunity`. Parent retained. Quality N/A (no arithmetic change).
First-pass acceptance: yes.

### Final outcome

- Status: `done`
- Verdict: `no_opportunity` (measured; parent retained)
- Acceptance evidence:
  [`fixtures/opt145_secondary_family.json`](../fixtures/opt145_secondary_family.json);
  [`evidence/optimization/opt145-secondary-family/REPORT.md`](../evidence/optimization/opt145-secondary-family/REPORT.md);
  verification record above (2026-09-15T01:26:00Z)
- Candidate measured delta: **N/A**; shipping delta: **0**; quality result:
  **nll_not_required_no_arithmetic_change**
- `residual_norm_quant` D128 middle +7.21 ms (fraction 0.0358); unselected
  `prompt_mmq` P4096 +27.11 ms (fraction 0.0201); OPT-141 fused-boundary mismatch;
  decode-mixer stalls not treated as RMSNorm diagnosis; no source-grounded mechanism
- OPT-137 MMA retained; OPT-110/112/131 not revived; `claims_throughput=false`;
  no production kernel or selector change
- First-pass acceptance: yes (2026-09-15T01:26:00Z)

