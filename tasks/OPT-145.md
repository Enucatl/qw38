# OPT-145 — Evaluate one remaining normalization or prompt-MMQ optimization

Status: pending. Dependencies: OPT-135, OPT-143, OPT-144. Coupled IDs: none.
Priority: P2. Plan impact: none; existing release and quality gates retained.

## Outcome

After attention work, refresh residual_norm_quant decode and prompt_mmq prefill evidence and evaluate at most one remaining candidate. Their original +6.66/+7.21 ms decode and +27.11 ms prefill excesses justify investigation, not two speculative implementation tasks.

## Selection and scope

Reauthenticate the accepted stack and reuse OPT-138/139/141 machinery to refresh positive excess and targeted counters for these two families. Compare each family's excess as a fraction of its matched whole-workload interval; never rank twelve decode evals against one prompt by raw milliseconds. Freeze the larger supported fraction as the target, with deterministic family-name tie break and a source-supported mechanism. If the winner lacks causal evidence, perform one bounded resolving measurement; do not substitute a favorite kernel.

For residual_norm_quant, map normalization, residual, quantization and downstream projections to the actual fused comparison boundary. The entire decode-mixer replay's stalls cannot identify an RMSNorm bottleneck. For prompt_mmq, separate quant_mmq_mma_quality_kernel from other prompt-ffn kernels and include quantization/staging/output costs. Read OPT-110/112/115/122/131 to avoid repeating shipped adapters or rejected fusion assumptions.

Freeze one concrete source-level candidate and falsifying experiment before screen timing. Restrict production changes to the selected family and necessary callers. No general fusion framework, new quantization format or simultaneous second-family optimization.

## Verification and completion

Use complete-family rotating real-input screen: one warmup plus three paired rounds. Freeze target/guard roles before sampling under OPT-135: winning decode region(s) or P4096 is the target; all other D128/D2048/D8192/D32768 decode_only/complete_request and P4096 metrics are guards, including complete_request for any decode target.

For survivors, run actual candidate OPT-058 quality, numerical/state tests appropriate to the change, graph/checkpoint and memory gates affected by workspace changes, then three warmups and ten AB/BA whole-engine pairs. Retain all losses and parent on no-keep.

Deliver evidence/optimization/opt145-secondary-family/REPORT.md with refreshed selection evidence, frozen candidate and keep/reject/inconclusive verdict. Measured no positive opportunity may close as no_opportunity; missing evidence is blocked. Record the unselected family's evidence for future prioritization without adding another automatic experiment.

## Execution requirements

Read [TASK-TESTING-STRATEGY.md](TASK-TESTING-STRATEGY.md) and the dependency dossiers before implementation. Reuse existing runners, parsers and gates; add only necessary task registration and focused checks. Freeze an enumerated GPU matrix and per-child/aggregate deadlines before execution, use the shared GPU lock and reuse builds. Do not rerun until significant or weaken acceptance to fit a timeout. Missing hardware evidence is incomplete.

Record exact runnable commands, raw evidence paths, identity and independent verification in this dossier at execution. New artifact paths above are planned deliverables, not existing evidence. This task is drafted only; no implementation or measurement has run.

