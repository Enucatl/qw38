# OPT-113 — Freeze and measure the coupled-stack recovery combination

Status: done. Dependencies: OPT-107, OPT-108, OPT-109, OPT-110, OPT-111,
OPT-114. Coupled IDs: none. Primary target: original comparative outcome gates.

## Run record

### Planning (2026-09-13T07:08:00Z)

skipped — pre-authored dossier admitted (coordinator)

### Implementation (cursor-grok-4.6, 2026-09-13T07:38:00Z)

Implemented the coupled-stack sitting gate modeled on OPT-106. Freeze is
authenticated OPT-106 `post106_control` (`integer_q8_late` / `kv_once` /
`warp_query`) versus production survivors OPT-107 `hybrid_crossover@1024`,
OPT-110 `llama_q4k_mmvq`, OPT-111 `opt111_base`. OPT-108/109 do not leak;
OPT-112 is omitted as `deferred_below_trigger`. Quartz ran host-native
(`QW38_HOST_NATIVE=1`); llama-bench/decode used pinned `cc83d7b` binaries inside
`qw38-cuda:13.0.2` because `qw38-llama-authority:cuda-13.0.2` is absent.
Exclusive sitting stopped zanzara parakeet/diarization for 128K and restored
them. Did not commit.

**Tests**
```
uv run ruff check tools/opt113_coupled_stack_gate.py tests/test_opt113_coupled_stack_gate.py
uv run pytest -q tests/test_opt113_coupled_stack_gate.py
# 14 passed
./build/qw38-cuda-opt113-coupled-stack-gate-test
# status=passed
```

**Commands**
```
docker run --rm --user "$(id -u):$(id -g)" -v /home/user/qw38:/workspace -w /workspace qw38-cuda:13.0.2 make cuda-opt113-diagnostics
QW38_HOST_NATIVE=1 uv run python tools/opt113_coupled_stack_gate.py --phase freeze --mode acceptance --run-dir build/optimization-runs/OPT-113/sitting
QW38_HOST_NATIVE=1 uv run python tools/opt113_coupled_stack_gate.py --phase quality --mode acceptance --run-dir build/optimization-runs/OPT-113/sitting
QW38_HOST_NATIVE=1 uv run python tools/opt113_coupled_stack_gate.py --phase state-memory --mode acceptance --run-dir build/optimization-runs/OPT-113/sitting
QW38_HOST_NATIVE=1 uv run python tools/opt113_coupled_stack_gate.py --phase performance --mode acceptance --run-dir build/optimization-runs/OPT-113/sitting
QW38_HOST_NATIVE=1 uv run python tools/opt113_coupled_stack_gate.py --phase two-k --mode acceptance --run-dir build/optimization-runs/OPT-113/sitting
QW38_HOST_NATIVE=1 uv run python tools/opt113_coupled_stack_gate.py --phase attribution --mode acceptance --run-dir build/optimization-runs/OPT-113/sitting
QW38_HOST_NATIVE=1 uv run python tools/opt113_coupled_stack_gate.py --phase report --mode acceptance --run-dir build/optimization-runs/OPT-113/sitting
```

Quality: `model_quality_pass=true`; candidate NLL measured (held-out 1024 mean
NLL 1.787599, ppl_ratio 0.999721); inherited task_arithmetic fail; recurrence
pass.

Independent verdicts (honest, all unpassed):
- `internal_improvement_with_quality`: false — tok/s CIs vs control passed
  (geo mean 1.0686, CI lower 1.0024) but 128K `memory_fit` arithmetic is false
  (same measured_delta as OPT-106; explicit ledger +19584 B; cap not raised)
- `llama_parity`: false — selected behind fresh llama on P4096/D128/D2048
- `opt056_plus5`: false — behind 1.05× llama; decode p95 worse than llama;
  `quality_v2.all` inherited false; OPT-016 2K 3152.97 vs 3190.47

**tok/s (this sitting, not reused from OPT-106)**

| Workload | selected | post106_control | llama | Δ vs control | Δ vs llama |
|---|---:|---:|---:|---:|---:|
| P4096 | 3064.77 | 3022.22 | 3227.71 | +42.54 | -162.94 |
| D128 | 57.297 | 53.575 | 68.614 | +3.722 | -11.317 |
| D2048 | 55.647 | 49.463 | 67.272 | +6.184 | -11.625 |

## Outcome

Freeze the exact production survivors from OPT-107 through OPT-112 (omitting
OPT-112 when its explicitly deferred verdict has no survivor) and measure them
against the authenticated OPT-106 production control and pinned llama.cpp in
one exclusive-GPU sitting. Report internal improvement, parity, and the
unchanged OPT-056 +5% outcome independently.

## Freeze rules

1. Include only configurations whose owning task completed its required kernel
   parity, full-model quality, state, memory, complete-component, and end-to-end
   keep gates. A rejected diagnostic path must not leak into production.
2. Preserve admitted vertical slices as units. Do not silently select an
   unmeasured subset of prepared query, KV representation, fast math,
   persistent state, or source-adapter code.
3. Authenticate source/build/container, GGUF, pinned llama revision, compiler
   flags by translation unit, effective selectors, layout/type versions, graph
   identities, persistent allocations, and copied-source license/provenance.
4. Use `post106_control` and one `post113_selected`. If no candidate survives,
   selected equals control and Quartz is measured once.
5. Treat OPT-112 as optional for this sitting. If it has not been admitted, a
   documented deferred verdict is sufficient; it must not be silently included
   as a candidate or block OPT-113.

## Verification and evidence

- Add batch/iteration contracts, fixture, runner, host validators, native
  selector/freeze test, and
  `evidence/optimization/opt113-coupled-stack-gate/REPORT.md`.
- Run the complete current quality suite before long timing, including
  candidate NLL, per-case deltas, recurrence, deterministic continuation,
  nonfinites, and the inherited absolute task-quality verdict kept separate
  from engine regression.
- In one serialized sitting run fresh P4096, D128, and D2048 samples for
  control, selected, and pinned llama with established counts/order. Report raw
  latency, tok/s, paired deltas/confidence intervals, p50, and p95. Do not reuse
  OPT-106 llama numbers.
- Rerun the frozen 2K gate, 128K allocation ledger, prompt-to-decode handoff,
  graph/eager equality, checkpoint/restore, cancellation/frontier, divergent
  prefix replay, and state-format round trips.
- Reconcile the 128K `memory_fit` result against the actual allocation
  inventory before declaring the gate failed. A small accounting discrepancy
  must be fixed in the ledger or explained; do not raise a cap without an
  allocation-backed reason.
- Publish matched attribution for the selected build only as diagnostic,
  non-additive evidence. Do not turn family event sums into a wall-time claim.

## Acceptance

- `internal_improvement_with_quality`: positive paired geometric throughput
  result over P4096/D128/D2048, with every workload meeting the existing
  non-regression bound and all quality/state/memory gates passing.
- `llama_parity`: Quartz throughput at least fresh pinned llama on every
  workload.
- `opt056_plus5`: Quartz at least 1.05× pinned llama on every workload, decode
  p95 no worse, full quality pass, and OPT-016 2K pass.
- Marking OPT-113 done means the complete sitting and honest independent
  verdicts are delivered. It does not imply that any comparative gate passed.

## Non-goals

No new optimization inside the gate, selector retuning, quality concession,
benchmark change, historical fixture rewrite, extra sampling after an
inconclusive result, or release claim from diagnostic attribution.

### Verification (composer-2.5, 2026-09-13T07:45:00Z)

**Verdict: pass** — complete sitting delivered; three independent outcomes
honestly reported (all unpassed). Gate pass not required for OPT-113 done.

**Quality gate (candidate NLL measured, not stubbed)**
- `fixtures/opt113_coupled_stack_gate.json`: `candidate_nll_measured=true`,
  `model_quality_pass=true`, held-out 1024 mean NLL **1.7875990840085783**,
  `held_out_ppl_ratio` **0.9997208519718781**, recurrence incremental NLL **0.0**.
- Sidecars: `evidence/optimization/opt113-coupled-stack-gate/quality-nll.json`,
  `quality-summary.json` (four scored NLL cases).

**Outcomes traced from fixture**
- `internal_improvement_with_quality.pass`: **false** — throughput CIs vs
  `post106_control` passed (geo mean **1.0686**, CI lower **1.0024**); tok/s
  deltas P4096 **+42.54**, D128 **+3.722**, D2048 **+6.184**; 128K
  `memory_fit` arithmetic **false** (`state_memory_pass=false`).
- `llama_parity.pass`: **false** — selected behind fresh llama on P4096/D128/D2048.
- `opt056_plus5.pass`: **false** — behind 1.05× llama; decode p95 worse than
  llama; `quality_v2.all` false; OPT-016 2K **3152.97** vs llama **3190.47**.

**Host validation**
```
uv run ruff format .
# 254 files left unchanged
uv run pytest -q tests/test_opt113_coupled_stack_gate.py
# 14 passed in 0.16s
```

**Native build (image `qw38-cuda:13.0.2`)**
```
docker run --rm --user "$(id -u):$(id -g)" -v /home/user/qw38:/workspace -w /workspace qw38-cuda:13.0.2 make cuda-opt113-diagnostics
# exit 0 (artifacts present; includes qw38-cuda-opt058-quality-baseline-test)
./build/qw38-cuda-opt113-coupled-stack-gate-test
# status=passed
```

### Documentation (composer-2.5, 2026-09-13T07:45:00Z)

Completed
[`evidence/optimization/opt113-coupled-stack-gate/REPORT.md`](../evidence/optimization/opt113-coupled-stack-gate/REPORT.md).
Added coupled-stack sitting paragraph to `docs/06-system-optimization.md` and
audit row to `docs/65-documentation-audit.md`. Cross-links verified against
`pins/opt113_coupled_stack_gate_contract.json`,
`pins/opt113_iteration_contract.json`, and
`fixtures/opt113_coupled_stack_gate.json`. Did not commit.

**Files changed**
- `evidence/optimization/opt113-coupled-stack-gate/REPORT.md`
- `docs/06-system-optimization.md`
- `docs/65-documentation-audit.md`
- `tasks/OPT-113.md` (this run record)

### Delivery (2026-09-13T07:45:00Z)

Delivery authorized after independent verification pass. Marked OPT-113 `done` in
`implementation_ledger.md`. Sitting complete; three independent outcomes
reported (internal/parity/+5% all **false**). **tok/s delta vs
post106_control:** P4096 **+42.54**, D128 **+3.722**, D2048 **+6.184**.
Candidate NLL measured pass; 128K `memory_fit` arithmetic miss noted (explicit
ledger +19584 B vs measured delta matching OPT-106). Coupled IDs: none. No
eligible pending recovery tasks remain in the OPT-107–113 batch. Committed and
pushed to `origin/main`.
