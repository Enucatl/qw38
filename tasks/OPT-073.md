# OPT-073 — Separate engine quality from the authority's arithmetic miss

Status: done. Dependencies: OPT-058, OPT-069, OPT-072. Coupled IDs: none.
Priority: P0. Target: valid production-quality decisions and cheap release stop.
Read [the post-069 protocol](PERFORMANCE-RECOVERY-POST-069.md).
Quality risk: policy/scoring; no arithmetic kernel changes or speedup claim.

## Run record

### Planning (2026-09-11T15:48:00Z)

skipped — pre-authored dossier admitted (coordinator)

### Implementation (2026-09-11T16:00:35Z)

- Agent/model: implementation (Cursor Grok 4.6). No commit. Ledger left
  `in_progress`. `plan.md` unchanged. No arithmetic kernel change. No
  throughput claim. OPT-056 and OPT-016 were not relabeled.
- Audit: all eight v2 cases match `render_user_turn(..., enable_thinking=false)`.
  17+25 independently correct answer remains B=42; A=41 remains wrong. Quartz
  and pinned llama both emit A (token 32). No objective scorer/template defect,
  so the v2 prompt is not rewritten. Four diagnostics frozen before measurement:
  original v2, fixed permutation (42 at C), direct numeric, one paraphrase.
- Policy: quality-v3 dual verdict. Absolute task accuracy stays **fail**.
  Engine non-regression **pass** on retained production (seven correct items
  plus authority A on the known miss). Internal admission may use v3 engine
  non-regression plus NLL; it does not replace OPT-056/OPT-016.
- OPT-069 preflight now parses all eight answers and records selected quality
  verdicts (token count alone is insufficient). Release stops before timed
  oracles while quality-v2 `all` is false. `--diagnostic-performance` is a
  non-release mode that retains the failure and forbids keep/release claims.
- Files changed:
  - `pins/opt073_quality_policy_contract.json`
  - `pins/opt073_iteration_contract.json`
  - `fixtures/opt073_quality_policy.json`
  - `tests/test_opt073_quality_policy.py`
  - `tests/test_opt069_batch_gate.py`
  - `tools/opt073_quality_policy.py`
  - `tools/opt069_batch_gate.py`
  - `tools/run_optimization_task.py` (plan extras for held-out/functional)
  - `Makefile` (`cuda-opt073-diagnostics`)
  - `evidence/optimization/opt073-quality-policy/REPORT.md`
  - `tasks/OPT-073.md`
- Commands:

```
uv run pytest -q tests/test_opt073_quality_policy.py tests/test_opt069_batch_gate.py
```

**pass** — 20 passed in 0.10s.

```
uv run python tools/run_optimization_task.py --task OPT-073 --mode feedback --phase arithmetic
```

**pass** — exit 0; `elapsed_s=0.0342`; deadline 300s; `host_only=true`;
`skip_compile=true`; product=128 (2 engines × 4 diagnostics × 16 tokens);
`historical_oracles=none`; run
`build/optimization-runs/OPT-073/feedback/20260911T160035Z-823b84ef`.

```
uv run python tools/run_optimization_task.py --task OPT-073 --mode feedback --phase preflight
```

**pass** — exit 0; `elapsed_s=0.0386`; deadline 300s; product=40
(32 held-out + 8 functional); skipped quality phases fail-closed; OPT-056
quality requirement unmet; run
`build/optimization-runs/OPT-073/feedback/20260911T160035Z-eea31708`.

```
uv run python tools/run_optimization_task.py --task OPT-073 --mode acceptance --phase quality
```

**pass** — exit 0; `elapsed_s=0.0407`; deadline 300s; product=12288;
quality-v2 `all=false`; quality-v3 absolute `fail`; engine non-regression
`pass`; run
`build/optimization-runs/OPT-073/acceptance/20260911T160035Z-a24e905d`.

## Implementation

1. Authenticate rendered text, tokenizer IDs, no-thinking assistant boundary,
   output termination and parser for all eight v2 cases. Reproduce 17+25 on
   both engines; B=42 remains the correct answer, A=41 remains wrong. Both
   engines failing does not by itself prove a kernel or scorer bug.
2. Freeze at most four arithmetic diagnostics before measuring a candidate:
   original v2, the same options in one fixed permutation, direct numeric
   completion under the supported template, and one independent paraphrase.
   Human-verifiable answers and parsers are written first. Run both controls,
   at most 16 generated tokens/case. No token mask, no teacher-forced answer,
   no selecting only paraphrases that happen to pass. Retain every result.
3. If rendering/parsing is objectively wrong, fix it in a versioned successor
   with text/ID evidence. If the pinned authority genuinely misses, maintain
   separate verdicts for absolute task accuracy and engine non-regression.
   Proposed internal quality-v3: all reference-correct functional items must
   remain correct; on the known missed item, candidate must either give the
   independently correct answer or preserve the authority behavior within
   frozen logit-error/margin bounds. Other wrong choices are not waived.
   Absolute arithmetic accuracy stays failed when it is failed.
4. Document the versioned rationale and full v2/v3 verdicts side by side before
   using the successor for internal candidate admission. The user authorized
   evaluating documented comparable-accuracy policies, not relabeling OPT-056
   or OPT-016. Do not replace their quality condition or mark them passed;
   any proposed historical gate successor is a separate explicit decision.
5. Keep both 1024-target NLL spans, PPL <=1.01, recurrence drift <=0.02,
   finite full vocabulary and structural state invariants. Freeze reference
   logits/margins on the actual contexts before candidate selection. Missing
   evidence yields incomplete, not pass. Same-input near-tie allowances cannot
   waive a known wrong semantic answer.
6. Fix release preflight: OPT-069 currently checks only the functional token
   count, and proceeds from failed quality into timed oracles. Validate parsed
   answers and each required selected quality verdict. A release attempting
   OPT-056 must stop while its actual quality requirement fails. If diagnostic
   performance under a known baseline failure is useful, make that an explicit
   non-release mode, retain failure, and prohibit release/keep claims.

## Tests and commands

Host tests use original A/B arithmetic answers, whitespace, extra text, missing
authority, wrong tokenizer/template, near-ties, and skipped quality phases.
Reuse `build/qw38-cuda-opt058-quality-baseline-test` and the existing pinned
llama quality oracle. One feedback phase: two engines x four fixed arithmetic
diagnostics x at most 16 generated tokens; another verifies 32 held-out targets
plus all eight functional controls. Model/reference setup is cached. Full NLL
acceptance is separate once, not an implementation-loop multiplier.

```sh
uv run pytest -q tests/test_opt073_quality_policy.py tests/test_opt069_batch_gate.py
uv run python tools/run_optimization_task.py --task OPT-073 --mode feedback --phase arithmetic
uv run python tools/run_optimization_task.py --task OPT-073 --mode feedback --phase preflight
uv run python tools/run_optimization_task.py --task OPT-073 --mode acceptance --phase quality
```

Accept a verified scorer repair or an explicit versioned dual-verdict policy,
complete authority coverage, and a fail-closed release path. Stop kernel blame
without a Quartz/authority discrepancy; stop prompt rewriting if no objective
defect exists. Do not manufacture a universally passing absolute baseline.

## Verification (2026-09-11T16:02:30Z)

Independent integration verifier. No commit. Ledger left `in_progress`.
`plan.md` unchanged.

### Diff vs dossier / ledger / plan

| Requirement | Status |
|---|---|
| `pins/opt073_quality_policy_contract.json` | present (untracked) |
| `pins/opt073_iteration_contract.json` | present (untracked), host-only |
| `fixtures/opt073_quality_policy.json` | present (untracked) |
| `tests/test_opt073_quality_policy.py` | present (untracked) |
| `tests/test_opt069_batch_gate.py` | modified |
| `tools/opt073_quality_policy.py` | present (untracked) |
| `tools/opt069_batch_gate.py` | modified (parsed answers + selected verdicts) |
| `tools/run_optimization_task.py` | modified (held-out/functional plan extras) |
| `Makefile` (`cuda-opt073-diagnostics`) | modified |
| `evidence/optimization/opt073-quality-policy/REPORT.md` | present (untracked) |
| `implementation_ledger.md` OPT-073 `in_progress` | modified (not `done`) |
| `plan.md` unchanged | confirmed (no diff) |
| No arithmetic kernel change | confirmed |
| No production pin change | confirmed |

Quality-v3 dual verdict: absolute task accuracy **fail**; engine
non-regression **pass**. OPT-069 preflight parses all eight functional answers
and records selected quality verdicts; release stops before timed oracles while
quality-v2 `all` is false.

### Commands

```
uv run ruff format .
```

**PASS** — 159 files left unchanged; no retest needed.

```
uv run ruff check tests/test_opt073_quality_policy.py tests/test_opt069_batch_gate.py tools/opt073_quality_policy.py tools/opt069_batch_gate.py tools/run_optimization_task.py
```

**PASS** — All checks passed.

```
uv run pytest -q tests/test_opt073_quality_policy.py tests/test_opt069_batch_gate.py
```

**PASS** — 20 passed in 0.09s.

```
uv run python tools/run_optimization_task.py --task OPT-073 --mode feedback --phase arithmetic
```

**PASS** — exit 0; `elapsed_s=0.0335`; deadline 300s; `host_only=true`;
`skip_compile=true`; product=128 (2 engines × 4 diagnostics × 16 tokens);
`historical_oracles=none`; run
`build/optimization-runs/OPT-073/feedback/20260911T160219Z-8ca4a411`.

```
uv run python tools/run_optimization_task.py --task OPT-073 --mode feedback --phase preflight
```

**PASS** — exit 0; `elapsed_s=0.0404`; deadline 300s; product=40
(32 held-out + 8 functional); skipped quality phases fail-closed; run
`build/optimization-runs/OPT-073/feedback/20260911T160222Z-aac01e3d`.

```
uv run python tools/run_optimization_task.py --task OPT-073 --mode acceptance --phase quality
```

**PASS** — exit 0; `elapsed_s=0.0403`; deadline 300s; product=12288;
quality-v2 `all=false`; quality-v3 absolute `fail`; engine non-regression
`pass`; run
`build/optimization-runs/OPT-073/acceptance/20260911T160222Z-4bc2931b`.

### Verdict

**PASS** — host contract, quality-v3 dual-verdict policy, OPT-069 fail-closed
preflight, and optimization runner phases meet OPT-073 acceptance. Ledger
correctly left `in_progress`; no arithmetic kernel change; no production pin
change; no tok/s claim (`claims_throughput=false`).

### Delivery (2026-09-11T16:03:01Z)

Delivery agent. Commit and push authorized by explicit `/run-ledger-task-cursor`
delivery stage. Marked OPT-073 `done` in ledger; coupled IDs: none.

### Final outcome (2026-09-11T16:03:01Z)

Quality-v3 dual-verdict policy separates absolute task accuracy (fail) from
engine non-regression (pass) on retained production items plus authority
behavior on the known arithmetic miss. All eight v2 cases audited; four frozen
arithmetic diagnostics; OPT-069 preflight parses functional answers and selected
quality verdicts and stops release before timed oracles while quality-v2 `all`
is false. Host contract, arithmetic/preflight/quality runner phases, and 20
pytest cases passed within 300 s. Coupled IDs: none. First-pass delivery.
Retries: 0. No arithmetic kernel change. No production pin change. No tok/s
claim (`claims_throughput=false`).
