# OPT-041 — Give prompt QK microtiles warp ownership

## Control

- Primary ID: OPT-041
- Coupled IDs: none
- Dependencies: OPT-038 (explicit prerequisite), OPT-035 done
- Status: pending
- Ledger acceptance: A/B warp-owned prompt QK microtiles preserving existing virtual-warp partial order against shared cparts reduction; keep only with byte-equal attention outputs, frozen gates, lower component time, improved P, 95% decode throughput floors, and 105% p95 ceilings; otherwise reject.

## Goal and boundaries

Remove inter-warp score-fragment barriers in the accepted Ncols1=16, Ncols2=2,
KV-tile-32, dual-F16-Q, register-VKQ, dual-F16-P×V, grid.z=2 path. Keep online
FP32 softmax, combine, allocations, APIs, prompt graphs, decode, and envelopes.
Classification: CUDA/numeric; plan impact none.

## Repository evidence

- `cuda/fattn_mma_f16.cuh:328-430` — current cparts/barrier QK assembly.
- `cuda/mma.cuh` — fragment mapping.
- `.cache/authorities/llama.cpp/ggml/src/ggml-cuda/fattn-mma-f16.cuh` — warp-owned KQ fragments.
- `cuda/fattn_pv_mma_ab_test.cu` — complete attention A/B harness.

## Implementation decisions

Candidate `warp_microtile` assigns each of eight 16x8 microtiles to one warp,
retains four virtual partials, visits K slices in current order, folds partials
in the current FP32 order, and writes scores. Keep shared cparts initially so
ownership is the only variable. No barrier inside a microtile; one final block
barrier protects score reuse. Baseline remains callable.

## Acceptance and validation

Use frozen P 1869.84412, D128 25.3816128, D2048 20.169548 and OPT-034 p95s.
Run 3 warmups and 30 alternating CUDA-event complete 4096-row attention pairs;
test row counts 16,17,31,32,33,63,64,65,512,2048,4096 and starts 0,1,31,128,
2048. Require byte equality versus current quality output, candidate KV exact,
OPT-005 max abs 5e-5/RMS 5e-6, occupancy >=1, and lower component time. Run
unchanged P/D end-to-end guards. Focused:
`uv run pytest -q tests/test_opt041_fattn_warp_qk.py tests/test_documentation.py`;
hardware adds `QW38_RUN_CUDA_TESTS=1`. Update attention/full-prefill docs and
retain a rejection on failure. No nsys/ncu.

## Run record

Planning proposed 2026-09-10; implementation and verification not run.
