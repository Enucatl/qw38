# OPT-128 — Remove measured host submission and synchronization stalls

Status: done. Dependencies: OPT-125, OPT-127. Coupled IDs: none.
Priority: P1.

## Run record

### Planning (2026-09-14T00:00:00Z)

skipped — pre-authored dossier admitted (coordinator)

### Implementation (cursor-grok-4.6-high, 2026-09-14T00:13:00Z)

NO-OPPORTUNITY. Parent is OPT-127 kept `decode_segments8`. Reprofiled the
Session sample/eval/output loop (greedy sample + eval, with and without the
server cancellation poller). Ranked blocking copies, stream/device syncs,
hot-path allocation, and launch-param updates with host clocks around the
measured call stacks. Two gated candidates were measured and not frozen:
(1) poll the host cancel flag without per-segment `cudaDeviceSynchronize`,
(2) defer `cudaEventSynchronize(stop)` until after scatter/commit launch.
Pins stay `kSelectedPollWithoutDeviceSync = false` and
`kSelectedDeferElapsedEventSync = false`. Production selector remains
`decode_segments8`. Did not commit. model `cursor-grok-4.6-high`.
telemetry_unavailable.

**Files created**
- `pins/opt128_host_stalls_contract.json`
- `pins/opt128_iteration_contract.json`
- `tools/opt128_host_stalls.py`
- `cuda/opt128_host_stalls_test.cu`
- `tests/test_opt128_host_stalls.py`
- `fixtures/opt128_host_stalls.json`
- `evidence/optimization/opt128-host-stalls/REPORT.md`

**Files modified**
- `cuda/full_scheduler.h` (HostStallTimings, dormant OPT-128 pins default false)
- `cuda/full_scheduler.cu` (instrumented poll/elapsed-sync; pin-false preserves parent)
- `Makefile` (`cuda-opt128-diagnostics`, `build/qw38-cuda-opt128-host-stalls-test`)
- `tools/run_optimization_task.py` (`QW38_OPT128_*` prefixes)
- `tasks/OPT-128.md` (this run record)

**Host validation**
```
uv run ruff format tools/opt128_host_stalls.py tests/test_opt128_host_stalls.py
# 2 files left unchanged (after format)
uv run ruff check tools/opt128_host_stalls.py tests/test_opt128_host_stalls.py tools/run_optimization_task.py
# All checks passed!
uv run pytest tests/test_opt128_host_stalls.py -q
# 5 passed in 0.04s
```

**GPU phases** (device NVIDIA GeForce RTX 5090, image `qw38-cuda:13.0.2`,
GGUF present). Sidecars in `build/optimization-runs/opt128/`.

```
docker run --rm --gpus all --user "$(id -u):$(id -g)" \
  -v /home/user/qw38:/workspace -w /workspace qw38-cuda:13.0.2 \
  make -j"$(nproc)" cuda-opt128-diagnostics
# ok (opt128 + opt058-quality + checkpoint + memory-fit)

uv run python tools/opt128_host_stalls.py --phase preflight --mode feedback --run-dir build/optimization-runs/opt128
# ok; parent OPT-127 kept decode_segments8

uv run python tools/opt128_host_stalls.py --phase profile --mode feedback --run-dir build/optimization-runs/opt128
# ok; 16-token sample/eval on prefix 128; lazy D2H 4 B/token; greedy_ok

uv run python tools/opt128_host_stalls.py --phase lifetime --mode feedback --run-dir build/optimization-runs/opt128
# ok; greedy_valid, repeat logits, eval changes, restore, cancel isolated, subsequent request

uv run python tools/opt128_host_stalls.py --phase cancellation --mode feedback --run-dir build/optimization-runs/opt128
# ok; mid_segment + commit; eight_layer cadence; atomic_commit=true

uv run python tools/opt128_host_stalls.py --phase logits-consumer --mode feedback --run-dir build/optimization-runs/opt128
# ok; lazy_d2h=16 for 4 tokens; full logits match cached greedy

uv run python tools/opt128_host_stalls.py --phase non-greedy --mode feedback --run-dir build/optimization-runs/opt128
# ok; materialize_ms=0.172; host_alloc_ms=0.895 (off greedy production path)

uv run python tools/opt128_host_stalls.py --phase rank --mode feedback --run-dir build/optimization-runs/opt128
uv run python tools/opt128_host_stalls.py --phase freeze --mode feedback --run-dir build/optimization-runs/opt128
# freeze admitted=[]; verdict=no_material_opportunity; threshold=0.20 ms/token

uv run python tools/opt128_host_stalls.py --phase screen-d128 --mode feedback --run-dir build/optimization-runs/opt128
uv run python tools/opt128_host_stalls.py --phase screen-d2048 --mode feedback --run-dir build/optimization-runs/opt128
uv run python tools/opt128_host_stalls.py --phase screen-p4096 --mode feedback --run-dir build/optimization-runs/opt128
# skipped — no candidate admitted

uv run python tools/opt128_host_stalls.py --phase quality --mode acceptance --run-dir build/optimization-runs/opt128
# skipped; required=false; candidate_nll_measured=false (no complete-request A/B)

uv run python tools/opt128_host_stalls.py --phase state-memory --mode acceptance --run-dir build/optimization-runs/opt128
# ok; lifetime+cancel+128k_fit; checkpoint skipped_no_candidate

uv run python tools/opt128_host_stalls.py --phase d128 --mode acceptance --run-dir build/optimization-runs/opt128
uv run python tools/opt128_host_stalls.py --phase d2048 --mode acceptance --run-dir build/optimization-runs/opt128
uv run python tools/opt128_host_stalls.py --phase p4096 --mode acceptance --run-dir build/optimization-runs/opt128
# skipped — no candidate admitted

uv run python tools/opt128_host_stalls.py --phase report --mode acceptance --run-dir build/optimization-runs/opt128
# {"phase": "report", "ok": true} verdict=no_material_opportunity
```

**Measured critical-path (16-token sample/eval, prefix 128)**

| Arm | wall ms/token | delaying host ms/token | overlapping wait ms/token |
|---|---:|---:|---:|
| parent bench (no poll) | 16.311 | 0.038 | 16.207 |
| parent server poll | 16.427 | 0.063 | 16.295 |
| poll host-only | 16.364 | 0.039 | 16.261 |
| defer elapsed sync | 16.360 | 0.041 | 0.047 |

Poll wall save **0.063 ms/token**. Defer wall save **-0.049 ms/token** (shifted wait
into scatter DeviceSynchronize; not a removed wait). Combined save **0.065
ms/token**. Freeze threshold **0.20 ms/token**. Attributable upper bound
**0.065 ms/token** (~0.4% of server-poll wall). OPT-058 quality and 3+10 A/B
were not required after the quantified bound.

Lazy greedy D2H remains **4 bytes**. Full-logit `copy_last_outputs` still
matches cached greedy. Cancellation mid-segment and commit preserve frontier.

**Blockers:** none. Nsight Systems still absent; CUDA-event/host-clock
attribution is sufficient here because the candidate walls were measured
directly.

### Verification (integration-verifier, 2026-09-14T00:15:00Z)

Independent integration verification against dossier acceptance, OPT-125
shared gates, and `build/optimization-runs/opt128/` sidecars. **Verdict: PASS**
(`no_material_opportunity`; parent `decode_segments8` unchanged; host-stall pins
stay false).

**Host / build gates**
```
uv run ruff check tools/opt128_host_stalls.py tests/test_opt128_host_stalls.py tools/run_optimization_task.py
# All checks passed!

uv run pytest tests/test_opt128_host_stalls.py -q
# 5 passed in 0.03s

uv run python tools/opt128_host_stalls.py --phase report --mode acceptance --run-dir build/optimization-runs/opt128
# {"phase": "report", "ok": true}
```

**NO-OPPORTUNITY acceptance claims → artifacts**
| Claim | Artifact |
|---|---|
| Parent OPT-127 kept `decode_segments8` | `preflight.json`: `opt127_kept=true`, `parent_execution_graphs=decode_segments8`; `cuda/execution_graph_path.cuh`: `kSelectedExecutionGraphPath[] = "decode_segments8"` |
| Host-stall pins dormant (parent preserved) | `full_scheduler.h`: `kSelectedPollWithoutDeviceSync=false`, `kSelectedDeferElapsedEventSync=false`; `report.json`: `shipping_poll_without_device_sync=false`, `shipping_defer_elapsed_event_sync=false`, `production_kept=true` |
| Profiled sample/eval loop; greedy 4-byte D2H | `profile.json`: 16-token arms ok; `lazy_logits=true`, `greedy_transfer_bytes=4` |
| Lifetime / cancellation / logits consumer | `lifetime.json` ok; `cancellation.json` mid_segment+commit `atomic_commit=true`; `logits-consumer.json` ok |
| Freeze threshold not met; no candidate admitted | `freeze.json`: `admitted_candidates=[]`, `verdict=no_material_opportunity`, threshold `0.2` ms/token; poll save `0.063` ms/token; defer save `-0.049` ms/token (shifted wait) |
| OPT-058 quality skipped (no admitted candidate) | `quality.json`: `required=false`, `reason=no_host_stall_candidate_admitted_to_complete_request_ab` |
| OPT-125 state/memory guards | `state-memory.json` ok (`lifetime_ok`, `cancellation_ok`, `128k_fit_with_graphs`, `checkpoint_ok`/`skipped_no_candidate`) |
| D128/D2048/P4096 A/B skipped | `d128.json`/`d2048.json`/`p4096.json`: `skipped=true`, `reason=no_host_stall_candidate_admitted` |
| Baseline unchanged | `report.json`: `tok_s_delta.speedup=0.0`, `baseline_unchanged=true` |
| REPORT published | `evidence/optimization/opt128-host-stalls/REPORT.md`; metrics match `report.json`/`rank.json` summaries |

### Delivery (2026-09-14T00:16:00Z)

model: composer-2.5, telemetry_unavailable

OPT-128 marked `done`. Coupled IDs: none. Production selector unchanged
`decode_segments8`; host-stall pins stay false. Next eligible pending task:
**OPT-129**.

### Final outcome

- Status: `done`
- Verdict: `no_material_opportunity`
- Acceptance evidence: [`fixtures/opt128_host_stalls.json`](../fixtures/opt128_host_stalls.json); [`evidence/optimization/opt128-host-stalls/REPORT.md`](../evidence/optimization/opt128-host-stalls/REPORT.md)
- Throughput delta versus sitting `decode_segments8` baseline: D128/D2048/P4096 tok/s delta **0.0**; speedup **0.0**; `baseline_unchanged=true`. Attributable host-stall upper bound **0.065 ms/token** (poll save 0.063; defer save -0.049 shifted wait).
- First-pass acceptance: yes
- Remaining risk: OPT-131 owns residual launch-chain fusion after OPT-130; OPT-132 owns combined decode-stack admission

## Outcome

Remove one or two causally demonstrated host-side stalls remaining after the
graph verdict. Read OPT-125; parent is OPT-127 if kept, otherwise post124.
This task may complete with no material opportunity.

## Implementation

1. Reprofile the selected parent's complete Session sample/eval/output loop.
   Inspect `src/engine.cpp`, `src/server_generation.cpp`,
   `cuda/full_scheduler.cu` and sampling/output transfer consumers. Rank
   blocking copies, stream/device synchronization, hot-path allocation,
   descriptor construction, callback/output work and CPU launch starvation.
   Identify which waits overlap useful GPU execution and which delay submission.
2. Freeze at most two specific changes against measured call stacks and
   critical-path spans. Examples only if evidenced: reuse a hot-path staging
   buffer, replace an unnecessarily broad wait with the owning stream event,
   or defer a host read until its actual consumer. No generic async runtime,
   thread pool, polling policy rewrite or blanket synchronization removal.
3. Preserve OPT-118's lazy-logits validity and existing 4-byte greedy transfer.
   Explicitly test full-logit consumers and non-greedy sampling where supported.
   Buffer/event lifetimes must cover cancellation, restore and a subsequent
   request. Keep required error propagation and public callbacks intact.
4. Measure CPU and device effects and full request wall, including event setup,
   output delivery and cancellation. A shifted wait is not a removed wait.
   If no remaining attributable span can clear noise, stop with a quantified
   upper bound rather than invent another candidate.

## Acceptance

Pass OPT-125 quality/state/memory and paired request gates on the actual
selected parent. Add targeted lifetime/order regressions for the changed
boundary and compare CLI/server callback behavior. Deliver
`evidence/optimization/opt128-host-stalls/REPORT.md` with before/after
critical-path attribution, candidates and keep/reject/no-opportunity outcome.

