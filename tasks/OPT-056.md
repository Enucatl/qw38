# OPT-056 — Close the measured speed gap with quality evidence

Status: blocked. Dependencies: OPT-045, OPT-046, OPT-047, OPT-048, OPT-049,
OPT-050, OPT-051, OPT-052, OPT-053, OPT-054, OPT-055. A dependency can finish
with a documented rejection; that does not make this outcome gate pass.
Read [the shared protocol](PERFORMANCE-RECOVERY-2026-09-10.md).

## Required outcome

On the same GGUF, pinned CUDA toolchain, exclusive RTX 5090 and same sitting,
the final selected production engine exceeds pinned llama.cpp P/D128/D2048
throughput by **at least 5%**, and both decode p95 measures are no worse than
llama's. Use unchanged OPT-021 and OPT-032 protocols and retain raw samples,
latencies and controls. Require confidence-supported improvement, not a rounded
tok/s headline. The 5% margin makes “faster” meaningful; it is a goal, not a
forecast. Current llama implies approximately 3347 P and 70.45 D2048 tok/s,
but actual same-sitting llama measurements determine thresholds.

Also run the existing frozen 2K OPT-016 parity gate. This task supplies evidence
to that owner and may update its run record/status when the original acceptance
passes; it does not redefine or replace it. Keep longer-context performance
and release gates separate. Do not mark done on partial parity or because the
candidate list is exhausted.

## Validation and report

Run OPT-044's full production quality suite on the **combined** selected paths,
including held-out NLL, continuation/task checks and recurrence drift. Report
per-family precision/staging/rounding, retained reference checks, numerical
differences from llama, and any fallback shapes. Re-run long-context memory,
checkpoint/cancellation/state isolation gates affected by layout/scheduler work.
Existing QLT-001 remains its own owner; this task cannot claim its completion
from a short optimization suite.

Refresh exclusive attribution and compare measured remaining milliseconds to
OPT-043. Report paired final component times and wall speed, fresh build/source
identity, graph/microbatch configuration, and precision choices. Include public
Session or server time-to-first-token and inter-token latency as secondary
metrics; they must not silently replace the historical workload protocol.

Add `pins/opt056_performance_gate_contract.json`,
`fixtures/opt056_performance_gate.json`, `tests/test_opt056_performance_gate.py`,
and `evidence/optimization/opt056-performance-gate/REPORT.md`. Gate validators
must reject missing quality, mismatched models/revisions/protocols, skipped
hardware execution, false success after a component rejection, and p95 failure.
Run new test, `tests/test_opt016_parity.py`, relevant production-numerics,
state/memory tests, and documentation checks on a fresh build.

If speed or quality fails, leave the outcome gate unpassed with exact remaining
gap and create only the next measured-bottleneck task(s). Potential new chunked
GDN algorithms or compact repacks require that evidence; do not embark on an
unbounded rewrite or loosen accuracy criteria in this task.

## Run record

### Planning

- Agent/model: coordinator (pre-authored dossier admitted)
- UTC/time/tokens/cost: 2026-09-10T23:00:00Z; telemetry_unavailable
- Outcome: skipped — pre-authored dossier admitted

### Implementation

- Agent/model: cursor-grok-4.6 (implementation; Task slug cursor-grok-4.6-high unavailable)
- UTC/time/tokens/cost: 2026-09-11T02:05:24Z; telemetry_unavailable
- Changes: contract/fixture/host test/evidence for the same-sitting exclusive RTX 5090 outcome gate; Makefile `QUANT_MMV_CUDA_OBJECTS` defined before CUDA eval/bench link; `cuda/pdl_launch.cuh` kernel-launch syntax wrapped in `__CUDACC__` so `engine.cpp` can include it; exclusive sitting reused cached P/D/2K/attribution sidecars and completed quality, Session TTFT, memory-fit, and checkpoint.
- Commands:
  - exclusive sitting: `QW38_RUN_CUDA_TESTS=1 QW38_CUDA_TEST_TIER=acceptance uv run pytest -s tests/test_opt056_performance_gate.py::test_opt056_exclusive_cuda_sitting` → 1 passed (gate recorded unpassed)
  - P Quartz **2808.49609** vs llama **3263.516321** tok/s (ratio 0.861; 5% bar 3426.69; remaining **618.19** tok/s; CI on Quartz−1.05×llama entirely negative)
  - D128 **37.4816246** vs **68.9318767** (remaining **34.90** tok/s); Quartz p95 **26.9079285** vs llama **14.576** ms
  - D2048 **35.7208481** vs **67.3394327** (remaining **34.99** tok/s); Quartz p95 **28.182457** vs llama **14.726** ms
  - OPT-016 2K **3012.69507** vs **3169.571249** (`gate_passed` false; original quartz ≥ llama bar)
  - quality: wikitext/continuation/recurrence/held-out NLL pass; all eight greedy tasks failed (token 271); `production_optimization.all` false
  - Session TTFT 119.808688 ms / ITL p50 25.03173 ms; memory-fit and checkpoint ok; cancellation frontier 0
- Outcome: `gate.passed` false. Remaining milliseconds versus llama: P **203.34** ms (OPT-043 remaining was 701.55 ms); D2048 **13.14** ms/token (OPT-043 remaining was 24.58 ms). Candidate-task completion is not a pass.

### Documentation

- Agent/model: coordinator (documentation)
- UTC/time/tokens/cost: 2026-09-11T02:15:00Z; telemetry_unavailable
- Changes: handbook rows in `docs/06-system-optimization.md`, `docs/61-benchmark-harness.md`, `docs/62-cuda-full-prefill.md`, `docs/sources.md`, `docs/README.md`, `docs/65-documentation-audit.md`; contract, fixture, report, and raw sidecars published under `evidence/optimization/opt056-performance-gate/`.
- Outcome: proof-limit phrases present in REPORT; audit row records measured gate failure.

### Verification

- Agent/model: coordinator (verification)
- UTC/time/tokens/cost: 2026-09-11T02:15:00Z; telemetry_unavailable
- Commands:
  - `uv run ruff format .` + `uv run ruff check .` → pass
  - `uv run pytest tests/test_opt056_performance_gate.py tests/test_documentation.py -q` → 7 passed, 1 skipped (`test_opt056_exclusive_cuda_sitting` skipped without `QW38_RUN_CUDA_TESTS=1`)
- Verdict: contract/fixture/report/raw sidecars validate; `gate.passed` false honestly recorded; delivery authorized as blocked evidence only.

### Delivery

- Agent/model: coordinator (delivery)
- UTC/time/tokens/cost: 2026-09-11T02:15:00Z; telemetry_unavailable
- Outcome: marked OPT-056 `blocked`. Commit and push authorized. Recovery: close remaining P/D gaps versus llama before re-pass.

### Final outcome

- Status: `blocked` (measured gate unpassed)
- Acceptance evidence: live fixture
  [`fixtures/opt056_performance_gate.json`](../fixtures/opt056_performance_gate.json)
  (`measurement_utc` 2026-09-11T01:50:43Z), contract
  [`pins/opt056_performance_gate_contract.json`](../pins/opt056_performance_gate_contract.json),
  report
  [`evidence/optimization/opt056-performance-gate/REPORT.md`](../evidence/optimization/opt056-performance-gate/REPORT.md),
  test
  [`tests/test_opt056_performance_gate.py`](../tests/test_opt056_performance_gate.py)
- Gate failure (same-sitting exclusive RTX 5090 vs pinned llama.cpp):
  - P: Quartz **2808.50** vs llama **3263.52** tok/s (ratio 0.861; 5% bar **3426.69**; remaining **+618** tok/s; 95% CI on Quartz−1.05×llama entirely negative)
  - D128: **37.48** vs **68.93** tok/s (remaining **+34.90** tok/s); decode p95 Quartz **26.91** ms vs llama **14.58** ms (worse)
  - D2048: **35.72** vs **67.34** tok/s (remaining **+34.99** tok/s); decode p95 Quartz **28.18** ms vs llama **14.73** ms (worse)
  - Quality: wikitext/continuation/recurrence/held-out NLL pass; all eight greedy production-optimization tasks fail (token 271)
  - OPT-016 2K: Quartz **3012.70** vs llama **3169.57** (`gate_passed` false)
- Remaining milliseconds versus llama: P **203.34** ms; D2048 **13.14** ms/token (improved versus OPT-043 but still far below llama)
- Recovery condition: close remaining P/D throughput and decode-p95 gaps versus llama, restore production-optimization greedy tasks, and re-pass the same-sitting outcome gate; candidate-task completion alone is insufficient
- First-pass acceptance: **no** (gate failed honestly)
- Commit: delivered 2026-09-11T02:15:00Z
- Push: authorized
