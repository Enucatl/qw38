# OPT-061 — Build real-input streaming replay and identify hardware limits

Status: done. Dependencies: OPT-060. Coupled IDs: none.

## Run record

### Planning (2026-09-11T08:00:00Z)

skipped — pre-authored dossier admitted (coordinator)
Primary target: reusable component evidence with realistic cache behavior.
Read [the batch protocol](PERFORMANCE-RECOVERY-2026-09-11.md).

### Implementation (2026-09-11T08:18:00Z)

- Agent/model: cursor-grok-4.6 (implementation subagent)
- UTC/time/tokens/cost: 2026-09-11T08:18:00Z; telemetry_unavailable
- Outcome: reusable capture/replay delivered; no production pin change; no throughput claim
- Files changed:
  - `cuda/full_scheduler.h` / `cuda/full_scheduler.cu` — residual + prompt-row capture (untimed)
  - `cuda/optimization_component_replay.h` / `cuda/optimization_component_replay.cu`
  - `Makefile` — `build/qw38-cuda-component-replay`, `cuda-opt061-diagnostics`
  - `tools/opt061_component_replay.py`
  - `pins/opt061_iteration_contract.json`, `pins/opt061_component_replay_contract.json`
  - `fixtures/opt061_component_replay.json`
  - `tests/test_opt061_component_replay.py`
  - `evidence/optimization/opt061-component-replay/` (REPORT, hardware, provenance, capture bundles, rounds)
- Commands:
  1. `uv run pytest -q tests/test_opt061_component_replay.py` — **pass** (9 passed)
  2. `uv run python tools/run_optimization_task.py --task OPT-061 --mode feedback --phase decode-ffn` — **pass**, 5.3 s / 300 s
  3. `uv run python tools/run_optimization_task.py --task OPT-061 --mode feedback --phase decode-mixer` — **pass**, 3.5 s / 300 s
  4. `uv run python tools/run_optimization_task.py --task OPT-061 --mode feedback --phase prompt-ffn` — **pass**, 10.4 s / 300 s
  5. `uv run python tools/run_optimization_task.py --task OPT-061 --mode acceptance` — first attempt **fail** (`cudaDeviceReset` before ResidentModel destructor, then mixer/prompt capture `CUDA hybrid prompt chunk failed`); after removing reset — **pass**, 29.6 s / 300 s (3 warmup + 10 rounds × 2 cache modes × 3 families)
  6. Native smoke Q4/Q8 guards — **pass**; `--corrupt-guard` detects altered bytes
  7. Native correctness 16×4 FP64 control dots — **pass** (64 dots, 0 failed)
- Focused validation: **passed**
- Measured rotating vs hot enclosing (acceptance means): decode-ffn 17.20 vs 15.75 ms; decode-mixer 5.44 vs 2.61 ms; prompt-ffn 699.91 vs 693.40 ms. Mixer is the large hot/rotating gap. Gate/up and down charged 64 calls. L2 96 MiB, FFN working set 9.626 GB, 400 W cap. `ncu` present / `nsys` absent; streaming useful-byte estimate ~1015 GB/s, not a DRAM-counter claim.
- Blockers / missing counters:
  - No `--set full` Nsight Compute kernel capture this sitting (optional; event replay usable)
  - Authority comparison uses OPT-060 enclosing dispatch labels; isolated OPT-043 MUL_MAT remains labeled isolated-op
  - Mixer replay is shared-input projections only (GDN/attention core excluded as stateful)
- Do not commit (implementation agent).

## Problem

OPT-046/047 microbenchmarks repeatedly reuse one shape/weight allocation.
That can reward cache-resident loads very differently from the 64-layer model.
FFN useful weight bytes total 9.626 GB/token. Per-family elapsed time alone
does not tell us whether DRAM, issue throughput, shared-memory barriers, or
CPU launches dominate. This task makes subsequent A/B tests representative.

## Implementation

1. Extend the OPT-043 capture structure or add a small capture bundle. Store
   exact BF16 projection inputs, FP32 residuals and intermediate outputs needed
   by FFN/mixer replay. Include FFN down input, output norm input for Q6 and
   actual prompt-row indices. Calibration layers 0,3,31,32; held-out62,63.
   Capture each once with role/layer/token/shape, GGUF tensor identity, selector
   set and hashes. Capture state separately for any stateful family; never time
   the capture copies. Do not copy hard-coded byte offsets from old harnesses;
   resolve tensor names through the validated inventory/model binding.
2. Add `cuda/optimization_component_replay.cu` and
   `build/qw38-cuda-component-replay`. Expose exactly these families initially:
   complete decode FFN, complete decode mixer projections, complete prompt FFN.
   The task-specific launchers select variants; the replay provides inputs,
   reset, timing and sampled reference utilities. Avoid a generic backend.
3. Support `cache_mode=hot|rotating`. Hot repeats one matrix and is diagnostic.
   Rotating visits distinct admitted layer weights in execution order. Use the
   resident model when available or a bounded set whose working set exceeds
   twice the queried L2 size. A timed round covers that full set. If a larger
   set cannot fit with current session/reserve, use an explicit cache-eviction
   pass outside the event interval and label it as an approximation. Do not
   charge flushing to one candidate only or count it as model work.
4. Match complete scope: norm, one shared activation stage, gate/up, SwiGLU,
   down stage/projection and residual for FFN; correct shared inputs plus all
   real mixer projections for mixer. Report kernel-only and enclosing time
   separately. Use OPT-060 frequencies, giving all gate/up/down exactly64 calls
   per token; a combined gate/up pair is 64 calls, not128 pairs.
5. Compare the same primitive inputs and complete family boundaries against
   the pinned authority adapter, using its actual selected dispatch. Do not
   call a standalone ggml MUL_MAT time the cost of fused llama FFN. Keep
   isolated-op output for numerical/reference use, labeled as such.
6. Record queried SM/L2/shared-memory limits, current400 W cap (or actual new
   readback), clocks, temperatures and throttling. For the dominant FFN decode,
   Q8 decode and Q4 prompt kernel only, request a bounded Nsight Compute capture
   if available: DRAM bytes/throughput, L2 hit rate, occupancy/register/local
   traffic, tensor activity and scheduler/barrier stalls. Query available metric
   names (`ncu --query-metrics`) rather than guessing architecture-specific names.
   Use `--launch-count 1` and a selected steady-state launch; no `--set full`.
7. If counters are unavailable, measure a streaming read calibration using a
   buffer larger than 2×L2 and a reduction/checksum preventing dead-code removal.
   Useful-byte rate is an estimate, never a DRAM-counter claim. Record compiled
   registers/shared/local bytes and the hot/rotating difference. Bound potential
   gain using measured family time; report uncertainty rather than declare
   bandwidth-bound from low arithmetic intensity alone.

## Tests and loop cost

Add `tests/test_opt061_component_replay.py`. Host tests cover capture hashes,
wrong role/shape, double-counted paired FFN, independent timing rounds, and
hot results refused as sole production acceptance. Native smoke: one tiny
Q4 case and one tiny Q8 case, no model; altered guard bytes must fail.
Correctness: at most 16 sampled output rows × 4 activation vectors ×one selected
K/family, independently decoded FP64. Reuse immutable OPT-059 references when
available; otherwise validate replay against independently decoded control dots
and record reference identity. Instrumentation can proceed while OPT-059 is
pending, but that does not admit changed arithmetic.

Screen one selected family: control only, 2 cache modes ×(1 warmup+3 samples)
on one bounded rotating set. Projection family outputs have full finite checks;
host reference is sampled. Prefill screen uses one 4096-row family, only 4 sampled
activation rows. A new capture is a separately labeled setup phase and cached
by the complete source/input identity. No repeated full-model captures per candidate.

```sh
uv run pytest -q tests/test_opt061_component_replay.py
uv run python tools/run_optimization_task.py --task OPT-061 --mode feedback --phase decode-ffn
uv run python tools/run_optimization_task.py --task OPT-061 --mode feedback --phase decode-mixer
uv run python tools/run_optimization_task.py --task OPT-061 --mode feedback --phase prompt-ffn
uv run python tools/run_optimization_task.py --task OPT-061 --mode acceptance
```

Each phase has a 300-second limit. Acceptance is 3 warmups+10 independent rounds
per cache mode/family. Hardware counters are a separate optional diagnostic
phase with one kernel launch per family; timeout/unavailability must not block
usable event replay. No end-to-end throughput sitting in this task.

## Acceptance

Deliver contract/fixture, reusable captures, hardware/provenance JSON and
`evidence/optimization/opt061-component-replay/REPORT.md`. Rank the next family
changes using full-engine attribution and rotating replay together. Explain
microbench/full-engine disagreement; do not demand invented reconciliation.
No production pin change. Export the exact replay command, row lists and
reference cache key for every dependent task.

### Verification (2026-09-11T08:21:30Z)

Independent verifier. No commit. Diff reviewed against dossier recipe,
`implementation_ledger.md` OPT-061 row, and acceptance section above.

**Deliverables present:** `pins/opt061_component_replay_contract.json`,
`pins/opt061_iteration_contract.json`, `fixtures/opt061_component_replay.json`,
`cuda/optimization_component_replay.h` / `cuda/optimization_component_replay.cu`,
`build/qw38-cuda-component-replay`, `tools/opt061_component_replay.py`,
`tests/test_opt061_component_replay.py`, capture bundles and sidecars under
`evidence/optimization/opt061-component-replay/`, and
`evidence/optimization/opt061-component-replay/REPORT.md`.

Commands:

```
uv run ruff format .
```

**PASS** — 144 files left unchanged.

```
uv run ruff check .
```

**PASS** — all checks passed.

```
uv run pytest -q tests/test_opt061_component_replay.py
```

**PASS** — 9 passed in 0.02s.

```
uv run python tools/run_optimization_task.py --task OPT-061 --mode feedback --phase decode-ffn
```

**PASS** — exit 0; `elapsed_s=3.41`; deadline 300s; run
`build/optimization-runs/OPT-061/feedback/20260911T081957Z-37627fa2`.

```
uv run python tools/run_optimization_task.py --task OPT-061 --mode feedback --phase decode-mixer
```

**PASS** — exit 0; `elapsed_s=3.26`; deadline 300s; run
`build/optimization-runs/OPT-061/feedback/20260911T082003Z-c1f7ec2d`.

```
uv run python tools/run_optimization_task.py --task OPT-061 --mode feedback --phase prompt-ffn
```

**PASS** — exit 0; `elapsed_s=10.20`; deadline 300s; run
`build/optimization-runs/OPT-061/feedback/20260911T082008Z-ac0bd7f0`.

```
uv run python tools/run_optimization_task.py --task OPT-061 --mode acceptance
```

**PASS** — exit 0; `result_class=ok`; `elapsed_s=27.90`; deadline 300s; run
`build/optimization-runs/OPT-061/acceptance/20260911T082020Z-30f95577`
(3 warmup + 10 rounds × 2 cache modes × 3 families).

Acceptance checklist (dossier § Acceptance):

| Criterion | Verdict | Notes |
|---|---|---|
| Contract + fixture | **PASS** | `claims_throughput=false`; hot refused as production; 64 gate/up/down calls |
| Reusable captures | **PASS** | Per-family bundles; layers 0,3,31,32 + held-out 62,63; tensor names via inventory |
| Hardware/provenance JSON | **PASS** | `hardware.json`, `provenance.json`; L2/SM/power/clocks recorded |
| `REPORT.md` | **PASS** | No throughput claim; hot/rotating table; ranking vs OPT-060; replay commands |
| Hot vs rotating replay | **PASS** | Acceptance 3+10 rounds per mode/family; mixer largest hot/rotating gap |
| Complete-family scope | **PASS** | Decode FFN, decode mixer projections, prompt FFN; kernel + enclosing times |
| No production pin change | **PASS** | Production selectors unchanged |
| Export for dependents | **PASS** | Replay command, row lists, capture keys in REPORT and provenance |

Blocker assessment:

- **`ncu` present, no `--set full` sweep** — acceptable; event replay usable.
- **`nsys` absent** — acceptable; streaming-byte estimate labeled, not DRAM counter.
- **Mixer excludes GDN/attention core** — labeled in REPORT; shared-input projections only.

**Overall verification: PASS**

### Delivery (2026-09-11T08:22:00Z)

- Agent/model: composer-2.5 (delivery)
- UTC/time/tokens/cost: 2026-09-11T08:22:00Z; telemetry_unavailable
- Commands: verifier PASS confirmed from Run record → Verification
  (2026-09-11T08:21:30Z)
- Outcome: Marked OPT-061 `done`. Final ledger history and audit row published.
  Commit and push authorized.

### Final outcome

- Status: `done` (real-input streaming component replay; no throughput claim)
- Acceptance evidence: fixture
  [`fixtures/opt061_component_replay.json`](../fixtures/opt061_component_replay.json),
  contracts
  [`pins/opt061_component_replay_contract.json`](../pins/opt061_component_replay_contract.json)
  and
  [`pins/opt061_iteration_contract.json`](../pins/opt061_iteration_contract.json),
  report
  [`evidence/optimization/opt061-component-replay/REPORT.md`](../evidence/optimization/opt061-component-replay/REPORT.md),
  capture bundles and sidecars under
  [`evidence/optimization/opt061-component-replay/`](../evidence/optimization/opt061-component-replay/)
- Throughput delta: none (`claims_throughput=false`)
- Replay: hot vs rotating complete-family events for decode-ffn, decode-mixer,
  prompt-ffn; gate/up/down 64 calls per token; mixer largest hot/rotating gap
  (2.61 vs 5.44 ms enclosing)
- Hardware: RTX 5090, L2 96 MiB, 400 W cap; `ncu` present, `nsys` absent;
  streaming useful-byte estimate ~1015 GB/s (`dram_counter_claim=false`)
- Proof limits: hot cache diagnostic only; mixer core excluded; no end-to-end
  sitting; isolated OPT-043 MUL_MAT labeled isolated-op
- Acceptance run: 27.90 s / 300 s
  (`build/optimization-runs/OPT-061/acceptance/20260911T082020Z-30f95577`)
- First-pass acceptance: **yes** (ruff + pytest + feedback P/D + acceptance)
- Next eligible recovery task: OPT-062
- Agent/model for this sitting: composer-2.5; telemetry_unavailable
