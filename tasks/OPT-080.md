# OPT-080 — Validate the admitted combination and unchanged outcome gates

Status: pending. Dependencies: OPT-070, OPT-071, OPT-072, OPT-073, OPT-074,
OPT-075, OPT-076, OPT-077, OPT-078, OPT-079. Coupled IDs: none.
Priority: final batch sitting. Target: quality, P4096, D128/D2048, p95 and 2K.
Read [the post-069 protocol](PERFORMANCE-RECOVERY-POST-069.md).
Quality risk: interactions among accepted arithmetic and graph/state choices.
Instrumentation: reuse; throughput is uninstrumented.

## Entry and implementation

1. Each dependency needs a concrete keep/reject/no-go, or an explicitly blocked
   outcome with a working unchanged production control and an exclusion record.
   Never treat a missing authority reference, broken harness or unresolved
   selected arithmetic as an acceptable rejected dependency. OPT-072's review
   must give every historical correctness candidate a disposition and owner.
2. Freeze current source/build/flags, actual launch selectors, model/token
   identity, per-family admission, graphs, buffers and current read-back power.
   Compare against the most recent verified complete configuration; retain
   OPT-069 and OPT-056 as history. Rebuild/recapture only changed artifacts.
3. Cheap preflight uses two same-path graph/eager tokens, full finite logits,
   32 held-out targets, all eight functional cases (at most 16 generated tokens
   each), and one D2048+32 control/candidate pair. Verify selected policies and
   all parsed quality outcomes, not merely process success or token count.
   Any required quality/admission failure prevents release execution.
4. Publish distinct internal numerical/non-regression, absolute task accuracy,
   pinned-llama parity and original OPT-056 outcomes. If arithmetic remains
   wrong under both engines, internal non-regression may pass but the original
   absolute quality gate stays failed. Only an explicitly documented authorized
   successor gate may replace it, never this task's convenient interpretation.
5. Once eligible, run full selected quality: both 1024-target spans, recurrence
   short/long, functional suite, finite vocabulary and calibrated logit margins.
   Stop before timing if it fails. Then run unchanged OPT-021 cold P4096
   (0 warmups, 3 replicates), OPT-032 D128/D2048 (3 warmups + 30 runs of256
   predetermined tokens per prefix/engine), and original OPT-016 2K protocol.
   All timed configurations use consistent publication/copy/state semantics.
6. Reuse builds and authenticated authority/model. Alternate comparable whole
   runs/blocks and log hardware throughout. The complete release is explicitly
   long; precompute cost from tokens/replicates, record every phase, and resume
   only same-sitting identity-matched completed work. A host report failure
   does not require a second GPU sitting. Reports and fixtures must agree.
7. Run checkpoint, cancellation, 128K post-graph fit and relevant long-context
   recurrence/attention quality on the final selected combination. Compute
   parity as Tq-Tl and +5% as Tq-Tl/1.05, separately from raw p95. Report
   intervals/variance and actual same-sitting llama. Internal component wins
   cannot substitute for no-worse decode p95 or the >=5% outcome requirement.

## Tests and commands

Host tests `tests/test_opt080_batch_gate.py` reject incomplete admissions,
screen promoted to release, wrong quality version, wrong counts, contradictory
report/fixture verdicts, lost state and historical gate relabeling. Reuse engine
probe, quality, P/D/2K and state/memory binaries, no duplicate CUDA oracle.

```sh
uv run pytest -q tests/test_opt080_batch_gate.py tests/test_documentation.py
uv run python tools/run_optimization_task.py --task OPT-080 --mode feedback --phase preflight
uv run python tools/run_optimization_task.py --task OPT-080 --mode release --dry-run
uv run python tools/run_optimization_task.py --task OPT-080 --mode release
```

Feedback has the common 300-second aggregate limit. Release requires an
explicit long-mode plan and is never reached from smoke/correctness/screen.
If original quality remains blocked, deliver that preflight verdict and an
explicitly separate diagnostic performance plan; do not run a futile release
or declare success. Update OPT-056/OPT-016 only when their actual conditions
pass under their owning gate rules; otherwise leave them blocked and report
the remaining latency budget. No automatic next optimization sweep.
