# OPT-134 — Enforce evidence discipline in both ledger runner skills

Status: done. Dependencies: OPT-133. Coupled IDs: none.
Priority: P0. Plan impact: none. This dossier specifies future work; it does
not execute either runner or authorize a commit/push by the planning turn.

## Outcome

Update both ledger runner prompts and dossier templates so incomplete timing
coverage, mismatched metrics, and conflicting measurements cannot become a
verified bottleneck or no-opportunity claim. Prompts can enforce how evidence is
requested and reviewed; they cannot themselves prove profiler completeness.
OPT-136 supplies the executable interval checks. Until those checks exist,
require independent raw-artifact reconstruction and label unavailable checks
as unavailable. Do not manufacture an executable-check pass.

Ledger acceptance: Both runner skills require metric identity, graph-aware
coverage, contradiction resolution, independent raw-evidence verification and
separate experiment/shipping verdicts; eight adversarial review cases pass.

## Review findings to address

- `.agents/skills/run-ledger-task-codex/SKILL.md` and
  `.cursor/skills/run-ledger-task-cursor/SKILL.md` both prefer OPT-020-style
  CUDA-event categories without first establishing coverage of production
  graph execution. A rebuilt binary is necessary but not sufficient.
- Both ask verifiers to trace claims to assertions/artifacts, but neither
  requires conservation of timing intervals or rejection of missing graph
  activity. A report with `hardware_gpu_trace=true` can still omit graphs.
- Both can rank a Quartz-internal sink without exposing missing corresponding
  llama work. Missing coverage must not become zero excess.
- Cursor documentation precedes verification in the stage list but asks for a
  verdict already determined by verification. Resolve that ordering explicitly.
- Cursor completion text can conflate a rejected candidate's measured result
  with zero shipping impact. Codex's generic delta instructions also need
  explicit metric/window matching.
- OPT-133's graph-level capture omitted `CUPTI_ACTIVITY_KIND_GRAPH_TRACE` from
  the GPU sum. Read OPT-136's reproduction specification before editing prose.

## Implementation

Only edit the following skill files, this dossier and its ledger row/log:

- `.agents/skills/run-ledger-task-codex/SKILL.md`
- `.agents/skills/run-ledger-task-codex/references/task-dossier-template.md`
- `.cursor/skills/run-ledger-task-cursor/SKILL.md`
- `.cursor/skills/run-ledger-task-cursor/references/task-dossier-template.md`
- Add identical policy content at each skill's
  `references/performance-evidence-checklist.md`. Keep paths relative within
  each skill so either package remains usable separately.
- Add matching `references/performance-evidence-review-cases.md` files with the
  eight cases below. No Python helpers, production code, pins or benchmarks.

Replace the performance-steering sections, rather than appending contradictory
instructions. Link the checklist from admission, planning, documentation,
verification and completion. Preserve task selection, existing delivery
authorization, runtime tool/model mappings, telemetry and quality requirements.
Do not change model routing merely because these dossiers are suitable for a
bounded Luna implementation; an explicit user model selection still prevails.

The checklist and both templates must require these fields and decisions:

1. **Measurement identity.** For each rate: engine/commit, actual loaded binary
   hash, build flags/image/tool versions, GGUF, selectors, input token hash,
   prefix, output/eval counts, first-token convention, allocated capacity,
   populated length, sampling/output policy, graph mode, clocks/residents,
   warmups/samples and exact numerator/start/end events. Distinguish prefill,
   decode-only, complete-request, component and public sample/eval/output.
   Setup, graph creation, warmup and checkpoint restore stay outside rate
   denominators. A ratio requires a matching metric identity.
2. **Coverage before ranking.** Name captured tables, units, clock domain,
   capture bounds, streams, expected and observed graph/node counts, family
   mapping and unclassified work. Graph envelopes are not leaf kernel time.
   Graph-level data cannot prove continuous activity or internal idle. Missing
   node tracing forbids graph-internal idle and component-completeness claims.
   Use OPT-136's checked `coverage.json` when available; do not substitute its
   existence or a success flag for inspecting the assertions and source rows.
3. **Time accounting.** Union overlapping intervals; do not sum graph parents
   and children, nested NVTX and kernels, or overlapping CPU waits and GPU work.
   CPU `cudaEventSynchronize` duration is wait duration, not removable GPU idle.
   A cross-run or cross-stack aggregate cannot identify overlap of intervals.
   Never use `min(old_unobserved, new_idle)` as proof of causal explanation.
4. **Contradiction register.** List conflicting sources, their identities,
   quantitative disagreement, current disposition and resolving check. A
   component exceeding its enclosing duration on the same run is an error;
   differing sittings are a comparability problem until resolved. Do not choose
   whichever number supports the planned candidate. Unresolved material
   contradictions stop affected rankings, not unrelated verified findings.
5. **Claim types.** Each conclusion is `measured`, `derived`, `hypothesis`,
   `incomplete` or `historical`. A derivation includes formula, units and input
   links. Bytes divided by assumed bandwidth is a conditional estimate; a
   removable-time upper bound requires a demonstrated bound on all relevant
   costs. Graphs do not make kernel-node/fusion overhead zero by definition.
6. **Target and guard roles.** Freeze region, primary metric, targets, guards,
   thresholds and policy ID before candidate timing. Apply OPT-135 when the
   task opts into it; do not invent a second policy in a stage prompt. A guard
   needs non-regression, not demonstrated improvement. Preserve historical
   OPT-130 rejection and release gates.
7. **Independent verification.** Pass task paths and required phases to the
   verifier without pre-deciding a verdict. The verifier independently queries
   raw records for the largest claimed gap and every claimed eliminated gap;
   checks coverage, units and identities; and records calculations. A second
   agent restating generated JSON is not independent evidence. Failed evidence
   checks return to implementation; missing hardware is incomplete/blocked,
   not a measured rejection or no opportunity.
8. **Reporting and delivery.** Record candidate measured delta, shipping delta,
   quality result and evidence completeness separately. Diagnostics use N/A
   for shipping throughput changes. Rejected candidates may have measured
   gains while shipping delta is zero. Historical numbers are references, not
   current denominators. Only verified findings enter the final narrative.

Keep documentation before verification as draft preparation in both runners;
label draft conclusions unverified. Verification assesses that draft and all
artifacts. Delivery may publish the verified verdict but must not invent or
revise scientific conclusions; a semantic report change returns to verification.
Remove Cursor's contradictory instruction to supply a future verifier verdict
to the earlier documentation stage.

## Acceptance

Record each case, expected decision and the exact policy passage enforcing it
in this dossier's verification record. Independently review the two skills
against all eight cases, without invoking their delivery workflow:

1. Graph capture: 196.740 ms window, 8.243 ms ordinary GPU activity, 186.016 ms
   graph envelopes. Reject 188.497 ms proven idle; internal activity unknown.
2. Quartz complete-request / llama decode-only ratio: refuse comparison.
3. API synchronize spans concurrent GPU work: refuse to count it as idle or
   add it to GPU wall time.
4. Same-run leaf sum exceeds enclosing time because nesting/overlap was summed:
   reconstruct a disjoint union before ranking.
5. OPT-108-like 0.033 ms vs OPT-129-like 0.113 ms component from different
   sittings: record contradiction; no production 4x claim without reconciliation.
6. Missing llama GDN mapping: excess null/unknown, never Quartz minus zero.
7. Target improves, guard CI contains 1 but lies above its non-regression floor:
   apply the opted-in target/guard policy, not a guard-improvement test.
8. Required capture or candidate NLL not run: incomplete/blocked; no fabricated
   `no_material_opportunity`, quality pass, or measured reject.

Commands (from repository root):

```sh
git diff --check
diff -u .agents/skills/run-ledger-task-codex/references/performance-evidence-checklist.md .cursor/skills/run-ledger-task-cursor/references/performance-evidence-checklist.md
diff -u .agents/skills/run-ledger-task-codex/references/performance-evidence-review-cases.md .cursor/skills/run-ledger-task-cursor/references/performance-evidence-review-cases.md
```

Read both complete skills and templates after editing; verify every relative
link and absence of old contradictory steering. Do not add tests that merely
assert prose substrings. CUDA, model NLL and throughput measurements: N/A,
because this task changes instructions only. Done requires all eight review
cases and synchronized references; it does not claim the profiler is repaired.

## Run record

Planning: skipped — pre-authored dossier admitted (2026-09-14T13:30:00Z).
Unresolved decisions: none.

### Implementation

- UTC: 2026-09-14T13:36:40Z
- Throughput delta: N/A (runner instructions only; no CUDA, NLL, or throughput
  measurements)
- Verification: complete (see below)
- Delivery: complete (see below)

Files changed:

- `.agents/skills/run-ledger-task-codex/SKILL.md`
- `.agents/skills/run-ledger-task-codex/references/task-dossier-template.md`
- `.agents/skills/run-ledger-task-codex/references/performance-evidence-checklist.md`
  (created)
- `.agents/skills/run-ledger-task-codex/references/performance-evidence-review-cases.md`
  (created)
- `.cursor/skills/run-ledger-task-cursor/SKILL.md`
- `.cursor/skills/run-ledger-task-cursor/references/task-dossier-template.md`
- `.cursor/skills/run-ledger-task-cursor/references/performance-evidence-checklist.md`
  (created)
- `.cursor/skills/run-ledger-task-cursor/references/performance-evidence-review-cases.md`
  (created)
- `tasks/OPT-134.md` (this run record)

Decisions:

- Replaced the OPT-020-first performance-steering sections instead of appending
  contradictory coverage rules. Ranking now requires measurement identity and
  graph-aware coverage first; CUDA-event categories cannot rank production
  graph execution until that coverage exists.
- Kept one identical checklist and one identical eight-case review file in each
  skill tree so `diff -u` is empty and either package stays usable separately.
- Linked the checklist from admission, planning, documentation, verification,
  and completion in both skills. Documentation remains stage 3 as unverified
  draft; verification assesses that draft. Removed Cursor's instruction to pass
  a future verifier verdict into documentation.
- Did not change Codex Luna/Sol/Terra routing, Cursor model slugs, task
  selection, delivery authorization, telemetry, or the keep/reject quality gate.
  OPT-136 `coverage.json` is required when available; its absence is labeled
  `unavailable`, not an executable-check pass. OPT-135 is applied only when a
  task opts in.
- Candidate measured delta, shipping delta, quality result, and evidence
  completeness are separate report fields. Rejected candidates may show a
  measured gain with shipping delta zero.

Acceptance commands (repository root):

```
git diff --check
```

stdout: empty. exit: 0.

```
diff -u .agents/skills/run-ledger-task-codex/references/performance-evidence-checklist.md .cursor/skills/run-ledger-task-cursor/references/performance-evidence-checklist.md
```

stdout: empty. exit: 0.

```
diff -u .agents/skills/run-ledger-task-codex/references/performance-evidence-review-cases.md .cursor/skills/run-ledger-task-cursor/references/performance-evidence-review-cases.md
```

stdout: empty. exit: 0.

Post-edit review: both complete skills and both templates were reread. Every
relative link resolves. Old contradictory steering
(`Prefer the current OPT-020-style…`, `those numbers are current enough`,
`Pass the measured verdict … from verification`, reject speedup conflated with
shipping zero) is absent.

Issues encountered: none.

### Verification

- UTC: 2026-09-14T13:37:00Z
- Agent/model: integration verifier (composer-2.5, low reasoning)
- Throughput delta: N/A (runner instructions only; no CUDA, NLL, or throughput
  measurements)

#### Commands

```
git diff --check
```

stdout: empty. exit: 0.

```
diff -u .agents/skills/run-ledger-task-codex/references/performance-evidence-checklist.md .cursor/skills/run-ledger-task-cursor/references/performance-evidence-checklist.md
```

stdout: empty. exit: 0.

```
diff -u .agents/skills/run-ledger-task-codex/references/performance-evidence-review-cases.md .cursor/skills/run-ledger-task-cursor/references/performance-evidence-review-cases.md
```

stdout: empty. exit: 0.

```
git diff --name-only HEAD
```

Changed paths only: `.agents/skills/run-ledger-task-codex/SKILL.md`,
`.agents/skills/run-ledger-task-codex/references/task-dossier-template.md`,
`.cursor/skills/run-ledger-task-cursor/SKILL.md`,
`.cursor/skills/run-ledger-task-cursor/references/task-dossier-template.md`,
`implementation_ledger.md` (OPT-134 row `pending` → `in_progress`),
`tasks/OPT-134.md`. Untracked (new, in scope):
`.agents/skills/run-ledger-task-codex/references/performance-evidence-checklist.md`,
`.agents/skills/run-ledger-task-codex/references/performance-evidence-review-cases.md`,
`.cursor/skills/run-ledger-task-cursor/references/performance-evidence-checklist.md`,
`.cursor/skills/run-ledger-task-cursor/references/performance-evidence-review-cases.md`.
No `.py`, `.cu`, or `pins/` files. exit: 0.

```
uv run ruff format . && uv run ruff check .
```

Not run — no Python files touched (N/A per dossier).

#### Link and steering review

Both complete skills and both templates reread. Every relative link resolves:

- `references/task-dossier-template.md`, `references/performance-evidence-checklist.md`,
  `references/performance-evidence-review-cases.md` (both trees; checklist ↔ review-cases
  cross-links)
- Codex `scripts/account_usage.py`
- Cursor in-skill anchors `#pre-authored-dossiers`, `#keepreject-quality-gate`

Checklist linked from admission, planning, documentation, verification, and
completion in both skills. Documentation is draft/`unverified` before
verification; neither skill asks documentation for a future verifier verdict.

Old contradictory steering absent from skill bodies:

- `Prefer the current OPT-020-style…` — absent; replaced by coverage-first rule
  (“OPT-020-style CUDA-event categories cannot rank production graph execution
  until coverage of that graph is established”).
- `those numbers are current enough` — absent; Cursor pre-authored admission now
  requires checklist satisfaction (identity, coverage, no unresolved material
  contradiction).
- `Pass the measured verdict … from verification` into documentation — absent;
  explicit prohibitions at Cursor keep/reject gate, documentation stage, and
  checklist §8.
- Reject speedup conflated with shipping zero — absent; both skills and
  checklist §8 separate candidate measured delta from shipping delta.

#### Eight adversarial case reviews

Independent review of both skills plus shared checklist/review-cases (delivery
workflow not invoked). Each case: **pass** with enforcing policy passage.

| Case | Expected decision | Result | Policy citation |
|------|-------------------|--------|-----------------|
| 1 Graph capture (196.740 ms window; 188.497 ms “idle”) | Reject proven idle; internal activity unknown | **pass** | Checklist §2: “Do not subtract ordinary kernel/copy union from a window that contains graph envelopes and call the remainder proven idle; internal graph activity is unknown until node tracing covers it.” |
| 2 Quartz complete-request / llama decode-only ratio | Refuse comparison | **pass** | Checklist §1: “Refuse a Quartz complete-request versus llama decode-only comparison.” |
| 3 API synchronize concurrent with GPU work | Refuse idle count or GPU wall-time add | **pass** | Checklist §3: “CPU `cudaEventSynchronize` duration is wait duration, not removable GPU idle; do not count an API synchronize span that is concurrent with GPU work as idle or add it to GPU wall time.” |
| 4 Same-run leaf sum exceeds enclosing (nested/overlap summed) | Reconstruct disjoint union before ranking | **pass** | Checklist §3: “Union overlapping intervals… If a same-run leaf sum exceeds its enclosing duration because nesting or overlap was summed, reconstruct a disjoint union before ranking.” Checklist §4: “A component exceeding its enclosing duration on the same run is an error;” |
| 5 OPT-108-like 0.033 ms vs OPT-129-like 0.113 ms (different sittings) | Record contradiction; no production 4× without reconciliation | **pass** | Checklist §4: “differing sittings are a comparability problem until resolved… Do not claim a production multiple (for example 4x) from unreconciled component times taken in different sittings.” |
| 6 Missing llama GDN mapping | Excess `null`/`unknown`; never Quartz minus zero | **pass** | Checklist §2: “Missing coverage must not become zero excess. Unmapped llama families make excess `null`/`unknown`; never treat missing corresponding llama work as Quartz minus zero.” |
| 7 Target improves; guard CI contains 1 above non-regression floor | Apply opted-in OPT-135 policy; guard is non-regression | **pass** | Checklist §6: “If a target improves and a guard CI contains 1 but lies above its non-regression floor, apply the opted-in target/guard policy, not a guard-improvement test.” |
| 8 Required capture or candidate NLL not run | `incomplete`/`blocked`; no fabricated outcomes | **pass** | Checklist §7: “Required capture or candidate NLL not run is `incomplete`/`blocked`; do not fabricate `no_material_opportunity`, a quality pass, or a measured reject.” |

Skills reinforce cases 1, 2, 4, and 6 in the shared “Performance evidence
(prefill/decode)” section (coverage before ranking, identity-matched comparisons,
disjoint union, missing coverage not zero excess). Case 8 is additionally
enforced in Cursor keep/reject quality gate and Codex/Cursor verification
stages (missing hardware / skipped NLL → `incomplete`/`blocked`).

#### Scope and plan.md

Diff scope matches dossier Implementation: skill files, dossier run record, and
ledger row status only. No Python, CUDA, pins, or benchmarks. `plan.md`
unchanged; dossier `Plan impact: none` holds. Codex model routing (Sol/Terra/Luna)
and Cursor slugs unchanged.

#### Verdict

**PASS** — acceptance commands clean; reference files identical across trees; all
eight adversarial cases enforced with cited policy; links resolve; contradictory
steering removed; scope bounded to allowed files.

### Delivery (2026-09-14T13:38:22Z)

model: composer-2.5, telemetry_unavailable

OPT-134 marked `done`. Coupled IDs: none. No production selector or runtime
change; runner instruction updates only.

### Final outcome

- Status: `done`
- Acceptance evidence: both runner skills and synchronized
  [`performance-evidence-checklist.md`](../.agents/skills/run-ledger-task-codex/references/performance-evidence-checklist.md)
  / [`performance-evidence-review-cases.md`](../.agents/skills/run-ledger-task-codex/references/performance-evidence-review-cases.md)
  reference trees (identical across Codex and Cursor); verification record above
- Throughput delta: **N/A** (runner instructions only; no CUDA, NLL, or throughput
  measurements)
- First-pass acceptance: yes (2026-09-14T13:37:00Z)
- Remaining risk: OPT-136 supplies executable interval checks; until those exist,
  prompts require independent raw-artifact reconstruction and label unavailable
  checks as unavailable
