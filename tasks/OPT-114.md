# OPT-114 — Calibrate the fresh sitting and measure real decode launch overhead

Status: done. Dependencies: OPT-106. Coupled IDs: none.
Primary target: measurement validity and conditional decode-graph opportunity.

## Run record

### Planning (2026-09-12T21:40:00Z)

skipped — pre-authored dossier admitted (coordinator)

### Implementation (cursor-grok-4.6, 2026-09-12T21:59:32Z)

Implemented the diagnostic sitting: task/iteration contracts, host runner,
native CUDA test, fixture, host tests, Makefile `cuda-opt114-diagnostics`,
`tools/run_optimization_task.py` observation prefixes, and
`evidence/optimization/opt114-sitting-and-launch-overhead/REPORT.md`.
Did not commit. Production selector remains `ffn_only`. No new profiling
framework and no kernel arithmetic change.

**Files created**
- `pins/opt114_sitting_launch_contract.json`
- `pins/opt114_iteration_contract.json`
- `tools/opt114_sitting_launch.py`
- `cuda/opt114_sitting_launch_test.cu`
- `fixtures/opt114_sitting_launch.json`
- `tests/test_opt114_sitting_launch.py`
- `evidence/optimization/opt114-sitting-and-launch-overhead/REPORT.md`

**Files modified**
- `Makefile` (`cuda-opt114-diagnostics`, `qw38-cuda-opt114-sitting-launch-test`)
- `tools/run_optimization_task.py` (`QW38_OPT114_NATIVE_COUNTS=`, `QW38_OPT114_SITTING_LAUNCH_RESULT=`)
- `implementation_ledger.md` (status `in_progress`; coordinator/planning already set this)
- `tasks/OPT-114.md` (this run record)

**Host validation**
```
uv run ruff format tools/opt114_sitting_launch.py tests/test_opt114_sitting_launch.py
# 1 file reformatted, 1 file left unchanged
uv run ruff check tools/opt114_sitting_launch.py tests/test_opt114_sitting_launch.py
# All checks passed!
uv run pytest -q tests/test_opt114_sitting_launch.py
# 10 passed in 0.06s (re-run after report: 10 passed in 0.04s)
```

**GPU phases** (device NVIDIA GeForce RTX 5090, image `qw38-cuda:13.0.2`,
GGUF present, native binary compiled in docker). Sidecars in `build/opt114-run/`.
Session capacity is `max(prefix+tokens+32, 4096)` so A/A and launch-overhead
do not allocate a full 128K session (that OOMs with ~5 GiB already resident).

```
uv run python tools/opt114_sitting_launch.py --phase preflight --mode acceptance --run-dir build/opt114-run
# ok (host)

uv run python tools/opt114_sitting_launch.py --phase launch-overhead --mode acceptance --run-dir build/opt114-run
# ok (~26s)

uv run python tools/opt114_sitting_launch.py --phase memory-reconcile --mode acceptance --run-dir build/opt114-run
# fixture reconcile ok; live ./build/qw38-cuda-memory-fit-test OOM:
# internal: cannot allocate CUDA scheduler session state

uv run python tools/opt114_sitting_launch.py --phase graph-eligibility --mode acceptance --run-dir build/opt114-run
# trigger met; decode_segments8 capture failed; retain ffn_only

uv run python tools/opt114_sitting_launch.py --phase aa-control --mode acceptance --run-dir build/opt114-run
# ok (~291s); combined aa_verdict=repeatable

uv run python tools/opt114_sitting_launch.py --phase report --mode acceptance --run-dir build/opt114-run
# {"task": "OPT-114", "phase": "report", "ok": true}
```

**A/A (fresh same-binary Quartz; 3 warmups + 10 interleaved AB/BA; not OPT-098 arrays)**
| Workload | A tok/s | B tok/s | geo ratio | 95% CI | Verdict |
|---|---:|---:|---:|---|---|
| P4096 | 2981.0894 | 2980.2403 | 0.9997 | 0.9989 .. 1.0005 | repeatable |
| D128 | 53.4602 | 53.4634 | 1.0001 | 0.9994 .. 1.0007 | repeatable |
| D2048 | 48.3830 | 47.9853 | 0.9915 | 0.9588 .. 1.0253 | repeatable |

Combined `aa_verdict`: **repeatable**. Historical OPT-098 vs OPT-106 remains
calibration only (P4096 3046 vs 3036; D128 37.29 vs 53.50; D2048 35.49 vs 49.40).

**Launch overhead** (leaf CUDA-event gaps; `other_idle` legacy only)
- D128: removable **10.9928 ms/token**, kernel 8.1174 ms, wall 19.5559 ms, 342 leaf kernels, legacy `other_idle` **0.3208 ms**
- D2048: removable **10.9674 ms/token**, kernel 9.7486 ms, wall 21.1553 ms, 342 leaf kernels, legacy `other_idle` **0.3288 ms**
- Both ≫ 0.50 ms/token. Using `other_idle` as the budget would have falsely said no opportunity.

**Graph opportunity:** trigger **yes** (`unhidden_idle_at_both_prefixes`). Existing
`decode_segments8` vs eager `ffn_only` was attempted. `ffn_only` graph-bench D128
succeeded; `decode_segments8` failed with `internal: cannot capture CUDA scheduler FFN graphs`.
Verdict `graph_ab_retain_ffn_only`. `production_kept=true`; shipping stays `ffn_only`.
OPT-096 native also cannot run a 128K session here (OOM).

**Memory reconcile:** OPT-106 `explicit_bytes=29571258208` vs inventory
`29571252064` (Δ **6144**). `arithmetic=false` so `memory_fit.ok=false` while
`reserve_ok=true`. Do not raise the 1.5 GiB cap. Live 128K `memory-fit-test` OOM
on this sitting (`cannot allocate CUDA scheduler session state`).

**Blockers for verification**
- Live 128K allocation (`qw38-cuda-memory-fit-test`) OOM; inventory vs OPT-106 fixture reconcile still recorded.
- `decode_segments8` capture fails on this sitting; graph A/B cannot admit the existing path.
- D2048 A/A has wider pair ratios (including ~0.92 / ~1.08) but CI still includes equality and geo stays inside the 2% guard.

**Outcome for later tasks:** sitting is `repeatable`. Removable launch overhead is
material (~11 ms/token). Production graph selector is unchanged (`ffn_only`).

### Documentation (cursor-grok-4.6, 2026-09-12T22:02:00Z)

Reviewed `evidence/optimization/opt114-sitting-and-launch-overhead/REPORT.md`
against dossier acceptance. Report was incomplete on p50/p95 latency summary,
launch wall/leaf-kernel/sync/graph-param columns, graph capture-failure prose,
live 128K OOM, session-capacity note, and repository cross-links. Updated
report prose; added OPT-114 freeze paragraph to `docs/06-system-optimization.md`
(mirroring OPT-106) and audit row to `docs/65-documentation-audit.md`.
Verified `report_path` and memory/opt106 fixture references are consistent
across `pins/opt114_sitting_launch_contract.json`,
`pins/opt114_iteration_contract.json`, and `fixtures/opt114_sitting_launch.json`.
Did not commit. No production code changes.

**Files changed**
- `evidence/optimization/opt114-sitting-and-launch-overhead/REPORT.md`
- `docs/06-system-optimization.md`
- `docs/65-documentation-audit.md`
- `tasks/OPT-114.md` (this run record)

### Verification (independent, 2026-09-12T22:02:00Z)

**Scope review (diff vs dossier / plan.md non-goals)**
- Modified tracked: `Makefile` (diagnostic target only), `tools/run_optimization_task.py` (OPT-114 observation prefixes only), docs/ledger/task run records.
- New: contracts, fixture, host runner, host tests, native diagnostic `cuda/opt114_sitting_launch_test.cu`, evidence report.
- No production scheduler/selector/kernel-arithmetic changes; `cuda/execution_graph_path.cuh` still `ffn_only`; no new profiling framework; no benchmark rewrite; no reserve-cap increase.

**Commands and outcomes**

```
git diff --stat HEAD
# 6 tracked files (+142/-3); 7 untracked OPT-114 artifacts (diagnostic scope only)

uv run ruff format .
# 240 files left unchanged

uv run ruff check .
# FAIL (pre-existing, outside OPT-114 diff): F401 unused import in tools/opt088_batch_gate.py:49

uv run ruff check tools/opt114_sitting_launch.py tests/test_opt114_sitting_launch.py
# All checks passed!

uv run pytest -q tests/test_opt114_sitting_launch.py
# 10 passed in 0.03s

uv run python tools/opt114_sitting_launch.py --phase report --mode acceptance --run-dir build/opt114-run
# {"task": "OPT-114", "phase": "report", "ok": true}

make cuda-opt114-diagnostics
# FAIL on host: nvcc not on PATH (attempted rebuild of build/full_scheduler.trace.cuda.o)

docker run --rm --gpus all -v /home/user/qw38:/workspace -w /workspace qw38-cuda:13.0.2 make cuda-opt114-diagnostics
# exit 0 (rebuilt qw38-cuda-opt096-decode-graphs-test; opt114/probe/memory-fit deps present)
```

**Artifact trace (acceptance claims → evidence, not labels alone)**
| Claim | Fixture / report | Sidecar or native artifact |
|---|---|---|
| A/A `repeatable` | `aa_verdict=repeatable`; P4096 geo 0.9997, D128 1.0001, D2048 0.9915; all `equality_excluded=false` | `build/opt114-run/aa-{p4096,d128,d2048}.txt` → `QW38_OPT114_SITTING_LAUNCH_RESULT` with `same_binary=true`, 3 warmups + 10 pairs |
| Launch budget ≠ `other_idle` | D128 removable **10.9928** ms/token vs legacy other_idle **0.3208**; `legacy_other_idle_is_launch_budget=false` | `launch-d128.txt` / `launch-d128-records.json` (650 records, 342 leaf kernels, wall 19.5559 ms) |
| Graph trigger met, eager retained | `graph_eligibility.trigger_met=true`; threshold 0.5 ms; verdict `graph_ab_retain_ffn_only`; `production_kept=true` | `graph-eligibility.json` → `decode_segments8` capture error `cannot capture CUDA scheduler FFN graphs`; `graph-ab-ffn_only-d128.txt` present |
| Fresh control identity | RTX 5090, gguf sha256, llama rev, dirty source `7409d864…`, selectors frozen to OPT-106 | `preflight.json`, fixture `identity` block |
| Memory reconcile honest | explicit Δ **6144** B; `opt106_memory_fit_ok=false`, `reserve_ok=true`; live 128K OOM recorded | `memory-reconcile.json` → live blocker `cannot allocate CUDA scheduler session state` |
| No throughput claim | `claims_throughput=false`, `claims_performance_improvement=false` | contract + fixture + REPORT.md |

**Verdict: PASS**

Independent verification accepts OPT-114 delivery against ledger acceptance and dossier stop rules: fresh same-binary A/A is repeatable; removable launch overhead is material (~11 ms/token) and correctly separated from legacy `other_idle`; graph reopen trigger is met but `decode_segments8` capture fails so production stays `ffn_only` with an honest measured no-admit verdict; memory reconcile and live 128K OOM are recorded without raising the reserve cap. Host gates pass (format unchanged, focused ruff, 10/10 pytest, report phase ok). Repo-wide `ruff check .` fails on a pre-existing OPT-088 import unrelated to this diff; host `make cuda-opt114-diagnostics` requires docker/nvcc but the target builds successfully inside `qw38-cuda:13.0.2`. No semantic fixes applied; not committed.

### Delivery (2026-09-12T22:04:12Z)

Delivery authorized after independent verification pass. Marked OPT-114 `done`.
Coupled IDs: none. Production selector unchanged (`ffn_only`).

### Final outcome (2026-09-12T22:04:12Z)

`diagnostic_delivered`: fresh same-binary A/A sitting is **repeatable**
(P4096 ~2980 tok/s, D128 ~53.46, D2048 ~48.38). Removable launch overhead is
material (~**11 ms/token** at D128/D2048) and correctly separated from legacy
`other_idle` (~0.32 ms). Graph reopen trigger met but `decode_segments8` capture
failed (`cannot capture CUDA scheduler FFN graphs`); verdict
`graph_ab_retain_ffn_only`; production stays `ffn_only`. Memory reconcile
recorded OPT-106 Δ6144 B and live 128K OOM without raising the reserve cap.
`claims_throughput=false`. Sitting calibrated for OPT-107+ tasks.

**A/A baseline (fresh sitting, not OPT-098 arrays):** P4096 ~2980 tok/s; D128
~53.46 tok/s; D2048 ~48.38 tok/s. **tok/s delta vs OPT-098 P4096 baseline
(3046.23 tok/s): 0** (diagnostic; no throughput claim).

Evidence: [`fixtures/opt114_sitting_launch.json`](fixtures/opt114_sitting_launch.json);
[`evidence/optimization/opt114-sitting-and-launch-overhead/REPORT.md`](evidence/optimization/opt114-sitting-and-launch-overhead/REPORT.md).

## Outcome

Establish a fresh same-binary Quartz A/A control and determine whether decode
launch gaps are large enough to justify the existing eight-layer segment-graph
path. This is a diagnostic and measurement task; it adds no production
selector or graph path by itself. It replaces the use of historical OPT-098
samples as a causal control and prevents the `other_idle` residual from being
misread as GPU idle.

## Evidence and rationale

OPT-106 reports selected Quartz equal to the post-098 combination, but its
current D128 result (53.50 tok/s) differs substantially from the historical
OPT-098 control fixture (37.29 tok/s). `tools/opt106_batch_gate.py` loads those
historical arrays for the internal-improvement interval. That is valid history,
not a same-sitting A/A control for new tasks.

The existing decode attribution computes `other_idle` as wall time minus
category-level CUDA-event intervals. CPU submission gaps that occur between
kernels inside a category are already included in those intervals, so a small
`other_idle` value does not prove that launch overhead is negligible. The
existing OPT-096 segment capture scaffold can be reused once actual overhead
is measured.

## Implementation

1. Authenticate one build, model/GGUF, pinned llama revision, CUDA device,
   selectors, compiler flags, clocks/power state, and execution-graph path.
   Record this identity in the report and use it for every A/A and graph run.
2. Run three warmups plus ten interleaved AB/BA pairs of the unchanged Quartz
   control at P4096, D128, and D2048. Report raw latency, tok/s, p50, p95,
   geometric ratios, and confidence intervals. Do not use OPT-098 arrays for
   this interval or reinterpret them as a current control.
3. Capture a representative decode timeline with the least intrusive available
   method (existing scheduler timestamps, CUPTI, or Nsight Systems). Separate
   host launch/submit gaps, GPU idle gaps, kernel intervals, synchronization,
   and graph parameter-update time. The event-category `other_idle` field is
   reported only as a legacy diagnostic, never as the measured launch budget.
4. If removable launch/submit overhead is at least 0.50 ms/token at both D128
   and D2048, run the existing `decode_segments8` capture path against the
   unchanged eager control. Include capture/update cost in token timing and
   verify graph/eager output, state ownership, cancellation, and address
   validity. If either prefix is below the trigger, record `no_graph_opportunity`
   for production and do not add another graph variant.
5. Reconcile the 128K allocation inventory and the OPT-106 `memory_fit` result
   while the build identity is frozen. Explain any byte-level accounting
   discrepancy; do not increase a reserve cap without an allocation-backed
   reason.

## Verification and evidence

- Add a task/iteration contract, fixture, host validator, runner entry, and
  `evidence/optimization/opt114-sitting-and-launch-overhead/REPORT.md`.
- The report must contain an A/A repeatability verdict. A control whose paired
  confidence interval excludes equality or whose point ratio is outside the
  existing 2% measurement guard is `measurement_unstable`; subsequent
  performance tasks must not issue keep/reject claims until a stable sitting
  is obtained.
- For a graph candidate, run 3 warmups plus 10 paired complete-token rounds at
  both prefixes and five uninstrumented engine AB/BA pairs. Report CPU and GPU
  timelines, graph node/update identities, launch counts, p50/p95, and memory.
- Preserve the current numerical, state, and cancellation tests; no quality
  concession is available for a graph path.

## Acceptance and stop rule

- Deliver a reproducible fresh control identity and an honest A/A verdict. A
  historical-vs-fresh discrepancy is recorded as calibration evidence, not
  called an optimization.
- Admit a graph path only when the measured removable overhead reaches
  0.50 ms/token at both D128 and D2048, the paired complete-token saving is
  positive with a 95% interval, and graph/eager/state/memory gates pass.
- If the A/A control is unstable, stop the post-106 performance ladder pending
  a stable environment. If launch overhead is below the trigger or graph A/B
  loses, retain the current eager path and record a measured no-op.

## Non-goals

No new profiling framework, persistent scheduler, prompt graphs, graph sweep,
kernel arithmetic change, benchmark change, historical fixture rewrite, or
production selector change.
