# OPT-103 — Port the one-query vector attention specialization

Status: done. Dependencies: OPT-099. Coupled IDs: none. Primary target: D2048 attention core.

## Run record

### Planning (2026-09-12T16:10:00Z)

skipped — pre-authored dossier admitted (coordinator)
Read [the post-098 protocol](PERFORMANCE-RECOVERY-POST-098.md).

### Implementation (2026-09-12T17:08:30Z)

- Agent/model: implementation (Cursor Grok 4.6). Production pin stays
  `warp_query` / `kSelectedVec128NParts = 16`. `warp_query_gqa6` stays rejected.
  `claims_throughput: false`.
- Candidate `vec128_online`: 128 threads per query head and sequence partition,
  register Q, vectorized BF16 K/V, online max/sum, register V, CTA merge of 16
  groups. Partition screen `{4,8,16}` selected **n_parts=16** (largest D2048
  screen saving).
- Native parity passed: seq_abs ~1e-8 vs warp_query, FP64 samples inside 5e-3,
  parts 4/8/16, zero/cancellation, candidate KV boundary, alignment fallback,
  occupancy; production pin remains `warp_query`.
- Quality reused authenticated OPT-073 engine non-regression (`pass`).
- Complete rotating 16-layer attention acceptance (3+10), n_parts=16:
  D128 control **1.600 ms** vs candidate **1.704 ms**, mean_diff **−0.104 ms**,
  CI95 **[−0.138, −0.070]**, `positive=false`. D2048 control **9.263 ms** vs
  **8.873 ms**, mean_diff **+0.390 ms**, CI95 **[0.016, 0.764]**, `positive=true`.
- D128 five-pair: 577.00 vs 586.74 ms (candidate slower; ITL p95 18.05 vs
  18.29). D2048 five-pair: 630.26 vs 594.45 ms (candidate faster). Keep requires
  both prefixes.
- P4096: warp_query 3063.26 tok/s vs vec128 3059.70 tok/s (ratio 0.999). Not a
  keep. Versus OPT-098 P4096 **3046.23 tok/s** production is unchanged.
- Foreign GPU processes blocked `run_optimization_task`; admission invoked
  `tools/opt103_vector_attention.py` directly after `make cuda-opt103-diagnostics`.
- Verdict: **reject / retain warp_query**. tok/s delta vs OPT-098 P4096
  **3046.23 tok/s: 0**.

**Commands**

```
uv run ruff format tools/opt103_vector_attention.py tests/test_opt103_vector_attention.py
uv run ruff check tools/opt103_vector_attention.py tests/test_opt103_vector_attention.py
uv run pytest -q tests/test_opt103_vector_attention.py
```

**pass** — 9 passed.

```
docker run --rm --user "$(id -u):$(id -g)" -v /home/user/qw38:/workspace -w /workspace qw38-cuda:13.0.2 make cuda-opt103-diagnostics
export QW38_HOST_NATIVE=1
uv run python tools/opt103_vector_attention.py --phase eligibility --mode feedback --run-dir build/optimization-runs/opt103
uv run python tools/opt103_vector_attention.py --phase parity --mode feedback --run-dir build/optimization-runs/opt103
uv run python tools/opt103_vector_attention.py --phase quality --mode release --run-dir build/optimization-runs/opt103
uv run python tools/opt103_vector_attention.py --phase parts --mode feedback --run-dir build/optimization-runs/opt103
uv run python tools/opt103_vector_attention.py --phase attention128 --mode acceptance --run-dir build/optimization-runs/opt103
uv run python tools/opt103_vector_attention.py --phase attention2048 --mode acceptance --run-dir build/optimization-runs/opt103
uv run python tools/opt103_vector_attention.py --phase d128 --mode acceptance --run-dir build/optimization-runs/opt103
uv run python tools/opt103_vector_attention.py --phase d2048 --mode acceptance --run-dir build/optimization-runs/opt103
uv run python tools/opt103_vector_attention.py --phase prefill-guard --mode acceptance --run-dir build/optimization-runs/opt103
```

**pass** — exit 0 each after one parts capture-key repair; D128 CI entirely
negative; production pin unchanged; `REJECTION.md` written.

### Verification (2026-09-12T17:08:30Z)

Same sitting as implementation. No semantic kernel fixes after measurement.

```
uv run ruff check tools/opt103_vector_attention.py tests/test_opt103_vector_attention.py
All checks passed!

uv run pytest -q tests/test_opt103_vector_attention.py
.........                                                                [100%]
9 passed in 0.04s
```

Fixture inspection (`fixtures/opt103_vector_attention.json`):
- `evidence_complete=true`, `production_kept=false`, `survivor=warp_query`
- `shipping_decode_attention_vec128=warp_query`, `selected_n_parts=16`
- `claims_throughput=false`, `tok_s_delta_vs_opt098_p4096=0`
- kernel parity pass; `model_quality_pass=true` via OPT-073 cache
- D128 n=10: 1.600 vs 1.704 ms; mean_diff −0.104 ms; CI95 [−0.138, −0.070]
- D2048 n=10: 9.263 vs 8.873 ms; mean_diff +0.390 ms; CI95 [0.016, 0.764]
- D128 five-pair 577.00 vs 586.74 ms; D2048 630.26 vs 594.45 ms
- P4096: 3063.26 vs 3059.70 tok/s; production unchanged

Rejection evidence (`evidence/optimization/opt103-vector-attention/REJECTION.md`):
- production pin remains `kSelectedDecodeAttentionVec128Path[] = "warp_query"`
- `warp_query_gqa6` stays rejected
- **tok/s delta vs OPT-098 P4096 3046.23 tok/s: 0**

Verdict: **PASS** (measured reject).

### Final outcome (2026-09-12T17:08:30Z)

Reject. Retain `warp_query`. tok/s delta vs OPT-098 P4096 **3046.23: 0**.
Planning skipped (pre-authored dossier). Models: cursor-grok-4.6-high /
composer-2.5 requested for staged subagents were unavailable; this sitting used
Cursor Grok 4.6 with `telemetry_unavailable`. Retries: 1 (stale capture key
across D128/D2048 parts). First-pass native parity passed.

## Outcome

Add a Quartz-owned 128-thread, one-query online-softmax attention kernel based
on the pinned llama vector FlashAttention organization. Compare it with the
selected `warp_query` path and keep only a complete 16-layer D128/D2048 win.

## Rationale

At D2048 Quartz attributes about 4.4 ms/token to attention core. OPT-095's
`warp_query_gqa6` combined six query heads in one CTA and was slower. Pinned
llama selects `flash_attn_ext_vec<256,1>` for one BF16/F16 query at the measured
prefixes: 128 threads cooperatively traverse K/V, maintain online softmax and
accumulate V in registers. That mechanism does not require the rejected GQA6
CTA or a separate query-preparation launch.

## Implementation

1. Confirm the pinned llama dispatch for the exact head width 256, 24 query
   heads, 4 KV heads, BF16 KV, D128 and D2048 shapes in OPT-099 evidence. Port
   only the necessary algorithmic organization with file-level MIT provenance;
   do not import ggml tensor/runtime machinery.
2. Implement one candidate `vec128_online`: 128 threads per query head and
   sequence partition, query held in registers, vectorized BF16 K/V loads,
   stable online max/sum, register V accumulation, and a bounded merge only
   when more than one sequence partition is measured faster. Keep head width
   and GQA mapping compile-time fixed for the model.
3. Choose the partition count from `{4,8,16}` in a single production-shape
   screen at D128 and D2048; select one candidate before acceptance. Report
   blocks, K/V bytes, registers, spills, occupancy and merge cost. Do not add a
   second GQA-sharing design.
4. Preserve partial RoPE, q/k RMSNorm, causal masking, stable softmax, KV write
   visibility, output gate, BF16 conversion and transactional KV commit. The
   current `warp_query` path remains the reference.
5. Require independent FP64 attention samples, per-head output parity, full
   logits/quality, long-prefix boundary tests, graph/eager equivalence,
   checkpoint/restore and cancellation.
6. Apply the shared 3+10 complete-attention and five-pair D128/D2048 protocol;
   P4096 is a non-regression guard.

## Acceptance

- Preparation-inclusive complete 16-layer attention saves at least
  0.10 ms/token with a positive paired 95% interval.
- D128 and D2048 point throughput and p95 improve; full quality, state and
  P4096 guards pass.
- Dispatch is limited to admitted one-query shapes with explicit fallback.
  Rejection restores `warp_query` and leaves `warp_query_gqa6` rejected.
- Add contract/fixture, native tests, runner and
  `evidence/optimization/opt103-vector-attention/`.

## Non-goals

No approximate/quantized KV, GQA6 retry, prompt-attention rewrite, changed RoPE,
reduced partitions without measurement, or current llama HEAD as authority.

