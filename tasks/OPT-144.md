# OPT-144 — Evaluate one complete-boundary prefill attention optimization

Status: done. Dependencies: OPT-135, OPT-141, OPT-143. Coupled IDs: none.
Priority: P1. Plan impact: none; existing release and quality gates retained.

## Outcome

Evaluate one supported P4096 attn_core change using OPT-140's production replay. The reported +186.58 ms family excess exceeds the approximately 87.2 ms net whole-engine gap; offsetting families and residual mean it is not a promised end-to-end saving.

## Eligibility and candidate freeze

Read OPT-111's shipped prompt adapter and OPT-139–141 counters/replay. Use the stack retained by OPT-143 and refresh identity-sensitive evidence if production changed. Require positive matched prefill attention excess and a source/counter-supported mechanism. Missing evidence blocks; measured no opportunity may close without a kernel change.

Freeze one change in the existing prompt attention path, its exact source/launch boundary, affected shapes, falsifying check and estimated complete-family benefit before timing. Do not propose porting OPT-111 again, reuse decode NCU as prefill evidence, or assume tensor activity must be zero from aggregate replay text.

## Scope and acceptance

Limit changes to prompt attention and necessary scheduling/workspace integration. Charge conversions, preparation, causal masking, merges and output consumers. Preserve BF16 authoritative state, chunk semantics, final-token output policy and existing reserve/tolerances.

Target P4096 prefill; guard D128/D2048/D8192/D32768 decode_only and complete_request using OPT-135 unchanged. Screen the full matched family on rotating layers with one warmup and three pairs. Survivors run actual candidate OPT-058 quality, causal/chunk/tail correctness, prompt-to-decode handoff, graph/eager and checkpoint checks, and populated 128K memory/reserve checks before three warmups and ten paired AB/BA acceptance rounds.

Deliver evidence/optimization/opt144-prefill-attention/REPORT.md, concrete frozen candidate contract and reproducible keep or measured no-keep evidence. Preserve parent on rejection/inconclusive outcome; missing gates are blocked. Show prompt and whole-request effects separately. Do not claim complete gap closure until OPT-142 reconciliation is valid.

## Execution requirements

Read [TASK-TESTING-STRATEGY.md](TASK-TESTING-STRATEGY.md) and the dependency dossiers before implementation. Reuse existing runners, parsers and gates; add only necessary task registration and focused checks. Freeze an enumerated GPU matrix and per-child/aggregate deadlines before execution, use the shared GPU lock and reuse builds. Do not rerun until significant or weaken acceptance to fit a timeout. Missing hardware evidence is incomplete.

Record exact runnable commands, raw evidence paths, identity and independent verification in this dossier at execution.

## Run record

### Planning (2026-09-15T00:45:00Z)
skipped — pre-authored dossier admitted

### Implementation (cursor-grok-4.6-high, 2026-09-15T01:12:27Z)

KEEP/REJECT: **no_opportunity** (measured). Production pins not flipped. Did not commit.
Parent retained: `post124_plus_opt127_decode_segments8_plus_opt137_mma` (`opt111_base` prompt attention, `kSelectedOpt137DenseMma = true`, MMA threshold 8192). OPT-143 short-decode keep was not applied.
Shipping prompt attention remains `opt111_base`. Shipping delta **0**. Candidate measured delta N/A. Quality N/A (no arithmetic change; OPT-058 not invoked; `candidate_nll_not_measured=false`).
`claims_throughput=false`. Prompt effect N/A. Whole-request effect N/A. OPT-142 complete gap closure not claimed.

Read OPT-140 prompt-attention replay, OPT-141 matched llama counters, OPT-138 family excess, OPT-111 shipping adapter (`opt111_base` / `fattn_mma_pipeline_opt111_base`), OPT-135 `target_guard_v2`, OPT-143 parent (no short-decode keep). Freeze ran against those artifacts; it did not invent a kernel.

**Freeze (measured, not pre-decided)**
- P4096 `attn_core` complete-family excess **+186.583 ms** (n=3, one-sided low 186.106 ms). This exceeds the approximately 87.2 ms net whole-engine gap and is not a promised end-to-end saving.
- Production identity: prompt-attention replay `opt111_base` / `fattn_mma_pipeline_opt111_base`. Decode-attention substitution rejected. Decode NCU is not prefill evidence.
- OPT-140 typed slots admitted: dram_read 252.12 Mbyte, dram_throughput 1.22%, sm 22.4%, occupancy 8.33%, tensor_activity **810024960 cycles (nonzero)**, L2 216065551 sector. `supported_mechanism=null`.
- OPT-141 P4096 attn_core **not comparable** (`fused_boundary_mismatch`, Quartz 16×2 vs llama 8×8). Leaf counters incomparable; tile shape is not a freeze. Llama mechanism null.
- Admission: `throughput_alone_cannot_establish_mechanism`. No named source/SASS observations.
- Forbidden: did not re-port OPT-111; did not reuse decode NCU; did not assume tensor activity is zero; did not lower OPT-137 MMA threshold; did not revive OPT-130.
- Candidate: **null**. Verdict: **no_opportunity**. No CUDA production change.

Quality was wired in `pins/opt144_iteration_contract.json` (`require_candidate_nll=true`, `--phase quality` → OPT-058 `--quality --quality-config --q4-decode`) so a frozen arithmetic candidate would run NLL. Because freeze produced no candidate, quality short-circuited with `nll_required=false` rather than stubbing `candidate_nll_not_measured`.

Frozen GPU matrix: RTX 5090, image `qw38-cuda:13.0.2`, GPU lock `build/optimization-runs/qw38-gpu.lock`, child 300s, long 1200s, aggregate 7200s ceiling. `make cuda-opt144-diagnostics` rc 0 under lock. No new native attention binary (no candidate).

**Files created**
- `tools/opt144_prefill_attention.py`
- `tests/test_opt144_prefill_attention.py`
- `pins/opt144_prefill_attention_contract.json`
- `pins/opt144_iteration_contract.json`
- `pins/opt144_prefill_attention_provenance.json`
- `fixtures/opt144_prefill_attention.json`
- `evidence/optimization/opt144-prefill-attention/REPORT.md`
- `evidence/optimization/opt144-prefill-attention/freeze.json`
- `evidence/optimization/opt144-prefill-attention/answers.json`

**Files modified**
- `Makefile` (`cuda-opt144-diagnostics` + PHONY; quality/checkpoint/memory-fit/component-replay)
- `tools/run_optimization_task.py` (`QW38_OPT144_*` result prefixes)

```
uv run ruff format tools/opt144_prefill_attention.py tests/test_opt144_prefill_attention.py tools/run_optimization_task.py
uv run ruff check tools/opt144_prefill_attention.py tests/test_opt144_prefill_attention.py tools/run_optimization_task.py
```
outcome: formatted; All checks passed.

```
uv run pytest tests/test_opt144_prefill_attention.py tests/test_opt140_prefill_attention_replay.py tests/test_opt141_matched_llama_counters.py tests/test_opt143_short_attention.py -q
```
outcome: 38 passed. Fixture present re-run: 11 passed (`test_opt144_prefill_attention.py`).

```
uv run python tools/opt144_prefill_attention.py --phase preflight --mode acceptance --run-dir build/optimization-runs/opt144
```
outcome: `{"phase": "preflight", "ok": true, "verdict": "no_opportunity"}` at 2026-09-15T01:12:05Z. GPU available, make rc 0, production pins parent, freeze as above.

```
uv run python tools/opt144_prefill_attention.py --phase correctness --mode acceptance --run-dir build/optimization-runs/opt144
uv run python tools/opt144_prefill_attention.py --phase screen --mode acceptance --run-dir build/optimization-runs/opt144
uv run python tools/opt144_prefill_attention.py --phase quality --mode acceptance --run-dir build/optimization-runs/opt144
uv run python tools/opt144_prefill_attention.py --phase state-memory --mode acceptance --run-dir build/optimization-runs/opt144
uv run python tools/opt144_prefill_attention.py --phase performance --mode acceptance --run-dir build/optimization-runs/opt144
uv run python tools/opt144_prefill_attention.py --phase mechanism --mode acceptance --run-dir build/optimization-runs/opt144
uv run python tools/opt144_prefill_attention.py --phase report --mode acceptance --run-dir build/optimization-runs/opt144
```
outcome: all `ok: true`. correctness/screen/quality/state-memory/performance skipped with `no_candidate_no_arithmetic_change`. mechanism recorded null mechanism. report `verdict=no_opportunity`.

Raw gates: `build/optimization-runs/opt144/*.json` copied to `evidence/optimization/opt144-prefill-attention/raw/`. Report: `evidence/optimization/opt144-prefill-attention/REPORT.md`.

GPU after: RTX 5090 idle (34 MiB / 32607 MiB, 0%). No blockers. Telemetry: `telemetry_unavailable`.

### Documentation (composer-2.5, 2026-09-15T01:13:26Z)

Drafted [`evidence/optimization/opt144-prefill-attention/REPORT.md`](../evidence/optimization/opt144-prefill-attention/REPORT.md) from implementation artifacts: freeze verdict `no_opportunity`, P4096 `attn_core` +186.58 ms family excess (not a promised end-to-end saving versus ~87.2 ms net gap), production `opt111_base` / `fattn_mma_pipeline_opt111_base`, OPT-141 fused-boundary mismatch (16×2 vs 8×8), null mechanism, parent retained. Quality section documents `nll_required=false` short-circuit (`no_candidate_no_arithmetic_change`); OPT-058 wiring present for arithmetic candidates. Performance evidence checklist items 1–6 and 8 populated; item 7 marked pending verifier. Shipping delta 0; candidate measured delta N/A. No production pin flip.

### Verification (2026-09-15T01:13:26Z)

**Verdict: PASS**

Independent verification against dossier scope, ledger acceptance (`One production-boundary P4096 attention candidate yields verified keep or measured no-keep with quality and decode guards`), and keep/reject quality gate. No semantic fixes applied.

#### Gates

```
uv run pytest tests/test_opt144_prefill_attention.py tests/test_opt140_prefill_attention_replay.py tests/test_opt141_matched_llama_counters.py tests/test_opt143_short_attention.py -q
```

outcome: **38 passed** in 0.11s.

```
uv run ruff check tools/opt144_prefill_attention.py tests/test_opt144_prefill_attention.py tools/run_optimization_task.py
```

outcome: All checks passed.

#### Acceptance evidence

| Criterion | Evidence | Result |
|-----------|----------|--------|
| Measured no_opportunity with evidence | `freeze.json` verdict `no_opportunity`; reasons include `throughput_alone_cannot_establish_mechanism`, `opt141_fused_boundary_mismatch_16x2_vs_8x8`, `decode_attention_substitution_rejected`, `tensor_activity_nonzero_not_assumed_zero` | PASS |
| Positive family excess authenticated | OPT-138 P4096 +186.58 ms (n=3, one-sided low 186.11 ms); exceeds ~87.2 ms net whole-engine gap | PASS |
| Production identity at P4096 | `opt111_base` / `fattn_mma_pipeline_opt111_base`; decode-attention substitution rejected; decode NCU not prefill evidence | PASS |
| No OPT-111 re-port / OPT-130 revival / OPT-137 threshold change | freeze `opt111_not_re_ported=true`, `opt130_not_revived=true`, `opt137_mma_threshold=8192` | PASS |
| Quality gate when NLL not required | `raw/quality.json` `nll_required=false`, `candidate_nll_not_measured=false`, `skip_reason=no_candidate_no_arithmetic_change`; iteration contract wires OPT-058 for arithmetic candidates | PASS |
| Parent retained on no-keep | `production_kept=false`; shipping prompt attention `opt111_base` unchanged | PASS |
| Shipping delta 0 | report `shipping_delta=0`; `candidate_measured_delta=null` | PASS |
| Required artifacts present | REPORT.md, freeze.json, answers.json, raw gates, fixture, pins, tool, tests | PASS |
| No production kernel change | candidate null; correctness/screen/performance skipped; mechanism null | PASS |

Verdict `no_opportunity`. Parent retained. Quality N/A (no arithmetic change).
First-pass acceptance: yes.

### Final outcome

- Status: `done`
- Verdict: `no_opportunity` (measured; parent retained)
- Acceptance evidence:
  [`fixtures/opt144_prefill_attention.json`](../fixtures/opt144_prefill_attention.json);
  [`evidence/optimization/opt144-prefill-attention/REPORT.md`](../evidence/optimization/opt144-prefill-attention/REPORT.md);
  verification record above (2026-09-15T01:13:26Z)
- Candidate measured delta: **N/A**; shipping delta: **0**; quality result:
  **nll_not_required_no_arithmetic_change**
- P4096 `attn_core` complete-family excess +186.58 ms; production path
  `fattn_mma_pipeline_opt111_base` (`opt111_base`); no source-grounded mechanism;
  OPT-141 fused-boundary mismatch (Quartz 16×2 vs llama 8×8); tensor activity
  measured nonzero; family excess exceeds net whole-engine gap — not a promised
  end-to-end saving
- OPT-137 MMA retained at >=8192; OPT-130 occupancy path not reopened; OPT-111
  not re-ported; `claims_throughput=false`; no production kernel or selector change
- First-pass acceptance: yes (2026-09-15T01:13:26Z)

