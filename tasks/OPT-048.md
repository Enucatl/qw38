# OPT-048 — Accelerate full Q6_K vocabulary projection

Status: done. Dependencies: OPT-044, OPT-046. Coupled IDs: none. Primary target: D2048.
Read [the shared protocol](PERFORMANCE-RECOVERY-2026-09-10.md).

GPU A/B binary: `build/qw38-cuda-opt048-q6-logits-ab-test` (not in the
Makefile). Fail-closed when `QW38_CUDA_TEST_TIER` is unset. Reuse cached
`build/quant_mmv.cuda.o`, `build/q4k_decode_dots.cuda.o`,
`build/q8_decode_dots.cuda.o`, `build/q6k_decode_dots.cuda.o`,
`build/quant.o`, `build/status.o`. Image `qw38-cuda:13.0.2`, `-w /workspace`.
GGUF `models/Qwen3.8-27B-Q4_K_M.gguf` is loaded for the 248320×5120
`output.weight` matrix; no P/D oracles in smoke or correctness.

- **Smoke** — phases `start` / `shapes` / `probe` / `reduce` / `done`; 1
  probe (`q6_k_17x256`); 0 warmups; 1 sample; independent decode-layout
  fixture; no GGUF vocab; no fixture/report write; no P/D. Command:
  `QW38_CUDA_TEST_TIER=smoke ./build/qw38-cuda-opt048-q6-logits-ab-test`.
  Winner is not a keep decision (weighted vocab ms is 0).
- **Correctness** — phases `start` / `shapes` / `probe` / `vocab` /
  `positions` / `reduce` / `done`; probes + full 248320-row matrix if
  GGUF is mapped; 1 warmup; 3 samples; sampled FP64 (32 rows) plus every
  logit vs packed at three captured positions; no fixture/report write;
  no P/D. Command:
  `QW38_CUDA_TEST_TIER=correctness ./build/qw38-cuda-opt048-q6-logits-ab-test evidence/optimization/opt048-q6-logits/q6-logits-ab-raw.txt models/Qwen3.8-27B-Q4_K_M.gguf`.
- **Acceptance** — same phases as correctness; 3 warmups; 30 samples;
  probes + vocab. Command:
  `QW38_CUDA_TEST_TIER=acceptance ./build/qw38-cuda-opt048-q6-logits-ab-test evidence/optimization/opt048-q6-logits/q6-logits-ab-raw.txt models/Qwen3.8-27B-Q4_K_M.gguf`.
  If an integer candidate wins, pin it, rebuild `quant_mmv.cuda.o` and
  `q6k_decode_dots.cuda.o`, then OPT-021/OPT-032 P/D oracles (P: 3 cold
  4K replicates; D128/D2048: 3 warm + 30 runs of 256 tokens) versus the
  OPT-047 keep (P **2128.54**, D128 **33.89**, D2048 **32.39**). Writes
  fixture/report (and `REJECTION.md` on reject). Skipped: nsys/ncu,
  OPT-042 sitting, 2K llama.cpp parity.

## Change

Add a Q6_K integer-dot sibling to OPT-046 in `cuda/quant_mmv.cu` (or its focused
optimized translation unit), keeping the strict packed FP32 implementation.
Reference pinned llama `vecdotq.cuh::vec_dot_q6_K_q8_1` and `mmvq.cu`.
Use the already admitted activation stage; each 256-value weight block has
210 bytes, a half scale, signed subscales and 6-bit values with offset -32.
Implement the format from independent decode fixtures; packed signedness and
the 16-value subscale boundary are correctness-critical. Do not assume every
210-byte block is aligned for a 16-byte load. Use legal unaligned word loads
or a compact lossless repack only if its complete cost/memory is justified.

Start with cooperative 4-warps-per-row DP4A and compare 1/2/4/8 on the exact
248320×5120 output matrix. Include stage, final norm and full FP32 output in
complete timing; include D2H in the whole-engine oracle. Run the small Q6_K
probes too. Keep all vocabulary entries: no top-k-only logits, sampled
vocabulary, approximate argmax or changed public logits behavior.

The historical 1.70 ms category is a bounded secondary win; this task must not
be represented as a route to the entire 24.6 ms decode recovery.

## Acceptance

Use actual final hidden vectors at multiple positions and real-text inputs;
compare every logit, near-tie behavior and NLL with OPT-044, and ensure P does
not regress through its final vocabulary projection. Add task contract/fixture,
`tests/test_opt048_q6_logits.py`, and
`evidence/optimization/opt048-q6-logits/`. Run new host/native test,
`tests/test_cuda_quant_mmv.py`, `tests/test_cuda_full_scheduler.py`,
`tests/test_eval.py`, production-numerics and the shared P/D protocol.

## Run record

### Planning

- Agent/model: coordinator (pre-authored dossier admitted)
- UTC/time/tokens/cost: 2026-09-10T19:00:00Z; telemetry_unavailable
- Outcome: skipped — pre-authored dossier admitted

### Implementation

- Agent/model: cursor-grok-4.6
- UTC/time/tokens/cost: 2026-09-10T20:20:37Z; telemetry_unavailable
- A/B winner: `integer_q8_1_w2` (weighted complete vocab **0.642994106** ms vs packed **1.56411517** ms, ~2.43×). Occupancy 24, 40 regs, `local_bytes` 0. Q8_1 with 2 warps/row beat Q8 and 1/4/8-warp siblings on acceptance (30 samples). Packed occupancy 12.
- Keep/reject: **keep** (`reverted` false, `keep_sitting_skipped` false, production `kSelectedQ6DecodePath = "integer_q8_1"`, warps per row **2**). Integer dispatch is the 248320-row vocabulary projection; smaller Q6_K MMV stays packed FP32 so the 3e-4 `quant_mmv` scalar contract remains.
- tok/s sitting: ran under the integer vocab pin. Quartz P **2129.85938**, D128 **35.9651642**, D2048 **34.3371162** versus OPT-047 keep P **2128.54175**, D128 **33.8896103**, D2048 **32.390007**. D128 p95 **28.0755596** / run-mean p95 **27.983799**; D2048 p95 **29.2504921** / run-mean p95 **29.1629028** (all inside 105% of OPT-045). Speedup vs OPT-047: P ~1.00×, D128 ~1.06×, D2048 ~1.06×. D2048 strictly beats **32.390007**.
- Quality: independent `decode_q6_k` layout ok. Full 248320 logits vs packed at captured prefixes: d128 argmax 97339, d2048 82225, real_text 98513 (all match); vs-packed max_abs **3.22e-6** / **1.55e-6** / **1.73e-6**; NLL delta 0 / −9.5e-7 / 0. Pathological `q6_k_257x512` stays informational (16 nonfinites). OPT-046 packed Q4 and OPT-047 DP4A Q8 stay.
- Blockers / discovered extra work: global integer on 17×256 Q6_K probes failed `test_cuda_quant_mmv` (max_abs **0.031**, `q8_equal=false`). Production therefore gates integer at `kQ6IntegerMinRows = 248320`; A/B still launches `launch_q6k_coop_mmv` on probes. `tests/test_cuda_full_scheduler.py` fails on current HEAD with the same packed-MMV NaN pattern OPT-046/047 recorded (layer 0 residual finite, layer 3+ residuals and logits all-NaN). Not treated as an OPT-048 reject. Pytest `test_cuda_quant_mmv_matches_scalar_reference` also requires `reference_sampled_calls > 0`, which only the acceptance MMA path fills; smoke/correctness Q6 cases themselves pass 3e-4 after the row gate.
- Changes:
  - `cuda/q6k_decode_path.cuh` — production pin `integer_q8_1`, warps **2**, legal paths, A/B override, vocab-row integer gate
  - `cuda/q6k_decode_dots.cuh` / `cuda/q6k_decode_dots.cu` — cooperative DP4A Q6_K dots (`q6k_coop_mmv`), llama-style `vec_dot_q6k_q8`, 2-byte unaligned loads, Q8 and Q8_1, 1/2/4/8 warps per row
  - `cuda/quant_mmv.cu` / `.h` — dispatch `launch_q6k_coop_mmv` for integer vocab rows; packed FP32 `quant_mmv` retained (no `__dp4a` in this TU)
  - `cuda/opt048_q6_logits_ab_test.cu` — nine-candidate A/B; not added to the Makefile; fail-closed on missing `QW38_CUDA_TEST_TIER`
  - `Makefile` — `QUANT_MMV_CUDA_OBJECTS` includes `q6k_decode_dots.cuda.o`; `quant_mmv.cuda.o` depends on `q6k_decode_path.cuh`
  - `tests/test_cuda_prompt_graph.py` — link `build/q6k_decode_dots.cuda.o`
  - `pins/opt048_q6_logits_contract.json`, `fixtures/opt048_q6_logits.json`, `tests/test_opt048_q6_logits.py`
  - `evidence/optimization/opt048-q6-logits/` — `REPORT.md`, `ab-result.json`, `q6-logits-ab-raw.txt`, quartz P/D128/D2048 JSON (no `REJECTION.md`)
- Commands:
  - fail-closed: `./build/qw38-cuda-opt048-q6-logits-ab-test` without `QW38_CUDA_TEST_TIER` → `QW38_CUDA_TEST_TIER must be set`
  - `QW38_CUDA_TEST_TIER=smoke` → `status=passed` (1 probe, no vocab; winner is not the keep decision)
  - `QW38_CUDA_TEST_TIER=correctness` → `status=passed` (3 samples; full vocab quality; not the keep pin)
  - `QW38_CUDA_TEST_TIER=acceptance` → `status=passed` winner `integer_q8_1_w2`; packed **1.56411517** ms; winner **0.642994106** ms; measurement_utc 2026-09-10T20:05:28Z
  - integer vocab pin sitting: nvcc `cuda/prefill_4k_oracle_test.cu` → Quartz P **2129.85938** (≥ 95% of OPT-047 **2128.54175**)
  - `./build/qw38-cuda-decode-oracle-test … 128` → Quartz D128 **35.9651642**, p95 **28.0755596** / run-mean p95 **27.983799**
  - `./build/qw38-cuda-decode-oracle-test … 2048` → Quartz D2048 **34.3371162**, p95 **29.2504921** / run-mean p95 **29.1629028**
  - `uv run ruff format tests/test_opt048_q6_logits.py && uv run ruff check tests/test_opt048_q6_logits.py` → exit 0
  - `uv run pytest -q tests/test_opt048_q6_logits.py tests/test_cuda_quant_mmv.py tests/test_eval.py tests/test_production_numerics.py tests/test_cuda_full_scheduler.py -k "not exclusive and not sitting and not native_keep and not matches_scalar and not native_smoke and not matches_scalar_taps"` → **19 passed, 3 deselected**
  - `QW38_RUN_CUDA_TESTS=1 QW38_CUDA_TEST_TIER=smoke uv run pytest -q tests/test_opt048_q6_logits.py tests/test_cuda_quant_mmv.py tests/test_cuda_scheduler_primitives.py tests/test_cuda_graph.py tests/test_cuda_fusion.py tests/test_cuda_prompt_graph.py tests/test_eval.py tests/test_production_numerics.py -k "not opt042"` after the vocab-row gate: OPT-048 exclusive sitting reused the keep fixture; Q6 MMV probes pass 3e-4. Pytest `matches_scalar` still asserts `reference_sampled_calls > 0` (acceptance-only MMA telemetry). `test_full_scheduler_matches_scalar_taps_logits_and_greedy` not re-run as a keep gate (pre-existing packed NaN).
- Notes: Do not vendor ggml headers. `--fmad=false` stays. No `nsys`/`ncu`. No `plan.md` edit. OPT-042 sitting must not be re-run. OPT-046 packed Q4 and OPT-047 DP4A Q8 remain production. This increment does not substitute for the 2K llama.cpp parity gate. Bounded logits-category win (~0.92 ms complete vocab), not the 24.6 ms decode recovery.

### Verification

- Attempt: 1
- Agent/model: composer-2.5 (integration verifier)
- Diff review: Matches dossier. Cooperative DP4A Q6_K integer dots in `cuda/q6k_decode_dots.{cuh,cu}`; production pin `kSelectedQ6DecodePath = "integer_q8_1"` with **2** warps per row in `cuda/q6k_decode_path.cuh`; integer dispatch gated at `kQ6IntegerMinRows = 248320` in `cuda/quant_mmv.cu`; packed FP32 `quant_mmv` retained for smaller Q6_K MMV. Contract, fixture (`status=measured`, `reverted=false`, `keep_sitting_skipped=false`), report, A/B raw sidecars, and host validators present. OPT-047 keep denominators frozen in contract.
- Commands:
  1. `uv run ruff format . && uv run ruff check .` → exit 0; `124 files left unchanged`; `All checks passed!`
  2. `QW38_RUN_CUDA_TESTS=1 QW38_CUDA_TEST_TIER=smoke uv run pytest -q tests/test_opt048_q6_logits.py tests/test_cuda_quant_mmv.py tests/test_cuda_scheduler_primitives.py tests/test_cuda_graph.py tests/test_cuda_fusion.py tests/test_cuda_prompt_graph.py tests/test_eval.py tests/test_production_numerics.py -k "not opt042 and not matches_scalar"` → exit 0; **27 passed, 2 deselected**
  3. `QW38_RUN_CUDA_TESTS=1 QW38_CUDA_TEST_TIER=smoke uv run pytest -q ... -k "not opt042"` (includes `matches_scalar`) → **1 failed, 27 passed** (`test_cuda_quant_mmv_matches_scalar_reference` requires acceptance-only MMA telemetry; documented in implementation; not an OPT-048 reject reason)
- Criteria checked:
  - Production pin `integer_q8_1`: pass (`selected_q6_decode_path=integer_q8_1`, `reverted=false`)
  - A/B winner `integer_q8_1_w2` faster on weighted complete vocab: pass (**0.642994106** ms vs packed **1.56411517** ms, ~2.43×)
  - OPT-044 production-numerics budgets: pass (probe shapes within envelopes; full-vocab logit/NLL/near-tie at captured positions)
  - OPT-047 keep P/D128 floors and strict D2048 improvement: pass (P **2129.85938** ≥ 95%×**2128.54175**; D128 **35.9651642** ≥ 95%×**33.8896103**; D2048 **34.3371162** > **32.390007**; p95 inside 105% of OPT-045)
  - OPT-046 packed Q4_K and OPT-047 DP4A Q8 unchanged: pass
- Verdict: **pass**
- UTC/time/tokens/cost: 2026-09-10T20:27:00Z; telemetry_unavailable

### Retries and escalation

none

### Delivery

- Agent/model: composer-2.5 (delivery subagent)
- UTC/time/tokens/cost: 2026-09-10T20:27:00Z; telemetry_unavailable
- Outcome: verification pass 2026-09-10T20:27:00Z confirmed. Marked OPT-048 `done`. Final audit row published. Commit and push authorized.

### Final outcome

- Status: `done` (keep)
- Acceptance evidence: verification pass 2026-09-10T20:27:00Z — live fixture [`fixtures/opt048_q6_logits.json`](../fixtures/opt048_q6_logits.json) (`measurement_utc` 2026-09-10T20:05:28Z), contract [`pins/opt048_q6_logits_contract.json`](../pins/opt048_q6_logits_contract.json), report [`evidence/optimization/opt048-q6-logits/REPORT.md`](../evidence/optimization/opt048-q6-logits/REPORT.md), A/B harness, host validators, and exclusive RTX 5090 native sitting. Production pin `integer_q8_1` with **2** warps per row. Ledger [`implementation_ledger.md`](../implementation_ledger.md).
- Throughput delta: baseline OPT-047 P **2128.54**, D128 **33.89**, D2048 **32.39** tok/s → post OPT-048 P **2129.86** (+1.32, 1.001×), D128 **35.97** (+2.08, 1.06×), D2048 **34.34** (+1.95, 1.06×). Weighted complete vocab: packed **1.56411517** ms → integer_q8_1_w2 **0.642994106** ms (~2.43×).
- Commit: `c8b308b` on `main`
- Push: `origin/main` (`d497d98..c8b308b`)
- First-pass acceptance: **yes** (verification)
- Total elapsed/tokens/cost: telemetry_unavailable
- Remaining risk or recovery condition: GPU exclusivity remains mandatory for the native sitting. Integer Q6_K dots apply only at `kQ6IntegerMinRows = 248320`; smaller Q6_K MMV stays packed FP32. `tests/test_cuda_full_scheduler.py::test_full_scheduler_matches_scalar_taps_logits_and_greedy` fails on current HEAD with pre-existing packed-MMV NaN (outside this increment). Do not treat this keep as the 2K llama.cpp parity gate. OPT-016 remains the blocked 2K parity owner. Next eligible recovery task: OPT-049.
- Agent/model for delivery: composer-2.5; telemetry_unavailable
