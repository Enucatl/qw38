# OPT-086 — Re-evaluate installed Q8 and MMQ paths

Status: done. Dependencies: OPT-064, OPT-066, OPT-070, OPT-082, OPT-084. Coupled IDs: none.

## Run record

### Planning (2026-09-11T22:02:00Z)

skipped — pre-authored dossier admitted (coordinator)
Priority: P0. Target: independent formal keep/revert for Q8 and prompt MMQ.
Read [the post-080 protocol](PERFORMANCE-RECOVERY-POST-080.md).
Quality risk: Q8 row grouping can change FP32 association; MMQ X-prefetch
must preserve stage lifetimes.
Instrumentation: scoped production overrides; separate graph capture per side.

### Implementation (2026-09-11T22:20:00Z)

No commit. Agent/model: cursor-grok-4.6 (`telemetry_unavailable`).
`opt074_coverage_unadmitted` is not a keep blocker. Independent family
verdicts. Q8 **revert** `r2_w2` → `r1_w4`. MMQ **keep** `fma_async_x`.
claims_throughput: q8=false, mmq=true. Official P/D tok/s delta versus the
then-current shipping baseline is **0** (no oracle rerun; MMQ was already
shipping; Q8 revert is component-only).

Created: `tools/opt086_q8_mmq_reevaluation.py`,
`pins/opt086_iteration_contract.json`,
`pins/opt086_q8_mmq_reevaluation_contract.json`,
`tests/test_opt086_q8_mmq_reevaluation.py`,
`fixtures/opt086_q8_mmq_reevaluation.json`,
`evidence/optimization/opt086-q8-mmq-reevaluation/REPORT.md`.

Changed: `Makefile` (`cuda-opt086-diagnostics` → OPT-082 parity test,
component replay, engine probe, OPT-058 quality diagnostic),
`tools/run_optimization_task.py` (`QW38_OPT086_*` observation prefixes),
`cuda/q8_decode_path.cuh` (production Q8 layout pin `r1_w4`: rows 1,
layout warps 4). MMQ pin `kSelectedMmqAsyncX = true` unchanged.

Historical OPT-070 report unmodified. Q8 capture identity shared with
OPT-070/071
(`5fdf3368e3a1898547bf5ecbe61333b4eaecdc9e4b70d97cdffce2edf7d20920`).
MMQ prompt-FFN capture
`b246f80b0eb6da052b81d779b4e4d92ef3f08311eb33ec05c2a9d4fa56eae611`.

Independent verdicts (this sitting):

| Family | Ident | kernel_parity_pass | model_quality_pass | performance_pass | production_kept |
|---|---|---|---|---|---|
| Q8 | r1_w4 | true | true | true | true (revert) |
| Q8 | r2_w2 | true | true | false | false |
| MMQ | fma_async | true | true | false | false |
| MMQ | fma_async_x | true | true | true | true (keep) |

Kernel parity: OPT-082 family catalogs this sitting (Q8 20 cases, MMQ 38
cases) plus typed `sum_q` vs `sum_x` reject. Documented Q8 M17 K2048
association misses do not fail admission.

Quality: identity-cached OPT-084 `--quality` freeze. Shipping and
replacement-control freeze rules both pass. OPT-073 engine non-regression
remains pass.

Performance (acceptance n=10, this sitting): Q8 complete rotating mixer
`r1_w4` 7.450 ms vs `r2_w2` 7.501 ms; paired CI -0.0650 .. -0.0368 ms
(control faster). D2048+32 five pairs: 866.721 vs 868.707 ms. MMQ complete
P4096 FFN `fma_async` 885.785 ms vs `fma_async_x` 837.391 ms; paired CI
46.690 .. 50.097 ms (positive). Five engine pairs: 1406.753 vs 1358.710 ms,
E2E non-regression.

Commands and outcomes:

```
uv run pytest -q tests/test_opt086_q8_mmq_reevaluation.py tests/test_optimization_loop.py
```

32 passed.

```
uv run python tools/run_optimization_task.py --task OPT-086 --mode feedback --phase q8
```

passed (`status=passed`, product=8, elapsed_s≈6.08, run_dir
`build/optimization-runs/OPT-086/feedback/20260911T221646Z-e241882a`).

```
uv run python tools/run_optimization_task.py --task OPT-086 --mode feedback --phase mmq
```

passed (`status=passed`, product=8, elapsed_s≈13.09, run_dir
`build/optimization-runs/OPT-086/feedback/20260911T221708Z-f15d7626`).

```
uv run python tools/run_optimization_task.py --task OPT-086 --mode acceptance --phase q8
```

passed (`status=passed`, product=26, elapsed_s≈23.15, run_dir
`build/optimization-runs/OPT-086/acceptance/20260911T221731Z-80f85566`).

```
uv run python tools/run_optimization_task.py --task OPT-086 --mode acceptance --phase mmq
```

passed (`status=passed`, product=26, elapsed_s≈56.30, run_dir
`build/optimization-runs/OPT-086/acceptance/20260911T221808Z-2f5d08cc`).
Acceptance MMQ recompiled after the Q8 pin revert.

**Verdict: Q8 revert to `r1_w4`; MMQ keep `fma_async_x`.** Official tok/s
delta versus the then-current shipping baseline: **0**.

### Verification (2026-09-11T22:22:00Z)

Agent/model: composer-2.5 (`telemetry_unavailable`). Independent verifier;
no semantic fixes.

```
uv run ruff format .
```

198 files left unchanged.

```
uv run pytest -q tests/test_opt086_q8_mmq_reevaluation.py tests/test_optimization_loop.py
```

32 passed in 0.15s.

```
uv run python tools/run_optimization_task.py --task OPT-086 --mode feedback --phase q8
```

passed (`status=passed`, product=8, `claims_throughput=false`, elapsed_s≈6.15,
run_dir `build/optimization-runs/OPT-086/feedback/20260911T222047Z-eb5822a7`).

```
uv run python tools/run_optimization_task.py --task OPT-086 --mode feedback --phase mmq
```

passed (`status=passed`, product=8, `claims_throughput=false`, elapsed_s≈13.03,
run_dir `build/optimization-runs/OPT-086/feedback/20260911T222055Z-8eb023dd`).

```
uv run python tools/run_optimization_task.py --task OPT-086 --mode acceptance --phase q8
```

passed (`status=passed`, product=26, `claims_throughput=false`, elapsed_s≈23.11,
run_dir `build/optimization-runs/OPT-086/acceptance/20260911T222109Z-72f724ad`).

```
uv run python tools/run_optimization_task.py --task OPT-086 --mode acceptance --phase mmq
```

passed (`status=passed`, product=26, `claims_throughput=false`, elapsed_s≈44.46,
run_dir `build/optimization-runs/OPT-086/acceptance/20260911T222134Z-c6e9e747`).

**Verdict: PASS**

### Delivery (2026-09-11T22:22:00Z)

- Agent/model: composer-2.5 (`telemetry_unavailable`)
- Marked OPT-086 `done`. Coupled IDs: none. `plan.md` unchanged. OPT-016 and
  OPT-056 stay blocked. Next eligible pending by dependency order: OPT-087.
- `claims_throughput`: q8=false, mmq=true. Tok/s delta versus the then-current
  shipping baseline: **0** (Q8 revert is component-only; MMQ was already
  shipping).

### Final outcome (2026-09-11T22:22:00Z)

Independent formal keep/revert for Q8 and MMQ. **Q8 reverted** to `r1_w4`
(`production_kept=true` for control; `r2_w2` rejected on performance).
**MMQ kept** `fma_async_x` (`production_kept=true`; `fma_async` rejected on
performance). `opt074_coverage_unadmitted` is not a keep blocker. Verdict:
**Q8 revert to `r1_w4`; MMQ keep `fma_async_x`**.

## Outcome

Resolve the installed paths from OPT-064/066 that OPT-070 left inconclusive
because of OPT-074 coverage. Q8 and MMQ verdicts stay independent. Do not
require OPT-074 admission. Do not presume the installed layout should remain:
OPT-070 slightly favored Q8 control `r1_w4` at component level, and measured
a P4096 complete-FFN improvement for `fma_async_x`.

Issue a formal keep/revert for each family with independent
`kernel_parity_pass`, `model_quality_pass`, `performance_pass`,
`production_kept`.

## Implementation

### Q8 decode

Compare control `r1_w4` vs installed `r2_w2`.

1. Structural correctness: legal alignment, staging reuse/invalidation,
   typed Q8_1 fields (production Q8 DP4A reads scale/values only; do not
   treat stored sums as a different type).
2. Q8 operation parity vs CPU/dequant reference (OPT-081 Q8_0 envelope) and
   same-math staging tests.
3. OPT-083/084 quality non-regression vs shipping baseline (shipping is
   currently `r2_w2`; if reverting, the quality/performance pair is vs
   `r1_w4` as the replacement control with the same freeze rules).
4. Component/E2E: repaired OPT-071 replay; all 48 GDN + 16 attention
   projection groups in true layer order including staging/norm; D2048
   3+10 pairs then five engine pairs; X-prefetch held fixed at the
   then-current MMQ decision or the shipping `fma_async_x` until the MMQ
   half of this task decides. Measure Q8 first if a four-configuration
   interaction screen is needed; that screen is at most one declared
   diagnostic, not an unbounded factorial.

OPT-070 D2048 rotating mixer n=10: `r1_w4` 7.455 ms vs `r2_w2` 7.506 ms
(paired CI favored control). Reuse for ranking only if identity remains
valid; keep/revert requires this sitting's confirmation.

### Prompt MMQ

Compare control `fma_async` vs installed `fma_async_x`, tile 128×128.

1. Q4_K MMQ parity vs CPU/dequant (`0.20*sqrt(K)` OR 5%).
2. Full-model quality under OPT-083/084.
3. Prompt performance: all 64 complete FFNs with staging, SwiGLU, down,
   residual; P4096 3+10 then five engine pairs; Q8 decision from the Q8
   half held fixed.

OPT-070 P4096 n=10: `fma_async` 887.292 ms vs `fma_async_x` 838.523 ms.
If kernel parity and model quality pass, formalize the keep when this
sitting still shows a positive complete-P interval and E2E non-regression.

### Shared rules

Prove actual launches after separate graph capture. Do not toggle only a
test TU selector. Keep OPT-058 staging invalidation on both sides. Revert
only when the replacement/control admission (parity + quality + performance)
passes; do not undo unrelated scheduler fixes. Missing OPT-074 coverage is
not inconclusive.

## Acceptance

- Two independent family verdicts in one report/fixture.
- `opt074_coverage_unadmitted` must not appear as a keep blocker.
- Historical OPT-070 report unmodified. Shipping may change per family.
- `claims_throughput` per family only when that family is kept.

## Tests and commands

```sh
uv run pytest -q tests/test_opt086_q8_mmq_reevaluation.py tests/test_optimization_loop.py
uv run python tools/run_optimization_task.py --task OPT-086 --mode feedback --phase q8
uv run python tools/run_optimization_task.py --task OPT-086 --mode feedback --phase mmq
uv run python tools/run_optimization_task.py --task OPT-086 --mode acceptance --phase q8
uv run python tools/run_optimization_task.py --task OPT-086 --mode acceptance --phase mmq
```

Host tests prove independent verdicts, reject OPT-074-unadmitted as a
blocker, and reject screen-only keep. Feedback: 1+3 plus OPT-082 parity
cases for that family. Acceptance: quality once per survivor plus 3+10 and
five engine pairs. Split Q8/MMQ at 300 s. Stop a family on parity, quality,
or correctness failure without blocking the other family.

## Non-goals

No Q4 path change (OPT-085). No new layouts. No OPT-088 release. No
historical OPT-070 rewrite.
