# OPT-101 — Transpose and register-shard decode GDN state

Status: done. Dependencies: OPT-099. Coupled IDs: none. Primary target: D128/D2048 GDN core.

## Run record

### Planning (2026-09-12T15:10:00Z)

skipped — pre-authored dossier admitted (coordinator)
Read [the post-098 protocol](PERFORMANCE-RECOVERY-POST-098.md).

### Implementation (2026-09-12T16:05:00Z)

- Agent/model: implementation (Cursor Grok 4.6). No commit. Ledger left
  `in_progress`. `plan.md` unchanged. Production pin stays `sequential`.
  `claims_throughput: false`. `telemetry_unavailable`.
- Prior sitting had parity (`grid=(48,32)`, registers=38, layout roundtrip)
  but performance admission incomplete. This sitting finished quality, state,
  3+10 complete GDN, D128/D2048 five-pair, and P4096 guard.
- Recapture of decode-gdn failed until replay used `prefix = tokens.size() -
  decode_tokens` (two GDN capture steps after a 128-token prefix). The previous
  `size-1` window read an OOB token (`>= vocab`) on the second decode step.
  Probe `gdn-ab` acceptance also allowed prefix 128 so the D128 five-pair
  sitting could run.
- Identity-cached OPT-073 quality reused (`model_quality_pass=true`). Native
  u1+u4 isolation passed (`seq_abs ~1e-9`, nonfinite 0, committed unchanged).
- Complete 48-layer GDN n=10 rotating, capture
  `8b954514af28526225acbdc344b48dcc98485daa454a1835f8f7c43f021433e6`:
  sequential **12.418 ms** vs transposed **13.339 ms**; paired mean_diff
  **-0.921 ms** with 95% CI **[-1.276, -0.566]** (entirely negative; required
  ≥0.10 ms saving with positive CI). Kernel-only ~1.49 ms vs ~2.31 ms.
  Dispatch `prepare_recurrence_decode_transposed` `grid=(48,32)`.
- D128 five pairs: 571.36 vs 584.72 ms, throughput ratio 0.977, guards fail.
- D2048 five pairs: 624.13 vs 636.30 ms, throughput ratio 0.981, guards fail
  (point estimate slower).
- P4096 one pair: 1322.56 vs 1322.91 ms (3097.02 vs 3096.21 tok/s, ratio
  0.9997). Gross guard OK; not a keep.
- Verdict: **reject / retain sequential**. `production_kept=false`.
  tok/s delta vs sitting sequential baseline: **0**. Versus OPT-098 P4096
  **3046.23 tok/s**: **+0 (speedup 0)** because production is unchanged.

**Commands**

```
uv run python tools/opt101_gdn_transposed.py --phase quality --mode release --run-dir build/optimization-runs/OPT-101/release/quality
```

**pass** — identity-cached OPT-073; `model_quality_pass=true`.

```
docker ... ./build/qw38-cuda-opt101-gdn-transposed-test --phase screen
```

**pass** — layout + prod_u1/u4 isolation; regs=38; local=0; occupancy=12.

```
uv run python tools/opt101_gdn_transposed.py --phase gdn --mode acceptance --run-dir build/optimization-runs/OPT-101/acceptance/gdn
```

**pass** — exit 0; n=10 sequential 12.418 vs transposed 13.339 ms; CI entirely
negative; `keep=false`. Invoked the tool directly (foreign GPU inference
services blocked `run_optimization_task`).

```
uv run python tools/opt101_gdn_transposed.py --phase d128 --mode acceptance --run-dir build/optimization-runs/OPT-101/acceptance/d128
uv run python tools/opt101_gdn_transposed.py --phase d2048 --mode acceptance --run-dir build/optimization-runs/OPT-101/acceptance/d2048
uv run python tools/opt101_gdn_transposed.py --phase prefill-guard --mode acceptance --run-dir build/optimization-runs/OPT-101/acceptance/prefill-guard
```

**pass** — exit 0 each; D128/D2048 candidate slower; P4096 ratio 0.9997;
`evidence_complete=true`; pin remains sequential; `REJECTION.md` written.

```
uv run pytest -q tests/test_opt101_gdn_transposed.py tests/test_opt077_gdn_decode.py
```

**pass** — 19 passed.

### Verification (2026-09-12T16:10:00Z)

Independent integration verifier. No semantic fixes applied.

```
uv run ruff format .
228 files left unchanged
exit 0

uv run ruff check tools/opt101_gdn_transposed.py tests/test_opt101_gdn_transposed.py
All checks passed!
exit 0

uv run pytest -q tests/test_opt101_gdn_transposed.py
..........                                                               [100%]
10 passed in 0.03s
exit 0
```

Fixture inspection (`fixtures/opt101_gdn_transposed.json`):
- `status=rejected`, `evidence_complete=true`
- `shipping_gdn_decode=sequential`, `selected_path=sequential`
- `claims_throughput=false`, `production_kept=false`, `tok_s_delta=0.0`
- complete GDN n=10: sequential 12.418 ms vs transposed 13.339 ms; mean_diff
  −0.921 ms; CI95 [−1.276, −0.566]; `positive=false`
- D128 five-pair: 571.36 vs 584.72 ms; guards fail (throughput_ratio 0.977)
- D2048 five-pair: 624.13 vs 636.30 ms; guards fail (throughput_ratio 0.981)
- P4096 guard: ratio 0.9997 (not a keep)
- `kernel_parity_pass=true`, `model_quality_pass=true` at top level

Rejection evidence (`evidence/optimization/opt101-gdn-transposed-state/REJECTION.md`):
- production pin remains `sequential`; transposed warp-column candidate not kept
- OPT-077/094 historical evidence unchanged
- **tok/s delta vs OPT-098 P4096 3046.23 tok/s: 0** (`claims_throughput=false`)

Verdict: **PASS**.

### Delivery (2026-09-12T16:10:00Z)

Model: composer-2.5 (verifier + delivery, one pass).

### Final outcome

`measured_reject`: transposed warp-column GDN decode failed complete 48-layer GDN
acceptance (3+10: sequential 12.418 ms vs transposed 13.339 ms; mean_diff
−0.921 ms; CI95 [−1.276, −0.566]; `positive=false`) and D128/D2048 engine
guards. Production pin remains `sequential`. Kernel parity and OPT-073 quality
reuse both passed. `claims_throughput=false`. `evidence_complete=true`.

**tok/s delta vs OPT-098 P4096 baseline (3046.23 tok/s): 0** (rejection; no
throughput claim).

Evidence: [`fixtures/opt101_gdn_transposed.json`](fixtures/opt101_gdn_transposed.json);
[`evidence/optimization/opt101-gdn-transposed-state/REPORT.md`](evidence/optimization/opt101-gdn-transposed-state/REPORT.md);
[`evidence/optimization/opt101-gdn-transposed-state/REJECTION.md`](evidence/optimization/opt101-gdn-transposed-state/REJECTION.md).

**Commit:** delivered 2026-09-12T16:10:00Z
**Push:** origin/main (authorized)

## Outcome

Replace the selected one-token row-major GDN recurrence with a transposed,
warp-column kernel that keeps each lane's state shard in registers. Keep it
only on complete 48-layer GDN and two-prefix end-to-end wins with full recurrent
quality and transactional-state correctness.

## Rationale

The selected Quartz kernel launches one 128-thread CTA per value head. Each
thread serially walks 128 key rows, reads state once for prediction and again
for update, then writes 128 values. Lane 0 also serially computes q/k norms,
repeated for three value heads sharing each key head. Pinned llama.cpp
`gated_delta_net_cuda<128>` uses transposed state, four warps per CTA, one warp
per output column and four register-resident state values per lane. OPT-077's
tile16/tile32 candidates retained row-major state and shared-memory reductions;
this is a new layout and ownership mechanism, not a rerun.

## Implementation

1. Define a transposed production GDN-state layout with the same element count.
   Preserve the canonical checkpoint payload by transposing only at save/restore
   boundaries, or version the payload with backward-compatible restore. The
   allocation ledger and hashes must identify logical versus device order.
2. Hoist q/k normalization to one computation per 16 key heads. Compare a
   fused-in-recurrence form only if it does not repeat work per reused value
   head. Convolution and gate preparation stay visible reference stages.
3. Implement the exact production specialization: key/value width 128, 48
   value heads, four warps per CTA, `grid=(48,32)`, each warp owning one output
   column and each lane owning four key rows. Load state once into registers,
   perform prediction and output reductions with warp shuffles, update the
   register shard, and write state once. Retain the current sequential kernel.
4. Preserve FP32 recurrence, q/k RMS conventions, decay/beta semantics, value
   head permutation and BF16 output boundary. Validate independent FP64 taps,
   long recurrence, chunk/decode equivalence and nonfinite behavior.
5. Prove candidate-versus-committed isolation, cancellation before and after
   recurrence, divergent-prefix restore, graph replay, checkpoint round trip and
   exact frontier ownership. In-place state is allowed only inside an already
   isolated candidate slot.
6. Apply the shared 3+10 complete-GDN and five-pair D128/D2048 protocol. Use
   OPT-099 matched excess as the expectation; do not add OPT-077 replay values
   to wall time.

## Acceptance

- Layout round trips are exact and checkpoint compatibility is explicit.
- Kernel parity, full quality and recurrent NLL pass with zero nonfinites.
- Complete 48-layer GDN saves at least 0.10 ms/token with positive paired CI;
  D128 and D2048 both improve and P4096 does not regress beyond the shared gate.
- State/checkpoint/cancellation and 128K memory gates pass. Rejection restores
  `sequential` without altering historical OPT-077/094 evidence.
- Add contract/fixture, diagnostic, runner and
  `evidence/optimization/opt101-gdn-transposed-state/`.

## Non-goals

No reduced-precision state, recurrence approximation, changed convolution,
tile32 reopening, speculative rollback policy or extra persistent state copy.

