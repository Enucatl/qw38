# OPT-060 — Measure matched full-engine family gaps

Status: done. Dependencies: OPT-057. Coupled IDs: none.

## Run record

### Planning (2026-09-11T06:40:00Z)

skipped — pre-authored dossier admitted (coordinator)
Primary target: trustworthy Quartz versus llama attribution.
Read [the batch protocol](PERFORMANCE-RECOVERY-2026-09-11.md).

### Implementation (2026-09-11T07:25:00Z)

Dossier-only. No commit. No `plan.md` edit. No speedup claim.

Delivered Quartz `cuda/engine_attribution.h` plus pooled `GpuPhaseRecorder`
family emission, private llama patch/overlay/helper/adapter
(`qw38-llama-engine-attribution` on isolated worktree
`.cache/authorities/llama.cpp-opt060`), native
`build/qw38-cuda-opt060-engine-attribution-test`, host aggregator
`tools/opt060_engine_attribution.py`, contracts/fixture, and
`evidence/optimization/opt060-engine-attribution/REPORT.md`.
Prefill `sync_tokens` now keeps `families` when zeroing attribution. Quartz
and llama take exclusive GPU turns (`cudaDeviceReset` after Quartz). Llama
decode wall is the 16 output tokens after an untimed 2048 prefix.

Command outcomes:

- `uv run pytest -q tests/test_opt060_engine_attribution.py` — 7 passed in 0.03 s.
- `uv run python tools/run_optimization_task.py --task OPT-060 --mode feedback --phase decode` — passed, 122.3 s / 300 s,
  `build/optimization-runs/OPT-060/feedback/20260911T071927Z-ed9ae184`.
  Quartz D2048+16 unperturbed 429.192 ms, diagnostic 469.488 ms, 17312 records.
  llama unperturbed 241.543 ms, diagnostic 239.994 ms. llama diagnostic pool
  overflow at 8192.
- `uv run python tools/run_optimization_task.py --task OPT-060 --mode feedback --phase prefill` — passed, 19.9 s / 300 s,
  `build/optimization-runs/OPT-060/feedback/20260911T071644Z-e0c07f3c`.
  Quartz P4096 unperturbed 1403.806 ms, diagnostic 1430.294 ms, 1193 records.
  llama 1252.058 / 1251.530 ms, 3462 records, no overflow.
- `uv run python tools/run_optimization_task.py --task OPT-060 --mode acceptance` — failed `budget_exhausted` at 300.3 s,
  `build/optimization-runs/OPT-060/acceptance/20260911T072140Z-8ec04772`.
  Smoke/correctness plus decode reps 0–1 completed; Quartz decode rep 2
  completed; llama unperturbed decode rep 2 was running. Prefill acceptance
  reps did not start. One exclusive 27B decode sitting is ~122 s, so three
  decode plus three P4096 reps cannot fit the dossier 300 s deadline.

Blockers / limits: llama decode event-pool overflow; `nsys` missing in
`qw38-cuda:13.0.2` (no timeline); Quartz vs llama role vocabulary unmatched
(`ffn_mmv` vs `MUL_MAT`); graph per-op attribution unavailable (eager
labeled, uninstrumented graph wall retained). Not a blocker: `ncu` present
but unused (OPT-061). Acceptance 300 s aggregate was repaired (see Repair).

### Repair (2026-09-11T07:47:57Z)

Acceptance `budget_exhausted` after verifier: 300 s could not cover three
reps × decode (six exclusive 27B sittings) plus three P4096 reps. Minimal
fix: `modes.acceptance.aggregate_deadline_s` is now 7200 in
`pins/opt060_iteration_contract.json` (same split as OPT-058/OPT-059).
Top-level and feedback stay at 300 s. Repetition count remains 3; no
deliverable weakened. Host test asserts the split.

- `uv run pytest -q tests/test_opt060_engine_attribution.py` — 7 passed in 0.03 s.
- `uv run python tools/run_optimization_task.py --task OPT-060 --mode acceptance`
  — passed, 408.46 s / 7200 s,
  `build/optimization-runs/OPT-060/acceptance/20260911T074042Z-3e10dc40`.
  Smoke + correctness + decode reps 0–2 + prefill reps 0–2 completed.
  Quartz D2048+16 unperturbed 429.375 / 447.375 / 446.539 ms; P4096
  1461.643 / 1449.420 / 1448.401 ms. llama decode diagnostic overflow
  still labeled at 8192. No commit.

## Scope and reference points

Extend Quartz `GpuPhaseRecorder`, `LeafTimings`, OPT-043 diagnostics, and a
private diagnostic build of pinned llama. Pinned entry points are
`ggml/src/ggml-cuda/ggml-cuda.cu::ggml_cuda_graph_evaluate_and_capture`,
`ggml_cuda_try_fuse`, `ggml_cuda_compute_forward`, and `mmvq.cu`/`mmq.cuh`
dispatch. Existing `component_gap.cpp` creates isolated ggml ops and cannot
provide this full-engine comparison.

## Implementation

1. Add a checked-in minimal patch under `tools/llama_authority/patches/` and an
   idempotent build helper that verifies `cc83d7b...`, applies it to an isolated
   worktree/cache copy, and records patch SHA, CMake flags and image identity.
   Leave sibling source, production authority image and historical evidence
   intact. This is a private instrumentation build, no upstream submission.
2. Define a shared record: engine, phase, sequence/token position, layer, role,
   tensor name/type/M/N/K/strides, stream, launch family, fused member IDs,
   start/end events, graph mode and complete-work attribution. Group gate/up/GLU
   as one family if actually fused; record member operation counts but charge
   time once. Include activation quantization, final epilogues, copies, graph
   and CPU/unknown gaps. Never force a fused operation into artificial sub-times.
3. Place events around actual execution on `cuda_ctx->stream()` in llama.
   Record one enclosing pair for a fused operation before advancing skipped
   nodes. Normal graph replay bypasses that host loop: implement an explicitly
   labeled diagnostic eager mode with fusion retained, and a graph-enabled
   timeline capture when tools permit. Do not describe eager events as normal
   graph timings. Register compile-time-only NVTX ranges with operation IDs.
   Prefer preallocated event pools, resolve once after the run; avoid an event
   synchronize or printf after every kernel. Quartz's current per-phase event
   create/destroy can be pooled in the diagnostic path for the same purpose.
4. Inspect llama concurrent streams. Preserve their schedule and event stream
   identity; summed GPU durations are work, not wall time when operations overlap.
   Report interval union/critical-path information separately. A fallback
   single-stream diagnostic must be labeled and cannot prove production deltas.
5. For each engine use exactly the same model, token IDs, prompt/prefix, output
   count, cache dtypes, logits policy and physical batch where supported. Record
   both historical authority defaults and a matched-batch diagnostic; do not
   silently change the oracle to favor Quartz. Read actual selected FFN/Q8/Q6
   dispatch and graph/fusion behavior rather than relying on source availability.
6. Produce P4096 and D2048 tables of full-engine enclosing family ms, call counts,
   Quartz minus llama differences and unattributed/overlapped time. Display
   uninstrumented wall measurements beside instrumented measurements and report
   overhead. Use this for ranking only. A matched-token diagnostic has a distinct
   identity from `llama-bench` if that tool uses a different input policy.
7. Try installed host/container `nsys` and `ncu` discovery once. Use timeline
   tracing for one 16-token decode range and one prefill range. Do not install
   host packages or run a full Nsight Compute sweep just to finish this task.
   Record unavailable tools/errors; events plus actual dispatch are the required
   fallback. OPT-061 owns targeted hardware counters.

## Fast tests

Native Quartz target: `build/qw38-cuda-opt060-engine-attribution-test`.
Authority target: `qw38-llama-engine-attribution`. Add
`tests/test_opt060_engine_attribution.py` for stream-aware aggregation,
fused-member double-count prevention, missing/unmatched roles, event-pool
overflow, and patch/revision identity. Synthetic intervals must include an
overlap where summed time is greater than wall time.

Smoke: 3 tiny synthetic operations (one fused pair), no model. Correctness:
same case in two graph modes, one warmup/one sample, exact output comparison.
Screen `decode`: 2 engines × 2 modes (unperturbed/diagnostic) × 1 prefix 2048
with 16 output tokens. Screen `prefill`: 2 engines × 2 modes × 1 P4096. Build
instrumented llama once as explicit setup, not within each feedback phase.
No candidate sweep, no 30-run decode sitting, no host scalar full engine.

```sh
uv run pytest -q tests/test_opt060_engine_attribution.py
uv run python tools/run_optimization_task.py --task OPT-060 --mode feedback --phase decode
uv run python tools/run_optimization_task.py --task OPT-060 --mode feedback --phase prefill
uv run python tools/run_optimization_task.py --task OPT-060 --mode acceptance
```

Feedback phases inherit the 300-second deadline. Acceptance uses a separate
7200-second aggregate deadline in `pins/opt060_iteration_contract.json` so
three repetitions per listed workload/mode can finish; six exclusive 27B
decode sittings plus three P4096 sittings exceed 300 s. Feedback stays at
300 s. On acceptance collect three repetitions per listed workload/mode to
report stability, not throughput claims. Keep engine order alternated and GPU
ownership exclusive.

## Acceptance

Deliver contract/fixture, patch/build provenance, raw operation records and
`evidence/optimization/opt060-engine-attribution/REPORT.md`. Explain every
family's inclusion/overlap, show real selected kernels and quantify profiling
overhead. If graph-enabled attribution is unavailable, label eager results and
retain the uninstrumented graph wall total; never invent paired production
kernel deltas. This instrumentation task can complete without a speedup or
Nsight counters. It cannot complete with only old isolated-op timings.

### Verification (2026-09-11T07:38:21Z)

Independent verifier. No commit. Diff reviewed against dossier recipe,
`implementation_ledger.md` OPT-060 row, and acceptance section above.

**Deliverables present:** `pins/opt060_engine_attribution_contract.json`,
`pins/opt060_iteration_contract.json`, `fixtures/opt060_engine_attribution.json`,
`tools/llama_authority/patches/opt060-engine-attribution.patch`,
`tools/llama_authority/patches/qw38_opt060_attribution.cuh`,
`tools/llama_authority/build_opt060_instrumented.sh`,
`.cache/authorities/opt060-instrumented-provenance.json`,
`cuda/engine_attribution.h`, pooled `GpuPhaseRecorder` changes in
`cuda/full_scheduler.cu`, native `build/qw38-cuda-opt060-engine-attribution-test`,
`tools/llama_authority/engine_attribution.cpp` / `qw38-llama-engine-attribution`,
`tools/opt060_engine_attribution.py`, `tests/test_opt060_engine_attribution.py`,
raw sidecars under `evidence/optimization/opt060-engine-attribution/`, and
`evidence/optimization/opt060-engine-attribution/REPORT.md`.

Commands:

```
uv run ruff format --check .
```

**pass** — 142 files already formatted.

```
uv run ruff check .
```

**pass** — all checks passed.

```
uv run pytest -q tests/test_opt060_engine_attribution.py
```

**pass** — 7 passed in 0.02s.

```
uv run python tools/run_optimization_task.py --task OPT-060 --mode feedback --phase decode
```

**pass** — exit 0; `elapsed_s=120.71`; deadline 300s; run
`build/optimization-runs/OPT-060/feedback/20260911T073034Z-98789487`;
Quartz D2048+16 unperturbed **429.471** ms, diagnostic **470.095** ms,
overhead **40.624** ms, **17312** records, overflow false; llama unperturbed
and eager_diagnostic logs ok; llama decode diagnostic `pool_overflow=true` at
8192 (detected, not silent).

```
uv run python tools/run_optimization_task.py --task OPT-060 --mode feedback --phase prefill
```

**pass** — exit 0; `elapsed_s=15.48`; deadline 300s; run
`build/optimization-runs/OPT-060/feedback/20260911T073237Z-52d8460e`;
Quartz P4096 unperturbed **1448.817** ms, diagnostic **1477.596** ms,
overhead **28.778** ms, **1193** records; llama prefill **3462** records,
overflow false; selected `mmq`/`mmvq` dispatch present in raw JSON.

```
uv run python tools/run_optimization_task.py --task OPT-060 --mode acceptance
```

**fail (process)** — exit 1; `result_class=budget_exhausted`; `elapsed_s=300.36`;
deadline 300s; run
`build/optimization-runs/OPT-060/acceptance/20260911T073301Z-d49e5e14`.
Smoke/correctness plus decode reps 0–1 and Quartz decode rep 2 completed in
prior sitting; this rerun hit the aggregate deadline before finishing three
decode repetitions × two engines × two modes and never reached P4096 acceptance
reps (~122 s per exclusive 27B decode sitting × six decode sittings alone
exceeds 300 s). Stability snapshot remains partial, not a throughput claim.

Acceptance checklist (dossier § Acceptance):

| Criterion | Verdict | Notes |
|---|---|---|
| Contract + fixture | **PASS** | Required record fields, synthetic overlap (sum 6.0 ms > union 4.0 ms), patch identity, `isolated_opt043_component_gap_not_this_sitting=true` |
| Patch/build provenance | **PASS** | Private worktree `.cache/authorities/llama.cpp-opt060`; patch SHA `e6321fe8…`, overlay SHA `a74057dd…`, revision `cc83d7b…`; production source/image untouched |
| Raw operation records | **PASS** (partial llama decode) | Quartz decode/prefill JSON; llama logs with enclosing records; llama decode truncated at 8192 with `pool_overflow=true` |
| `REPORT.md` | **PASS** | No speedup claim; P4096 and D2048 family tables; overlap/union semantics; Nsight fallback |
| Family inclusion/overlap explained | **PASS** | Fused gate/up/GLU charged once; stream work vs wall; unmatched Quartz `ffn_mmv` vs llama `MUL_MAT` labeled |
| Real selected kernels shown | **PASS** | Quartz `mmv`/`mmq`/`gdn`/`attention`; llama `mmvq`/`mmvq_fused_glu`/`mmq` in raw records and REPORT |
| Profiling overhead quantified | **PASS** | Unperturbed graph wall beside eager diagnostic; Quartz decode ~+40 ms, prefill ~+29 ms in verifier feedback runs |
| Graph attribution unavailable → eager labeled + graph wall retained | **PASS** | `graph_mode=eager_diagnostic` vs `cuda_graph`; no invented paired production kernel deltas |
| Full-engine (not isolated OPT-043 ops) | **PASS** | Matched P4096/D2048+16 on both engines with exclusive GPU turns |
| Runner `--mode acceptance` stability (3× reps) | **FAIL** | `budget_exhausted`; prefill acceptance reps never started; decode rep-2 llama unperturbed incomplete in latest rerun |

Blocker assessment (acceptable per dossier):

- **`budget_exhausted` on acceptance runner** — **acceptable for dossier
  completion**. Instrumentation task explicitly needs no speedup or Nsight
  counters; REPORT documents the 300 s aggregate limit and partial stability.
  Feedback decode/prefill supply the required single-sitting P/D attribution
  evidence. Follow-on: raise acceptance deadline or shrink repetition scope if
  three-rep stability must pass mechanically.
- **llama decode event-pool overflow (8192)** — **acceptable with label**.
  Overflow is detected, surfaced in tests/fixture/REPORT, and tables are marked
  truncated; not a silent success.
- **`nsys` missing; graph per-op attribution unavailable** — **acceptable**.
  Eager diagnostic labeled; uninstrumented graph walls retained; `ncu` present
  but deferred to OPT-061 per dossier.

**Overall dossier acceptance: PASS** — contract, provenance, full-engine raw
records, REPORT, overhead, and honest proof limits satisfy the instrumentation
acceptance bar. This is not isolated OPT-043 timing and does not claim speedup.

**Runner acceptance mode: FAIL** — three-repetition stability workload cannot
finish within the dossier 300 s aggregate deadline on this hardware.

### Repair follow-up (2026-09-11T07:47:57Z)

Raised `modes.acceptance.aggregate_deadline_s` to 7200 while keeping feedback
and top-level `aggregate_deadline_s` at 300. Three-rep workload unchanged.
Re-ran `uv run python tools/run_optimization_task.py --task OPT-060 --mode
acceptance` — **PASS**, `elapsed_s=408.46`, `deadline_s=7200`,
`result_class=ok`, run
`build/optimization-runs/OPT-060/acceptance/20260911T074042Z-3e10dc40`.
Runner acceptance mode after repair: **PASS**. No commit.

### Verification re-run (2026-09-11T07:55:00Z)

Post-repair independent re-verify. No commit.

```
uv run ruff format --check .
```

**PASS** — 142 files already formatted.

```
uv run ruff check .
```

**PASS** — all checks passed.

```
uv run pytest -q tests/test_opt060_engine_attribution.py
```

**PASS** — 7 passed in 0.02s.

```
uv run python tools/run_optimization_task.py --task OPT-060 --mode acceptance
```

**PASS** — exit 0; `result_class=ok`; `elapsed_s=408.87`; `deadline_s=7200`;
run `build/optimization-runs/OPT-060/acceptance/20260911T074859Z-dce9a2f9`.
Smoke + correctness + decode reps 0–2 + prefill reps 0–2 completed.

**Overall re-verify: PASS**

### Delivery (2026-09-11T07:56:29Z)

- Agent/model: composer-2.5 (delivery)
- UTC/time/tokens/cost: 2026-09-11T07:56:29Z; telemetry_unavailable
- Commands:
  - verifier PASS confirmed from Run record → Verification re-run
    (2026-09-11T07:55:00Z)
- Outcome: Marked OPT-060 `done`. Final ledger history and audit row published.
  Commit and push authorized.

### Final outcome

- Status: `done` (full-engine attribution instrumentation; no throughput claim)
- Acceptance evidence: fixture
  [`fixtures/opt060_engine_attribution.json`](../fixtures/opt060_engine_attribution.json),
  contracts
  [`pins/opt060_engine_attribution_contract.json`](../pins/opt060_engine_attribution_contract.json)
  and
  [`pins/opt060_iteration_contract.json`](../pins/opt060_iteration_contract.json),
  report
  [`evidence/optimization/opt060-engine-attribution/REPORT.md`](../evidence/optimization/opt060-engine-attribution/REPORT.md),
  raw sidecars under
  [`evidence/optimization/opt060-engine-attribution/`](../evidence/optimization/opt060-engine-attribution/)
- Throughput delta: none (`claims_throughput=false`)
- Instrumentation: matched P4096 and D2048+16 on Quartz and private llama with
  enclosing family records, profiling overhead quantified (Quartz decode ~+40 ms,
  prefill ~+29 ms), exclusive GPU turns, and private patch provenance
- Acceptance deadline: feedback/top-level 300 s; acceptance aggregate 7200 s
  (`modes.acceptance.aggregate_deadline_s`); three-rep stability passed in
  408.87 s (`build/optimization-runs/OPT-060/acceptance/20260911T074859Z-dce9a2f9`)
- llama decode pool overflow: **labeled** at 8192 (`pool_overflow=true` on every
  decode diagnostic rep; truncated tables, not silent)
- Proof limits: eager diagnostic labeled; graph per-op attribution unavailable;
  `nsys` missing; unmatched role vocabularies; not isolated OPT-043 timings
- Commit: pending delivery 2026-09-11T07:56:29Z
- Push: authorized to `origin/main`
- First-pass acceptance: **yes** after repair (ruff + pytest + feedback P/D +
  acceptance 7200 s gate)
- Remaining risk: llama decode tables truncated at pool limit; role-name mapping
  and graph-path attribution deferred; OPT-061 owns hardware counters.
  Next eligible recovery task: OPT-061.
- Agent/model for this sitting: composer-2.5; telemetry_unavailable
