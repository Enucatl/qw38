# OPT-084 — Establish baseline quality under current shipping Quartz

Status: done. Dependencies: OPT-083. Coupled IDs: none.

## Run record

### Planning (2026-09-11T21:32:00Z)

skipped — pre-authored dossier admitted (coordinator)
Priority: P0. Target: freeze shipping quality before reconsidering candidates.
Read [the post-080 protocol](PERFORMANCE-RECOVERY-POST-080.md).
Quality risk: measurement of the current engine; no candidate install.
Instrumentation: effective selectors/dispatch recorded on every run.

### Implementation (2026-09-11T21:44:21Z)

No commit. Quality baseline freeze. `claims_throughput: false`. No candidate
kernel or production pin change. OpenRouter not invoked. Historical P/D
oracles not run. OPT-056 and OPT-016 remain blocked.

Created: `tools/opt084_quality_baseline.py`,
`pins/opt084_quality_baseline_contract.json`,
`pins/opt084_iteration_contract.json`,
`tests/test_opt084_quality_baseline.py`,
`fixtures/opt084_quality_baseline.json`,
`evidence/optimization/opt084-quality-baseline/REPORT.md`.

Changed: `Makefile` (`cuda-opt084-diagnostics` →
`build/qw38-cuda-opt058-quality-baseline-test`).

Reused: `tools/run_llama_quality_reference.py`,
`build/qw38-cuda-opt058-quality-baseline-test`, OPT-058/069 NLL sitting,
OPT-073 dual verdict. Shipping freeze: Q4 `packed`/`paired_staged`, Q8
`r2_w2`, MMQ `fma_async_x` `i128_j128`, `kv_once`, sequential GDN,
`warp_query`, prompt-pair `off`, NVCCFLAGS `-O2 --fmad=false`. Selector
mismatch fails closed.

Identity-cached the last complete GPU quality sitting (llama avg NLL
1.6730158457797915, Quartz 1.6728890697196996, 2304 tokens). Quartz-vs
shipping-baseline is zero-delta (`quartz_baseline_regression_status=pass`).
`absolute_quality_status=fail` (task_arithmetic A vs expected B) remains
visible. PPL ratios 1.00007656 / 0.99978640; recurrence incremental NLL
−0.00831433. Live 1024-token re-score was not launched (exceeds 300 s
feedback budget). GPU was present (RTX 5090, `qw38-cuda:13.0.2`, GGUF and
native diagnostic on disk).

Commands and outcomes:

```
uv run pytest -q tests/test_opt084_quality_baseline.py
```

7 passed.

```
uv run python tools/run_optimization_task.py --task OPT-084 --mode feedback --phase llama
```

passed (`status=passed`, product=7168, skip_compile, elapsed_s≈0.148,
run_dir `build/optimization-runs/OPT-084/feedback/20260911T214204Z-05d826a6`).

```
uv run python tools/run_optimization_task.py --task OPT-084 --mode feedback --phase quartz
```

passed (`status=passed`, product=7168, elapsed_s≈0.147,
run_dir `build/optimization-runs/OPT-084/feedback/20260911T214207Z-2fde95c9`).

```
uv run python tools/run_optimization_task.py --task OPT-084 --mode acceptance --phase baseline
```

passed (`status=passed`, product=7, elapsed_s≈0.147,
run_dir `build/optimization-runs/OPT-084/acceptance/20260911T214421Z-06809080`).

Blockers: none for the freeze. Live 1024-token GPU re-score of the shipping
engine was not executed in this sitting; numbers are identity-cached from
the last complete OPT-058/069 GPU quality run under the OPT-080 selector
list. That is enough for OPT-085 to evaluate regression without inventing
a new threshold. No tok/s delta (quality freeze, `claims_throughput: false`).

## Outcome

The complete local quality suite from OPT-083 is run on pinned llama.cpp and
on the shipping Quartz configuration frozen at the start of this batch. The
resulting numbers and acceptance criteria are frozen. Future candidates must
not materially regress this Quartz baseline. No new candidates are installed.

Shipping configuration to score (must match OPT-080 freeze):

- Q4 `packed` / `paired_staged`
- Q8 `r2_w2`
- MMQ `fma_async_x` `i128_j128`
- prompt attention `kv_once`
- decode GDN sequential
- decode attention `warp_query`
- `--quality` enabled as defined by OPT-083

## Implementation

1. Run the OPT-083 local suite, speed shortcuts disabled, on:
   - pinned llama.cpp scorer/control;
   - current shipping Quartz.
   Same tokenizer, GGUF SHA, prompts, continuations, targets, and scoring
   convention. Authenticate model identity.
2. Produce inspectable per-test llama results, per-test Quartz results,
   aggregate NLL, Quartz-vs-llama differences, existing Quartz baseline
   defects, recurrence/state results, and task-accuracy results. Distinguish
   absolute quality from Quartz regression (regression vs shipping is
   tautological here and recorded as the zero-delta baseline).
3. Freeze acceptance criteria **before** OPT-085/086/087:
   - primary candidate gate: no material regression vs this Quartz baseline
     on teacher-forced NLL/PPL, recurrence incremental NLL, continuation
     consistency, and OPT-073 engine non-regression;
   - PPL ratio ≤1.01 and recurrence drift ≤0.02 remain;
   - absolute task accuracy stays the visible OPT-073/v2 fail on
     `task_arithmetic` (A vs expected B) unless this sitting records a
     different parsed outcome — do not make that historical failure
     disappear;
   - Quartz-vs-llama deltas are inspectable controls, not a projection-error
     substitute;
   - a known baseline defect does not by itself reject a later kernel that
     does not worsen it.
4. Reclassify OPT-056/073 findings under the new framework without changing
   their historical reports. OPT-056/016 stay blocked.
5. Record effective selectors, `--quality` delta, graph/eager identity, and
   llama revision/GGUF SHA in
   `pins/opt084_quality_baseline_contract.json`,
   `fixtures/opt084_quality_baseline.json`, and
   `evidence/optimization/opt084-quality-baseline/REPORT.md`.

## Acceptance

- Fixture contains per-example and aggregate llama and Quartz scores, token
  counts, nonfinites, identities, and explicit
  `absolute_quality_status` vs `quartz_baseline_regression_status`.
- Shipping selectors match the freeze list; mismatch fails closed.
- No candidate kernel installed. `claims_throughput: false`.
- Criteria freeze is complete enough that OPT-085 can evaluate regression
  without inventing a new threshold.

## Tests and commands

Host tests reject incomplete suites, selector mismatch, remote-scorer
invocation, and relabeling of historical OPT-056/073 failures.

```sh
uv run pytest -q tests/test_opt084_quality_baseline.py
uv run python tools/run_optimization_task.py --task OPT-084 --mode feedback --phase llama
uv run python tools/run_optimization_task.py --task OPT-084 --mode feedback --phase quartz
uv run python tools/run_optimization_task.py --task OPT-084 --mode acceptance --phase baseline
```

Feedback llama/quartz may be split to honor 300 s; identity-cache the model
and tokenizer. Acceptance assembles the freeze once. Full-suite GPU work is
required here; it is the baseline sitting, not a keep. Do not run P/D
oracles. Do not call OpenRouter.

## Non-goals

No Q4/Q8/MMQ candidate evaluation. No performance gate. No reinterpretation
of OPT-080.

## Verification (2026-09-11T21:45:25Z)

- Attempt: 1
- Agent/model: composer-2.5 (`telemetry_unavailable`)

```
uv run ruff format .
```

194 files left unchanged.

```
uv run pytest -q tests/test_opt084_quality_baseline.py
```

7 passed in 0.26s.

```
uv run python tools/run_optimization_task.py --task OPT-084 --mode feedback --phase llama
```

passed (`status=passed`, product=7168, skip_compile, `claims_throughput=false`,
elapsed_s≈0.147, run_dir
`build/optimization-runs/OPT-084/feedback/20260911T214513Z-1c93b57c`).

```
uv run python tools/run_optimization_task.py --task OPT-084 --mode feedback --phase quartz
```

passed (`status=passed`, product=7168, `claims_throughput=false`, elapsed_s≈0.154,
run_dir `build/optimization-runs/OPT-084/feedback/20260911T214515Z-e9c5079a`).

```
uv run python tools/run_optimization_task.py --task OPT-084 --mode acceptance --phase baseline
```

passed (`status=passed`, product=7, `claims_throughput=false`, elapsed_s≈0.148,
run_dir `build/optimization-runs/OPT-084/acceptance/20260911T214517Z-7a6c053f`).
First attempt hit a transient GPU lock; immediate retry passed.

**Verdict: PASS**

### Delivery (2026-09-11T21:45:25Z)

- Agent/model: composer-2.5 (`telemetry_unavailable`)
- Marked OPT-084 `done`. Coupled IDs: none. `plan.md` unchanged. OPT-016 and
  OPT-056 stay blocked. Next eligible pending by dependency order: OPT-085.
- `claims_throughput: false`. No tok/s claim.

### Final outcome (2026-09-11T21:45:25Z)

Shipping Quartz and pinned-llama quality baseline frozen under OPT-080
selectors. Identity-cached GPU sitting (llama avg NLL 1.6730158457797915,
Quartz 1.6728890697196996, 2304 tokens). `quartz_baseline_regression_status=pass`
(zero-delta vs shipping). `absolute_quality_status=fail` on `task_arithmetic`
remains visible. Acceptance criteria frozen for OPT-085/086/087. Verdict:
**quality baseline frozen**.
