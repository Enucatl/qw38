# PIN-004 — Decouple source provenance from feature contracts

## Control

- Primary ID: `PIN-004`
- Coupled IDs: `BLD-004`
- Dependencies: `PIN-003`
- Status: `done`
- Ledger acceptance: feature contracts contain no routine local source hashes; release provenance records Git/build/container/evidence identity; BLD-004 owns diagnostic linkage and workspace accounting.

## Goal and boundaries

Feature validation remains stable across harmless source, documentation, and Makefile edits while external artifact and semantic fixture boundaries remain authenticated. `make release-provenance` is release-only and is not invoked by ordinary pytest.

- Non-goals: removing model/runtime authentication, changing artifact hashes, or changing `plan.md`.
- Plan impact: `none`
- Affected interfaces: `tools/release_provenance.py`, Makefile target, pin schemas, tests, docs, and ledger metadata.

## Implementation decisions

- Remove only `local_sources` maps and their repository-byte assertions; retain model, tensor, output, and evidence hashes.
- Emit compact sorted JSON with Git commit/tree/status, selected build hashes, container identities, and evidence hashes; `--require-clean` rejects dirty state.
- Unresolved decisions: `none`

## Acceptance and validation

- Focused: `pytest -q tests/test_release_provenance.py tests/test_cli.py tests/test_build.py`
- Repository-wide: `uv run ruff format .`; `uv run ruff check .`; `pytest -q`
- CUDA: `pytest -q tests/test_cuda_*.py` where hardware/container availability permits.

## Run record

### Planning

- Agent/model: local fallback; delegated planner hit service usage limit.
- UTC/time/tokens/cost: unavailable.
- Outcome: scope inspected and fixed; unresolved decisions `none`.

### Implementation

- Changes: removed source maps/assertions, added release command/tests, and updated docs.
- UTC/time/tokens/cost: unavailable.

### Verification

- Attempt: 1; Agent/model: coordinator; `uv run ruff format .`, `uv run ruff check .`, focused pytest, and repository-wide pytest passed after restoring the retained CUDA admission fields. Repository-wide result: 168 passed, 20 skipped.
- Verdict: `pass`.

### Final outcome

- Status: `done`
- Commit: not created
- Push: not attempted
