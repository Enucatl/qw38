# OPT-135 — Separate target improvement from guard non-regression

Status: done. Dependencies: OPT-134. Coupled IDs: none.
Priority: P0. Plan impact: none; prospective internal admission only.

## Outcome

Implement a versioned keep policy in which a context-specific candidate must
improve its declared targets while preserving declared guards. Neutrality on a
guard is acceptable when uncertainty is narrow enough to establish
non-regression. This is not automatic readmission of OPT-130 and does not
change OPT-016, OPT-056, CMP-003 or `plan.md` release requirements.

Ledger acceptance: A tested prospective policy freezes target/guard roles and
matched metrics, admits proven target gains with non-regressing neutral guards,
rejects guard losses and missing evidence, and leaves historical verdicts and
release gates unchanged.

## Repository evidence and file boundary

Read `pins/opt130_dense_attention_contract.json`,
`tools/opt130_dense_attention.py`, `fixtures/opt130_dense_attention.json` and
`tasks/OPT-130.md`: both D128 and D2048 were declared targets; the implemented
gate followed that declaration. Fix role specification as well as statistics.
D2048 decode 49.969 -> 51.062 tok/s and request geo 1.019385; D128 request geo
0.999981 and CI lower 0.999752 are historical examples, not new acceptance.

Allowed implementation files:

- New `pins/performance_keep_policy_v2.json` and
  `tools/performance_keep_policy.py` (pure host validation/statistics).
- New `tests/test_performance_keep_policy.py`,
  `fixtures/opt135_target_guard_policy.json`, and
  `evidence/optimization/opt135-target-guard-policy/REPORT.md`.
- `tools/run_optimization_task.py` and `tests/test_optimization_loop.py`:
  opt-in schema validation only, retaining legacy behavior without the new ID.
- The two skill checklists introduced by OPT-134: link the implemented policy
  and exact validator command; keep them identical.
- `tasks/TASK-TESTING-STRATEGY.md`: one link to the new internal policy with
  explicit historical/release scope. This dossier and its ledger row/log.

No CUDA changes, production pin changes, historical fixture rewrites, global
replacement of old validators, or reclassification of OPT-130 as shipped.

## Frozen policy

Policy ID: `target_guard_v2`. Require explicit opt-in; unknown IDs fail closed.
New OPT-137 uses it. Completed historical tasks continue using their frozen
policy. Diagnostic OPT-136/138 do not perform keep admission.

1. A candidate contract lists disjoint, nonempty `targets` and `guards`, one
   primary metric per target, the dispatch region and unchanged branches,
   required quality/state gates, and all thresholds. Every required workload
   has exactly one role. Freeze and hash this contract before the first screen
   sample. Acceptance must have the same hash. Reject post-hoc role/threshold
   changes; a new contract needs a new candidate ID and fresh samples.
2. Require matching metric identities on each pair: workload/input trajectory,
   prefix, eval count, capacity, metric boundary, sampling/output policy and
   relevant engine configuration except explicitly frozen candidate differences.
   Candidate binary/selector differences are recorded, not required to equal the
   control. Non-finite or non-positive rates, missing or
   duplicated sample IDs, incomplete pairs and changed identities are errors.
3. Screen: one warmup, three paired component rounds; optional one 32-token
   engine pair. Screen only selects a survivor, never admits a keep. At most
   one candidate advances to acceptance under this task's first consumer.
4. Acceptance: three warmups and ten independent paired AB/BA rounds per
   required workload; alternate order, restore identical initial state outside
   the timed interval. Use pairs, not individual tokens, as independent
   statistical observations. No automatic extra samples until significant.
5. For each workload let `x_i = log(candidate_rate_i / control_rate_i)`.
   Compute `g = exp(mean(x))`, `se = sample_sd(x)/sqrt(10)`,
   `L = exp(mean(x) - 1.8331129326536335*se)` and
   `U = exp(mean(x) + 1.8331129326536335*se)`. These are one-sided 95% bounds
   for ten pairs/9 degrees of freedom; do not label [L,U] a two-sided 95% CI.
   For zero variance use L=U=g. Reject any other acceptance sample count for
   this version. Keep the statistical implementation small; no new dependency
   is needed for the fixed critical value.
6. Every target must satisfy `L > 1.0`. Every throughput guard must satisfy
   `L >= 0.98`; never require its CI to exclude 1. An interval [0.99,1.01]
   passes the guard; [0.97,1.03] does not establish non-regression. For every
   decode workload, per-run p95 ITL candidate/control point ratio must be
   <=1.05 in each paired round; report the complete ratios, not a pooled-token
   percentile. Zero/missing p95 on decode is invalid.
7. For decode targets use `decode_only` as primary and `complete_request` as
   an additional guard with the same 0.98 lower-bound floor. Measure prefill
   separately. For a prefill target use `prefill` as primary. Unchanged decode
   regions guard both decode-only and complete-request rates. An arbitrary
   prefill denominator must not erase a real decode gain or hide a request
   regression. Shared report labels retain actual eval and emitted counts.
8. Report `material_2pct = (g >= 1.02)` separately for each target; it is not
   a hidden keep threshold. An improvement smaller than 2% can pass with
   adequate evidence. No aggregate geometric mean can hide a failed required
   target, guard, quality or state gate. All targets must win, so this version
   does not select the best of several target results after sampling.
9. Verdicts: `keep` only when all required evidence is present and passes;
   `reject` for measured quality/state failure or a target with U<=1 or a
   throughput guard with U<0.98 or a p95 violation; `inconclusive` for valid
   intervals that establish neither acceptance nor those measured failures;
   `incomplete` for absent/invalid required evidence. Never promote an
   inconclusive/incomplete result. Retain all reasons when several gates fail;
   precedence is incomplete, reject, inconclusive, keep.
10. Output candidate rate/delta and shipping impact separately. A policy-only
    task has no shipping throughput change. A replay under changed roles is
    labeled counterfactual and cannot alter historical admission.

## Implementation and acceptance

Expose a pure `evaluate(contract, records)` function and CLI:

```sh
uv run python tools/performance_keep_policy.py --contract pins/performance_keep_policy_v2.json --self-check fixtures/opt135_target_guard_policy.json
```

The policy file contains the fixed rules; self-check fixtures contain candidate
contracts and raw paired records with independently calculated expected
decisions. OPT-137 imports the same evaluator; do not copy its formulas into
the runner. The CLI exits nonzero on a mismatched expected decision, reports
each case and writes no historical fixture.

Required tests:

- Target 1.02 and guard 1.00 with narrow CIs -> keep, even though guard L<=1.
- Guard CI crossing 1 but wholly above 0.98 -> pass that guard.
- Target win with definite guard loss below 0.98 -> reject.
- Target CI straddles 1 -> inconclusive, not measured loss or keep.
- Guard CI straddles 0.98 -> inconclusive; equality at 0.98 passes.
- Target L exactly 1 -> not keep; target all ratios 1 -> reject/no gain.
- Target g=1.019 with L>1 -> keep, material_2pct=false.
- Decode target wins but complete-request guard fails -> not keep.
- Aggregate positive with an individual required failure -> not keep.
- p95 >1.05 in any pair -> reject; boundary 1.05 passes.
- Missing quality/state, NaN, zero rate, duplicate/unpaired samples, mixed
  metrics/capacity or count other than ten -> incomplete/nonzero validation.
- Frozen-role hash mismatch and unknown policy ID -> fail closed.
- Historical OPT-130 raw ratios with D2048 target and D128/P4096 guards:
  report the counterfactual per-metric decisions that available raw records
  support. Missing required records remain missing; do not manufacture a
  complete keep from summary means. Preserve old verdict `reject` exactly.
- Existing legacy contract loading and release thresholds stay unchanged.

Run:

```sh
uv run ruff format tools/performance_keep_policy.py tests/test_performance_keep_policy.py tools/run_optimization_task.py tests/test_optimization_loop.py
uv run ruff check tools/performance_keep_policy.py tests/test_performance_keep_policy.py tools/run_optimization_task.py tests/test_optimization_loop.py
uv run pytest -q tests/test_performance_keep_policy.py tests/test_optimization_loop.py tests/test_opt130_dense_attention.py
uv run python tools/performance_keep_policy.py --contract pins/performance_keep_policy_v2.json --self-check fixtures/opt135_target_guard_policy.json
git diff --check
```

Verifier independently recomputes at least the neutral-guard, failed-guard and
inconclusive cases from raw pairs. Report policy scope and preserved historical
verdict in the new REPORT. GPU/NLL execution is N/A: this task implements host
policy, not a candidate. Done requires tested evaluator and opt-in wiring.

## Run record

Planning: skipped — pre-authored dossier admitted (2026-09-14T13:40:00Z).
Unresolved decisions: none.

Implementation (2026-09-14T13:55:56Z): host `target_guard_v2` evaluator, pin,
self-check fixture, tests, opt-in runner schema validation, identical skill
checklist links, testing-strategy scope note, and policy REPORT. No commit.
No CUDA, production pin, or historical fixture rewrite.

Files changed:
- new `pins/performance_keep_policy_v2.json`
- new `tools/performance_keep_policy.py` (`evaluate(contract, records)` + CLI)
- new `tests/test_performance_keep_policy.py`
- new `fixtures/opt135_target_guard_policy.json`
- new `evidence/optimization/opt135-target-guard-policy/REPORT.md`
- `tools/run_optimization_task.py` (opt-in schema only)
- `tests/test_optimization_loop.py` (legacy skip / unknown ID / overlap schema)
- `.agents/skills/run-ledger-task-codex/references/performance-evidence-checklist.md`
- `.cursor/skills/run-ledger-task-cursor/references/performance-evidence-checklist.md`
  (identical policy link + validator command)
- `tasks/TASK-TESTING-STRATEGY.md` (internal policy; historical/release out of scope)
- `tasks/OPT-135.md` (this run record)

Decisions:
- Frozen rules live in the policy pin; OPT-137 must import `evaluate`, not copy
  formulas. Thresholds on a candidate contract that disagree with the pin fail
  closed as a post-hoc change.
- `load_contract` remains legacy unless `keep_policy_id` /
  `performance_keep_policy_id` / nested `keep_policy.id` is present. Unknown
  IDs fail closed. Target/guard schema runs only when those fields exist.
- Per-metric decisions stay `pass`/`reject`/`inconclusive`/`incomplete`; overall
  `keep` requires every required gate to pass. Precedence is incomplete, reject,
  inconclusive, keep.
- Decode targets always require `complete_request` as an additional 0.98 guard
  and per-pair p95 ITL `<= 1.05`. Prefill guards do not use ITL.
- One-sided df=9 critical value `1.8331129326536335`; zero variance sets
  `L=U=g`. This is not the historical two-sided `2.262` gate.
- OPT-130 raw pairs replayed with D2048 as target and D128/P4096 as guards:
  available per-metric intervals pass, including a D2048 target `L>1` and
  D128 guard `L>=0.98` with an interval that contains 1. Replay is labeled
  counterfactual; preserved historical verdict is `reject`. Shipping impact 0.

Command outcomes:

```
uv run ruff format tools/performance_keep_policy.py tests/test_performance_keep_policy.py tools/run_optimization_task.py tests/test_optimization_loop.py
```

exit 0; `4 files left unchanged` (after an earlier format pass).

```
uv run ruff check tools/performance_keep_policy.py tests/test_performance_keep_policy.py tools/run_optimization_task.py tests/test_optimization_loop.py
```

exit 0; `All checks passed!`

```
uv run pytest -q tests/test_performance_keep_policy.py tests/test_optimization_loop.py tests/test_opt130_dense_attention.py
```

exit 0; `60 passed in 0.26s`

```
uv run python tools/performance_keep_policy.py --contract pins/performance_keep_policy_v2.json --self-check fixtures/opt135_target_guard_policy.json
```

exit 0; 23/23 cases `ok`, including `target_gain_neutral_guard_keep` → keep,
`target_win_guard_loss_reject` → reject, `target_ci_straddles_one` /
`guard_ci_straddles_floor` → inconclusive, `opt130_counterfactual_d2048_target`
→ reject.

```
git diff --check
```

exit 0; no whitespace errors.

GPU/NLL: N/A (host policy). Throughput delta: N/A (policy only; OPT-130 remains
rejected). Verification: PASS (2026-09-14T14:00:00Z). Delivery: complete.

## Verification

Verifier: integration subagent (2026-09-14T14:00:00Z). **Verdict: PASS.**

### Acceptance commands

All dossier acceptance commands rerun from `/home/user/qw38`:

| Command | Exit | Result |
|---------|------|--------|
| `uv run ruff format tools/performance_keep_policy.py tests/test_performance_keep_policy.py tools/run_optimization_task.py tests/test_optimization_loop.py` | 0 | `4 files left unchanged` |
| `uv run ruff check tools/performance_keep_policy.py tests/test_performance_keep_policy.py tools/run_optimization_task.py tests/test_optimization_loop.py` | 0 | `All checks passed!` |
| `uv run pytest -q tests/test_performance_keep_policy.py tests/test_optimization_loop.py tests/test_opt130_dense_attention.py` | 0 | `60 passed in 0.26s` |
| `uv run python tools/performance_keep_policy.py --contract pins/performance_keep_policy_v2.json --self-check fixtures/opt135_target_guard_policy.json` | 0 | `23/23` cases `ok` |
| `git diff --check` | 0 | no whitespace errors |

### Independent recomputation (raw pairs)

Recomputed from fixture `records.pairs` using dossier formulas
(`x_i = log(candidate/control)`, `g = exp(mean(x))`,
`se = sample_sd(x)/sqrt(10)`, `L,U = exp(mean(x) ± 1.8331129326536335·se)`;
zero variance → `L=U=g`). Metric rules: target pass when `L>1`; guard pass when
`L>=0.98`; reject when target `U<=1` or guard `U<0.98`; else inconclusive.

| Case | Key metric | Ratios (10 pairs) | g | L | U | Metric decision | Overall |
|------|------------|-------------------|---|---|---|-----------------|---------|
| `target_gain_neutral_guard_keep` | d2048.decode_only (target) | all 1.02 | 1.020000 | 1.020000 | 1.020000 | pass | **keep** |
| | d128.decode_only (guard) | all 1.00 | 1.000000 | 1.000000 | 1.000000 | pass (neutral) | |
| `target_win_guard_loss_reject` | d2048.decode_only (target) | all 1.05 | 1.050000 | 1.050000 | 1.050000 | pass | **reject** |
| | d128.decode_only (guard) | all 0.97 | 0.970000 | 0.970000 | 0.970000 | reject (`U<0.98`) | |
| `target_ci_straddles_one` | d2048.decode_only (target) | five 0.99, five 1.01 | 0.999950 | 0.993858 | 1.006079 | inconclusive (straddles 1) | **inconclusive** |
| `guard_ci_straddles_floor` | d128.decode_only (guard) | five 0.97, five 0.99 | 0.979949 | 0.973858 | 0.986078 | inconclusive (straddles 0.98) | **inconclusive** |

Recomputed `g/L/U` and per-metric decisions match `evaluate()` output and fixture
`expected.verdict` for all four cases.

### Scope and preserved historical verdict

- **CUDA:** no `.cu`/`.cuh` diff (`git diff HEAD -- '*.cu' '*.cuh'` → 0 lines).
- **Production pins:** no modifications to existing `pins/*.json`; only new
  `pins/performance_keep_policy_v2.json` (untracked addition).
- **OPT-130 fixture:** `fixtures/opt130_dense_attention.json` unchanged
  (`git diff HEAD` → 0 lines); `verdict` remains `"reject"`.
- **OPT-130 historical verdict:** counterfactual case
  `opt130_counterfactual_d2048_target` self-checks `reject`; evaluator preserves
  `historical_verdict: reject` under `counterfactual: true`.
- **Legacy behavior:** `tools/run_optimization_task.py` adds opt-in
  `validate_opt_in_keep_policy` only; contracts without a keep-policy ID are
  unaffected. `tests/test_opt130_dense_attention.py` still passes (60/60).

### Skill checklist links

```sh
diff -u .agents/skills/run-ledger-task-codex/references/performance-evidence-checklist.md \
        .cursor/skills/run-ledger-task-cursor/references/performance-evidence-checklist.md
```

Exit 0; no output (files identical). Both link
[`pins/performance_keep_policy_v2.json`](../pins/performance_keep_policy_v2.json)
and the same validator command:

```sh
uv run python tools/performance_keep_policy.py --contract pins/performance_keep_policy_v2.json --self-check fixtures/opt135_target_guard_policy.json
```

### REPORT scope note

`evidence/optimization/opt135-target-guard-policy/REPORT.md` documents policy-only
scope, counterfactual OPT-130 replay, preserved `reject` admission, and unchanged
release gates (OPT-016/OPT-056/CMP-003).

### Delivery (2026-09-14T13:58:00Z)

model: composer-2.5, telemetry_unavailable

OPT-135 marked `done`. Coupled IDs: none. No CUDA, production pin, or historical
fixture change; prospective internal admission policy only.

### Final outcome

- Status: `done`
- Acceptance evidence: [`pins/performance_keep_policy_v2.json`](../pins/performance_keep_policy_v2.json);
  [`tools/performance_keep_policy.py`](../tools/performance_keep_policy.py);
  [`fixtures/opt135_target_guard_policy.json`](../fixtures/opt135_target_guard_policy.json);
  [`tests/test_performance_keep_policy.py`](../tests/test_performance_keep_policy.py);
  [`evidence/optimization/opt135-target-guard-policy/REPORT.md`](../evidence/optimization/opt135-target-guard-policy/REPORT.md);
  verification record above
- Throughput delta: **N/A** (policy only; OPT-130 remains rejected)
- First-pass acceptance: yes (2026-09-14T14:00:00Z)
- Remaining risk: OPT-137 must import `evaluate()` rather than copy formulas;
  legacy contracts without a keep-policy ID remain on historical gates
