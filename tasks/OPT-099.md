# OPT-099 — Close matched engine attribution blind spots

Status: done. Dependencies: OPT-098. Coupled IDs: none. Primary target: D2048 diagnostics.
Read [the post-098 protocol](PERFORMANCE-RECOVERY-POST-098.md).

## Run record

### Planning (2026-09-12T13:50:00Z)

skipped — pre-authored dossier admitted (coordinator)

### Implementation (2026-09-12T14:22:00Z)

Diagnostic tooling only. No commit. Production selectors/kernels/quality policy
unchanged. `claims_throughput=false`. `post098_selected` freeze matches compiled
pins. Llama family mapping no longer fills missing families with zero; FFN down
is first-class (`ffn_out` / `ffn_down.weight`). Quartz enclosing `ffn_mmv` /
`mixer_mmv` parents are dropped when leaves exist.

Files created:
- `pins/opt099_matched_attribution_contract.json`
- `pins/opt099_iteration_contract.json`
- `tools/opt099_matched_attribution.py`
- `tests/test_opt099_matched_attribution.py`
- `fixtures/opt099_matched_attribution.json`
- `evidence/optimization/opt099-matched-attribution/REPORT.md` plus quartz/llama sidecars

Files changed:
- `Makefile` (`cuda-opt099-diagnostics`)
- `tools/run_optimization_task.py` (`QW38_OPT099_NATIVE_COUNTS=`, `QW38_OPT099_RESULT=`, `QW38_OPT099_ATTRIBUTION_RESULT=`)
- `cuda/opt060_engine_attribution_test.cu` (`post098_selected`, `--evidence-dir`, dual OPT-090/OPT-099 prefixes)
- `tools/llama_authority/engine_attribution.cpp` (`--evidence-dir`, pinned-llama JSON dumps, OPT-099 prefix)
- `tools/llama_authority/patches/qw38_opt060_attribution.cuh` (layer suffix `-N`, `weight_name`/`op`, FFN down, FLASH_ATTN before Qcur, attn_output by tensor name)
- `implementation_ledger.md` (status `in_progress`)

Host validation:
```
uv run ruff format tools/opt099_matched_attribution.py tests/test_opt099_matched_attribution.py
2 files left unchanged

uv run ruff check tools/opt099_matched_attribution.py tests/test_opt099_matched_attribution.py tools/run_optimization_task.py
All checks passed!

uv run pytest -q tests/test_opt099_matched_attribution.py tests/test_opt071_attribution_repair.py
.......................                                                  [100%]
23 passed in 0.17s
```

Host freeze / conservation / report (no GPU lock):
```
uv run python tools/opt099_matched_attribution.py --phase freeze --mode feedback --run-dir build/optimization-runs/OPT-099/feedback/host-freeze
QW38_OPT099_RESULT={...,"phase":"freeze",...,"claims_throughput":false,...}  exit 0

uv run python tools/opt099_matched_attribution.py --phase conservation --mode feedback --run-dir build/optimization-runs/OPT-099/feedback/host-conservation
QW38_OPT099_RESULT={...,"phase":"conservation",...,"claims_throughput":false,...}  exit 0

uv run python tools/opt099_matched_attribution.py --phase report --mode acceptance --run-dir build/optimization-runs/OPT-099/feedback/host-report
QW38_OPT099_RESULT={...,"phase":"report",...,"claims_throughput":false,...}  exit 0
```
Freeze `ok=true` mismatches={}. Conservation `ffn_down_first_class=true`.

GPU dossier:
```
uv run python tools/run_optimization_task.py --task OPT-099 --mode feedback --phase freeze
status=failed result_class=setup_failed
message=foreign GPU compute process present; 235855 python; not killing foreign work
run_dir=build/optimization-runs/OPT-099/feedback/20260912T140454Z-591908bc
EXIT:1

# later, GPU free; llama adapter -Werror=address on name[] (fixed in overlay)
uv run python tools/run_optimization_task.py --task OPT-099 --mode feedback --phase freeze
status=failed result_class=setup_failed
message=setup command failed (1): bash tools/llama_authority/build_opt060_instrumented.sh
  qw38_opt060_attribution.cuh:350: error: the address of 'ggml_tensor::name' will never be NULL [-Werror=address]
run_dir=build/optimization-runs/OPT-099/feedback/20260912T140907Z-02948686
EXIT:1

# overlay name[0] check; llama rebuild succeeded; D128 sitting
uv run python tools/run_optimization_task.py --task OPT-099 --mode feedback --phase d128
status=passed success=true elapsed_s=217.838 deadline_s=900
run_dir=build/optimization-runs/OPT-099/feedback/20260912T141141Z-50354552
compile_cold_s=6.083 d128_cold_s=126.614
```

D128 sitting outcome (not a keep; `claims_throughput=false`):
- Quartz `post098_selected` call counts matched (ffn_gate_up_glu/ffn_down=2048, gdn=1536, attention=512, logits=32). Three measured windows valid. graph ITL p95≈18.05 ms/token. FFN down first-class (~87 ms / 32-token window).
- Pinned llama dumps parsed (`llama-pinned_llama-decode-rep{0,1,2}-records.json`); walls from unperturbed+eager logs (uninstrumented≈468 ms, not zero-filled).
- Llama call counts failed on that sitting: `attention_core` 0 vs 512, `q6_attention_output` 2048 vs 512. Cause: `FLASH_ATTN_EXT` haystack contained `Qcur` so it was labeled q8 input; GDN `RMS_NORM` src `attn_output-N` was labeled q6. Overlay now checks FLASH/ROPE ops before Qcur and requires tensor name `attn_output*` / `attn_output.weight` for q6.
- Ranking `incomplete`, `llama_ms=null` (not 0), triggers no-go `gap_attribution_incomplete`.

```
bash tools/llama_authority/build_opt060_instrumented.sh
OPT-060 instrumented llama ready: .cache/authorities/llama-build-opt060/bin/qw38-llama-engine-attribution
BUILD_EXIT:0
```

Re-sit after mapping fix:
```
uv run python tools/run_optimization_task.py --task OPT-099 --mode feedback --phase d128
status=failed result_class=setup_failed
message=foreign GPU compute process present; 380943 python; not killing foreign work
run_dir=build/optimization-runs/OPT-099/feedback/20260912T141838Z-62d4dde9
EXIT:1
```
D2048, counters, and separate-process P4096 were not executed: GPU remained occupied by foreign python compute. Instrumented llama binary is rebuilt with the repaired mapping and is ready for those sittings.

Blockers:
- Matched llama ranking and OPT-100–105 go/no-go remain incomplete until D128 is re-sat with the repaired overlay and D2048/P4096/ncu run exclusive.
- Nsight Compute counters not collected (`ncu_available` unset).
- P4096 attribution invalid / unmeasured (`opt105` reason `invalid_p4096_attribution`).

### Implementation repair (2026-09-12T15:00:00Z)

P4096 llama sample 0 unexplained_share=0.503 because unperturbed wall included first-sample CUDA graph capture (2462.64 ms vs eager 1253.25 ms). Diagnostic-only repair; no production kernel change; no commit.

Files changed:
- `tools/opt099_matched_attribution.py` (`llama_graph_create_excluded_ms`; pass `graph_create_ms` into `accounting_split` for pinned llama)
- `tools/llama_authority/engine_attribution.cpp` (untimed prefill graph capture + `cudaDeviceSynchronize` after warmups, before measured samples)
- `tests/test_opt099_matched_attribution.py` (host coverage for capture-contaminated wall)

```
bash tools/llama_authority/build_opt060_instrumented.sh
OPT-060 adapter source is newer than the binary; rebuilding adapter only
OPT-060 instrumented llama ready: .cache/authorities/llama-build-opt060/bin/qw38-llama-engine-attribution
BUILD_EXIT:0

QW38_CUDA_TEST_TIER=screen uv run python tools/opt099_matched_attribution.py --phase p4096 --mode feedback --run-dir build/optimization-runs/OPT-099/feedback/repair-p4096
QW38_OPT099_RESULT={...,"phase":"p4096",...,"observed_samples":3,...}  exit 0  elapsed_s≈102.5

uv run python tools/opt099_matched_attribution.py --phase report --mode acceptance --run-dir build/optimization-runs/OPT-099/acceptance/repair-report
QW38_OPT099_RESULT={...,"phase":"report",...}  exit 0

uv run pytest -q tests/test_opt099_matched_attribution.py tests/test_opt071_attribution_repair.py
........................  24 passed in 0.31s
```

Re-sit outcome:
- Unperturbed sample 0 wall 1246.06 ms (was 2462.64); log `llama_graph_create_excluded=true window=pre_sample`.
- P4096 `attribution_valid=true`. Llama unexplained_share sample0=0.0126, sample1=0.0215, sample2=0.0196 (all ≤0.05). Quartz windows 0.0.
- Report `evidence_complete=true`. `opt105_mmq_x2` reason `excess_below_trigger` (no longer `invalid_p4096_attribution`).
- Host tests 24 passed. `claims_throughput=false`.

### Verification (2026-09-12T14:53:00Z)

Independent integration verifier. No semantic fixes applied.

```
uv run ruff format .
224 files left unchanged
exit 0

uv run ruff check tools/opt099_matched_attribution.py tests/test_opt099_matched_attribution.py tools/run_optimization_task.py
All checks passed!
exit 0

uv run pytest -q tests/test_opt099_matched_attribution.py tests/test_opt071_attribution_repair.py tests/test_opt090_decode_attribution.py
......................................                                   [100%]
38 passed in 0.33s
exit 0

uv run python tools/opt099_matched_attribution.py --phase report --mode acceptance --run-dir build/optimization-runs/OPT-099/acceptance/verify-report
QW38_OPT099_RESULT={"acceptance_executed": false, "claims_throughput": false, "keep": false, "mode": "acceptance", "observed_candidates": 1, "observed_samples": 1, "observed_shapes": 1, "observed_tier": "correctness", "observed_warmups": 0, "pairs": 1, "phase": "report", "sample_ids": [0], "samples": 1, "task": "OPT-099", "warmups": 0}
QW38_OPT099_NATIVE_COUNTS={...same...}
exit 0
```

Fixture inspection (`fixtures/opt099_matched_attribution.json`):
- `evidence_complete=true`
- `gap_attribution_complete=true`
- `ffn_down_first_class=true`
- `d128.attribution_valid=true`, `d2048.attribution_valid=true`, `p4096.attribution_valid=true`
- `triggers` present for `opt100_q8_aligned` … `opt105_mmq_x2`
- max `unexplained_share`: d128=0.0, d2048=0.0, p4096=0.0196 (≤0.05)

Acceptance trace (claims → executed assertions/artifacts):
- Freeze/conservation/report host-only: `test_conservation_freeze_and_report_are_host_only`, `test_freeze_matches_compiled_selectors`
- FFN down first-class / parent exclusion: `test_ffn_down_is_first_class_not_unmapped`, `test_mixer_parent_is_not_unmapped_mul_mat`, `test_llama_mapping_distinguishes_ffn_down_and_layers`
- No zero-filled llama / skip-GPU honesty: `test_missing_llama_is_null_not_zero`, `test_skip_gpu_decode_does_not_invent_llama_zeros`, `test_llama_logs_and_walls_are_parsed_not_zero_filled`
- P4096 graph-capture exclusion: `test_llama_prefill_graph_capture_is_excluded_from_measured_wall`
- Contracts/runner/native hooks: `test_contracts_makefile_and_native_hooks`, `test_opt099_result_prefix_is_parsed`
- GPU evidence: `evidence/optimization/opt099-matched-attribution/` (quartz/llama record JSON, logs, REPORT.md); d128/d2048 stdout shows `prefix` 128/2048; p4096 `separate_process=true`
- Counters: `ncu_available=true`, `full_ncu_sweep=false`, `sequential_gdn` launch `ok=false` (`launch_failed_use_event_evidence`)

Diff/dossier/plan boundaries:
- Diagnostic-only scope respected (`claims_throughput=false`; no production selector/kernel edits in diff)
- Deliverables present: contracts, tool, tests, fixture, evidence, Makefile target, runner prefixes, native overlay/adapter hooks
- Minor side effect: `fixtures/opt090_decode_attribution.json` timestamp-only drift from report write (non-semantic)

**Verdict: PASS** (integration verification). Residual notes (non-blocking): `sequential_gdn` NCU launch failed (`full_ncu_sweep=false`); `opt101_gdn_transpose` / `opt103_attention_vec` triggers `go=false` (`llama_family_unmapped` — aggregate `gdn_core` and attention alias families not fully llama-covered).

### Delivery (2026-09-12T14:54:13Z)

Delivery authorized via `/run-ledger-task-cursor`. Marked OPT-099 `done`.
Coupled IDs: none. Production selectors/kernels unchanged.

### Final outcome

`diagnostic_delivered`: matched Quartz-versus-pinned-llama attribution for D128,
D2048 and P4096 with `post098_selected` freeze, FFN down first-class, parent/leaf
exclusion, and no zero-filled llama families. All three shapes `attribution_valid=true`;
max unexplained_share ≤0.0196; P4096 llama graph-capture excluded from measured walls.
`evidence_complete=true`; `gap_attribution_complete=true`; go/no-go triggers for
OPT-100–105 published. `claims_throughput=false`; no production kernel or selector change.

**tok/s delta vs OPT-098 P4096 baseline (3046.23 tok/s): 0** (unchanged).

Evidence: [`fixtures/opt099_matched_attribution.json`](fixtures/opt099_matched_attribution.json);
[`evidence/optimization/opt099-matched-attribution/REPORT.md`](evidence/optimization/opt099-matched-attribution/REPORT.md).

**Commit:** delivered 2026-09-12T14:54:13Z
**Push:** origin/main (authorized)

## Outcome


Produce one current, matched Quartz-versus-pinned-llama attribution for D128,
D2048 and P4096 that can explain at least 95% of each measured GPU window and
assign nonzero llama time to every family the authority actually executes. This
task changes diagnostic tooling only and makes no throughput claim.

## Implementation

1. Freeze `post098_selected` from actual compiled selectors: Q4
   `integer_q8_late`/four warps, FFN `paired_integer`, Q8
   `grouped_r1_w4`, Q6 `integer_q8_1`/two warps, GDN `sequential`, attention
   `warp_query`, prompt MMQ `i128_j128_fma_async_x` with joined waits, and
   `ffn_only` graphs. Fail if requested and observed selectors differ.
2. Repair the pinned-llama diagnostic patch so CUDA operator/kernel ranges map
   to the same semantic families as Quartz. At minimum distinguish FFN
   gate/up/GLU from FFN down, Q8 input projections from mixer outputs, GDN
   convolution/norm/recurrence/gate, attention query prep/core/merge, Q6
   attention output, final Q6 logits, pointwise/state work, copies and unhidden
   idle. Record actual llama dispatch identities; never fill missing values with
   zero and call the comparison complete.
3. Run 3 warmup plus 3 measured 32-token windows after fixed D128 and D2048
   prefixes for each engine. Run 3 warmup plus 3 P4096 samples in a separate
   process/sitting. Uninstrumented wall/ITL/p95 and eager CUDA-event traces are
   separate records; prefix/load/graph construction/restoration stay outside
   measured windows.
4. Use event unions and explicit parent/leaf exclusion. Validate per-token call
   counts from graph/operator structure, not hard-coded tensor offsets. Report
   family sum, union, overlap, wall, idle and instrumentation overhead. Families
   remain non-additive.
5. Collect one targeted Nsight Compute record for the hottest Quartz and llama
   kernel in Q4 gate/up, FFN down, Q8 input, GDN and attention when the metrics
   are available. Record DRAM bytes/rate, L2 hit rate, issue/active cycles,
   registers, spills, occupancy and dominant warp stalls. Explicitly record
   unavailable metrics; event timing is not a substitute for counters.
6. Publish matched excess and an upper bound on removable Quartz time per
   family. Emit go/no-go trigger fields consumed by OPT-100 through OPT-105.

## Acceptance

- D128, D2048 and P4096 each have valid call counts, no dropped events, at most
  5% unexplained wall and measured instrumentation overhead.
- Pinned llama coverage is nonzero for executed families; unsupported mappings
  are `unknown` and make `gap_attribution_complete=false`.
- FFN down is a first-class family rather than `unmapped_mul_mat`.
- Raw timelines and machine-readable samples reproduce the ranking. No replay
  time is added to engine time and no speedup/keep is claimed.
- Add the task contract/fixture, runner, native diagnostic, host tests and
  `evidence/optimization/opt099-matched-attribution/`.

## Non-goals

No production selector, kernel, quality policy, historical fixture or outcome
gate changes. Do not benchmark current `../llama.cpp` HEAD as the authority.

