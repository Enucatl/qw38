# OPT-038 — Refresh post-ladder attribution and source gap map

## Control

- Primary ID: OPT-038
- Coupled IDs: none
- Dependencies: OPT-032, OPT-034, OPT-035, OPT-036, OPT-037 (done)
- Status: pending
- Ledger acceptance: Retain fresh frozen-protocol P/D128/D2048 measurements, current subsystem breakdowns, independent raw host-wall accounting, pinned llama.cpp comparisons and matched-component experiment specifications, and a reproducible next-task order; `claims_performance_improvement: false`; accepted keep denominators remain unchanged.

## Goal and boundaries

Measurement-only attribution and source comparison. Preserve the frozen OPT-021
and OPT-032 protocols, old fixtures, production kernels, public schemas,
`plan.md`, compiler flags, allocation formula, and OPT-016 ownership. Do not run
nsys/ncu or 128K quality. Implementation classification: CUDA/numeric
diagnostic, not mechanical. Plan impact: none. No unresolved decisions.

## Repository evidence

- `fixtures/opt034_packed_mmv.json` — frozen denominators P 1869.84412, D128 25.3816128, D2048 20.169548 and p95s.
- `tasks/OPT-021.md`, `tasks/OPT-032.md` — exact P/D protocol.
- `cuda/full_scheduler.cu:450-515` — attribution wall adjustment caveat.
- `cuda/full_scheduler.cu:636-760`, `cuda/attention_decode.cu:693-850`, `cuda/gdn_fused_quality.cuh:100-175` — current sinks.
- `.cache/authorities/llama.cpp/ggml/src/ggml-cuda/{mmq.cuh,mmvq.cu,vecdotq.cuh,fattn-vec.cuh,fattn-mma-f16.cuh,gated_delta_net.cu}` — pinned authority.

## Implementation decisions

Add a test-owned diagnostic, contract, fixture, report, raw sidecars, and host
validator under `evidence/optimization/opt038-post-ladder-gap/`. Reuse existing
oracle executables. Use three P cold runs, and D128/D2048 with three warmups and
30 measured runs of 256 tokens, exact deterministic token IDs, exclusive RTX
5090, pinned GGUF/toolchain, graphs outside timing, null attribution on oracle
runs, and full host wall including D2H/commit. Retain warmups and all samples.

Run separate diagnostic attribution passes for P and one decode token. Store raw
host wall, GPU-event sum, graph-host interval, adjusted reconstruction, and
component intervals as different fields. Do not modify `finalize_decode_attribution`.
Write a component protocol comparing real Quartz inputs and pinned llama.cpp
entry points for Q4_K FFN, Q8_0 mixer, Q6_K output, attention, and GDN; label
unavailable dispatch facts as unavailable. `claims_performance_improvement` is
false and no successor oracle is published.

## Acceptance and validation

Required artifacts: `pins/opt038_post_ladder_gap_contract.json`,
`fixtures/opt038_post_ladder_gap.json`, `tests/test_opt038_post_ladder_gap.py`,
`evidence/optimization/opt038-post-ladder-gap/REPORT.md`, and
`COMPONENT-PROTOCOL.md`. Focused command:
`uv run pytest -q tests/test_opt038_post_ladder_gap.py tests/test_documentation.py`.
Hardware command:
`QW38_RUN_CUDA_TESTS=1 uv run pytest -q tests/test_opt038_post_ladder_gap.py`.
Repository checks: `uv run ruff check .` and `uv run pytest -q`. Update only
linked handbook/audit/source documentation. Definition of done is reproducible
evidence with honest raw-wall accounting and a host-recomputed order.

## Run record

Planning proposed 2026-09-10; implementation, documentation, and verification
not run. No commit or push in the task record; this dossier admission is the
outer planning commit.
