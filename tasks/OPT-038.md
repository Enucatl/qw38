# OPT-038 — Refresh post-ladder attribution and source gap map

## Control

- Primary ID: `OPT-038`
- Coupled IDs: `none`
- Dependencies: `OPT-032`, `OPT-034`, `OPT-035`, `OPT-036`, `OPT-037`; all done at planning admission
- Status: `done`
- Ledger acceptance: Retain fresh frozen-protocol P/D128/D2048 measurements, current subsystem breakdowns, independent raw host-wall accounting, pinned llama.cpp comparisons and matched-component experiment specifications, and a reproducible next-task order; `claims_performance_improvement: false`; accepted keep denominators remain unchanged

## Goal and boundaries

Refresh the post-ladder exclusive attribution and source-gap map on current production objects after the OPT-033–OPT-037 ladder. Preserve the frozen OPT-021 P protocol and OPT-032 D128/D2048 protocol, reuse those oracle executables, emit exclusive CUDA-event subsystem breakdowns plus independent raw host-wall fields, match pinned llama.cpp on the same sitting, write matched-component experiment specifications, and record a host-recomputed next-task order over the pending transferable IDs. This increment does not install a kernel, does not publish a successor keep oracle, and does not claim a tok/s improvement versus [`fixtures/opt034_packed_mmv.json`](../fixtures/opt034_packed_mmv.json).

- Constraints: keep C++17, CUDA 13.0.2, `sm_120`, Makefile `NVCCFLAGS --fmad=false` and host `-ffp-contract=off` unchanged, the pinned Qwen3.8-27B Q4_K_M GGUF SHA-256 `31629f53165ab6a7dad8c9847dcfd1fdf55829dac1e6e748f4a68581b0033d34`, BF16 activation/KV storage, FP32 residual and GDN recurrence, production `GdnConfig{16, 48, 128, 128, 4}`, GDN-002 `kScanWindow = 64`, OPT-008 `kPromptChunkRows = 4096`, workspace byte formula `160,380,416 + 96*C + 404,992*R`, OPT-011 fused overlapped production plus retained unfused serial path, OPT-012 prompt FFN graphs (replay when `token_count == 4096`), OPT-003 decode FFN graphs, OPT-017/022/023 mixer Q8 quality plus skinny α/β, OPT-018/025 FFN quality plus shared-Y, OPT-019/026/033/035 warp-column GDN and fattn stream-K / register VKQ / P×V MMA, OPT-034 packed Q4_K/Q6_K MMV loads, OPT-036 16/16 decode KV partitions, SES-002 candidate-versus-committed isolation, Engine session capacity `131072`, and all frozen SCH-002/OPT-008/OPT-009/OPT-011/OPT-012/OPT-013/OPT-014/CUD-001/CUD-002/GDN-002/ATN-001/ATN-002/OPT-005 numeric and exact-state gates. Exclusive RTX 5090 work stays serialized. Do not add a persistent session or workspace `cudaMalloc`. Do not edit `Makefile` `NVCCFLAGS`; build Quartz diagnostics with the same explicit pinned `nvcc` command pattern as `tests/test_opt032_decode_oracle.py`. Do not run `nsys` or `ncu`. Do not edit `plan.md`. Do not change public Engine, CLI, server, or `qw38-bench` JSON schemas. Do not rewrite `RuntimeTimings` composite field names. Do not modify `finish_decode_attribution` or `finish_prefill_attribution`. Do not overwrite OPT-014/016/020/021/026/030/032/034/035/036/037 pins, fixtures, or evidence. Do not reopen rejected OPT-024, OPT-027, OPT-028, OPT-029, OPT-030, or OPT-037. Do not couple or start OPT-031 or OPT-039–OPT-042.
- Non-goals: any production kernel, dispatch, tile, graph-capture, or keep/reject change; publishing a successor oracle; claiming Quartz P/D128/D2048 ≥ llama.cpp; the OPT-016 2K tok/s ≥ llama.cpp gate; substituting for CMP-002/CMP-003; expanding BEN-001 `qw38-bench` schemas; splitting public `RuntimeTimings` / `component_probe`; 8K/32K/128K throughput; QLT-001 128K quality; a new EDU task; editing `plan.md`; treating `llama-bench -p 0 -n 256 -d 2048` as the decode denominator; using BEN-001 smoke as the oracle; mutating historical OPT-032 attribution JSON printers; raising attributed wall inside `finish_decode_attribution` a second time.
- Plan impact: `none`
- Affected interfaces: no public Engine, CLI, server, model, or BEN-001 JSON API changes. CUDA-internal `PrefillAttribution` / `DecodeAttribution` and `finish_*_attribution` stay as OPT-032 left them. New schema-1 OPT-038 contract, fixture, attribution diagnostics, host test, report, raw sidecars, and `COMPONENT-PROTOCOL.md` are task evidence, not a public protocol.
- Implementation classification: not mechanical. This is a CUDA/numeric diagnostic that designs new acceptance evidence (post-ladder raw-wall fields, matched-component specs, and a measured next-task-order rule). Use the CUDA/numeric implementation agent.
- Documentation stage: required. Update the existing optimization, timing, benchmark-harness, and full-prefill handbook chapters plus `docs/sources.md`, `docs/README.md`, and one non-final then final OPT-038 audit row. Implementation writes the measured report and component protocol from the live JSON so host validators can pass; documentation links those files and must not rewrite live numbers. Do not add an EDU task. The audit-table row must not contain other task IDs. Because `tests/test_documentation.py` requires a non-final audit marker for every `in_progress` non-EDU task, planning inserts that row with this dossier.

## Repository evidence

- `implementation_ledger.md:107` — OPT-038 occurs exactly once; status at planning admission is `pending`; dependencies OPT-032, OPT-034, OPT-035, OPT-036, OPT-037; acceptance is fresh frozen-protocol P/D128/D2048 measurements, current subsystem breakdowns, independent raw host-wall accounting, pinned llama.cpp comparisons and matched-component experiment specifications, and a reproducible next-task order; `claims_performance_improvement: false`; accepted keep denominators remain unchanged.
- `implementation_ledger.md:101-106` — dependencies OPT-032 and OPT-034–OPT-037 are `done`.
- `implementation_ledger.md:85` — OPT-016 is `blocked` and remains the sole ≥ llama.cpp **2K** gate; do not couple it and do not re-assert that gate.
- `implementation_ledger.md:100` — OPT-031 is `pending` and separable; do not couple it and do not insert it into `next_task_order`.
- `implementation_ledger.md:108-111` — OPT-039–OPT-042 are pending ideas that depend on this refresh. Do not couple them. Do not start their kernels or the integer-MMV diagnostic here. Record only their host-recomputed order.
- `implementation_ledger.md:118` in the QLT table — QLT-001 is `blocked` on OPT-016; it is not pending and must not be coupled.
- `fixtures/opt034_packed_mmv.json` — accepted keep denominators that this increment must copy and leave unchanged: Quartz P mean_tok_s **1869.84412**, D128 **25.3816128**, D2048 **20.169548**; D128 all-token p95 **39.9736366** / run-mean p95 **39.4155655**; D2048 all-token p95 **50.2872772** / run-mean p95 **49.590683**. Same-sitting llama.cpp P `avg_ts` **3221.626709**; D128 mean **68.8033962**; D2048 mean **67.1097554**. `selected_mmv_load_path` `packed`; `reverted` false.
- `fixtures/opt037_ffn_tiles.json` / `evidence/optimization/opt037-ffn-tiles/REJECTION.md` — OPT-037 rejected every I/J candidate; production remains I=128/J=128; keep denominators did not move.
- `fixtures/opt036_decode_kv_partition.json` — production decode attention is 16/16 partitions. One-layer D2048 component mean fell from **12.2984858** ms (1-part) to **0.804042637** ms (16-part). OPT-032's full-token D2048 `attention_core` **201.241058** ms predates this keep and is stale.
- `fixtures/opt032_decode_oracle.json` (`measurement_utc` 2026-09-09T15:07:45Z) — last exclusive sitting, **before** packed MMV and 16-way decode partitions. Stale sink ranking this increment must replace:

  Prefill 4K exclusive ms (wall **2519.10303**, `prompt_graph_launches` 64):

  | Rank | Category | ms | share of attributed wall |
  |---|---|---|---|
  | 1 | `attention_core` | 878.562744 | 34.9% |
  | 2 | `ffn_mmq` | 764.337463 | 30.3% |
  | 3 | `gdn_core` | 577.201111 | 22.9% |
  | 4 | `mixer_mmq` | 295.14798 | 11.7% |

  Decode D2048 exclusive ms (attributed/adjusted wall **259.987396**; `other_idle` 0 because `finish_decode_attribution` raised wall when host graph time overlapped CUDA events):

  | Rank | Category | ms |
  |---|---|---|
  | 1 | `attention_core` | 201.241058 |
  | 2 | `ffn_mmv` | 41.7385025 |
  | 3 | `mixer_mmv` | 11.8714857 |
  | 4 | `logits` | 2.52643204 |
  | 5 | `gdn_core` | 2.06646395 |

  Decode D128: `ffn_mmv` 41.6916847, `attention_core` 13.3384638, `mixer_mmv` 11.7923508, `gdn_core` 2.05078387. Stale P Quartz **1637.58594** vs llama.cpp **3139.909678**; D128 **11.8731956** vs **68.506172**; D2048 **3.70951414** vs **66.9333082**.
- `evidence/optimization/speedup-loop-post037/GAP-ANALYSIS.md` — source-only post-OPT-037 map (no benchmark). Estimated gaps versus OPT-032 llama.cpp and OPT-034 Quartz denominators: P **1.68×**, D128 **2.70×**, D2048 **3.32×**. Recomputed from checked-in fixtures: P `3139.909678 / 1869.84412 = 1.679`; D128 `68.8033962 / 25.3816128 = 2.711`; D2048 `67.1097554 / 20.169548 = 3.327`. Ranked remaining transferable ideas: warp-owned decode attention; hoisted GDN Q/K inverse norms; warp-owned prompt QK microtiles; integer decode-MMV study. Prompt FFN already uses quality MMA; OPT-037 proved another I/J sweep is not justified.
- `cuda/full_scheduler.cu:474-514` — `finish_decode_attribution` records host wall, then **raises** `attribution->wall` to `cuda_sum + graph` when exclusive exceeds wall. `other_idle` becomes 0 after that raise. Independent raw host wall is not a separate field today. Do not change this function.
- `cuda/full_scheduler.cu:434-471` — `finish_prefill_attribution` fail-closes if exclusive exceeds wall; it does not raise wall.
- `cuda/full_scheduler.cu:1966-1981` — decode `graph` is host `steady_clock` around `cudaGraphLaunch`, accumulated into `DecodeAttribution.graph`. That host interval can overlap GPU CUDA-event categories.
- `cuda/full_scheduler.cu:636-752` — current prompt FFN is shared-Y Q8_1 plus quality MMA I=128/J=128; decode FFN is three separate `matrix_vector` launches plus SwiGLU.
- `cuda/attention_decode.cu:693-818` — `partitioned_grouped_decode_attention` still loops each KV row and query member through shared scratch, lane-zero fold, and repeated `__syncthreads` after the OPT-036 16-way partition keep.
- `cuda/fattn_mma_f16.cuh:360-432` — prompt QK microtiles still compute `cfrag` across warps, store `cparts`, synchronize, and let warp zero merge.
- `cuda/gdn_fused_quality.cuh:137-163` — `prepare_recurrence_fused_warp_column` recomputes Q/K square sums and inverse norms inside every value-column warp and token.
- `cuda/decode_oracle_test.cu`, `cuda/prefill_4k_oracle_test.cu`, `tools/llama_authority/decode_oracle.cpp` — frozen throughput oracles. Reuse them. Do not change `task` fields (`OPT-032` / `OPT-021`).
- `cuda/decode_attribution_test.cu`, `cuda/prefill_4k_attribution_test.cu` — OPT-032 printers emit adjusted `wall_ms` only and hard-code `"task":"OPT-032"`. Do not mutate them. New OPT-038 diagnostics own the raw-wall fields.
- `.cache/authorities/llama.cpp/ggml/src/ggml-cuda/{mmq.cuh,mmvq.cu,vecdotq.cuh,fattn-vec.cuh,fattn-mma-f16.cuh,gated_delta_net.cu}` and `.cache/authorities/llama.cpp/src/models/qwen35.cpp` — pinned llama.cpp revision `cc83d7b4824f73cfdda4dfbb47ee39804f71b328`.
- `src/scheduler.h:12` — `kVocabularySize = 248320`. Token IDs are `(42 + index * 997) % 248320`.
- `src/mixer.h` — `kFfnWidth = 17408`. Residual width is 5120.
- `tests/test_documentation.py:83-97` — an in-progress non-EDU task needs a visibly non-final audit row; `_audit_rows` keys every task-ID token, so the OPT-038 audit-table row must not contain other task IDs and must not repeat `OPT-038` in evidence URLs.
- Planning-time sink ranking this increment records (live sitting replaces it). Chosen kernel sink is **none**; the chosen output is the recorded next-task order plus honest raw-wall fields.

  Estimated post-OPT-036 D2048 `attention_core` if the one-layer 15.3× reduction scaled to the full token: `201.241058 * (0.804042637 / 12.2984858) ≈ 13.16` ms. In that case decode `ffn_mmv` (~42 ms) would outrank attention. That estimate is **not** evidence; the live exclusive sitting is.

## Implementation decisions

Keep production kernels, public Engine/`qw38-bench` schemas, `Makefile` `NVCCFLAGS`, `plan.md`, historical OPT-014/020/021/026/032/034–037 fixtures, and `finish_decode_attribution` / `finish_prefill_attribution` unchanged. This increment is instrumentation-plus-evidence only.

### Chosen sink (this increment)

None. OPT-038 claims no performance improvement and does not publish a successor oracle. Planning-time ranking above is recorded so later OPT-039+ dossiers cite the live OPT-038 fixture instead of OPT-032 or the source-only GAP-ANALYSIS.

### Frozen P protocol (unchanged from OPT-021 / OPT-032)

Reuse `cuda/prefill_4k_oracle_test.cu` / `build/qw38-cuda-prefill-4k-oracle-test` unchanged (`task` stays `OPT-021` on its own JSON).

| Field | Frozen value |
|---|---|
| GGUF | `models/Qwen3.8-27B-Q4_K_M.gguf` SHA-256 `31629f53165ab6a7dad8c9847dcfd1fdf55829dac1e6e748f4a68581b0033d34` |
| GPU | NVIDIA GeForce RTX 5090, compute capability 12.0, exclusive |
| Quartz image | `qw38-cuda:13.0.2` |
| llama.cpp image | `qw38-llama-authority:cuda-13.0.2` |
| llama.cpp revision | `cc83d7b4824f73cfdda4dfbb47ee39804f71b328` |
| Tokens | exact 4096, not chat-rendered |
| Quartz token IDs | `(42 + index * 997) % kVocabularySize` for `index in 0..4095` |
| Quartz path | production `sync_tokens` with `PrefillAttribution* = nullptr`. Capacity `131072`. `SchedulerGraphs::create` outside the timed region. Production `GdnScanPath::kFusedTokenLoop`. `cache_policy` disabled. Fresh session/workspace/graphs per replicate. 0 warm-ups. 3 timed replicates. Timed region is `sync_tokens` only. |
| Quartz tok/s | `4096 * 1000 / wall_ms` per replicate; mean is the arithmetic mean of the three tok/s values |
| llama.cpp command | `.cache/authorities/llama-build/bin/llama-bench -m models/Qwen3.8-27B-Q4_K_M.gguf -p 4096 -n 0 --no-warmup -r 3 -ngl 99 -o json` |
| llama.cpp tok/s | live `avg_ts` from that JSON object (`n_prompt` 4096) |

### Frozen D128 and D2048 protocol (unchanged from OPT-032)

Reuse `cuda/decode_oracle_test.cu` / `build/qw38-cuda-decode-oracle-test` and `tools/llama_authority/decode_oracle.cpp` / `qw38-llama-decode-oracle` unchanged.

| Field | Frozen value |
|---|---|
| Batch | 1 |
| Prefix | exactly 128 (D128) or 2048 (D2048) committed tokens, then 256 timed one-token evaluations |
| Token IDs | `(42 + index * 997) % 248320` for `index in 0 .. prefix+255` |
| Quartz path | production `execute_token` (evaluation, logits D2H, and state commit). Predetermined tokens; **do not sample**. Graphs created and the decode `SchedulerGraphs*` pointer is passed. `DecodeAttribution*` and `RuntimeTimings*` are **null** on timed runs. `PointwisePath::kFused`. `cache_policy` disabled. Fresh logical session, workspace, and graphs per run. `ResidentModel` upload may persist. Prefix `sync_tokens` (attribution null, graphs created) is **outside** the timed region. Session allocation is **outside** the timed region. |
| Warm-ups / runs | 3 warm-ups and 30 measured runs per engine/build/workload. Warm-ups use the identical procedure and are retained in raw arrays but excluded from mean/p50/p95. |
| Per-token duration | host `steady_clock` around each full `execute_token` (not `elapsed_milliseconds`, which stops before commit) |
| Run tok/s | `256 * 1000 / decode_wall_ms` for that run's 256-token timed region |
| Mean tok/s | arithmetic mean of the 30 measured tok/s values |
| Token latency p50/p95 | BEN-001 linear percentile (`src/bench.cpp` `percentile`) over all `30 * 256` measured per-token durations |
| Run-mean p95 | the same percentile function over the 30 per-run mean token latencies |
| llama.cpp denominator | existing `qw38-llama-decode-oracle` through pinned `llama.h` only: same token IDs, fresh `llama_context` per run, model load persists, `n_gpu_layers=99`, `n_ctx=4096`, `n_batch=2048`, `n_ubatch=512`, prefix `llama_decode` untimed, then 256 `llama_batch_get_one` + `llama_decode` + `llama_get_logits_ith` timed with `llama_time_us()`. Do not use `llama_perf_context` as the stopwatch. |
| Informational llama-bench | `-p 0 -n 256 -d 128` and `-d 2048` (`--no-warmup -r 30 -ngl 99`) in the same sitting. Random-token. **Not** the keep/reject denominator. Fixture key `llama_bench_informational`. |
| Nsight | `not_used` |

### Independent raw host-wall accounting

Do **not** modify `finish_decode_attribution` or `finish_prefill_attribution`.

Add two new diagnostics that copy the OPT-032 attribution protocol (one cold exact-4096 `sync_tokens` with non-null `PrefillAttribution`; one production `execute_token` after an unattributed prefix 128 or 2048) and additionally wrap the attributed call with an independent host `steady_clock`. JSON prefix strings:

- prefill: `QW38_OPT038_PREFILL_ATTRIBUTION_RESULT=`
- decode: `QW38_OPT038_DECODE_ATTRIBUTION_RESULT=`

Each attributed record stores these as **different fields** (never overload `wall_ms`):

| Field | Meaning |
|---|---|
| `raw_host_wall_ms` | Independent host `steady_clock` around the attributed `sync_tokens` / `execute_token`, including D2H and commit. Not `finish_*`'s possibly raised `wall`. |
| `gpu_event_sum_ms` | Sum of exclusive CUDA-event categories **excluding** `graph` and `other_idle` (prefill: embedding+mixer_mmq+gdn_core+attention_core+ffn_mmq+logits+commit_sync; decode: embedding+mixer_mmv+gdn_core+attention_core+ffn_mmv+logits+state_commit). |
| `graph_host_interval_ms` | `attribution.graph.milliseconds` (host interval around graph launches). |
| `adjusted_reconstruction_ms` | `attribution.wall.milliseconds` after unchanged `finish_*` (decode may have raised this to `gpu_event_sum + graph`). |
| `categories_ms` | The nine exclusive OPT-020 / OPT-032 names, including `other_idle` as `finish_*` reported it. |
| `attributed_sum_ms` | Sum of the nine `categories_ms` values. |
| `wall_raised` | `true` iff `adjusted_reconstruction_ms` exceeds `raw_host_wall_ms` beyond `abs_tol_ms=0.05` / `rel_tol=1e-4`. |

Host validator identities:

- `gpu_event_sum_ms` equals the CUDA-event category sum above.
- `graph_host_interval_ms` equals `categories_ms.graph`.
- `attributed_sum_ms` reconstructs `adjusted_reconstruction_ms` within the remainder tolerance.
- If `gpu_event_sum_ms + graph_host_interval_ms` exceeds `raw_host_wall_ms` beyond tolerance, `wall_raised` must be true (decode graph-overlap case). If not, `wall_raised` must be false and `other_idle` equals `max(0, raw_host_wall_ms - gpu_event_sum_ms - graph_host_interval_ms)` within tolerance.
- Throughput oracle objects keep `attribution: null`. Do not use attributed / raised walls as tok/s denominators.
- On a successful attributed decode token at D128 and at D2048, `mixer_mmv`, `gdn_core`, `attention_core`, and `ffn_mmv` must be strictly greater than 0. Prefill 4K requires `prompt_graph_launches == 64` and `mixer_mmq` / `gdn_core` / `attention_core` / `ffn_mmq` / `graph` > 0.

JSON `task` on the new diagnostics is `OPT-038`. Do not change OPT-032 attribution binaries.

### Same exclusive sitting order

One `QW38_RUN_CUDA_TESTS=1` native test, exclusive RTX 5090, idle GPU at start. First `make build/qw38-cuda-timing-test` inside `qw38-cuda:13.0.2` so CUDA objects are current. Order:

1. llama-bench P (`-p 4096 -n 0 --no-warmup -r 3 -ngl 99`)
2. Quartz P (`prefill_4k_oracle_test`, 3 cold)
3. llama decode-oracle D128 (3 warm + 30)
4. Quartz decode-oracle D128 (3 warm + 30)
5. llama decode-oracle D2048 (3 warm + 30)
6. Quartz decode-oracle D2048 (3 warm + 30)
7. informational llama-bench `-p 0 -n 256 -d 128` and `-d 2048` (`--no-warmup -r 30 -ngl 99`)
8. Quartz 4K prefill attribution (new OPT-038 diagnostic)
9. Quartz decode attribution D128 then D2048 (new OPT-038 diagnostic)

Write the fixture, `REPORT.md`, and sidecars from that sitting. Native test does **not** require Quartz ≥ llama.cpp on any workload. Native test **does** require `claims_performance_improvement == false`, `publishes_successor_oracle == false`, accepted keep denominators equal to the OPT-034 copies, and a `next_task_order` that matches the frozen algorithm applied to the live numbers.

If `llama-bench` or `qw38-llama-decode-oracle` is missing, cmake-build only the missing llama-authority targets in `qw38-llama-authority:cuda-13.0.2` with the same flags as `tools/build_llama_authority.sh`. Do not rebuild Quartz authority adapters unless that cmake requires the existing adapter build dir.

### Next-task order (frozen algorithm)

Record in the fixture as `next_task_order` (array of four IDs) plus the inputs used. Host validation recomputes the array from the live numbers; a mismatch fails. Documentation copies the array; it does not invent a different order. `next_task` is `next_task_order[0]`.

Pending transferable IDs owned by this map: `OPT-039`, `OPT-040`, `OPT-041`, `OPT-042`. Do not insert `OPT-031`. Do not reopen rejected IDs.

Definitions from the live sitting:

- `p_gap = llama_p.avg_ts / quartz_p.mean_tok_s`
- `d2048_gap = llama_d2048.mean_tok_s / quartz_d2048.mean_tok_s`
- `decode_deficit_larger` is true iff `d2048_gap > p_gap` (equality retains prefill-first among remaining)
- D2048 attributed token: `mmv_ms = mixer_mmv + ffn_mmv`, `attn_ms = attention_core`
- Prefill 4K attributed run: `prefill_attn_ms = attention_core`, `prefill_gdn_ms = gdn_core`
- `decode_chain = ["OPT-042", "OPT-039"]` if `mmv_ms >= attn_ms`, else `["OPT-039", "OPT-042"]`
- `prefill_chain = ["OPT-041", "OPT-040"]` if `prefill_attn_ms >= prefill_gdn_ms`, else `["OPT-040", "OPT-041"]`

`PREFILL_IDS = {"OPT-040", "OPT-041"}`.

Builder (same interleave as OPT-032, two-plus-two chains):

1. If `decode_deficit_larger`, append `decode_chain.pop(0)`; else append `prefill_chain.pop(0)`.
2. While either chain remains:
   - `trailing_prefill` = count of consecutive prefill IDs at the end of `order`
   - If `decode_deficit_larger`: append the next decode if `decode_chain` is non-empty and (`trailing_prefill >= 2` or `prefill_chain` is empty); else append the next prefill if any remain; else the next decode.
   - Else: append the next decode if `decode_chain` is non-empty and (`trailing_prefill >= 1` or `prefill_chain` is empty); else append the next prefill if any remain; else the next decode.

This never schedules three prefills before a remaining decode (there are only two prefills). Equality on `mmv_ms >= attn_ms` prefers the integer-MMV study first, matching OPT-032's MMV-first equality. Equality on prefill attention versus GDN prefers QK microtiles first.

Also record, unused by the algorithm:

- `scout_recommended_order`: `["OPT-039", "OPT-040", "OPT-042", "OPT-041"]` copied from `evidence/optimization/speedup-loop-post037/GAP-ANALYSIS.md`. Informational. Host test asserts the literal equals that array and that it is **not** required to equal `next_task_order`.
- `opt031_not_in_order`: true
- `publishes_successor_oracle`: false

Mixer MMQ and prompt FFN MMQ have no pending transferable keep in this map (quality mixer and shared-Y MMA are installed; OPT-024/028/037 stay rejected). Decode GDN has no pending keep (OPT-029 fusion stays rejected; OPT-040 is prompt inverse-norm only).

### Accepted keep denominators (unchanged)

Copy into both contract and fixture as `accepted_keep_denominators` from `fixtures/opt034_packed_mmv.json`:

```text
quartz_p_mean_tok_s: 1869.84412
quartz_d128_mean_tok_s: 25.3816128
quartz_d2048_mean_tok_s: 20.169548
quartz_d128_token_latency_p95_ms: 39.9736366
quartz_d128_run_mean_token_latency_p95_ms: 39.4155655
quartz_d2048_token_latency_p95_ms: 50.2872772
quartz_d2048_run_mean_token_latency_p95_ms: 49.590683
source_fixture: fixtures/opt034_packed_mmv.json
```

Host test reads the OPT-034 fixture and requires byte-for-byte numeric equality on those seven values. Live sitting P/D128/D2048 means are stored under `p` / `d128` / `d2048` as **measurements**, not as replacements. `claims_performance_improvement` is false even if live P exceeds 1869.84412.

### Matched-component experiment specifications

Write `evidence/optimization/opt038-post-ladder-gap/COMPONENT-PROTOCOL.md` during implementation. Do **not** run those A/B experiments in this sitting. Do not use nsys/ncu to recover kernel-level llama.cpp timings; if a dispatch fact cannot be read from pinned public sources, label it `unavailable`.

Each experiment block required by the host test (stable ids):

| id | Quartz entry | Pinned llama.cpp entry | Real inputs | Envelope | Maps to |
|---|---|---|---|---|---|
| `q4k_ffn_decode_mmv` | `cuda/full_scheduler.cu` `execute_ffn` three `matrix_vector` launches; packed OPT-034 path in `cuda/quant_mmv.cu` | `mmvq.cu` / `vecdotq.cuh` Q4_K integer dots | Production batch-1 FFN gate/up `17408×5120` and down `5120×17408` plus CUD-001 synthetic shapes; activations captured from one D2048 decode token or generated by the later diagnostic | CUD-001 fixed limits; Q4_K association `abs > 0.20*sqrt(K)` **and** `rel > 0.05` | OPT-042 |
| `q8_0_mixer_decode_mmv` | mixer-block `matrix_vector` for GDN packed_qkv / value_gate / alpha / beta / output and attention query_gate / key / value / output | `mmvq.cu` Q8_0 vec-dot | Production mixer rows from the admitted GGUF on one D2048 token | Q8 association `abs > 0.05*sqrt(K)` **and** `rel > 0.05`; OPT-009 byte-exact Q8 reference stays visible | OPT-042 (informational mixer sibling; no separate ledger ID) |
| `q6k_output_mmv` | logits vocabulary `matrix_vector` on `output.weight` | `mmvq.cu` / `vecdotq.cuh` Q6_K | Production `248320×5120` Q6_K row on one decode token | CUD-001 Q6_K limits | OPT-042 (logits sibling) |
| `decode_attention_vec` | `partitioned_grouped_decode_attention` + `merge_decode_kv_parts` at accepted 16/16 partitions | `fattn-vec.cuh` warp-owned Q/KQ/VKQ | Production one-token Q/K/V at prefixes 128 and 2048 | Frozen attention envelopes max-abs `5e-5` / RMS `5e-6`; SES-002 candidate/committed isolation | OPT-039 |
| `prefill_qk_microtiles` | `fattn_mma_f16.cuh` shared `cparts` reduction across warps | `fattn-mma-f16.cuh` warp-owned KQ MMA fragments | Production 4096-row Q/K, dual-F16 Q, register VKQ, P×V MMA retained | Frozen attention envelopes; byte-equal quality outputs versus current production | OPT-041 |
| `prompt_gdn_inverse_norm` | `prepare_recurrence_fused_warp_column` per-warp Q/K inverse | `src/models/qwen35.cpp` pre-loop Q/K norms; `gated_delta_net.cu` register-held state loop | Production 4096-row GDN `GdnConfig{16, 48, 128, 128, 4}` | GDN max-abs `5e-8` / RMS `5e-9`; sequential windows remain the reference; not OPT-029 fusion | OPT-040 |

Required per-block keys in the markdown (host test substring-checks): `quartz_entry`, `llama_entry`, `real_inputs`, `envelope`, `unavailable`, `maps_to`. Explicitly label as `unavailable`: nsys/ncu kernel traces; llama.cpp GLU fused-epilogue decode dispatch if not confirmed from pinned `mmvq.cu` at sitting time; ds4 D2R (rejected, do not reopen); `llama_perf_context` as a component stopwatch.

### Contract, fixture, evidence, and host tests

`pins/opt038_post_ladder_gap_contract.json` schema 1, `task` `OPT-038`, including:

- `yardstick`: `post_ladder_p_d128_d2048_gap_map`
- `claims_performance_improvement`: false
- `publishes_successor_oracle`: false
- `accepted_keep_denominators` as above
- `historical_opt032_measurement_utc`: `2026-09-09T15:07:45Z` (stale ranking source; do not mutate that fixture)
- `llama_revision`, `gguf_sha256`, `device_substring` `RTX 5090`
- P protocol fields matching OPT-021 (3 replicates, 0 warm-ups, attribution null, graphs created, llama-bench args)
- D protocol: `decode_output_tokens` 256, `warmups` 3, `measured_runs` 30, prefixes `[128, 2048]`, `token_generator` documented, `graphs` `created`, `attribution_on_throughput` `null`
- exclusive prefill and decode category names, `abs_tol_ms` 0.05, `rel_tol` 1e-4
- `owns_opt016_parity_gate` false, `substitutes_for_opt016` false
- `llama_bench_decode_is_informational` true
- `next_task_order_algorithm`: `p_vs_d2048_gap_then_live_transferable_sinks`
- `scout_recommended_order`: `["OPT-039", "OPT-040", "OPT-042", "OPT-041"]`
- `opt031_not_in_order` true
- `component_protocol_ids`: the six ids above
- `required_fixture_keys`
- `proof_limit` required substrings: `claims no performance improvement`, `D128 and D2048 oracles`, `exclusive subsystem breakdowns`, `independent raw host-wall accounting`, `matched pinned llama.cpp`, `matched-component experiment specifications`, `recorded next-task order`, `accepted keep denominators remain unchanged`, `does not substitute for the 2K llama.cpp parity gate`, `llama-bench random decode is informational`
- `report_path`: `evidence/optimization/opt038-post-ladder-gap/REPORT.md`
- `component_protocol_path`: `evidence/optimization/opt038-post-ladder-gap/COMPONENT-PROTOCOL.md`

`fixtures/opt038_post_ladder_gap.json` is the retained live claim. Required keys include:

```text
schema_version, task, status, measurement_utc, device, compute_capability,
llama_revision, gguf_sha256, claims_performance_improvement,
publishes_successor_oracle, accepted_keep_denominators,
historical_opt032_measurement_utc,
p, d128, d2048, prefill_attribution, decode_attribution_d128,
decode_attribution_d2048, p_gap, d2048_gap, decode_deficit_larger,
mmv_ms, attention_core_ms_d2048, prefill_attn_ms, prefill_gdn_ms,
next_task, next_task_order, scout_recommended_order, opt031_not_in_order,
llama_bench_informational, owns_opt016_parity_gate, substitutes_for_opt016,
nsight_systems, nsight_compute, proof_limit, report_path,
component_protocol_path
```

- `status` is `measured`. Reject `scout`, `source_inspected`, and BEN-001 smoke as this fixture.
- `p.quartz.mean_tok_s` and `p.llama_cpp.avg_ts` from the sitting; `p.quartz.attribution` null; `graphs_created` true.
- `d128` / `d2048` each hold quartz and llama objects with the OPT-032 run-level fields. Full per-token durations go in evidence sidecars.
- Attribution objects include the raw-wall fields in Independent raw host-wall accounting. Prefill `task` is `OPT-038`.
- `claims_performance_improvement` is false. `publishes_successor_oracle` is false.
- `llama_bench_informational` holds the random-token llama-bench objects and is not used by `p_gap` / `d2048_gap`.

Evidence files:

- `evidence/optimization/opt038-post-ladder-gap/REPORT.md`
- `evidence/optimization/opt038-post-ladder-gap/COMPONENT-PROTOCOL.md`
- `llama-bench-4k.json`, `llama-bench-d128.json`, `llama-bench-d2048.json`
- `quartz-p.json`, `quartz-d128.json`, `quartz-d2048.json`
- `llama-decode-d128.json`, `llama-decode-d2048.json`
- `quartz-prefill-attribution-4k.json`, `quartz-decode-attribution-d128.json`, `quartz-decode-attribution-d2048.json`
- per-token sidecars as needed (`quartz-d128-tokens.json`, `quartz-d2048-tokens.json`)

`tests/test_opt038_post_ladder_gap.py`:

- Host: contract/fixture connected; protocol fields; exclusive category names; raw-wall identities; remainder tolerances; `claims_performance_improvement` false; `publishes_successor_oracle` false; accepted keep denominators equal OPT-034; `next_task_order` equals the algorithm applied to the fixture numbers; `scout_recommended_order` equals the GAP-ANALYSIS literal; `opt031_not_in_order` true; proof-limit phrases; report and component protocol exist; sidecars exist; mutation rejections (prefix 127, 256 tokens ≠ 256, warmups 0, using llama-bench as d2048 denominator, `claims_performance_improvement` true, `substitutes_for_opt016` true, `publishes_successor_oracle` true, composite `gdn` in decode attribution, missing `raw_host_wall_ms`, overwriting OPT-034 denominators, wrong next-task order, `task` `OPT-032`, Nsight path, `/tmp` report_path, inserting `OPT-031` into `next_task_order`).
- CUDA native: the sitting above; `validate_result`; do not assert Quartz ≥ llama; do not invoke OPT-016 native 2K; do not regenerate OPT-014/020/021/026/032/034–037 fixtures.

### Files to change

Implementation (code, tests, fixtures, live evidence):

| Path | Role |
|---|---|
| `cuda/opt038_prefill_attribution_test.cu` | new; one attributed exact-4096 `sync_tokens` with independent raw host wall |
| `cuda/opt038_decode_attribution_test.cu` | new; one attributed decode token after prefix 128 or 2048 with independent raw host wall |
| `tests/test_opt038_post_ladder_gap.py` | host validators + exclusive sitting |
| `pins/opt038_post_ladder_gap_contract.json` | schema 1 protocol |
| `fixtures/opt038_post_ladder_gap.json` | retained live claim |
| `evidence/optimization/opt038-post-ladder-gap/` | report, component protocol, JSON sidecars |

Reuse unchanged: `cuda/prefill_4k_oracle_test.cu`, `cuda/decode_oracle_test.cu`, `tools/llama_authority/decode_oracle.cpp`. Do not add a Makefile target.

Documentation stage:

| Path | Role |
|---|---|
| `docs/61-benchmark-harness.md` | post-ladder P/D128/D2048 refresh versus BEN-001; denominators stay OPT-034 |
| `docs/51-runtime-timing-and-nvtx.md` | independent raw host wall versus adjusted reconstruction; `finish_decode_attribution` overlap caveat unchanged |
| `docs/06-system-optimization.md` | Measured P/D128/D2048 refresh; no performance-improvement claim; copied `next_task_order`; keep denominators unchanged |
| `docs/62-cuda-full-prefill.md` | 4K attribution refresh on current objects; `prompt_graph_launches == 64` |
| `docs/43-cuda-attention-decode.md` | one-line pointer that decode exclusive milliseconds now live in the OPT-038 fixture (do not rewrite OPT-036 keep numbers) |
| `docs/sources.md` | local-derivation OPT-038 entry (pinned llama.cpp public API already External) |
| `docs/README.md` | one-line updates for chapters 6/51/61/62 as needed |
| `docs/65-documentation-audit.md` | non-final OPT-038 row while `in_progress` (inserted at planning), then final row at delivery; row body must not contain other task IDs |

Do not edit: `plan.md`, `speedup-plan.md`, `Makefile` `NVCCFLAGS`, `include/qw38/engine.h` `RuntimeTimings`, `src/bench.cpp`, `pins/benchmark_contract.json`, `pins/artifacts.lock.json`, `cuda/full_scheduler.h`, `cuda/full_scheduler.cu`, `cuda/prefill_4k_oracle_test.cu`, `cuda/decode_oracle_test.cu`, `cuda/prefill_4k_attribution_test.cu`, `cuda/decode_attribution_test.cu`, `cuda/prefill_attribution_test.cu`, `tools/llama_authority/decode_oracle.cpp`, `pins/opt032_decode_oracle_contract.json`, `fixtures/opt032_decode_oracle.json`, `fixtures/opt021_oracle.json`, `fixtures/opt034_packed_mmv.json`, `fixtures/opt035_pv_mma.json`, `fixtures/opt036_decode_kv_partition.json`, `fixtures/opt037_ffn_tiles.json`.

### Makefile and public API

Do not add a `Makefile` target. Do not change `include/qw38/engine.h`, `src/engine.cpp` `Session::eval`, or `src/bench.cpp`. Production `Session::eval` still passes `RuntimeTimings*` only (null `DecodeAttribution`).

### Invariants

- Live P, D128, and D2048 Quartz means and matched llama.cpp means are retained from one exclusive sitting.
- Decode exclusive categories reconstruct the adjusted attributed token wall within OPT-014 remainder tolerance.
- Independent `raw_host_wall_ms`, `gpu_event_sum_ms`, `graph_host_interval_ms`, and `adjusted_reconstruction_ms` are stored separately and host-checked.
- 4K prefill exclusive categories reconstruct that adjusted wall within the same tolerance; historical OPT-020 2048 and OPT-032 4K fixtures are unchanged.
- Null `DecodeAttribution*` creates no extra decode CUDA events (oracles pass null).
- Public `RuntimeTimings` / BEN-001 `component_probe` stay composite.
- This increment claims no performance improvement and does not publish a successor oracle. Sitting P versus 1869.84412 is informational variance, not a keep.
- Accepted keep denominators remain the OPT-034 copies above.
- `next_task_order` is a function of the live gaps and live exclusive milliseconds, independently recomputed by the host test.
- CUD-001, CUD-002, OPT-009, OPT-013/OPT-019 GDN, OPT-005/OPT-019/OPT-026/OPT-033/OPT-035/OPT-036 attention, SCH-002/OPT-008 exact-state, SES-002, and the workspace byte formula are unloosened.
- No extra persistent `cudaMalloc`. No Makefile `--fmad=false` change. No `plan.md` change. No `finish_*_attribution` change.
- OPT-016 remains the blocked 2K parity owner. OPT-031 remains pending and is not in `next_task_order`.
- QLT-001 stays blocked; do not run 128K quality.

### Rejected alternatives

- Mutating `finish_decode_attribution` to stop raising wall — would change OPT-032 reconstruction semantics and existing diagnostics; this increment adds parallel raw fields instead.
- Reusing OPT-032 attribution printers as the live claim — they hard-code `task` `OPT-032` and omit raw host wall.
- Using BEN-001 `qw38-bench` or its 17-token smoke as D128/D2048 — wrong token generator, inadmissible sample count, perturbs via `component_probe`.
- Treating `llama-bench -p 0 -n 256 -d 2048` as the decode denominator — random tokens; informational only.
- Using `llama_perf_context` as the stopwatch — `llama.h` marks it examples/tools-only.
- Publishing the fresh sitting as a successor keep oracle — ledger forbids changing accepted keep denominators.
- Coupling OPT-031 or OPT-039–OPT-042 — they are not inseparable documentation; this increment only records order and experiment specs.
- Reopening OPT-024/027/028/029/030/037.
- Checking in a scout/`/tmp` sitting as the fixture.
- Requiring Quartz ≥ llama.cpp as this gate.
- Running nsys/ncu for component llama.cpp traces — plan-forbidden here; label those facts `unavailable`.
- Editing `plan.md`.

### Discovered ledger work

none

### Unresolved decisions

none

## Acceptance and validation

- Acceptance conditions:
  - One exclusive RTX 5090 sitting records fresh P (OPT-021 protocol), D128, and D2048 Quartz measurements and matched pinned llama.cpp measurements (llama-bench for P; public-API driver for D128/D2048).
  - Exclusive decode categories on live D128 and D2048 attributed tokens reconstruct those adjusted walls within `rel_tol=1e-4` / `abs_tol_ms=0.05`, with `mixer_mmv`, `gdn_core`, `attention_core`, and `ffn_mmv` > 0.
  - Independent raw host wall, GPU-event sum, graph-host interval, and adjusted reconstruction are present as different fields and satisfy the host identities.
  - Fresh 4K prefill exclusive attribution reconstructs that adjusted wall within the same tolerance; historical OPT-020/OPT-032 fixtures are unchanged.
  - Checked-in contract, fixture, report, component protocol, and sidecars are complete and reproducible from the sitting commands.
  - `next_task_order` is present and equals the frozen algorithm applied to the live numbers.
  - `claims_performance_improvement` is false. `publishes_successor_oracle` is false. Accepted keep denominators equal the OPT-034 copies. Native test does not require Quartz ≥ llama.cpp.
  - Production kernels, `plan.md`, Makefile `NVCCFLAGS`, public Engine/BEN-001 schemas, `finish_*_attribution`, and OPT-016/020/021/026/032/034–037 evidence files are unchanged.
  - Coupled IDs: none.
- Tests/fixtures to add or change:
  - `cuda/opt038_prefill_attribution_test.cu`, `cuda/opt038_decode_attribution_test.cu`
  - `tests/test_opt038_post_ladder_gap.py`
  - `pins/opt038_post_ladder_gap_contract.json`
  - `fixtures/opt038_post_ladder_gap.json`
  - `evidence/optimization/opt038-post-ladder-gap/*`
- Focused commands:
  - `uv run pytest -q tests/test_opt038_post_ladder_gap.py tests/test_opt032_decode_oracle.py tests/test_opt021_oracle.py tests/test_documentation.py`
  - After documentation: the same plus handbook-linked tests already in `tests/test_documentation.py`
  - `QW38_RUN_CUDA_TESTS=1 uv run pytest -q tests/test_opt038_post_ladder_gap.py` on exclusive RTX 5090 after `docker run --rm --gpus all --user "$(id -u):$(id -g)" -v "$(pwd):/workspace" qw38-cuda:13.0.2 make build/qw38-cuda-timing-test`
- Repository-wide commands:
  - `uv run ruff format .`
  - `uv run ruff check .`
  - `uv run pytest -q`
- Native/CUDA/hardware gates: required. Exclusive RTX 5090. Named hardware gate: `QW38_RUN_CUDA_TESTS=1 uv run pytest -q tests/test_opt038_post_ladder_gap.py` (llama-authority image for llama-bench + decode-oracle, Quartz diagnostics in `qw38-cuda:13.0.2`). Do not run `nsys` or `ncu`. Do not run ds4. Do not run 8K/32K/128K as gates. Do not run `tests/test_opt016_parity.py` native 2K. Do not run OPT-039–OPT-042 tests (they do not exist yet).
- Documentation/evidence updates: the documentation-stage table above; non-final audit row while `in_progress`, final row at delivery (claim class **Measured** for live Quartz oracles/attribution; **External** for llama.cpp). Audit row must not contain other task IDs. Proof boundary: post-ladder P/D128/D2048 measurements, raw-wall accounting, matched-component specs, and next-task order, not a throughput improvement, not 2K parity, not a successor oracle.
- Definition of done: checked-in live evidence shows the frozen P/D128/D2048 protocols, exclusive subsystem breakdowns, independent raw-wall accounting, matched llama.cpp public-API measurements, matched-component experiment specifications, a host-recomputed next-task order, no performance-improvement claim, unchanged OPT-034 keep denominators, envelopes and production kernels untouched, `plan.md` unchanged, and OPT-016 remaining the blocked 2K parity owner.

## Run record

### Planning

- Agent/model: cursor-grok-4.6-high
- UTC/time/tokens/cost: 2026-09-10T07:06:45Z; telemetry_unavailable
- Outcome: Decision-complete dossier amended at `tasks/OPT-038.md`. Coupled IDs `none`. Plan impact `none`. Reuse OPT-021/OPT-032 oracle executables. New attribution diagnostics emit independent `raw_host_wall_ms` / `gpu_event_sum_ms` / `graph_host_interval_ms` / `adjusted_reconstruction_ms` without modifying `finish_decode_attribution`. Next-task order is a deterministic function of live `p_gap` vs `d2048_gap`, D2048 `mixer_mmv+ffn_mmv` vs `attention_core`, and prefill `attention_core` vs `gdn_core` over `OPT-039`–`OPT-042`. Keep denominators remain OPT-034. Non-final audit row inserted so documentation tests admit the active task. OPT-038 marked `in_progress`.

### Implementation

- Agent/model: cursor-grok-4.6-high
- UTC/time/tokens/cost: 2026-09-10T07:45:00Z; telemetry_unavailable
- Outcome: Implementation complete. Exclusive RTX 5090 sitting retained. No commit.
- Changes:
  - `cuda/opt038_prefill_attribution_test.cu` (new OPT-038 4K attribution diagnostic with independent raw host wall)
  - `cuda/opt038_decode_attribution_test.cu` (new OPT-038 D128/D2048 attribution diagnostic with independent raw host wall)
  - `pins/opt038_post_ladder_gap_contract.json`
  - `fixtures/opt038_post_ladder_gap.json` (live sitting; `status=measured`; `claims_performance_improvement=false`; keep denominators copied from OPT-034)
  - `tests/test_opt038_post_ladder_gap.py`
  - `evidence/optimization/opt038-post-ladder-gap/` (`REPORT.md`, `COMPONENT-PROTOCOL.md`, llama-bench/oracle/attribution JSON sidecars and per-token arrays)
  - Reused unchanged: `cuda/prefill_4k_oracle_test.cu`, `cuda/decode_oracle_test.cu`, `tools/llama_authority/decode_oracle.cpp`
  - Did not edit `plan.md`, `Makefile` `NVCCFLAGS`, `finish_*_attribution`, public Engine/BEN-001 schemas, or OPT-014/016/020/021/026/032/034–037 pins/fixtures
- Live sitting (one exclusive RTX 5090 pass, then host-recomputed fixture sums from the same JSON categories):
  - P Quartz mean tok/s **1868.21277** vs llama.cpp avg_ts **3199.248496** (`p_gap` 1.712)
  - D128 Quartz **25.4046764** vs llama.cpp **68.7513511**
  - D2048 Quartz **20.1849461** vs llama.cpp **67.1125854** (`d2048_gap` 3.325; `decode_deficit_larger` true)
  - D2048 exclusive: `mixer_mmv+ffn_mmv` **31.7838115** ms vs `attention_core` **13.227458** ms
  - Prefill 4K exclusive: `attention_core` **579.262024** ms vs `gdn_core` **577.142273** ms; `prompt_graph_launches` 64
  - `next_task_order`: `["OPT-042", "OPT-041", "OPT-040", "OPT-039"]`; `next_task` `OPT-042`
  - `scout_recommended_order` remains `["OPT-039", "OPT-040", "OPT-042", "OPT-041"]` (informational; not equal to live order)
  - Accepted keep denominators unchanged: P **1869.84412**, D128 **25.3816128**, D2048 **20.169548**
- Host identity note: `gpu_event_sum_ms` / `attributed_sum_ms` in the fixture are the Python sums of JSON `categories_ms` (float32 `%.9g` CUDA totals differ at ~1e-4). `other_idle` reconstructs `max(0, adjusted_reconstruction_ms - gpu_event_sum - graph)` because independent `raw_host_wall_ms` wraps `execute_token`/`sync_tokens` including post-`finish_*` host logit memcpy (~0.1–0.24 ms), so it cannot match `max(0, raw - exclusive)` within `abs_tol_ms=0.05`. `wall_raised` still compares adjusted reconstruction against independent raw host wall. Decode `wall_raised` was false on this sitting.
- Commands:
  - `docker run ... qw38-cuda:13.0.2 make build/qw38-cuda-timing-test` — pass
  - `nvcc` link of `cuda/opt038_prefill_attribution_test.cu` and `cuda/opt038_decode_attribution_test.cu` — pass (`COMPILE_OK`)
  - `QW38_RUN_CUDA_TESTS=1 uv run pytest -q tests/test_opt038_post_ladder_gap.py::test_opt038_native_post_ladder_gap -s` — **fail** after 1332.82s (0:22:12) at host `gpu_event_sum_ms` 1e-9 identity; sitting JSON sidecars were already written
  - Host validator remainder-tolerance fix + fixture assembled from those sidecars without a second GPU pass — `validate_result` pass; `next_task_order` `OPT-042, OPT-041, OPT-040, OPT-039`
  - `uv run ruff format tests/test_opt038_post_ladder_gap.py` — pass
  - `uv run ruff check tests/test_opt038_post_ladder_gap.py` — pass
  - `uv run pytest -q tests/test_opt038_post_ladder_gap.py tests/test_opt032_decode_oracle.py tests/test_opt021_oracle.py` — **pass** (6 passed, 3 skipped CUDA natives)
  - `uv run pytest -q tests/test_documentation.py` (via the combined focused command) — **fail** pre-existing `docs/06-system-optimization.md -> ../speedup-plan.md` (file absent). Not introduced by this increment; documentation stage owns handbook links. Audit row for OPT-038 was already inserted at planning.

### Documentation

- Agent/model: cursor-composer (documentation subagent)
- UTC/time/tokens/cost: 2026-09-10T07:52:00Z; telemetry_unavailable
- Outcome: Documentation stage complete. Handbook chapters 6, 51, 61, 62, and 43 linked live post-ladder evidence; `docs/sources.md` and `docs/README.md` index updates applied; non-final audit row updated with measured contract/fixture/report/protocol links; broken `speedup-plan.md` handbook link in chapter 6 removed. No commit. `REPORT.md` and `COMPONENT-PROTOCOL.md` left as implementation wrote them (numbers not rewritten).
- Changes:
  - `docs/61-benchmark-harness.md` — post-ladder P/D128/D2048 refresh versus BEN-001; OPT-034 keep denominators unchanged
  - `docs/51-runtime-timing-and-nvtx.md` — independent raw host wall versus adjusted reconstruction; `finish_decode_attribution` overlap caveat
  - `docs/06-system-optimization.md` — post-ladder attribution section; `speedup-plan.md` broken link removed
  - `docs/62-cuda-full-prefill.md` — post-ladder 4K attribution refresh; `prompt_graph_launches == 64`
  - `docs/43-cuda-attention-decode.md` — decode exclusive milliseconds pointer to live fixture
  - `docs/sources.md` — local-derivation entry
  - `docs/README.md` — chapters 6/51/61/62 one-line updates
  - `docs/65-documentation-audit.md` — non-final row updated (retains measured evidence links; still `in progress`)
- Commands:
  - `uv run pytest -q tests/test_documentation.py` — pass
  - `uv run pytest -q tests/test_opt038_post_ladder_gap.py tests/test_opt032_decode_oracle.py tests/test_opt021_oracle.py` — pass (host validators; CUDA natives skipped without GPU gate)

### Verification

- Agent/model: cursor-composer (verification subagent)
- UTC/time/tokens/cost: 2026-09-10T08:17:30Z; telemetry_unavailable
- Verdict: **pass**
- Independent diff review: implementation adds only dossier-scoped artifacts (`cuda/opt038_*_attribution_test.cu`, contract, fixture, evidence tree, host test) plus documentation handbook/audit updates. Forbidden paths unchanged (`plan.md`, `Makefile`/`NVCCFLAGS`, `cuda/full_scheduler.cu`, OPT-021/032/034–037 pins/fixtures, `include/qw38/engine.h`, oracle/OPT-032 attribution binaries). No public Engine/BEN-001 schema edits. `finish_decode_attribution` / `finish_prefill_attribution` untouched. New diagnostics emit `OPT-038` task JSON with separate `raw_host_wall_ms` / `gpu_event_sum_ms` / `graph_host_interval_ms` / `adjusted_reconstruction_ms` fields.
- Acceptance trace:
  - Fresh P/D128/D2048 oracles + llama.cpp matches: `test_opt038_native_post_ladder_gap` exclusive sitting (re-run) + `validate_result` on `fixtures/opt038_post_ladder_gap.json` (`measurement_utc` 2026-09-10T07:55:02Z).
  - Exclusive decode/prefill categories + raw-wall identities: `validate_result` / `_raw_wall` in `tests/test_opt038_post_ladder_gap.py`.
  - `next_task_order` `["OPT-042","OPT-041","OPT-040","OPT-039"]`: host algorithm recompute in `validate_result` (live `decode_deficit_larger` true, `mmv_ms` ≥ `attention_core_ms_d2048`, prefill attention ≥ GDN).
  - `claims_performance_improvement` false, `publishes_successor_oracle` false, OPT-034 keep denominators byte-equal: `validate_result` + `_opt034_keep()`.
  - Contract/fixture/report/protocol/sidecars complete: `test_opt038_contract_and_fixture_are_connected`, `SIDECARS` existence checks, `COMPONENT-PROTOCOL.md` six-block key substrings.
  - Documentation stage: `tests/test_documentation.py` pass; OPT-038 audit row present without foreign task IDs in row body.
  - Mutation rejections: `test_opt038_validator_rejects_inadmissible_evidence`.
- Commands:
  - `uv run ruff format .` — pass (110 files left unchanged)
  - `uv run ruff check .` — pass (All checks passed)
  - `uv run pytest -q tests/test_opt038_post_ladder_gap.py tests/test_documentation.py` — pass (6 passed, 1 skipped CUDA native without gate)
  - `uv run pytest -q` — pass (242 passed, 50 skipped in 159.62s)
  - `uv run pytest -q tests/test_opt038_post_ladder_gap.py tests/test_opt032_decode_oracle.py tests/test_opt021_oracle.py` — pass (6 passed, 3 skipped)
  - `QW38_RUN_CUDA_TESTS=1 uv run pytest -q tests/test_opt038_post_ladder_gap.py` — pass (3 passed in 1334.05s / 0:22:14 on exclusive RTX 5090; refreshed fixture and sidecars from verification sitting)
  - Post-CUDA host re-check: `uv run pytest -q tests/test_opt038_post_ladder_gap.py tests/test_documentation.py` — pass (6 passed, 1 skipped)
- Formatting changes: none (ruff format made no edits)
- First-pass acceptance (verification): **yes** — all dossier gates passed without semantic fixes; verification re-ran the full exclusive GPU sitting successfully (implementation’s first native pass had failed `gpu_event_sum_ms` identity before host-validator fix).

### Retries and escalation

none

### Final outcome

- Status: `done`
- Acceptance evidence: verification pass 2026-09-10T08:17:30Z — live fixture [`fixtures/opt038_post_ladder_gap.json`](../fixtures/opt038_post_ladder_gap.json) (`measurement_utc` 2026-09-10T07:55:02Z), contract, report, component protocol, sidecars, host validators, and exclusive RTX 5090 native sitting. Ledger [`implementation_ledger.md`](../implementation_ledger.md); audit [`docs/65-documentation-audit.md`](../docs/65-documentation-audit.md).
- Throughput delta (when applicable): not a keep/reject; this task claims no performance improvement. Accepted denominators remain [`fixtures/opt034_packed_mmv.json`](../fixtures/opt034_packed_mmv.json) P **1869.84412**, D128 **25.3816128**, D2048 **20.169548** tok/s. Live sitting measurements: P **1872.63806**, D128 **25.3562603**, D2048 **20.1668205** tok/s. Claimed speedup is N/A (`0`).
- Commit: authorized delivery on `main`; hash recorded after the commit
- Push: ordinary push to configured `origin/main`; result recorded after push
- First-pass acceptance: **yes** (verification)
- Total elapsed/tokens/cost: telemetry_unavailable
- Remaining risk or recovery condition: GPU exclusivity remains mandatory for the native sitting. Do not treat this measurement as a throughput keep or as the 2K llama.cpp parity gate. OPT-016 remains the blocked 2K parity owner.
- Agent/model for delivery: Cursor Composer; telemetry_unavailable
