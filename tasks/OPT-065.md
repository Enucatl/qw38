# OPT-065 — Retune MMQ tiles with the active pipeline preserved

Status: done. Dependencies: OPT-059, OPT-061. Coupled IDs: none.

## Run record

### Planning (2026-09-11T09:48:00Z)

skipped — pre-authored dossier admitted (coordinator)
Primary target: P4096 FFN. Read
[the batch protocol](PERFORMANCE-RECOVERY-2026-09-11.md).

### Implementation (2026-09-11T10:33:38Z)

- Agent/model: cursor-grok-4.6 (implementation subagent)
- UTC/time/tokens/cost: 2026-09-11T10:33:38Z; telemetry_unavailable
- Outcome: four Q4 tiles wired with FMA/async Y on aligned full tiles.
  Screen kept **i128_j128** for gate/up and down. Production pins unchanged.
  No tok/s sitting.
- Commands:
  1. `uv run pytest -q tests/test_opt065_mmq_tiles.py` — **pass** (4 passed)
  2. `uv run python tools/run_optimization_task.py --task OPT-065 --mode feedback`
     — **pass**, 26.1 s / 300 s,
     `build/optimization-runs/OPT-065/feedback/20260911T103300Z-9820fe68`
  3. `uv run python tools/run_optimization_task.py --task OPT-065 --mode acceptance`
     — **pass**, 8.4 s / 7200 s,
     `build/optimization-runs/OPT-065/acceptance/20260911T103338Z-ae4e88a7`
- Isolated 4096-row GEMM means (1 warmup + 3 samples), real layer-0 weights:
  gate/up i128_j128 **2.866** ms, i64_j128 3.011, i128_j64 3.064, i64_j64 3.766;
  down i128_j128 **2.910** ms, i64_j128 3.024, i128_j64 3.083, i64_j64 3.819.
  Complete FFN control **9.649** ms; selected pair also i128_j128 (**9.629** ms,
  keep=false). Occupancy 1 on all four; i128_j128 spills 80 local bytes.
- Keep: retain `kSelectedFfn* = 128/128`. Pipeline path stays `fma_async`.
  Diagnostic and production aligned tiles report actual kernel ids
  `q4_i*_fma_async`; tails use `q4_*_sync_fallback`.
- tok/s: not measured. Speedup is 0 (baseline 128×128 unchanged). OPT-069 owns
  combined E2E.
- Blockers: none. Do not commit.

### Verification (2026-09-11T10:36:00Z)

Independent verifier (composer-2.5). Model: composer-2.5;
`telemetry_unavailable`. Verdict: **PASS** (128×128 retained; FMA/async Y
pipeline extended to all four Q4 tiles).

Commands:

- `uv run ruff format .` — **pass** (148 files unchanged)
- `uv run ruff check .` — **pass**
- `uv run pytest -q tests/test_opt065_mmq_tiles.py` — **pass** (4 tests, 0.02 s)
- `uv run python tools/run_optimization_task.py --task OPT-065 --mode feedback` — **pass** (`ok`, 3.45 s, [feedback run](../build/optimization-runs/OPT-065/feedback/20260911T103524Z-7c89b930))
- `uv run python tools/run_optimization_task.py --task OPT-065 --mode acceptance` — **pass** (`ok`, 8.24 s, screen 4.77 s, [acceptance run](../build/optimization-runs/OPT-065/acceptance/20260911T103529Z-b625efb0))

Screen complete FFN at 4096 prompt rows: control i128_j128 **9.649** ms;
winner i128_j128 **9.629** ms (`keep=false`). Gate/up and down both retain
i128_j128. Pipeline path stays `fma_async` on aligned full tiles for all four
(I,J) pairs; tails use `q4_*_sync_fallback`. `installed=false`.
`claims_throughput=false`. No D2048 sitting; OPT-069 owns combined E2E.

Throughput: **no tok/s** (component FFN ms only). tok/s delta vs baseline
**0** (128×128 unchanged).

### Final outcome (2026-09-11T10:36:00Z)

Retained **i128_j128** for gate/up and down. Extended the FMA/async Y pipeline
to all four Q4 tile candidates with actual dispatch/resource evidence.
Coupled IDs: none. First-pass acceptance. Retries: 0.

## Concrete gap

OPT-053 kept FMA plus double-buffered Y at I128/J128 and reported occupancy1.
The old tile choice predates that resource change. Current
`launch_quant_mmq_mma_y_ij` enables the pipeline only for I128/J128; choosing
a different existing tile silently returns to the old non-pipelined dispatch.
An ordinary parameter sweep would therefore conflate tile and pipeline changes.

Read `cuda/quant_mmq_mma.cuh::quality_mma_process_tile`, `quality_shared_ints`,
`quality_rows_per_warp`, `launch_quality_mma`, pipeline dispatch/occupancy
helpers and `launch_quant_mmq_mma_y_ij`; `full_scheduler.cu::execute_prompt_ffn_projections` call
sites; and pinned llama `mmq.cuh`/`mmq-config-ampere.cuh` mappings.

## Implementation

1. Instantiate exactly four Q4 configurations: `(I,J)=(128,128)` control,
   `(64,128)`, `(128,64)`, `(64,64)`. All use the current accepted FMA/Y pipeline
   on full tiles. Extend the pipeline launcher and occupancy helper together;
   report the actual template variant, not only requested I/J.
2. Preserve `kNtx`, `kNi`, `kSum` formulas and static assertions. Verify all
   lane-to-output indices using small unique-pattern matrices before timing.
   Accumulation order within each output stays unchanged as far as the mapping
   permits. Use v2 numerics where output rounding changes, not exact equality
   between different reduction algorithms.
3. Compute dynamic shared allocation for the actual format and active buffers.
   The existing helper reserves `QualityI*kQualitySramMax` for all formats;
   investigate a format-specific allocation only if the required row stride and
   metadata extents are proven from the code. Treat allocation-only change as
   part of the variant identity. Query real occupancy after setting opt-in bytes,
   capture ptxas registers/local bytes, and refuse launches beyond device limits.
4. Keep synchronous tail/misalignment handling. Extend an aligned full-tile
   pipeline independently of fallback; tests must show the fallback is selected
   for output or prompt tails. Do not retune stream-K, physical microbatch or
   activation quantization. Keep FFN shared-Y and SwiGLU-Q8 staging.
5. Measure gate/up shape 17408× 5120 and down 5120× 17408 at 4096 prompt rows.
   Select one gate/up tile and one down tile if distinct winners are supported;
   avoid per-layer autotuning. Complete FFN replay includes staging and residual
   epilogues. Apply no new tile to Q6/Q8 by accident.
6. Once Q4 outcome is known, use one existing large Q8 and one skinny Q8 shape
   as cross-family regression checks of shared helpers, without a second tuning
   grid. Record effective dispatch and restore all unsupported paths unchanged.

## Fast tests

Target `build/qw38-cuda-opt065-mmq-tiles-test`; pytest
`tests/test_opt065_mmq_tiles.py`. Reuse OPT-061 reference helpers.

Smoke: M17/K256/N17, control plus one alternative; full17× 17 reference.
Correctness: four candidates on `(M,K,N)=(65,256,65),(129,512,129)` with
sampled16 output rows × 8 prompt rows (128 dots/case), plus one full tiny case.
Include N1/31/32/33 tails as narrow dispatcher/output-guard cases; they need
no production matrices. Production correctness uses cached real16× 4 row pairs
at K5120 and K17408, not a 4096× 17408 host GEMM.

Screen: four configurations ×two FFN shape groups ×(1 warmup+3 paired samples),
then one complete-FFN screen for the selected pair of tiles. Reuse resident
weights and preallocated workspaces across variants. No512/1024/2048 prompt
length sweep; this task targets the unchanged4096 physical batch.

```sh
uv run pytest -q tests/test_opt065_mmq_tiles.py
uv run python tools/run_optimization_task.py --task OPT-065 --mode feedback
uv run python tools/run_optimization_task.py --task OPT-065 --mode acceptance
```

## Acceptance

Use batch acceptance with P4096 target and short D guards. A tile wins on
complete FFN/target measurements, not occupancy alone. Ensure diagnostic and
production paths really use FMA/async Y on the selected full tile. Deliver
contract/fixture, resource table and
`evidence/optimization/opt065-mmq-tiles/REPORT.md`. Retain128× 128 on loss.
No additional shapes or candidates after the bounded screen is exhausted.
