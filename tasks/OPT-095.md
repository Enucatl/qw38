# OPT-095 — Share decode KV across query heads
Status: done. Dependencies: OPT-094. Coupled IDs: none.
Priority: P1. Target: remove repeated KV loads within each six-query-head group without adding query preparation launches.
Read [the post-088 protocol](PERFORMANCE-RECOVERY-POST-088.md).
Quality risk: incorrect grouped-head mapping, synchronization or candidate-KV visibility can break causality.
Instrumentation: actual KV loads/bytes, CTA occupancy, prep/core/merge/gate complete cost and D128/D2048 ITL/p95.

## Outcome

Share decode KV loads across six query heads without a prep launch, with one
production keep or retention of warp_query. This is not a repeat of OPT-078:
its prepared_q acceptance was 9.092 →10.315 ms (CI −1.970 to −0.476 ms),
and the extra launch erased the proposed benefit. Both old prep candidates
remain rejected.

## Implementation

1. Create `pins/opt095_attention_gqa_contract.json`, the iteration contract,
   `tools/opt095_attention_gqa.py`, `tests/test_opt095_attention_gqa.py`,
   `fixtures/opt095_attention_gqa.json` and
   `evidence/optimization/opt095-attention-gqa/REPORT.md`. Add
   `cuda/opt095_attention_gqa_test.cu`, native
   `build/qw38-cuda-opt095-attention-gqa-test`, Makefile
   `cuda-opt095-diagnostics` with existing replay/probe targets.
2. Exactly two configurations in `cuda/attention_decode_path.cuh`:
   `warp_query` control and new `warp_query_gqa6` candidate. Implement
   `warp_query_gqa6_decode_attention` in `cuda/attention_decode.cu` and wire
   its existing scheduler call in `cuda/full_scheduler.cu`. Candidate
   `prep_launches=0`; retain K/V preparation and commit boundaries, all 16
   partitions, output gate, BF16 casts and deterministic ascending-part merge.
3. Production configuration: 24 query heads, four KV heads, width256,
   rotary64, one token. Launch block=(32,6), grid=(4,16): one warp per query
   head within a KV group, query_head=6*kv_head+warp. Prepare each Q inside
   its warp with the exact existing lane0 ordered norm/RoPE arithmetic.
   At each existing key position, cooperate to load K and V once into two
   shared BF16[256] slabs, synchronize before consumption and before overwrite;
   every warp retains its original lane*8+i order and softmax operations.
   Use legal 16-byte loads only on aligned segments; otherwise use safe scalar
   loads with identical values. Do not change KV layout or materialize head copies.
4. Parity positions {0,1,31,32,127,128,2047,2048}; fixed 24/4/256/64 shape,
   random seed89 plus zero and cancellation at positions1/32. Compare all
   outputs bitwise to warp_query for small cases; at production positions also
   compare full outputs bitwise and independently sample four query heads×eight
   output dimensions against the existing attention FP64 checker. Preserve
   existing ATN-001/OPT-078 tolerances; graph/eager and aligned/scalar load
   variants must be identical. Check last committed KV, current candidate KV,
   new input/position at the same address, empty partitions and output guards.
5. Entry trigger: valid OPT-090 attention evidence must show repeated KV load
   work, with ≥0.10 ms/token measured attention cost available. If not, finish
   `no_go_no_measured_sink`. Screen all 16 attention layers at position128 and
   2048 in separate 1+3 AB/BA phases; at most one D2048+32 engine screen.
   The reduced grid can lose occupancy or synchronization time; reject if
   neither prefix saves ≥0.10 ms/token or either complete component regresses
   >2%. No partition or warp-count sweep is allowed.
6. Run/reuse full Q with exact-output evidence and frozen-anchor guards. Then
   3+10 complete-attention pairs at each prefix and five engine pairs each at
   D128/D2048+32 with p95. Require positive component CI and ≥0.10 ms/token
   at D2048, and ≤2% complete-component regression at D128, plus common E2E
   guards and one P4096 guard. Select one global candidate only; no per-context
   exception table. Otherwise keep warp_query. Include shared-memory size,
   registers, occupancy and actual bytes to explain keep/reject.

## Acceptance

- Bitwise per-head outputs, same-path graph replay and causal KV boundaries
  pass; no new prep kernel or altered quantization/softmax.
- Complete 16-layer component saving transfers through D128/D2048+p95 guards;
  isolated DRAM-byte reduction is insufficient.
- Independent verdicts: `kernel_parity_pass`, `model_quality_pass`,
  `performance_pass`, `production_kept`.
- `claims_throughput: true`; screened or rejected candidates claim no gain.

## Tests and commands

```sh
uv run pytest -q tests/test_opt078_decode_attention.py tests/test_opt095_attention_gqa.py
uv run python tools/run_optimization_task.py --task OPT-095 --mode feedback --phase eligibility
uv run python tools/run_optimization_task.py --task OPT-095 --mode feedback --phase parity
uv run python tools/run_optimization_task.py --task OPT-095 --mode feedback --phase attention128
uv run python tools/run_optimization_task.py --task OPT-095 --mode feedback --phase attention2048
uv run python tools/run_optimization_task.py --task OPT-095 --mode release --phase quality
uv run python tools/run_optimization_task.py --task OPT-095 --mode acceptance --phase attention128
uv run python tools/run_optimization_task.py --task OPT-095 --mode acceptance --phase attention2048
uv run python tools/run_optimization_task.py --task OPT-095 --mode acceptance --phase d128
uv run python tools/run_optimization_task.py --task OPT-095 --mode acceptance --phase d2048
uv run python tools/run_optimization_task.py --task OPT-095 --mode acceptance --phase prefill-guard
```

Use the common 300 s feedback deadline and conditional long Q invocation.
No-repeat stop conditions include parity failure, occupancy-driven screen loss,
negative complete saving and either prefix's E2E/p95 regression.

## Non-goals

No OPT-078 standalone prepared-Q replay, new softmax, tensor-core attention,
partition retuning, KV quantization, persistent KV expansion or prefill change.

## Run record

### Planning (2026-09-12T09:16:00Z)

skipped — pre-authored dossier admitted (coordinator checklist pass).
Model constraint: `cursor-grok-4.6-high` unavailable on Task surface; implementation/repair stages use inherit per OPT-094 precedent.

### Implementation (2026-09-12T09:22:07Z)

**Files changed**

- Created: `pins/opt095_attention_gqa_contract.json`, `pins/opt095_iteration_contract.json`, `tools/opt095_attention_gqa.py`, `tests/test_opt095_attention_gqa.py`, `fixtures/opt095_attention_gqa.json`, `evidence/optimization/opt095-attention-gqa/REPORT.md`, `cuda/opt095_attention_gqa_test.cu`
- Modified: `cuda/attention_decode_path.cuh`, `cuda/attention_decode.cu`, `cuda/attention_decode.h`, `cuda/optimization_component_replay.cu`, `cuda/optimization_engine_probe.cu`, `Makefile`, `tools/run_optimization_task.py`

**CUDA kernel design (`warp_query_gqa6`)**

- Launch `block=(32,6)`, `grid=(4,16)` for production 24/4/256/64: one warp per query head within a KV group (`query_head = 6*kv_head + warp`).
- Each warp performs in-kernel lane-0 RMS norm + RoPE (same arithmetic as `warp_query`).
- Per key position, CTA threads cooperatively load K/V once into shared `BF16[256]` slabs (`shared_key`, `shared_value`), `__syncthreads` before consumption and before overwrite.
- 16-byte `load_bf16x8` on aligned 8-dim segments with scalar BF16 fallback; per-warp softmax/VKQ accumulation unchanged; ascending-part merge and output gate unchanged.
- `prep_launches=0`; selector in `attention_decode_path.cuh` (`warp_query` control / `warp_query_gqa6` candidate); production pin stays `warp_query`.

**Command outcomes**

| Command | Exit | Summary |
|---------|------|---------|
| `make cuda-opt095-diagnostics` | 2 | `nvcc: No such file or directory` (no host CUDA toolkit) |
| `uv run pytest -q tests/test_opt078_decode_attention.py tests/test_opt095_attention_gqa.py` | 0 | 18 passed |
| `uv run python tools/run_optimization_task.py --task OPT-095 --mode feedback --phase eligibility` | 1 | Docker compile_cold failed (cgroup/systemd timeout, 124.6s) |
| `uv run python tools/opt095_attention_gqa.py --phase eligibility --mode feedback` | 0 | OPT-090 gate `eligible=true`, `verdict=proceed`, `attention_ms_per_token=9.092` |
| `uv run python tools/run_optimization_task.py --task OPT-095 --mode feedback --phase parity` | skipped | Requires GPU/docker build |
| `uv run python tools/run_optimization_task.py --task OPT-095 --mode feedback --phase attention128` | skipped | Requires GPU/docker build |
| `uv run python tools/run_optimization_task.py --task OPT-095 --mode feedback --phase attention2048` | skipped | Requires GPU/docker build |

**Preliminary verdict**

- Implementation complete; host contracts/tests pass.
- Eligibility proceeds from OPT-090 (`opt078_complete_attention_ms=9.092`, GQA group 6, `attention_core` ranked).
- GPU parity/performance phases not executed (no `nvcc`, docker compile timeout); production keep/reject undecided — retain `warp_query` until measured acceptance.

### Implementation repair (2026-09-12T09:30:00Z)

Coordinator repair after subagent build/parity gaps (inherit model).

- Fixed `decode_kv_warp_query_gqa6_occupancy` API (`kWarpThreads*6`, static smem).
- Added `atomicOr` unaligned-fallback flag in `warp_query_gqa6` scalar KV path.
- Record `record_decode_attention_gqa_launch` for warp_query control (`grid_x=24`,
  `block_y=1`, `n_parts=16`) and pass `n_parts` into GQA launch recorder.
- Built via extracted `/tmp/qw38-cuda-toolkit` (docker cgroup/systemd broken).

### Documentation (2026-09-12T09:40:00Z)

Model: `composer-2.5` unavailable; coordinator completed doc stage.

- Updated [`evidence/optimization/opt095-attention-gqa/REPORT.md`](evidence/optimization/opt095-attention-gqa/REPORT.md) with eligibility, dispatch, component CI, rejection verdict.
- Finalized [`fixtures/opt095_attention_gqa.json`](fixtures/opt095_attention_gqa.json) from host-native acceptance runs.
- Ruff format on OPT-095 Python; removed unused imports/variables.

### Verification (2026-09-12T09:41:00Z)

Model: `composer-2.5` unavailable; coordinator verifier pass.

| Command | Exit | Result |
|---------|------|--------|
| `make cuda-opt095-diagnostics` (extracted nvcc) | 0 | Built test/replay/probe |
| `uv run pytest -q tests/test_opt078_decode_attention.py tests/test_opt095_attention_gqa.py` | 0 | 18 passed |
| `uv run ruff format .` | 0 | 2 files reformatted |
| `uv run ruff check tools/opt095_attention_gqa.py tests/test_opt095_attention_gqa.py` | 0 | clean |
| native parity `--workload correctness` | 0 | `status=passed`, `nonfinite=0` |
| feedback `attention128` | 0 | saving −0.135 ms/token (reject screen) |
| feedback `attention2048` | 0 | saving −0.563 ms/token (reject screen) |
| acceptance `attention128` (3+10 pairs) | 0 | saving +0.072 ms (<0.10, CI not positive) |
| acceptance `attention2048` (3+10 pairs) | 0 | saving −0.278 ms, CI [−0.599, −0.292] |
| acceptance `d128` engine (5 pairs) | 0 | regression upper within guard |
| acceptance `d2048` engine (5 pairs) | 0 | regression upper within guard |
| acceptance `prefill-guard` | 0 | pass |
| release `quality` | 0 | OPT-073 identity-cached pass |

**Overall: PASS** (measured rejection; `warp_query` retained).

Independent verdicts: `kernel_parity_pass=true`, `model_quality_pass=true`,
`performance_pass=false`, `production_kept=false` (control path kept as shipping pin).

Telemetry: `telemetry_unavailable`.

### Delivery (2026-09-12T09:42:00Z)

Delivery authorized via `/run-ledger-task-cursor`. Marked OPT-095 `done`.
Coupled IDs: none. Production pin unchanged (`warp_query`).

### Final outcome

`performance_rejected`: `warp_query_gqa6` passes bitwise parity and OPT-073 quality
but complete 16-layer attention is slower at both prefixes (acceptance D2048
control 9.659 ms vs candidate 10.105 ms; paired CI entirely negative). Neither
prefix meets ≥0.10 ms/token saving. Retained shipping decode attention `warp_query`.
`claims_throughput=false`. Shared-memory/registers: gqa6 occupancy 6 vs warp_query 24;
shared_bytes≈7192.

**tok/s delta vs sitting D2048 baseline (OPT-088 35.73 tok/s): 0** (baseline unchanged).

Evidence: [`fixtures/opt095_attention_gqa.json`](fixtures/opt095_attention_gqa.json);
[`evidence/optimization/opt095-attention-gqa/REPORT.md`](evidence/optimization/opt095-attention-gqa/REPORT.md).

**OPT-096 blockers:** OPT-090 `graph_reopen_eligible` gate still required; OPT-095
did not change graph capture or layer-segment overhead — no new graph-reopen signal.
