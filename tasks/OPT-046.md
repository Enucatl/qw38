# OPT-046 — Implement cooperative packed Q4_K decode dots

Status: done. Dependencies: OPT-044, OPT-045. Coupled IDs: none. Primary target: D2048.
Read [the shared protocol](PERFORMANCE-RECOVERY-2026-09-10.md).

## Change

Retain `cuda/quant_mmv.cu::quant_mmv` packed FP32 path. Add a focused Q4_K
integer-dot implementation based on pinned llama `mmvq.cu::mul_mat_vec_q` and
`vecdotq.cuh::vec_dot_q4_K_q8_1`. Do not simply promote OPT-042's diagnostic:
its one-warp row, per-block shuffles and lane-0 fold leave much of the execution
design unchanged.

1. Add optimized Q8_1 staging in a distinct type/buffer contract. Use pinned
   llama's round/scale/sum semantics exactly for that staging variant; retain
   original FP32-scale Q8Block unchanged for strict calls. Start with unchanged
   Q8Block staging as a control so staging vs reduction effects are separable.
2. Map packed loads to four int8 products per instruction. Respect Q4_K's
   256-value, 144-byte blocks, 6-bit scales/minima and eight 32-value groups.
   For group g compute `dy * (d * scale[g] * dot(q4,q8)
   - dmin * min[g] * sum(q8))`; use integer intermediates with overflow bounds
   and FP32 scale/accumulation. Precompute activation sums once per group,
   not once per output weight row. Match any Q8_1 half-scale rounding to its
   own reference rather than assuming this formula is bit-equal to FP32 Q8.
3. Distribute K blocks across 4 warps per row (llama-style starting point).
   Each lane owns packed dot chunks and keeps independent accumulators through
   the K loop; reduce at row completion, not via eight lane broadcasts every
   block. Compare 1/2/4/8 warps per row, bounded to production gate/up
   17408×5120 and down 5120×17408. Clearly distinguish warps per **row** from
   the current selector's warps per CTA/rows per CTA.
4. Use aligned 32-bit loads where legal; Q4_K block size is a multiple of 16.
   Avoid byte arrays assembled back into words. Confirm DP4A and no spill
   regression in compiler/disassembly evidence. No tensor-core GEMM padded
   to batch 16 unless it wins a separately measured complete control.
5. Wire accepted output into `matrix_vector`/`execute_ffn`, with strict
   diagnostic selection. Recapture decode FFN graphs. This task does not fuse
   gate/up and does not change Q6_K or Q8_0 production dispatch.

## Acceptance

Test independently decoded FP64 dots and OPT-044 budgets on old probes, full
production shapes and actual layer captures. Report staging bytes, quantization
error, accumulation error, complete vs prequant timings and real projection
frequency-weighted latency. Existing OPT-042 evidence remains a historical
rejection, not a test requiring all future selectors to stay packed.
Add `tests/test_opt046_q4_decode.py`, task contract/fixture, and
`evidence/optimization/opt046-q4-decode/`. Run new host/native tests,
`tests/test_cuda_quant_mmv.py`, `tests/test_opt034_packed_mmv.py`,
`tests/test_opt042_mmv_integer_study.py`, `tests/test_cuda_full_scheduler.py`,
graph tests, production-numerics suite and full P/D acceptance protocol.

## Run record

### Planning

- Agent/model: coordinator (pre-authored dossier admitted)
- UTC/time/tokens/cost: 2026-09-10T17:38:00Z; telemetry_unavailable
- Outcome: skipped — pre-authored dossier admitted

### Implementation

- Agent/model: cursor-grok-4.6-high
- UTC/time/tokens/cost: 2026-09-10T18:57:28Z; telemetry_unavailable
- A/B winner: `integer_q8_w4` (weighted complete **0.0191882669** ms vs packed **0.067196091** ms, ~3.50×). Occupancy 12, 40 regs, `local_bytes` 0. Q8_1 llama staging is slightly slower than Q8Block (`integer_q8_1_w4` 0.0213653333 ms). Warps-per-row 4 beat 1/2/8.
- Preliminary keep/reject: **reject** (`reverted` true, `keep_sitting_skipped` false, production `kSelectedQ4DecodePath = "packed"`, warps per row **4**)
- tok/s sitting: ran under the integer pin then reverted. Quartz P **2130.48462**, D128 **40.7888718**, D2048 **38.3443794**. Those decode numbers are **not** a keep: CUD-001 and full-scheduler quality fail, so production stays packed and speedup vs OPT-045 is **0**.
- Blockers / discovered extra work: `tests/test_cuda_full_scheduler.py` fails on current HEAD with **packed** MMV too (layer 0 residual finite, layer 3+ residuals and logits all-NaN, greedy cuda=0 vs scalar 3649/1277). Reproduced with original `cuda/quant_mmv.cu`/`.h`, without linking `q4k_decode_dots.cuda.o`, and with a temporary serial RMSNorm pin. OPT-045 verification did not run this test. This is outside OPT-046 integer-dot keep/reject; coordinator should add a follow-up rather than silently expanding this increment.
- Changes:
  - `cuda/q4k_decode_path.cuh` — production path pin, legal paths, thread-local A/B override (`Q4DecodePathScope`); host-only so packed `quant_mmv.cu` does not include integer device code
  - `cuda/q4k_decode_dots.cuh` / `cuda/q4k_decode_dots.cu` — cooperative DP4A Q4_K dots (`q4k_coop_mmv`), K distributed across 1/2/4/8 warps per row, Q8Block control + llama Q8_1 staging; group mapping matches packed `decode_q4` / `q4_scale_min` (not llama's uint16 scale unpack)
  - `cuda/quant_mmv.cu` — packed FP32 `quant_mmv` retained; `launch_quant_mmv` dispatches integer only when the pin is integer; `kSelectedMmvLoadPath` remains `"packed"`; no `__dp4a` in this TU
  - `cuda/quant_mmv.h` — `launch_q4k_coop_mmv` / prequant / occupancy declarations; Q8_1Block stays out of this header (cuda_fp16 isolation)
  - `cuda/opt046_q4_decode_ab_test.cu` — nine-candidate A/B; not added to the Makefile
  - `Makefile` — `QUANT_MMV_CUDA_OBJECTS` = `quant_mmv.cuda.o` + `q4k_decode_dots.cuda.o`
  - `tests/test_cuda_prompt_graph.py` — link `build/q4k_decode_dots.cuda.o`
  - `pins/opt046_q4_decode_contract.json`, `fixtures/opt046_q4_decode.json`, `tests/test_opt046_q4_decode.py`
  - `evidence/optimization/opt046-q4-decode/` — `REPORT.md`, `REJECTION.md`, `ab-result.json`, `q4-decode-ab-raw.txt`, quartz P/D128/D2048 JSON
- Commands:
  - nvcc `cuda/opt046_q4_decode_ab_test.cu` → `build/qw38-cuda-opt046-q4-decode-ab-test` → exit 0; winner `integer_q8_w4`; packed **0.067196091** ms occ 3; winner **0.0191882669** ms occ 12 regs 40 `local_bytes` 0; measurement_utc 2026-09-10T18:02:50Z
  - integer pin sitting: nvcc `cuda/prefill_4k_oracle_test.cu` → Quartz P **2130.48462** (≥ 95% of OPT-045 **2130.41089**)
  - `./build/qw38-cuda-decode-oracle-test … 128` → Quartz D128 **40.7888718**, p95 **24.7228546** / run-mean p95 **24.6338348**
  - `./build/qw38-cuda-decode-oracle-test … 2048` → Quartz D2048 **38.3443794**, p95 **26.1881905** / run-mean p95 **26.103157**
  - integer `q4_k_17x256` CUD-001 max_abs **0.000305175781** (envelope 3.0e-4) — fail; packed **0.000244140625** — pass
  - pin restored to `packed` / 4 in `cuda/q4k_decode_path.cuh` and the contract
  - `QW38_CUDA_TEST_TIER=smoke ./build/qw38-cuda-quant-test` → `status=passed`; `q4_k_17x256` max_abs **0.000244140625**
  - `uv run ruff format tests/test_opt046_q4_decode.py tests/test_cuda_prompt_graph.py && uv run ruff check tests/test_opt046_q4_decode.py tests/test_cuda_prompt_graph.py` → exit 0
  - `uv run pytest -q tests/test_opt046_q4_decode.py tests/test_opt034_packed_mmv.py tests/test_opt042_mmv_integer_study.py tests/test_production_numerics.py tests/test_cuda_quant_mmv.py tests/test_cuda_graph.py tests/test_cuda_fusion.py tests/test_cuda_prompt_graph.py tests/test_cuda_full_scheduler.py -k "not exclusive and not sitting and not native_keep and not matches_scalar and not native_smoke and not matches_scalar_taps"` → exit 0; `28 passed, 2 skipped, 6 deselected`
  - `QW38_RUN_CUDA_TESTS=1 QW38_CUDA_TEST_TIER=smoke uv run pytest -q tests/test_opt046_q4_decode.py tests/test_opt034_packed_mmv.py tests/test_opt042_mmv_integer_study.py tests/test_cuda_graph.py tests/test_cuda_fusion.py tests/test_cuda_prompt_graph.py -k "not sitting"` → exit 0; `20 passed, 2 deselected` (OPT-046 exclusive sitting reused the rejected fixture; OPT-042 sitting not re-run — it overwrites historical OPT-042 evidence)
  - `tests/test_cuda_full_scheduler.py` → **fail** on packed production (see blockers). Not a reason to install integer.
- Notes: Do not vendor ggml headers. Kernel name is `q4k_coop_mmv`, not `q4k_integer_mmv`. `--fmad=false` stays. No `nsys`/`ncu`. No `plan.md` edit. No commit. OPT-042 sitting must not be re-run. `tests/test_cuda_quant_mmv.py` live path needs `QW38_CUDA_TEST_TIER=acceptance` for `reference_sampled_calls>0`; smoke CUD-001 cases pass with packed. Integer is A/B-only behind `launch_q4k_coop_mmv`.

### Verification

- Attempt: 1
- Agent/model: composer-2.5
- Diff review: Matches dossier. Cooperative integer Q4_K decode dots live in `cuda/q4k_decode_dots.{cuh,cu}` behind `launch_q4k_coop_mmv`; production pin `kSelectedQ4DecodePath = "packed"` with warps per row **4** in `cuda/q4k_decode_path.cuh`. Packed FP32 `quant_mmv` retained; no `__dp4a` in `cuda/quant_mmv.cu`. Contract, fixture (`status=rejected`, `reverted=true`, `keep_sitting_skipped=false`), report, rejection, A/B raw sidecars, and host validators present. OPT-045 keep denominators frozen in contract.
- Commands:
  1. `uv run pytest -q tests/test_opt046_q4_decode.py tests/test_documentation.py -k "not sitting and not exclusive"` — exit 0; **7 passed, 1 deselected**
  2. `uv run pytest -q tests/test_opt046_q4_decode.py tests/test_opt034_packed_mmv.py tests/test_opt042_mmv_integer_study.py tests/test_production_numerics.py tests/test_cuda_quant_mmv.py tests/test_cuda_graph.py tests/test_cuda_fusion.py tests/test_cuda_prompt_graph.py -k "not exclusive and not sitting and not native_keep and not matches_scalar and not native_smoke and not matches_scalar_taps"` — exit 0; **28 passed, 2 skipped, 6 deselected** (implementation record)
  3. `QW38_RUN_CUDA_TESTS=1 QW38_CUDA_TEST_TIER=smoke uv run pytest -q tests/test_opt046_q4_decode.py tests/test_opt034_packed_mmv.py tests/test_opt042_mmv_integer_study.py tests/test_cuda_graph.py tests/test_cuda_fusion.py tests/test_cuda_prompt_graph.py -k "not sitting"` — exit 0; **20 passed, 2 deselected** (implementation record)
- Criteria checked:
  - Production stays packed: pass (`selected_q4_decode_path=packed`, integer behind A/B only)
  - A/B winner `integer_q8_w4` faster on weighted complete MMV but rejected: pass (CUD-001 `q4_k_17x256` max_abs **0.000305175781** > **3e-4**; full-scheduler quality fails under integer pin)
  - tok/s sitting ran then reverted: pass (`keep_sitting_skipped=false`, `reverted=true`)
  - OPT-045 keep denominators unchanged: pass (P **2130.41**, D128 **28.55**, D2048 **27.54** tok/s)
- Verdict: **pass**
- UTC/time/tokens/cost: 2026-09-10T19:01:30Z; telemetry_unavailable

### Retries and escalation

none

### Delivery

- Agent/model: composer-2.5 (delivery subagent)
- UTC/time/tokens/cost: 2026-09-10T19:01:30Z; telemetry_unavailable
- Outcome: verification pass 2026-09-10T19:01:30Z confirmed. Marked OPT-046 `done`. Final audit row published. Commit and push authorized. Delivery re-check: `uv run pytest -q tests/test_opt046_q4_decode.py tests/test_documentation.py -k "not sitting and not exclusive"` — **7 passed, 1 deselected**.

### Final outcome

- Status: `done` (reject)
- Acceptance evidence: verification pass 2026-09-10T19:01:30Z — live fixture [`fixtures/opt046_q4_decode.json`](../fixtures/opt046_q4_decode.json) (`measurement_utc` 2026-09-10T18:02:50Z), contract, report, rejection, A/B raw sidecars, host validators, and exclusive RTX 5090 native sitting. A/B winner `integer_q8_w4`; production `kSelectedQ4DecodePath=packed`. Ledger [`implementation_ledger.md`](../implementation_ledger.md); audit [`docs/65-documentation-audit.md`](../docs/65-documentation-audit.md).
- Throughput delta: baseline OPT-045 P **2130.41**, D128 **28.55**, D2048 **27.54** tok/s → post OPT-046 production (packed retained) **unchanged**; speedup **0**. Integer sitting measured P **2130.48462**, D128 **40.7888718**, D2048 **38.3443794** tok/s but rejected (CUD-001 and full-scheduler quality fail). Weighted complete MMV: packed **0.067196091** ms vs winner **0.0191882669** ms (~3.50×).
- Commit: pending on `main`
- Push: pending `origin/main`
- First-pass acceptance: **yes** (verification)
- Total elapsed/tokens/cost: telemetry_unavailable
- Remaining risk or recovery condition: GPU exclusivity remains mandatory for the native sitting. Do not install cooperative integer Q4_K decode without passing frozen CUD-001 and full-scheduler quality. `tests/test_cuda_full_scheduler.py` fails on current HEAD with packed MMV too (pre-existing; outside this increment). Do not treat this reject as the 2K llama.cpp parity gate. OPT-016 remains the blocked 2K parity owner. Next eligible recovery task: OPT-047.
- Agent/model for delivery: composer-2.5; telemetry_unavailable

### Verification

- Attempt: 1
- Agent/model: composer-2.5 (integration verifier)
- Diff review: **in scope** — cooperative Q4_K integer-dot kernels in `cuda/q4k_decode_dots.{cuh,cu}`, host path pin in `cuda/q4k_decode_path.cuh`, packed FP32 `quant_mmv` retained in `cuda/quant_mmv.cu`, A/B harness `cuda/opt046_q4_decode_ab_test.cu`, contract/fixture/tests/evidence. Production pins unchanged: `kSelectedQ4DecodePath = "packed"`, `kSelectedQ4DecodeWarpsPerRow = 4`, `kSelectedMmvLoadPath = "packed"`. `evidence/optimization/opt046-q4-decode/REJECTION.md` present. Fixture `fixtures/opt046_q4_decode.json`: `status=rejected`, `reverted=true`, `keep_sitting_skipped=false`, A/B winner `integer_q8_w4` (~3.50× complete MMV) but CUD-001 `q4_k_17x256` max_abs **0.000305175781** exceeds **3.0e-4** envelope.
- Commands:
  - `uv run ruff check .` → exit 0; `All checks passed!`
  - `QW38_RUN_CUDA_TESTS=1 QW38_CUDA_TEST_TIER=smoke uv run pytest -q tests/test_opt046_q4_decode.py tests/test_cuda_quant_mmv.py tests/test_opt034_packed_mmv.py tests/test_opt042_mmv_integer_study.py tests/test_documentation.py -k "not sitting and not exclusive and not matches_scalar"` → exit 0; `19 passed, 3 deselected` (OPT-046 exclusive sitting and OPT-042 sitting not re-run; `matches_scalar` needs `QW38_CUDA_TEST_TIER=acceptance` for `reference_sampled_calls>0`)
- Formatting changed files: none
- Verdict: **pass** — REJECT confirmed. Production Q4_K decode remains **`packed`** (4 warps per row pin retained for A/B). Integer cooperative path stays behind `launch_q4k_coop_mmv` only. `REJECTION.md` retained. No commit.
- UTC/time/tokens/cost: 2026-09-10T19:00:23Z; telemetry_unavailable
