# OPT-130 — Implement the evidenced dense decode-attention improvement

Status: done. Dependencies: OPT-129. Coupled IDs: none.
Priority: P1.

## Outcome

Implement the best bounded mechanism from OPT-129 with unchanged full-context
dense KV storage. Read OPT-125's shared protocol. Parent is post124 unless a
kept graph dependency is explicitly frozen and verified; OPT-132 owns final
cross-track interaction. This is not a repeat of OPT-120 packed KV.

## Implementation

1. Freeze at most two candidates rooted in the matched study, such as reducing
   duplicated KV loads across grouped query heads, a demonstrably better
   split/merge strategy, or the evidenced flash-attention consumer structure.
   Name the source function, causal work saved, projected enclosing benefit
   and exact production files before editing. Stop if the study shows no
   material mechanism or a representation incompatibility.
2. Change only `cuda/attention_decode.cu`, its necessary dispatch/workspace
   integration and task diagnostics. Preserve dense BF16 representation,
   causal visibility, partial RoPE conventions, gate application, GQA head
   mapping and output semantics. No full-cache conversion or shadow cache.
   Attribute any borrowed code and retain required licenses.
3. Reopen crossover only where the chosen algorithm changes measured cost.
   Freeze one bounded dispatch rule from screen data; no per-prefix tuning
   grid. Verify both sides of 1024 and all split boundaries with true populated
   prefixes. Keep P4096 prefill behavior guarded.
4. Reuse OPT-129 replay for output/consumer checks, then measure the complete
   attention branch and sample/eval/output requests. Include merge workspace,
   launch overhead and cold preparation. Treat component and request speedups
   separately; a faster leaf does not admit production.
5. Ensure changing frontier and topology remain representable by OPT-126's
   graph inputs if available. Preserve an eager fallback and report any graph
   incompatibility explicitly for OPT-132.

## Acceptance

Full OPT-116 quality, same-path attention/state tests at long cache, cancellation,
restore, 128K memory/reserve and OPT-125 paired gates must pass. Publish
`evidence/optimization/opt130-dense-attention/REPORT.md` with bounded
candidate results and keep/reject/no-opportunity. No new quality tolerance
or packed representation can be admitted by this task.

## Run record

### Implementation (cursor-grok-4.6-high, 2026-09-14T01:07:00Z)

KEEP/REJECT: **reject**. Production pins not flipped. Did not commit.
Parent is post124 combined plus OPT-127 `decode_segments8`. Shipping remains
`hybrid_crossover@1024` / `n_parts=16` / `verified_max=4096`.
`claims_throughput=false`. Sitting tok/s delta versus parent is **0**.
model `cursor-grok-4.6-high`.

Primary OPT-129 transfer `occupancy_partition_or_gqa_kv_reuse` frozen as one
bounded dispatch `hybrid_crossover@1024_vec128_open_nparts8`: occupancy-snapped
legal `n_parts=8` (GPU occupancy=2, nsm=170, D2048 raw n_parts=9 → snap 8) and
`vec128_online` past verified_max 4096 (candidate verified_max 131072).
`n_parts` frozen per topology, not per-token, so OPT-126/127 graphs stay
representable. GQA6 not admitted. Rank-2 `llama_mma_decode_consumer` not
launched (unmatched BF16 MMA; shadow F16 cache forbidden). Dense BF16 KV,
causal, RoPE, GQA, gates unchanged.

**Files created**
- `pins/opt130_dense_attention_contract.json`
- `pins/opt130_iteration_contract.json`
- `pins/opt130_dense_attention_provenance.json`
- `cuda/opt130_dense_attention_test.cu`
- `tools/opt130_dense_attention.py`
- `tests/test_opt130_dense_attention.py`
- `fixtures/opt130_dense_attention.json`
- `evidence/optimization/opt130-dense-attention/REPORT.md`

**Files modified**
- `cuda/attention_decode.cu` / `cuda/attention_decode.h` (occupancy
  raw/snapped `n_parts` helper)
- `cuda/attention_decode_path.cuh` (verified_max override, Opt130 RAII
  scopes; production pins unchanged)
- `cuda/opt058_quality_baseline_test.cu` (decode_attention /
  verified_max / vec128_n_parts / execution_graphs overrides)
- `Makefile` (`cuda-opt130-diagnostics`; binary links OPT-058 + checkpoint
  + memory-fit)
- `tools/run_optimization_task.py` (`QW38_OPT130_*` prefixes)
- `tasks/OPT-130.md` (this run record)

**Host validation**
```
uv run ruff format tools/opt130_dense_attention.py
uv run ruff check tools/opt130_dense_attention.py tests/test_opt130_dense_attention.py
# All checks passed!
uv run python tools/opt130_dense_attention.py --phase report --mode acceptance \
  --run-dir build/optimization-runs/opt130
# {"task": "OPT-130", "phase": "report", "ok": true, "verdict": "reject"}
uv run pytest tests/test_opt130_dense_attention.py -q
# 5 passed in 0.04s
```

**GPU phases** (device NVIDIA GeForce RTX 5090, image `qw38-cuda:13.0.2`).
Sidecars in `build/optimization-runs/opt130/`. Stale empty `qw38-gpu.lock`
was not held. Full OPT-125 gates including `--phase quality --mode acceptance`
with real OPT-058 harness (not stub); both parent and candidate NLL measured
(`same_math_copied_from_control=false`).

```
docker run --rm --gpus all --user "$(id -u):$(id -g)" \
  -v /home/user/qw38:/workspace -w /workspace qw38-cuda:13.0.2 \
  make -j"$(nproc)" cuda-opt130-diagnostics
# ok

uv run python tools/opt130_dense_attention.py --phase inspect --mode feedback --run-dir build/optimization-runs/opt130
# ok; occupancy=2 nsm=170 raw_n_parts_2048=9 snap=8; authenticate_post113
# expected execution_graphs=ffn_only vs actual decode_segments8 (OPT-127 keep)

uv run python tools/opt130_dense_attention.py --phase identity --mode feedback --run-dir build/optimization-runs/opt130
# ok; bitwise at 128/1023; max_abs ~1e-9 at 1024–32768; nonfinite 0

uv run python tools/opt130_dense_attention.py --phase primitive --mode feedback --run-dir build/optimization-runs/opt130
# ok; D1024 −0.038 ms; D8192 −0.195 ms; D32768 −1.320 ms (component)

uv run python tools/opt130_dense_attention.py --phase same-math --mode feedback --run-dir build/optimization-runs/opt130
# ok; exact=true; matched=8; eager_fallback=true

uv run python tools/opt130_dense_attention.py --phase cancellation --mode feedback --run-dir build/optimization-runs/opt130
# ok; mid_segment + commit; eight_layer cadence; atomic_commit=true

uv run python tools/opt130_dense_attention.py --phase handoff --mode feedback --run-dir build/optimization-runs/opt130
# ok; finite; max_abs=0

uv run python tools/opt130_dense_attention.py --phase screen-d128 --mode feedback --run-dir build/optimization-runs/opt130
uv run python tools/opt130_dense_attention.py --phase screen-d2048 --mode feedback --run-dir build/optimization-runs/opt130
uv run python tools/opt130_dense_attention.py --phase screen-p4096 --mode feedback --run-dir build/optimization-runs/opt130

uv run python tools/opt130_dense_attention.py --phase quality --mode acceptance --run-dir build/optimization-runs/opt130
# OPT-058 --quality both arms; candidate_nll_measured=true; held-out and
# wikitext NLL identical (1.7875990840085783 / 1.5249350773895778); PPL ratio 1.0

uv run python tools/opt130_dense_attention.py --phase state-memory --mode acceptance --run-dir build/optimization-runs/opt130
# ok; state_equals; checkpoint_ok; 128k_fit_with_graphs; cancellation_ok;
# memory_fit_parent_admitted=true

uv run python tools/opt130_dense_attention.py --phase d128 --mode acceptance --run-dir build/optimization-runs/opt130
uv run python tools/opt130_dense_attention.py --phase d2048 --mode acceptance --run-dir build/optimization-runs/opt130
uv run python tools/opt130_dense_attention.py --phase p4096 --mode acceptance --run-dir build/optimization-runs/opt130
# 3+10 AB/BA, 256 decode tokens:
# D128 request 57.969 → 57.968 geo 1.000 CI 0.99975 p95 1.000 (fail CI>1.00)
# D2048 request 44.150 → 45.006 geo 1.019 CI 1.019 p95 0.965 (pass)
# P4096 request 2895.56 → 2895.44 geo 1.000 CI 0.999 (non-target)

uv run python tools/opt130_dense_attention.py --phase report --mode acceptance --run-dir build/optimization-runs/opt130
# {"phase": "report", "ok": true, "verdict": "reject"}
```

Raw traces: `d128-raw.txt`, `d2048-raw.txt`, `p4096-raw.txt`, `screen-*-raw.txt`.
Quality sidecars: `quality-hybrid_crossover.txt`,
`quality-occupancy_vec128_open.txt`.

**Reject reasons:** `d128_throughput_gate`,
`measured_loss_or_no_request_win`. Keep needs both D128 and D2048 request
CI lower > 1.00. D2048 won; D128 is noise. Production stays parent.

**Measured candidate vs parent (informational; not admitted)**
| Workload | parent request tok/s | candidate request tok/s | Δ | decode-only Δ |
|---|---:|---:|---:|---:|
| D128 | 57.969 | 57.968 | −0.001 | +0.002 |
| D2048 | 44.150 | 45.006 | +0.856 | +1.093 |
| P4096 | 2895.56 | 2895.44 | −0.116 | −0.116 |

Sitting tok/s delta versus parent (reject, pins unchanged): **0**.

**Blockers:** none. Ledger status left pending for documentation /
verification / delivery. Did not commit.

### Verification (integration-verifier, 2026-09-14T01:08:00Z)

Independent integration verification against dossier acceptance, OPT-125
shared gates, and `build/optimization-runs/opt130/` sidecars. **Verdict: PASS**
(reject; `hybrid_crossover@1024` / `n_parts=16` / `verified_max=4096`
unchanged; D128 request CI lower 0.99975 fails keep gate).

**Host / build gates**
```
uv run ruff check tools/opt130_dense_attention.py tests/test_opt130_dense_attention.py
# All checks passed!

uv run ruff format --check tools/opt130_dense_attention.py
# 1 file already formatted

uv run pytest tests/test_opt130_dense_attention.py -q
# 5 passed in 0.03s

uv run python tools/opt130_dense_attention.py --phase report --mode acceptance --run-dir build/optimization-runs/opt130
# {"task": "OPT-130", "phase": "report", "ok": true, "verdict": "reject"}
```

**REJECT acceptance claims → artifacts**
| Claim | Artifact |
|---|---|
| Parent `hybrid_crossover@1024` unchanged | `report.json`: `production_kept=false`, `shipping_decode_attention=hybrid_crossover`; `attention_decode_path.cuh`: `kSelectedVec128NParts=16`, `kSelectedDecodeAttentionVerifiedMax=4096` |
| D128 request throughput gate fails (CI lower ≤ 1.00) | `d128.json` summary `complete_request`: geo 0.99998, `ci_lower=0.9997517817002282`, `ci.pass=false` |
| D2048 request throughput gate passes (informational) | `d2048.json` summary `complete_request`: geo 1.01939, `ci_lower=1.0192701018051358`, `ci.pass=true` |
| OPT-058 quality measured (not stub) | `quality.json`: `opt058_invoked=true`, `candidate_nll_measured=true`, `same_math_copied_from_control=false`; sidecars `quality-hybrid_crossover.txt` / `quality-occupancy_vec128_open.txt` contain `QW38_OPT058_RESULT` with scored NLL (wikitext 1.524935, held-out 1.787599) |
| PPL ratio within OPT-116 bounds | `quality.json`: `ppl_ratio=1.0`, `wikitext_ppl_ratio=1.0` |
| OPT-125 state/memory / cancellation | `state-memory.json` ok; `cancellation.json` `atomic_commit=true`; `handoff.json` finite |
| Identity / same-math / primitive feedback | `identity.json`, `same-math.json`, `primitive.json` ok |
| REPORT published with reject reasons | `evidence/optimization/opt130-dense-attention/REPORT.md`; `report.json` `verdict=reject`, reasons `d128_throughput_gate`, `measured_loss_or_no_request_win` |
| Sitting tok/s delta 0 on reject | `report.json` tok_s_deltas; dossier sitting delta **0** |

**Run-dir sidecars present:** `inspect.json`, `identity.json`, `primitive.json`,
`same-math.json`, `cancellation.json`, `handoff.json`, `screen-d128.json`,
`screen-d2048.json`, `screen-p4096.json`, `quality.json`,
`quality-hybrid_crossover.txt`, `quality-occupancy_vec128_open.txt`,
`state-memory.json`, `d128.json`, `d2048.json`, `p4096.json`, `report.json`
(+ raw traces).

**Verdict: PASS (reject).** Production pins not flipped. Sitting tok/s delta
versus parent: **0**.

### Delivery (2026-09-14T01:10:00Z)

model: composer-2.5, telemetry_unavailable

OPT-130 marked `done`. Coupled IDs: none. Production selector unchanged
`hybrid_crossover@1024` / `n_parts=16` / `verified_max=4096`; no throughput
claim. Next eligible pending task: **OPT-131**.

### Final outcome

- Status: `done`
- Verdict: `reject` (`d128_throughput_gate`, `measured_loss_or_no_request_win`)
- Acceptance evidence: [`fixtures/opt130_dense_attention.json`](../fixtures/opt130_dense_attention.json); [`evidence/optimization/opt130-dense-attention/REPORT.md`](../evidence/optimization/opt130-dense-attention/REPORT.md)
- Throughput delta versus sitting baseline: tok/s delta **0** (reject; `hybrid_crossover@1024` unchanged; `claims_throughput=false`)
- First-pass acceptance: yes
- Remaining risk: OPT-131 owns residual launch-chain fusion; OPT-132 owns combined decode-stack admission

