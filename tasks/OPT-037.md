# OPT-037 — Select 4K FFN tiles per projection

## Control

- Primary ID: `OPT-037`
- Coupled IDs: `none`
- Dependencies: `OPT-032`, `OPT-025`; all done at admission
- Status: `done`
- Ledger acceptance: Sweep quality-MMQ I={64,128}, J={32,64,128} independently for 4096-row gate/up/down, preserve shared-Y and recapture graphs; keep only with admitted component wins, improved P, frozen MMQ envelopes, and the cross-workload guard; otherwise reject

## Goal and boundaries

A/B production 4096-row Q4_K FFN quality MMA **independently** for gate, up, and down: sweep I∈{64,128} × J∈{32,64,128} (six 2D tiles per projection) against the current production I=128, J=128. Keep 2D scheduling, OPT-025 shared-Y / SwiGLU-into-Q8 staging, and existing Fallback tail dispatch. Recapture 4096 prompt FFN graphs when any installed tile changes grid/shared. Keep a non-baseline pin on a projection only when that projection's CUDA-event mean is **strictly lower** than same-sitting I=128/J=128 among envelope-passing candidates (ties retain production). Keep the production dispatch only when **at least one** projection has an admitted component win, live cold exact-4096 Quartz P **strictly beats** the then-current accepted OPT-034 oracle **1869.84412**, frozen CUD-002 MMQ envelopes hold, and the D128/D2048 cross-workload guard holds versus the OPT-034 post-keep baselines. Otherwise revert every FFN tile pin to I=128/J=128 and retain rejection evidence. This increment does not own OPT-016, does not change mixer Q8 tiles, and does not retarget attention-output Q6_K or decode MMV.

- Constraints: keep C++17, CUDA 13.0.2, `sm_120`, Makefile `NVCCFLAGS --fmad=false` and host `-ffp-contract=off` unchanged, the pinned Qwen3.8-27B Q4_K_M GGUF SHA-256 `31629f53165ab6a7dad8c9847dcfd1fdf55829dac1e6e748f4a68581b0033d34`, BF16 activation/KV storage, FP32 residual and GDN recurrence, production `GdnConfig{16, 48, 128, 128, 4}`, GDN-002 `kScanWindow = 64`, OPT-008 `kPromptChunkRows = 4096`, workspace byte formula `160,380,416 + 96*C + 404,992*R`, OPT-011 fused overlapped production plus retained unfused serial path, OPT-012 prompt FFN graphs (replay when `token_count == 4096`; recapture via existing `SchedulerGraphs::create` if MMA grid/shared change), OPT-003 decode FFN graphs, OPT-017/022/023 mixer Q8 quality plus skinny α/β (`kSelectedSkinnyMixerPath = mma_i32_j128`), OPT-018/025 FFN quality plus shared-Y (`kSelectedFfnPath = shared_y_swiglu_q8`), OPT-019/026 warp-column GDN and fattn stream-K (`kSelectedFattnPath = stream_k`, `grid.z=2`), OPT-033 register-resident VKQ (`kSelectedVkqAccum = registers`), OPT-035 dual-F16 P×V MMA (`kSelectedPvPath = mma`), OPT-036 decode 16/16 KV partitions, OPT-034 packed MMV (`kSelectedMmvLoadPath = packed`), SES-002 candidate-versus-committed isolation, Engine session capacity `131072`, and all frozen SCH-002/OPT-008/OPT-009/OPT-011/OPT-012/OPT-013/OPT-014/CUD-001/CUD-002/GDN-002/ATN-001/ATN-002/OPT-005 numeric and exact-state gates. Do not vendor ggml headers or `../ds4/cuda/mmq/`. Do not add a persistent session or workspace `cudaMalloc`. Exclusive RTX 5090 work stays serialized. Do not edit `plan.md`. Do not edit `speedup-plan.md`. Do not edit Makefile `NVCCFLAGS`. Do not run `nsys` or `ncu`. Do not change public Engine, CLI, server, or `qw38-bench` JSON schemas. Do not overwrite OPT-014/016/018/020/021/025/026/028/030/032/033/034/035/036 pins, fixtures, or evidence numeric admission fields. Do not reopen failed ideas OPT-024/027/028/029/030 without new evidence. Historical OPT-026 P **1746.71973**, OPT-032 P **1637.58594**, OPT-033 P **1745.10315**, OPT-035 P **1865.21155**, and OPT-036 P **1644.04822** are **not** this increment's P keep denominator.
- Non-goals: coupling OPT-031; changing mixer Q8 I/J or skinny `mma_i32_j128`; changing `selected_mma_mmq_prompt_tile()` (global J=128 for attention-output Q6_K and OPT-018 2048 joint sweep); stream-K MMQ (`kSelectedMmqStreamKPath` stays `off`); I=32 FFN tiles; Q8_0 through `launch_quant_mmq`; decode `execute_ffn` / packed MMV; changing fattn / register VKQ / P×V MMA / decode KV partitions; the OPT-016 2K tok/s ≥ llama.cpp gate; requiring Quartz P/D128/D2048 ≥ llama.cpp; loosening CUD-002 ds4 Q4_K association (`abs_scale=0.2`, `rel_tol=0.05`) or OPT-009 Q8 byte equality; extra persistent `cudaMalloc`; public API or BEN-001 schema changes; a new EDU task; editing `plan.md` or `speedup-plan.md`; treating `llama-bench` as a keep denominator; requiring byte-equal MMA output versus I=128/J=128 (tile reassociation); 8K/32K/128K throughput; QLT-001 128K quality; CMP-002/CMP-003; mutating `fixtures/opt034_packed_mmv.json`, `fixtures/opt035_pv_mma.json`, `fixtures/opt033_register_vkq.json`, `fixtures/opt036_decode_kv_partition.json`, `fixtures/opt032_decode_oracle.json`, `fixtures/opt025_ffn_shared_y.json`, or `pins/cuda_mmq_contract.json` numeric fields; retasking `cuda/prefill_4k_oracle_test.cu` or `cuda/decode_oracle_test.cu`.
- Plan impact: `none`
- Affected interfaces: no public Engine, CLI, server, model, or BEN-001 JSON API changes. CUDA-internal production prompt FFN MMA stays behind `execute_prompt_ffn_projections`. New schema-1 OPT-037 contract, fixture, A/B diagnostic, host test, and evidence files are task evidence, not a public protocol.
- Implementation classification: not mechanical. This designs per-projection quality-MMA I/J dispatch, occupancy/envelope eligibility, paired CUDA-event A/B, graph recapture when tiles change, and a P/D128/D2048 keep/reject evidence path with an explicit revert.
- Documentation stage: required. Update the existing prompt-MMQ, full-prefill, and optimization handbook chapters plus `docs/sources.md`, `docs/README.md`, and one non-final then final OPT-037 audit row. Implementation writes the measured keep/reject report from the live JSON so host validators can pass; documentation links that report and must not rewrite live numbers. Do not add an EDU task; EDU-026 already covers chapter 40. The audit-table row must not contain other task IDs. Because `tests/test_documentation.py` requires a non-final audit marker for every `in_progress` non-EDU task, planning inserts that row with this dossier.

## Repository evidence

- `implementation_ledger.md:106` — OPT-037 occurs exactly once; status at planning admission is `pending`; dependencies OPT-032, OPT-025; acceptance is sweep quality-MMQ I={64,128}, J={32,64,128} independently for 4096-row gate/up/down, preserve shared-Y and recapture graphs; keep only with admitted component wins, improved P, frozen MMQ envelopes, and the cross-workload guard; otherwise reject.
- `implementation_ledger.md:101` and `implementation_ledger.md:94` — dependencies OPT-032 and OPT-025 are `done`.
- `implementation_ledger.md:103` — OPT-034 is `done` (KEEP, production `kSelectedMmvLoadPath = packed`). Then-current accepted Quartz oracles after that keep are this prefill task's denominators. Do not mutate that fixture.
- `implementation_ledger.md:85` — OPT-016 is `blocked` and remains the sole ≥ llama.cpp **2K** gate; do not couple it and do not re-assert that gate.
- `implementation_ledger.md:100` — OPT-031 is `pending` and separable; do not couple it.
- QLT-001 is `blocked` on OPT-016; it is not pending and must not be coupled.
- EDU-026 is `done` and owns chapter 40; do not open a new EDU task.
- `fixtures/opt034_packed_mmv.json` — then-current accepted Quartz oracles after the packed-MMV keep (`measurement_utc` 2026-09-09T20:44:11Z). Confirmed exact values before this dossier: P `mean_tok_s` **1869.84412**; D128 **25.3816128** with all-token p95 **39.9736366** and run-mean p95 **39.4155655**; D2048 **20.169548** with all-token p95 **50.2872772** and run-mean p95 **49.590683**. Copy these into the OPT-037 contract. Do not mutate the OPT-034 fixture.
- `speedup-plan.md:49,90-92,94-124` — Rank 6 / OPT-037: measure Q4_K `17408×5120` gate/up and `5120×17408` down separately at 4096 rows. Keep 2D scheduling, quality staging, existing tail dispatch, and the baseline on any leg without a win; recapture shared-Y graphs when needed. P protocol unchanged from OPT-021. D128/D2048 protocol owned by OPT-032. All keeps require D128/D2048 throughput and p95 latency regression of no more than 5%. Prefill keeps must strictly beat the then-current accepted Quartz P oracle. Component A/Bs use three warm-ups and 30 alternating measurements; lowest mean among correctness-passing candidates; ties retain production. Component wins never authorize installation alone. An envelope miss is rejection.
- `plan.md:63-68` — restricted C++17; focused MIT llama.cpp techniques with file-level provenance; do not fork DwarfStar. **Do not edit `plan.md`.**
- `plan.md:104-107` — optimized fused paths keep a visible unfused reference; an optimization cannot loosen the tolerance used to admit it.
- `pins/cuda_mmq_contract.json` — CUD-002: ds4 Q4_K association `abs_scale=0.2`, `rel_tol=0.05`, zero non-finites. Do not edit these numbers.
- `pins/artifacts.lock.json` and `pins/llama_authority_contract.json` — GGUF SHA-256 `31629f53165ab6a7dad8c9847dcfd1fdf55829dac1e6e748f4a68581b0033d34`; llama.cpp revision `cc83d7b4824f73cfdda4dfbb47ee39804f71b328`.
- `cuda/quant_mmq_mma.cuh:352,1310,1474-1522,1836` — production quality MMA is I=`kQualityI=128`, J=`selected_mma_mmq_prompt_tile()=128`, 2D `dim3(32,8)`, stream-K path `off`, FFN path `shared_y_swiglu_q8`. `launch_quant_mmq_mma_y` always uses that global J. Kernel already templates `QualityI` and `PromptTile`; Q8 skinny instantiates I=32/64 at J=128; Q4_K occupancy already instantiates J∈{32,64,128} at I=128. OPT-037 adds Q4_K I=64 across those J values.
- `cuda/quant_mmq_mma.cuh:434-435,539-549` — `quality_rows_per_warp` is 16 for J=32 and 32 for J∈{64,128}. Static asserts hold for every I∈{64,128} × J∈{32,64,128}.
- `cuda/full_scheduler.cu:636-707` — production prompt FFN: one `launch_quantize_mmq_q8_1` of the FFN input, two `launch_quant_mmq_mma_y` (gate 17408×5120, up 17408×5120), `launch_swiglu_quantize_mmq_q8_1`, one `launch_quant_mmq_mma_y` down (5120×17408). This is the only production call site that may pass per-leg I/J. Decode `execute_ffn` stays MMV.
- `cuda/prompt_graph_test.cu:158-167` — prompt FFN capture requires `kernel_nodes >= 6` and rowwise residual `grid.x == 4096`, `block.x == 256`. Graph-versus-ordinary fused equality must hold. Recapture is automatic on `SchedulerGraphs::create`. Update this diagnostic only if a captured field would otherwise lie.
- `cuda/quant_mmv_test.cu:3082-3151` — OPT-018 2048-row joint J sweep still pins `selected_mma_mmq_prompt_tile()==128`. Do not change that pin. OPT-037 is a 4096-row per-projection overlay, not a global J retune.
- `src/mixer.h` / `src/weights.cpp` — FFN shapes: gate/up Q4_K `17408×5120`, down Q4_K `5120×17408`. `17408 % 64 == 0`, `17408 % 128 == 0`, `5120 % 64 == 0`, `5120 % 128 == 0`, `4096 % {32,64,128} == 0`, so 4K production tiles do not need Fallback. Keep Fallback for non-multiples.
- `tests/test_documentation.py:57-97` — a done non-EDU task needs a final audit row; an in-progress row must be visibly non-final. `_audit_rows` keys every task-ID token found in a row, so the OPT-037 audit-table row must not contain other task IDs.
- `tests/test_opt034_packed_mmv.py` and `tests/test_opt035_pv_mma.py` currently assert `opt037_not_started()` (no `pins/opt037_ffn_tile_contract.json`). Starting this increment requires removing those host-only guards. Do not mutate OPT-034/035 fixtures.
- Live 4K attribution from `speedup-plan.md` (informational sink magnitude; this increment targets FFN MMQ tiles):

| Sink | Time | Share | Note |
|---|---:|---:|---|
| Attention core | 867.45 ms | 36.9% | already improved by OPT-033/035; not this increment |
| FFN MMQ | 738.27 ms | 31.4% | quality MMA + shared-Y installed; dispatch follows the 2K J=128 pin rather than independent 4K projection shapes |
| GDN core | 453.23 ms | 19.3% | not this increment |
| Mixer MMQ | 285.69 ms | 12.2% | not this increment |

- Then-current keep/guard denominators (copy into the OPT-037 contract; do not mutate OPT-034):

| Oracle | Quartz mean tok/s | Quartz all-token p95 (ms) | Quartz run-mean p95 (ms) |
|---|---:|---:|---:|
| P | **1869.84412** | n/a (3 cold walls) | n/a |
| D128 | **25.3816128** | **39.9736366** | **39.4155655** |
| D2048 | **20.169548** | **50.2872772** | **49.590683** |

- Chosen sink: **4096-row Q4_K FFN quality MMA tiles** (gate, up, down independently). Keep denominator for P is the OPT-034 sitting Quartz mean **1869.84412**. Guard floors are ≥95% of OPT-034 D128/D2048 throughput and ≤105% of both p95 flavors. D2048 tok/s improvement is **not** required for this prefill keep.

## Implementation decisions

Keep decode packed MMV, fattn stream-K, register VKQ, P×V MMA, tiled prefill, mixer Q8 quality, skinny `mma_i32_j128`, FFN shared-Y / SwiGLU-Q8, GDN warp-column, decode 16/16 KV partitions, public Engine/`qw38-bench` schemas, `Makefile` `NVCCFLAGS`, `plan.md`, `speedup-plan.md`, `selected_mma_mmq_prompt_tile()==128`, `kSelectedMmqStreamKPath="off"`, and historical OPT-014/018/020/021/025/026/028/030/032/033/034/035/036 fixtures unchanged.

### Chosen sink (this increment)

Production **prompt FFN Q4_K quality MMA** behind `execute_prompt_ffn_projections`. Sweep I and J independently per projection at 4096 rows. Do not retarget mixer Q8, attention-output Q6_K, decode MMV, or stream-K.

### Production FFN MMA path today

| Symbol | Role |
|---|---|
| `execute_prompt_ffn_projections` | fused and unfused prompt FFN; shared-Y + SwiGLU-Q8 |
| `launch_quantize_mmq_q8_1` | frozen DS4 Q8_1 of FFN input; **must stay** |
| `launch_quant_mmq_mma_y` | quality MMA on prequantized Y at global I=128, J=128 |
| `launch_swiglu_quantize_mmq_q8_1` | frozen down-leg Y; **must stay** |
| `selected_mma_mmq_prompt_tile()` | global J=128 for Q4_K/Q6_K including attention-output; **must stay 128** |
| `launch_quant_mmq_variant` | OPT-009/CUD-002 scalar tiled kernel; **visible unloosened reference** |
| decode `execute_ffn` | MMV; **unchanged** |

### Per-projection I/J pins

Export from `cuda/quant_mmv.h`:

- `unsigned int selected_ffn_gate_quality_i() noexcept` / `selected_ffn_gate_prompt_tile() noexcept`
- same for `up` and `down`
- `bool legal_ffn_quality_i(unsigned int) noexcept` — `{64,128}`
- `bool legal_ffn_prompt_tile(unsigned int) noexcept` — `{32,64,128}`
- `int mma_mmq_occupancy_ij(QuantKind kind, unsigned int prompt_tile, unsigned int quality_i) noexcept`
- `cudaError_t launch_quant_mmq_mma_y_ij(QuantKind, weights, output_rows, columns, y, prompt_rows, output, quality_i, prompt_tile, stream, tmp_fixup…)` — 2D `launch_quality_dispatch` only; invalid I/J returns `cudaErrorInvalidValue`; never enables stream-K

Compile-time enumerators in `cuda/quant_mmq_mma.cuh`, default until A/B:

```
kSelectedFfnGateQualityI = 128, kSelectedFfnGatePromptTile = 128
kSelectedFfnUpQualityI   = 128, kSelectedFfnUpPromptTile   = 128
kSelectedFfnDownQualityI = 128, kSelectedFfnDownPromptTile = 128
```

`launch_quant_mmq_mma_y` remains I=128/J=`selected_mma_mmq_prompt_tile()` (attention-output and identity tests). Production FFN call sites switch to `_ij` with the per-leg pins. Shared-Y quantize and SwiGLU-into-Q8 stay. No extra `cudaMalloc`. Scheduler still does not grow a new public enumerator.

Pin the three winners in `pins/opt037_ffn_tile_contract.json` as `selected_ffn_{gate,up,down}_{quality_i,prompt_tile}` and hard-code the same enumerators after A/B.

### Kernel behavior (frozen)

Retain:

- 2D grid `ceil(output_rows/I)` × `ceil(prompt_rows/J)`, block `dim3(32, 8)`
- `MMQ_ITER_K=256`, packed load-tiles, Q8_1 MMQ Y in existing `prompt_q8_`
- Fallback kernel when `output_rows % I != 0` or `prompt_rows % J != 0`
- Stream-K path `off` for every FFN tile variant
- Shared-Y identity (one quantize, two MMA) and SwiGLU-Q8 Y identity
- CUD-002 ds4 Q4_K association versus CPU dequant GEMM

I=64 instantiations are new Q4_K template parameters on the existing quality kernel. Do not change MMA math, SRAM layout formulas, or association numbers.

### Paired CUDA-event A/B (required)

New `cuda/ffn_tile_ab_test.cu` on exclusive RTX 5090. Three independent projections:

| id | Shape | Staging |
|---|---|---|
| `gate` | Q4_K `17408×5120`, prompt_rows=4096 | one `launch_quantize_mmq_q8_1` of BF16 input (not timed) |
| `up` | Q4_K `17408×5120`, prompt_rows=4096 | same staging independently (not timed) |
| `down` | Q4_K `5120×17408`, prompt_rows=4096 | one `launch_quantize_mmq_q8_1` of BF16 input (not timed) |

Candidates per projection, fixed order:

`i64_j32`, `i64_j64`, `i64_j128`, `i128_j32`, `i128_j64`, `i128_j128`

`i128_j128` is the production baseline. Time **MMA only** (`launch_quant_mmq_mma_y_ij`) so shared-Y staging is not attributed to a tile. Identical packed weights and Y for all six candidates. Protocol: **3 warmup rounds** then **30 measured rounds**, each round launching all six candidates in the fixed order. Arithmetic mean of the 30 measured CUDA-event samples. Occupancy ≥ 1. Zero non-finites on the timed output. Envelope: ds4 Q4_K association versus CPU dequant GEMM on a smaller legal shape (`prompt_rows=64`, `output_rows=256`, `columns=256`) per (I,J); do not host-GEMM the 4096 production shape. Write every sample to `evidence/optimization/opt037-ffn-tiles/ffn-tile-ab-raw.txt`. Prefix `QW38_FFN_TILE_AB_RESULT=`.

Winner rule **per projection**: lowest arithmetic mean among candidates that launch, occupancy ≥ 1, zero non-finites, and pass the envelope. If that winner is `i128_j128` **or** its mean is not **strictly less** than the `i128_j128` mean, that leg retains production I=128/J=128 (`win=false`). Equality is not a win. Install a non-baseline pin only on legs with `win=true`.

Do not time decode MMV. Do not change mixer Q8 tiles. Do not add a Makefile object; keep the dispatcher in `quant_mmq_mma.cuh` included by `quant_mmv.cu`. Do not edit `NVCCFLAGS`.

### Production dispatch after A/B

Inside `execute_prompt_ffn_projections` (fused and unfused):

- Keep shared-Y and SwiGLU-Q8 predicates unchanged.
- Gate MMA: `launch_quant_mmq_mma_y_ij(..., selected_ffn_gate_quality_i(), selected_ffn_gate_prompt_tile(), ...)`
- Up MMA: corresponding up pins
- Down MMA: corresponding down pins
- Attention-output Q6_K continues through `matrix_prompt` → `launch_quant_mmq_mma` at global J=128
- If no projection wins: every enumerator stays 128/128; `_ij` remains callable for the diagnostic

If any installed pin differs from 128/128, 4096 FFN graphs recapture automatically on the next `SchedulerGraphs::create`. Update `cuda/prompt_graph_test.cu` only if a captured field would lie; keep `kernel_nodes >= 6`, rowwise `grid.x == 4096`, and graph-versus-ordinary fused byte equality.

Host `tests/test_opt034_packed_mmv.py` / `tests/test_opt035_pv_mma.py`: delete `opt037_not_started` so OPT-037's contract file does not fail those retained fixtures. Do not rewrite OPT-034/035 numbers.

### Keep / reject

Use the frozen OPT-021 P protocol and OPT-032 D128/D2048 protocol on exclusive RTX 5090. Reuse `cuda/prefill_4k_oracle_test.cu` and `cuda/decode_oracle_test.cu` (do not retask). Host `tests/test_opt037_ffn_tiles.py` runs llama.cpp first in the keep sitting, then Quartz P, D128, D2048, writes evidence JSON, and applies:

**Keep** (all required):

- At least one of gate/up/down has `win=true` with an envelope-passing candidate whose mean is strictly below `i128_j128`
- Production enumerators match those per-leg winners (baseline 128/128 on legs without a win)
- `kSelectedFfnPath` remains `shared_y_swiglu_q8`
- `selected_mma_mmq_prompt_tile()` remains 128; mixer skinny remains `mma_i32_j128`; stream-K remains `off`
- Frozen CUD-002 envelopes hold on every installed (I,J)
- Graph-versus-ordinary fused equality holds; `kernel_nodes >= 6`
- `quartz.mean_tok_s` (P) **> 1869.84412** (strict)
- Cross-workload guard versus OPT-034: D128/D2048 mean tok/s ≥ 95% of **25.3816128** / **20.169548**; D128 all-token p95 ≤ 105% of **39.9736366** and run-mean p95 ≤ 105% of **39.4155655**; D2048 all-token p95 ≤ 105% of **50.2872772** and run-mean p95 ≤ 105% of **49.590683**
- `reverted` is false; `status` is `measured`; `keep_sitting_skipped` is false
- `owns_opt016_parity_gate` and `substitutes_for_opt016` are false

**Reject** (all required):

- No projection won **or** keep sitting failed P improvement / guard / envelope
- Production FFN tiles are **reverted** to I=128/J=128 on every leg
- Shared-Y / SwiGLU-Q8 stay installed (this increment does not revert OPT-025)
- `reverted` is true, `status` is `rejected`
- `keep_sitting_skipped` is true if A/B produced no component win (no P/D128/D2048 sitting); false if the sitting ran and failed
- retained `evidence/optimization/opt037-ffn-tiles/REJECTION.md` plus the live JSON

Equality on P is a reject. Quartz ≥ llama.cpp is informational and must not be a pass/fail predicate. Nsight is `not_used`. D2048 tok/s improvement is not required.

If association, launch, or A/B instrumentation fails before a legal sitting, that is an implementation failure (repair / block), not the keep/reject revert path.

### Files to add or change (keep path)

| Path | Role |
|---|---|
| `cuda/quant_mmq_mma.cuh` | per-leg I/J enumerators; `launch_quant_mmq_mma_y_ij`; occupancy_ij |
| `cuda/quant_mmv.h` | exported helpers |
| `cuda/full_scheduler.cu` | fused/unfused FFN MMA `_ij` dispatch |
| `cuda/quant_mmv_test.cu` | legal pins, occupancy, small-shape I=64 CUD-002 via `_ij`; do not change the 2048 J=128 pin gate |
| `cuda/ffn_tile_ab_test.cu` | new; independent 3+30 A/B; prefix `QW38_FFN_TILE_AB_RESULT=` |
| `cuda/prompt_graph_test.cu` | only if a captured FFN field would lie |
| `tests/test_opt037_ffn_tiles.py` | contract, mutations, CUDA sitting, keep/reject |
| `tests/test_opt034_packed_mmv.py` | remove `opt037_not_started` |
| `tests/test_opt035_pv_mma.py` | remove `opt037_not_started` |
| `pins/opt037_ffn_tile_contract.json` | schema 1; OPT-034 Quartz denominators |
| `fixtures/opt037_ffn_tiles.json` | live keep or reject record |
| `evidence/optimization/opt037-ffn-tiles/` | REPORT.md, quartz JSON, llama JSON, ffn-tile-ab-raw.txt; REJECTION.md only on reject |

Do not edit: `plan.md`, `speedup-plan.md`, `Makefile` `NVCCFLAGS`, `include/qw38/engine.h`, `src/engine.cpp`, `src/bench.cpp`, `pins/benchmark_contract.json`, `pins/artifacts.lock.json`, `pins/cuda_mmq_contract.json` numeric fields, `pins/cuda_prompt_mmq_contract.json` numeric/selection fields, `pins/opt016_parity_contract.json`, `fixtures/opt016_parity.json`, `tests/test_opt016_parity.py`, `cuda/prefill_2k_parity_test.cu`, `cuda/prefill_4k_oracle_test.cu` (invoke unchanged), `cuda/decode_oracle_test.cu` (invoke unchanged), any OPT-018/025/028/032/033/034/035/036 pin, fixture, or evidence numeric file.

### Documentation/evidence updates

| Path | Content |
|---|---|
| `docs/40-cuda-prompt-mmq.md` | 4K FFN per-projection I/J, A/B winner, keep/reject, graph recapture |
| `docs/62-cuda-full-prefill.md` | production prompt FFN tile pins and 4K keep/reject outcome |
| `docs/06-system-optimization.md` | Measured 4K FFN tile selection; 4K keep or retained rejection |
| `docs/sources.md` | llama.cpp Ampere MMQ I/J config provenance (already cited; add OPT-037 use) |
| `docs/README.md` | one-line chapter 6/40/62 index updates |
| `docs/65-documentation-audit.md` | non-final OPT-037 row while `in_progress`, then final row at delivery; row body must not contain other task IDs |

- Invariants: OPT-009 Q8_0 tiled-versus-reference remains byte-exact; CUD-002 numbers unloosened; Q4_K MMA association unloosened; workspace formula unloosened; no extra persistent `cudaMalloc`; production Q8_0 does not go through `launch_quant_mmq`; decode FFN/MMV unchanged; mixer Q8 quality + skinny `mma_i32_j128` + no D2R unchanged; `selected_mma_mmq_prompt_tile()` stays 128; stream-K stays off; shared-Y / SwiGLU-Q8 stay; SES-002 isolation unchanged; graphs still created/replayed only for prompt FFN at 4096 and recaptured if MMA I/J nodes change; keep requires at least one admitted component win **and** strictly greater 4K mean tok/s than **1869.84412** **and** the OPT-034 cross-workload guard; reject restores I=128/J=128 on every FFN leg; OPT-016 remains the blocked 2K parity owner; `plan.md` unchanged; OPT-034 fixture unchanged.
- Rejected alternatives:
  - Changing global `selected_mma_mmq_prompt_tile()` — would retune attention-output Q6_K and break the frozen OPT-018 2048 J=128 pin.
  - Joint gate+up timing as one sweep — ledger requires independent gate/up/down.
  - Including I=32 — not in the admitted set; skinny I=32 stays mixer-only.
  - Timing quantize with MMA — staging is shared-Y and unchanged; the tile is the MMA kernel.
  - Byte equality versus I=128/J=128 — tile reassociation is expected; CUD-002 association is the envelope.
  - Enabling stream-K on a winning J=128 tile — OPT-028 rejected stream-K; keep 2D scheduling.
  - Requiring D2048 tok/s improvement — this is a prefill task; the guard is no more than 5% regression.
  - Coupling OPT-016, OPT-031, QLT-001, or a new EDU task — none is pending-eligible inseparable documentation.
  - Editing `plan.md` or `speedup-plan.md`.
  - Overwriting the OPT-034 fixture — the denominator must stay.
  - Treating Quartz ≥ llama.cpp as this gate.
- Discovered ledger work: `none`
- Unresolved decisions: `none`

## Acceptance and validation

- Acceptance conditions:
  - Quality-MMQ I∈{64,128} × J∈{32,64,128} is swept independently for 4096-row gate, up, and down; shared-Y / SwiGLU-Q8 remain; 2D scheduling and Fallback tail dispatch remain; graphs recapture if installed tiles change.
  - Each projection installs a strictly faster envelope-passing winner or retains I=128/J=128.
  - Keep requires at least one admitted component win, live P **> 1869.84412**, frozen CUD-002 envelopes, and the OPT-034 D128/D2048 throughput/p95 guard; otherwise every FFN pin is reverted to 128/128 with retained rejection.
  - `launch_q8_mmq_bf16_variant` versus `launch_q8_mmq_bf16_reference` remains byte-exact; `selected_mma_mmq_prompt_tile()` stays 128; mixer skinny stays `mma_i32_j128`.
  - `owns_opt016_parity_gate` false; `substitutes_for_opt016` false; native test does not require `quartz_meets_llama`.
  - Workspace formula unchanged; no extra persistent `cudaMalloc`; `plan.md` and Makefile `NVCCFLAGS` unchanged; OPT-034 fixture unchanged.
  - Coupled IDs: none.
- Tests/fixtures to add or change:
  - `cuda/quant_mmq_mma.cuh`, `cuda/quant_mmv.h`, `cuda/full_scheduler.cu`
  - `cuda/quant_mmv_test.cu`
  - `cuda/ffn_tile_ab_test.cu`
  - `tests/test_opt037_ffn_tiles.py`
  - `tests/test_opt034_packed_mmv.py`, `tests/test_opt035_pv_mma.py` (remove `opt037_not_started` only)
  - `pins/opt037_ffn_tile_contract.json`
  - `fixtures/opt037_ffn_tiles.json`
  - `evidence/optimization/opt037-ffn-tiles/`
- Focused commands: `uv run pytest -q tests/test_opt037_ffn_tiles.py tests/test_documentation.py`
- Repository-wide commands: `uv run ruff format .`; `uv run ruff check .`; `uv run pytest -q`
- Native/CUDA/hardware gates: `QW38_RUN_CUDA_TESTS=1 uv run pytest -q tests/test_opt037_ffn_tiles.py tests/test_cuda_quant_mmv.py tests/test_cuda_prompt_graph.py` on exclusive RTX 5090 (A/B + optional keep sitting). Also `make build/qw38-cuda-quant-test` inside `qw38-cuda:13.0.2` and run that binary. Do not run `nsys`/`ncu`. Do not run OPT-016 2K parity. Do not retask oracle binaries.
- Documentation/evidence updates: chapters 40, 62, 6; `docs/sources.md`; `docs/README.md`; audit row; REPORT.md from live JSON.
- Definition of done: exclusive RTX 5090 A/B recorded; production FFN tiles either keep with admitted wins + improved P + envelopes + guard, or revert to 128/128 with rejection evidence; dossier complete; verification pass; one delivery commit and push.

## Run record

### Planning

- Agent/model: sequential multi-stage agent (Cursor Grok 4.6) under run-ledger-task-cursor; planning effort medium
- UTC/time/tokens/cost: 2026-09-09T21:47:38Z; `telemetry_unavailable` (no matching Codex session JSONL for `account_usage.py`)
- Outcome: decision-complete dossier at `tasks/OPT-037.md`. Coupled IDs: none. Unresolved decisions: none. Marked OPT-037 `in_progress`. Non-final audit row inserted. Copied OPT-034 Quartz denominators P **1869.84412**, D128 **25.3816128**, D2048 **20.169548**. `plan.md` unchanged.

### Implementation

- Agent/model: sequential multi-stage agent (Cursor Grok 4.6) under run-ledger-task-cursor; implementation effort medium
- Changes:
  - `cuda/quant_mmq_mma.cuh`: compile-time per-leg pins `kSelectedFfn{Gate,Up,Down}{QualityI,PromptTile}` default 128/128; `mma_mmq_occupancy_ij` instantiates Q4_K I∈{64,128} × J∈{32,64,128}; `launch_quant_mmq_mma_y_ij` uses 2D `launch_quality_dispatch` only (never stream-K). `launch_quant_mmq_mma_y` stays I=128 / `selected_mma_mmq_prompt_tile()==128`.
  - `cuda/quant_mmv.h`: `legal_ffn_quality_i/prompt_tile`, `selected_ffn_{gate,up,down}_{quality_i,prompt_tile}`, `mma_mmq_occupancy_ij`, `launch_quant_mmq_mma_y_ij`.
  - `cuda/full_scheduler.cu`: fused/unfused `execute_prompt_ffn_projections` gate/up/down MMA call `_ij` with per-leg pins. Shared-Y / SwiGLU-Q8 staging unchanged.
  - `cuda/quant_mmv_test.cu`: `run_mma_ij_case` + `run_ffn_tile_pin_suite` (legal pins; reject I=32 and Q6_K I=64; all six combos on 64×256×256). OPT-018 2048 J=128 pin gate unchanged.
  - `cuda/ffn_tile_ab_test.cu`: independent 3+30 MMA-only A/B; prefix `QW38_FFN_TILE_AB_RESULT=`; per-tile nonfinite check after timing.
  - `tests/test_opt037_ffn_tiles.py`, `pins/opt037_ffn_tile_contract.json`.
  - `tests/test_opt034_packed_mmv.py` / `tests/test_opt035_pv_mma.py`: removed `opt037_not_started`.
  - Sitting artifacts: `fixtures/opt037_ffn_tiles.json`, `evidence/optimization/opt037-ffn-tiles/{REPORT.md,REJECTION.md,ffn-tile-ab-raw.txt}`.
  - Production pins remain 128/128; `reverted` true; `status` rejected. `plan.md` unchanged. No commit during this stage.
- Commands:
  - `make build/qw38-cuda-quant-test` — exit 0. `./build/qw38-cuda-quant-test` — exit 0; all six I/J combos occupancy 1, association `bad=0`, `nonfinite=0`. Restored OPT-023/024/025/028 A/B raw files to HEAD after that binary rewrote them.
  - Exclusive RTX 5090 A/B (`measurement_utc` 2026-09-09T22:23:11Z, device NVIDIA GeForce RTX 5090 compute 12.0): every candidate eligible; production `i128_j128` strictly fastest on every projection. Means (ms) from `fixtures/opt037_ffn_tiles.json`: gate 6.378 / 5.030 / 4.507 / 4.707 / 3.911 / **3.519**; up 6.393 / 5.041 / 4.518 / 4.718 / 3.921 / **3.530**; down 6.327 / 4.943 / 4.461 / 4.701 / 4.010 / **3.794**. `any_win=false`; keep sitting skipped; pins remain 128/128.
  - Host pytest without native sitting: `uv run pytest -q tests/test_opt037_ffn_tiles.py tests/test_documentation.py` — 14 passed (native keep-reject skipped). Later native revalidation of the rejected fixture: 9.48s, short-circuit on existing `status=rejected`.
- UTC/time/tokens/cost: 2026-09-09T22:23:11Z sitting; `telemetry_unavailable` (no matching Codex session JSONL for `account_usage.py`)

### Documentation

- Agent/model: sequential multi-stage agent (Cursor Grok 4.6) under run-ledger-task-cursor; documentation effort medium
- Changes and evidence:
  - `docs/40-cuda-prompt-mmq.md`: OPT-037 section after OPT-028; independent gate/up/down I/J sweep; A/B retained `i128_j128`; graphs not recaptured; live numbers linked to the report/rejection.
  - `docs/62-cuda-full-prefill.md`: production FFN pins stay 128/128 after reject; graphs not recaptured.
  - `docs/06-system-optimization.md`: measured reject of 4K FFN I/J selection versus frozen OPT-034 P/D128/D2048 denominators; intro notes production stays I=128/J=128.
  - `docs/sources.md`: OPT-037 reuse of already-cited llama.cpp Ampere MMQ I/J; measured reject; proof limit includes admitted component wins / improved P / frozen MMQ envelopes / cross-workload guard.
  - `docs/README.md`: chapter 6 one-liner adds the 4K FFN per-projection tile reject.
  - `docs/65-documentation-audit.md`: non-final in-progress row kept until delivery; first cell `OPT-037` only; evidence URLs lowercase.
  - Report already written from live JSON in the implementation sitting; this stage links it and does not rewrite live tok/s.
- UTC/time/tokens/cost: 2026-09-09T22:27:00Z; `telemetry_unavailable`

### Verification

- Attempt: 1
- Agent/model: sequential multi-stage agent (Cursor Grok 4.6) under run-ledger-task-cursor; verification effort low
- Diff review: Uncommitted work matches dossier REJECT scope. Touched: `cuda/quant_mmq_mma.cuh` (per-leg 128/128 pins, `mma_mmq_occupancy_ij`, `launch_quant_mmq_mma_y_ij` 2D `launch_quality_dispatch` only), `cuda/quant_mmv.h` (legal/selected/`_ij` helpers), `cuda/full_scheduler.cu` (gate/up/down MMA `_ij` with per-leg pins; shared-Y/SwiGLU-Q8 unchanged), `cuda/quant_mmv_test.cu` (`run_mma_ij_case` + `run_ffn_tile_pin_suite`; 2048 J=128 pin gate unchanged), new `cuda/ffn_tile_ab_test.cu`, contract/fixture/evidence, host test, handbook chapters 40/62/6 plus `docs/sources.md` / `docs/README.md`, non-final audit row, ledger `in_progress` + planning row. `selected_mma_mmq_prompt_tile()` stays `return 128U`. `kSelectedFfnPath` stays `shared_y_swiglu_q8`. Stream-K stays `off`. Mixer skinny stays `mma_i32_j128`. Packed MMV stays `packed`. Forbidden paths unchanged (`plan.md`, `speedup-plan.md`, Makefile `NVCCFLAGS`, public Engine/`qw38-bench` schemas, OPT-014/016/018/020/021/025/026/028/030/032/033/034/035/036 pins/fixtures numeric fields, `cuda/prefill_4k_oracle_test.cu` / `cuda/decode_oracle_test.cu`). Named CUDA quant-test rewrote OPT-023/024/025/028 A/B raw files; those four evidence files were restored to HEAD so the OPT-037 diff does not mutate them. Keep/reject traced to `fixtures/opt037_ffn_tiles.json` plus `evidence/optimization/opt037-ffn-tiles/ffn-tile-ab-raw.txt`: recomputed 30-sample means match recorded `mean_ms`; every projection's lowest eligible mean is `i128_j128`; raw warmup round-0..2 samples match fixture `warmup_ms`; `any_win=false`; `status=rejected`; `reverted=true`; `keep_sitting_skipped=true`; `p`/`d128`/`d2048` null. Production pins in source remain 128/128. Frozen OPT-034 denominators in `fixtures/opt034_packed_mmv.json` are unchanged (P **1869.84412**, D128 **25.3816128**, D2048 **20.169548**).
- Commands:
  - `uv run ruff format .` — exit 0; 1 file reformatted (`tests/test_opt037_ffn_tiles.py`, untracked), 108 files left unchanged. Focused pytest below ran after that format.
  - `uv run ruff check .` — exit 0; all checks passed.
  - `uv run pytest -q tests/test_opt037_ffn_tiles.py tests/test_documentation.py` — exit 0; 7 passed, 1 skipped (native keep-reject CUDA-gated).
  - `uv run pytest -q` — exit 0; 240 passed, 49 skipped in 159.53s.
  - `QW38_RUN_CUDA_TESTS=1 uv run pytest -q tests/test_opt037_ffn_tiles.py tests/test_cuda_quant_mmv.py tests/test_cuda_prompt_graph.py` — exit 0; 10 passed in 1442.06s (0:24:02) on exclusive RTX 5090. Native keep-reject revalidated the existing rejected fixture (did not re-sit). `test_cuda_quant_mmv_matches_scalar_reference` rebuilt and ran `qw38-cuda-quant-test` in `qw38-cuda:13.0.2`. Prompt-graph native smoke passed without regenerating `fixtures/cuda_prompt_graph.json`.
- Formatting changed files: `tests/test_opt037_ffn_tiles.py` (ruff); focused pytest rerun after format (same command chain). Neighbor A/B raws restored to HEAD after CUDA quant-test.
- Verdict: `pass` — REJECT. No admitted component win (`any_win=false` from `fixtures/opt037_ffn_tiles.json`; gate/up/down CUDA-event means **3.51894069** / **3.52976418** / **3.79385281** ms for `i128_j128` strictly below every other eligible tile). Keep sitting skipped. Production FFN tiles remain I=128 / J=128. P/D128/D2048 unchanged at OPT-034 oracles **1869.84412** / **25.3816128** / **20.169548**. `reverted` true; `status` rejected; `REJECTION.md` retained. `plan.md` unchanged. OPT-016 remains the blocked 2K parity owner. Task left `in_progress` until delivery; no commit during verification.
- UTC/time/tokens/cost: 2026-09-09T23:03:58Z; `telemetry_unavailable`

### Retries and escalation

none

### Final outcome

- Status: `done`
- Acceptance evidence: verification Attempt 1 passed. Production 4096-row Q4_K FFN quality MMA stays I=128 / J=128 on gate, up, and down (`kSelectedFfn{Gate,Up,Down}{QualityI,PromptTile}` = 128). Independent I∈{64,128} × J∈{32,64,128} A/B retained `i128_j128` on every projection (`any_win=false`). Shared-Y / SwiGLU-Q8 stays. 2D scheduling stays. Prompt FFN graphs were not recaptured. Retained live reject is [`fixtures/opt037_ffn_tiles.json`](../fixtures/opt037_ffn_tiles.json) (`measurement_utc` 2026-09-09T22:23:11Z) plus [`evidence/optimization/opt037-ffn-tiles/REPORT.md`](../evidence/optimization/opt037-ffn-tiles/REPORT.md), [`REJECTION.md`](../evidence/optimization/opt037-ffn-tiles/REJECTION.md), and [`ffn-tile-ab-raw.txt`](../evidence/optimization/opt037-ffn-tiles/ffn-tile-ab-raw.txt). `status` rejected; `reverted` true; `keep_sitting_skipped` true. Native test does not require Quartz ≥ llama.cpp. Ledger [`implementation_ledger.md`](../implementation_ledger.md); audit [`docs/65-documentation-audit.md`](../docs/65-documentation-audit.md).
- Throughput delta (when applicable): P **1869.84412** → **1869.84412**; D128 **25.3816128** → **25.3816128**; D2048 **20.169548** → **20.169548** (tok/s sitting skipped; denominators unchanged).
- Commit: authorized delivery on `main`; hash recorded after the commit
- Push: ordinary push to configured `origin/main`; result recorded after push
- First-pass acceptance: yes (verification Attempt 1 `pass`)
- Total elapsed/tokens/cost: telemetry_unavailable
- Remaining risk or recovery condition: GPU exclusivity remains mandatory for later native sittings. Do not claim Quartz ≥ llama.cpp. Do not treat this reject as the 2K llama.cpp parity gate. OPT-016 remains blocked independently. Do not edit `plan.md`.
- Agent/model for delivery: Cursor Grok 4.6; telemetry_unavailable

