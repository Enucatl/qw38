# OPT-091 — Bound the fast-Q4 quality tradeoff
Status: done. Dependencies: OPT-090. Coupled IDs: none.

## Run record

### Planning (2026-09-12T07:46:00Z)

skipped — pre-authored dossier admitted (coordinator)
Priority: P0. Target: a named successor quality gate with one conditional late_w4 concession.
Read [the post-088 protocol](PERFORMANCE-RECOVERY-POST-088.md).
Quality risk: a small PPL increase may conceal task-specific damage; inherited arithmetic inaccuracy remains visible.
Instrumentation: strict and successor NLL/PPL verdicts, recurrence deltas, task/continuation outputs, fresh D128/D2048 benefit and p95 if a concession is exercised.

## Outcome

Establish the successor quality gate and decide the narrowly bounded late_w4
concession. Do not waive candidate measurement. If OPT-089 already kept late
under strict quality, or late failed anything other than the specified PPL
limit, complete with `concession_used=false` and no new production selection.

## Implementation

1. Create `pins/opt091_quality_tradeoff_contract.json`, the iteration contract,
   `tools/opt091_quality_tradeoff.py`, `tests/test_opt091_quality_tradeoff.py`,
   `fixtures/opt091_quality_tradeoff.json` and
   `evidence/optimization/opt091-quality-tradeoff/REPORT.md`. Reuse OPT-089's
   native diagnostics through Makefile `cuda-opt091-diagnostics`. Extend
   `tools/quality/compare.py` and `suite.py` with an explicit contract argument;
   never silently change the historical default or OPT-084 pins.
2. Freeze fields: `quality_contract_id=opt091_late_w4_v1`,
   `eligible_candidate=late_w4`, `default_ppl_ratio_max=1.01`,
   `candidate_ppl_ratio_max=1.015`, `aggregate_ppl_ratio_max=1.015`,
   `anchors=[opt084_frozen,opt088_authenticated]`,
   `recurrence_incremental_nll_max=0.02`, `require_full_candidate_Q=true`,
   `allow_incomplete_quality=false`, `new_functional_failures_max=0`,
   `allow_new_greedy_mismatch=false`, `allow_changed_inherited_answer=false`,
   `decode_speed_ratio_ci_lower_min=1.15`, `decode_p95_ratio_max=0.90`,
   `kernel_parity_contract=kernel_parity_v1`, `historical_gate_relabel=false`.
   These numbers were proposed before OPT-089's new results; do not fit them
   to measured candidate errors. No automatic extension to new Q4 candidates.
3. Always produce the policy record and separate
   `absolute_quality_status`, `strict_model_quality_pass`,
   `successor_model_quality_pass`, `regression_release_quality_pass` and
   `quality_contract_id`. The last release-quality field requires complete
   finite/state/regression evidence, not absolute arithmetic success. Document
   the successor in `docs/06-system-optimization.md` during implementation;
   keep original OPT-056/016 owners and failures visible. Update the plan only
   with a dated note reflecting this explicit user-authorized quality scope.
4. Decision is fixed: if late has complete Q and fails strict admission solely
   because at least one PPL ratio exceeds 1.01 but all are ≤1.015, evaluate the
   concession. Any missing Q, parity/same-math issue, new task/continuation
   failure, recurrence violation or ratio >1.015 means retain the OPT-089
   selection. Reuse authenticated OPT-089 scores; the long quality command
   computes missing real records only and cannot fabricate a pass. A changed
   candidate binary/selector/input invalidates its cache.
5. Conditional timing compares exactly `packed_paired_staged` against
   `late_w4` (Q4 `integer_q8_late`, FFN `paired_integer`, four warps, FP32 Q8),
   with Q8 r1 and every other selector fixed to OPT-089. Full 64-layer FFN
   shapes gate/up=(17408,1,5120), down=(5120,1,17408). Use 3+10 component
   pairs then five fresh D128+32 and five D2048+32 AB/BA pairs, each with p95.
   In each prefix compute paired log throughput ratios and the one-sided 95%
   Student-t lower bound (df4=2.132), exponentiated; require >1.15. Require
   observed p95 ≤0.90× control at both, plus the common P4096 guard.
6. Promote at most late in `cuda/q4k_decode_path.cuh` and
   `cuda/ffn_decode_path.cuh` only after all successor and performance gates.
   Explicitly record the accepted risk: at most 1.5% PPL drift on frozen
   spans does not bound every task or long context. Roll back to the exact
   OPT-089 selection if any later combined Q/PPL/recurrence/state/task test
   fails, or the benefit fails OPT-098 confirmation. Do not ratchet baselines.

### Implementation record (2026-09-12T07:52:00Z)

Created dossier files and wired successor policy. OPT-089 authenticated
`late_w4` strict quality reused; policy records `concession_used=false`,
`claims_throughput=false`, `timed_phases_status=not_applicable`, no pin changes.

```text
uv run pytest -q tests/test_opt084_quality_baseline.py tests/test_opt089_q4_promotion.py tests/test_opt091_quality_tradeoff.py
# 45 passed in 0.37s

uv run python tools/run_optimization_task.py --task OPT-091 --mode feedback --phase policy
# status=passed success=true result_class=ok elapsed_s=0.047 concession_used=false
```

Policy outputs: `absolute_quality_status=visible_and_separate`,
`strict_model_quality_pass=true`, `successor_model_quality_pass=true`,
`regression_release_quality_pass=true`, `quality_contract_id=opt091_late_w4_v1`,
PPL ratios `opt088_authenticated≈0.999`, `opt084_frozen≈0.999`.

## Acceptance

- Policy fields, formulas and conditional routing are machine-validated;
  quality-incomplete, PPL=1.016, recurrence=0.021, changed task answer and
  selector-mismatched-cache negative examples reject. Passing PPL alone rejects.
- No concession is applied to a same-math failure or another candidate.
- Record independent `kernel_parity_pass`, `model_quality_pass`,
  `performance_pass`, `production_kept` with the active contract ID; old-contract
  failure remains false even when successor quality passes.
- `claims_throughput: true` only for the intended conditional measured decision;
  policy-only/no-concession evidence emits false.

## Tests and commands

```sh
uv run pytest -q tests/test_opt084_quality_baseline.py tests/test_opt089_q4_promotion.py tests/test_opt091_quality_tradeoff.py
uv run python tools/run_optimization_task.py --task OPT-091 --mode feedback --phase policy
uv run python tools/run_optimization_task.py --task OPT-091 --mode release --phase quality
uv run python tools/run_optimization_task.py --task OPT-091 --mode acceptance --phase q4
uv run python tools/run_optimization_task.py --task OPT-091 --mode acceptance --phase d128
uv run python tools/run_optimization_task.py --task OPT-091 --mode acceptance --phase d2048
uv run python tools/run_optimization_task.py --task OPT-091 --mode acceptance --phase decision
```

`policy` is host-only under 300 s. Quality reuses exact authenticated scores or
runs full Q in the explicit long mode. Timed phases run only for the eligible
concession branch; otherwise record `not_applicable` without GPU work.
`decision` includes one P4096 guard only for an otherwise admitted survivor.
Do not repeat a losing timing sitting or expand the numeric budget.

## Non-goals

No kernel implementation, incomplete-NLL promotion, automatic near-tie allowance,
recurrence relaxation, absolute-accuracy claim, remote scorer or old-gate waiver.
