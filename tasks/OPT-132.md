# OPT-132 — Admit the combined decode stack and publish corrected headroom

Status: done. Dependencies: OPT-125, OPT-126, OPT-127, OPT-128, OPT-129, OPT-130, OPT-131. Coupled IDs: none.
Priority: P1.

## Run record

### Planning (2026-09-14T01:30:00Z)

skipped — pre-authored dossier admitted (coordinator)

### Implementation (cursor-grok-4.6, 2026-09-14T02:03:45Z)

KEEP. Admitted stack is post124 combined OPT-118+119 `ffn_only` plus OPT-127
`decode_segments8` only. Shipping selector stays `decode_segments8` (already
the OPT-127 keep). Excluded: rejected OPT-130 selector (`hybrid_crossover@1024`,
`n_parts=16`, `verified_max=4096`), OPT-128 pins
(`kSelectedPollWithoutDeviceSync=false`, `kSelectedDeferElapsedEventSync=false`),
OPT-131 fusion `none`. OPT-126+127 is one unit; leave-one-out of that unit is
the post124 control. Graph × attention interaction is not applicable. Did not
commit.

Quality: OPT-058 `--quality` on combination graphs; same-math exact; held-out
and wikitext PPL ratio 1.0 versus post124; OPT-116 budgets not ratcheted.
API: logits/eval/restore/replay-after-restore, divergent-prefix and stale
session both `kInvalidArgument`, cancel isolated, request loop.

Paired 3+10 AB/BA complete-request vs post124 (OPT-125 boundaries, independently
restored): D128 54.25→58.07 tok/s (**+3.82**, geo 1.070, CI lower 1.070);
D2048 41.71→44.27 tok/s (**+2.57**, geo 1.062, CI lower 1.061); decode-only
p95 ratios 0.933 / 0.935. P4096 non-target 2969.4→2964.7 (geo 0.998 ≥ 0.98).
Aggregate complete-request CI lower **1.033 > 1.0** (geo 1.043, n=30).

Matched llama `cc83d7b4824f73cfdda4dfbb47ee39804f71b328`: P4096 prefill
2964.7 vs 3093.8 (0.958×, −129 tok/s); D128 decode-only 59.26 vs 68.77
(0.862×, −9.50); D2048 decode-only 50.00 vs 67.24 (0.744×, −17.24). OPT-124
D2048 0.620× mixed-request/decode-only does not survive these boundaries.

Blocked (not inferred passes): long-cache 8192/32768/131040 @ capacity 131072
OOM; OPT-016 2K Quartz OOM; Nsight Systems absent. Proven device inactive 0 ms;
leaf gaps remain unobserved. GPU sitting was non-exclusive (foreign python
868+3216 MiB); processes not killed.

**Files created**
- `cuda/opt132_combined_decode_test.cu`
- `tools/opt132_combined_decode.py`
- `tests/test_opt132_combined_decode.py`
- `pins/opt132_combined_decode_contract.json`
- `pins/opt132_iteration_contract.json`
- `fixtures/opt132_combined_decode.json`
- `evidence/optimization/opt132-combined-decode/REPORT.md`

**Files modified**
- `Makefile` (`cuda-opt132-diagnostics`)
- `tools/run_optimization_task.py` (`QW38_OPT132_*` prefixes)
- `cuda/opt058_quality_baseline_test.cu` (`graphs.create(..., session)` so
  combination `decode_segments8` quality can capture)
- `cuda/full_scheduler.cu` (segment-graph decode folds mixer/gdn/attention/ffn
  categories into `graph` so CUDA-event attribution can finish)
- `tasks/OPT-132.md` (this run record)

**Host validation**
```
uv run ruff check tools/opt132_combined_decode.py tests/test_opt132_combined_decode.py
# All checks passed!
uv run pytest tests/test_opt132_combined_decode.py -q
# 5 passed in 0.04s
```

**GPU phases** (device NVIDIA GeForce RTX 5090, image `qw38-cuda:13.0.2`,
GGUF present). Sidecars in `build/optimization-runs/opt132/`.

```
docker make cuda-opt132-diagnostics
uv run python tools/opt132_combined_decode.py --phase freeze|identity|recapture-proof|cancellation|same-math|api --mode feedback
# all ok; same-math exact; api replay_after_restore + divergent_prefix_invalidated
uv run python tools/opt132_combined_decode.py --phase quality --mode acceptance
# ok; --quality invoked; ppl_ratio=1.0
uv run python tools/opt132_combined_decode.py --phase state-memory|p4096|d128|d2048|leave-one-out|llama|two-k|long-context|activity|report --mode acceptance
# keep; shipping decode_segments8
```

### Verification (integration-verifier, 2026-09-14T02:05:00Z)

Independent final verification against dossier acceptance, OPT-058 quality,
OPT-125 corrected metric boundaries, and aggregated fixture artifacts.
**Verdict: PASS** (`keep`; shipping `decode_segments8`; post124+OPT-127
`decode_segments8` admitted; rejected OPT-130/128/131 paths excluded).

**Host / build gates**
```
uv run ruff check tools/opt132_combined_decode.py tests/test_opt132_combined_decode.py
# All checks passed!

uv run pytest tests/test_opt132_combined_decode.py -q
# 5 passed in 0.04s

uv run python tools/opt132_combined_decode.py --phase report --mode acceptance --run-dir build/optimization-runs/opt132
# {"task": "OPT-132", "phase": "report", "ok": true, "verdict": "keep"}
```

**KEEP acceptance claims → artifacts**
| Claim | Artifact |
|---|---|
| Admitted stack is post124 + OPT-127 `decode_segments8` only | `fixtures/opt132_combined_decode.json`: `verdict=keep`, `shipping_execution_graphs=decode_segments8`, `production_kept=true`; `cuda/execution_graph_path.cuh`: `kSelectedExecutionGraphPath[] = "decode_segments8"` |
| OPT-058 quality measured (`--phase quality --mode acceptance`) | `build/optimization-runs/opt132/quality.json`: `opt058_invoked=true`, `candidate_nll_measured=true`, `ppl_ratio=1.0`, `wikitext_ppl_ratio=1.0`, `ok=true` |
| Aggregate complete-request CI lower >1.0 vs post124 | `fixtures/opt132_combined_decode.json` `performance.aggregate.ci`: `ci_lower=1.033`, `pass=true`, `threshold=1.0`, `n_pairs=30` |
| Decode-only vs complete-request metrics separated (OPT-125 boundaries) | `performance.d128`: request 54.25→58.07 tok/s (CI lower 1.070); decode-only 55.26→59.26 tok/s; decode p95 ratio 0.933. `performance.d2048`: request 41.71→44.27 (CI lower 1.061); decode-only 46.75→50.00; decode p95 ratio 0.935. P4096 non-target guard geo 0.998 ≥ 0.98 |
| Rejected paths excluded (OPT-130/128/131) | `freeze.json`/`identity.json`: `opt130_reject=reject`, `opt128_no_material_opportunity`, `opt131_no_material_opportunity`, `rejected_no_leak=true`; `opt130_selector_excluded`, `opt128_pins_excluded`, `opt131_fusion_excluded`; `excluded_from_admitted_runtime` lists OPT-130 selector, OPT-128 pins, OPT-131 fusion |
| Machine-readable interaction verdicts | `fixtures/opt132_combined_decode.json` `interaction_matrix`: control=`ffn_only`, combination=`decode_segments8`, `combination_minus_opt126_127`=`ffn_only`, `graph_attention_interaction=not_applicable_opt130_rejected`; `independent_verdicts` all applicable gates true |
| REPORT published | `evidence/optimization/opt132-combined-decode/REPORT.md`; metrics match `fixtures/opt132_combined_decode.json` and `build/optimization-runs/opt132/report.json` |
| Build sidecars present (41 files) | `build/optimization-runs/opt132/`: freeze, identity, quality, p4096/d128/d2048, leave-one-out, llama, activity, report.json |

Blocked gates retained (not inferred passes): `long_context_resource_blocked`,
`opt016_2k_resource_blocked`, `nsight_systems_absent`. GPU sitting was
non-exclusive during implementor run; verifier did not re-execute GPU phases.

### Delivery (2026-09-14T02:06:00Z)

model: composer-2.5, telemetry_unavailable

OPT-132 marked `done`. Coupled IDs: none. Admitted stack is post124 combined
OPT-118+119 `ffn_only` plus OPT-127 `decode_segments8` only; shipping selector
`decode_segments8` unchanged. Rejected OPT-130 occupancy selector, OPT-128 host
pins, and OPT-131 fusion remain excluded. Post-124 decode investigation batch
(OPT-125–132) complete; no further eligible pending OPT tasks in this batch.

### Final outcome

- Status: `done`
- Verdict: `keep`
- Admitted stack: post124 + `decode_segments8` only
- Acceptance evidence: [`fixtures/opt132_combined_decode.json`](../fixtures/opt132_combined_decode.json); [`evidence/optimization/opt132-combined-decode/REPORT.md`](../evidence/optimization/opt132-combined-decode/REPORT.md); [`build/optimization-runs/opt132/report.json`](../build/optimization-runs/opt132/report.json)
- Throughput delta versus post124 (complete-request, OPT-125 boundaries): D128 **54.25→58.07 tok/s** (**+3.82**, **1.070×**, CI lower 1.070); D2048 **41.71→44.27 tok/s** (**+2.57**, **1.062×**, CI lower 1.061); aggregate CI lower **1.033 > 1.0** (geo 1.043, n=30). P4096 non-target guard geo 0.998 ≥ 0.98.
- Llama headroom (matched decode-only / prefill, pinned `cc83d7b4824f73cfdda4dfbb47ee39804f71b328`): D128 **0.862×** (59.26 vs 68.77 tok/s); D2048 **0.744×** (50.00 vs 67.24 tok/s); P4096 prefill 0.958×. OPT-124 D2048 **0.620×** mixed boundary does not survive corrected decode-only comparison.
- Quality: OPT-058 `--quality` measured; held-out and wikitext PPL ratio **1.0** versus post124.
- First-pass acceptance: yes
- Remaining risk: OPT-016 2K blocked by `decode_segments8_requires_matching_session_capacity` (historical OPT-056 gate, not OPT-132 keep); CUDA-event activity phase leaves leaf gaps unobserved at proven-inactive **0 ms** (see OPT-133 Nsight reconciliation); DRAM/L2 not counter-sampled. Long-cache D8192/D32768/D131040 measured on exclusive sitting 2026-09-14.

### Evidence amendment (2026-09-14T12:50:00Z) — OPT-133 Nsight reconciliation

[`OPT-133`](OPT-133.md) bounded Nsight capture on this admitted `decode_segments8`
stack (nsys **2025.3.2**) reconciles OPT-125/132 CUDA-event **unobserved** leaf
gaps as **hardware GPU idle** (fraction **1.0** of ~126 ms/12-token window), not
missing kernels. Nsight Systems is installed in the pinned CUDA image
(`cuda-nsight-systems-13-0`); the OPT-132 activity phase still used CUDA-event
attribution only. **`nsight_systems_absent` is cleared** as an infrastructure
blocker. Removable decode headroom is still **not proven** — dominant bounded-window
API time is `cudaEventSynchronize` from event instrumentation.

Evidence: [`fixtures/opt133_decode_nsys_trace.json`](../fixtures/opt133_decode_nsys_trace.json);
[`evidence/optimization/opt133-decode-nsys-trace/REPORT.md`](../evidence/optimization/opt133-decode-nsys-trace/REPORT.md).

### Evidence amendment (2026-09-14T10:33:00Z)

Exclusive GPU sitting (zanzara-archive `audioset_ast` + `diarization` stopped).
Re-ran `--phase long-context` and `--phase two-k`; regenerated report/fixture.
`long_context_resource_blocked` cleared; `long_context_measured=true`.

**Fresh long-cache (`decode_segments8`, capacity 131072)**

| Probe | request tok/s | decode-only tok/s |
|---|---:|---:|
| D8192 | 8.00 | 35.20 |
| D32768 | 1.34 | 15.36 |
| D131040 | 0.128 | 4.72 |

OPT-016 2K: llama **3150.7 tok/s** measured; Quartz blocked (`decode_segments8_requires_matching_session_capacity`, not OOM). Remaining blocked gates: `opt016_2k_resource_blocked` only (`nsight_systems_absent` cleared by OPT-133 image upgrade; see 2026-09-14T12:50:00Z amendment).

Sidecars: `build/optimization-runs/opt132/long-context.json`, `two-k.json`.

## Outcome

Deliver a verified combined stack and an honest final assessment using the
corrected OPT-125 metrics. Read its shared protocol and OPT-123's combination
rules. Dependency completion may be a rejection or no-opportunity; use its
retained production path. A resource blocker is not an acceptance pass.

## Implementation and verification

1. Freeze authenticated post124 combined control, selected survivors and
   pinned llama. Preserve post113 and original gate definitions as historical
   anchors. Exclude rejected selectors and prerequisite-only code paths from
   the admitted runtime unless required by a kept survivor.
2. Freeze a minimal matrix: control, combination, combination minus each
   independent survivor, and graph × attention interaction when both survive.
   Reuse isolated measurements only with matching identity and boundaries.
   Treat OPT-126+127 as one valid unit, not an invalid partial ablation.
3. Run exact-combination OPT-116 quality and all state/API cases, including
   token parity, 1024/split crossings, replay after restore, divergent prefixes,
   failures and real cancellation response. Prove steady-state graph capture
   counts remain zero if replay survives and quantify transition counts.
4. Run fresh exclusive paired P4096/D128/D2048 with decode-only and complete
   request rates clearly separated; publish TTFT, p50/p95, emitted outputs
   and raw latency distributions. Run matched populated long-cache controls
   at 8192/32768 and 131040+32 where resources allow, plus original OPT-016
   2K and live 128K/reserve. Do not substitute a capacity calculation for live
   fit. If exclusivity is unavailable, retain blocked gates and describe it.
5. Require aggregate paired throughput lower bound >1 over matched P/D
   workloads, OPT-125 per-workload guards and quality/state/memory success.
   Predeclare the aggregate's metric boundaries. Report improvements versus
   post124 separately from llama parity and original OPT-056 +5% conditions.
   Never retrofit corrected successor metrics into an old gate silently.
6. If combination fails, use leave-one-out attribution to remove one offending
   independent unit and permit one frozen fresh confirmation. No new tuning.
   If still failing, retain the last independently admitted valid stack.
7. Recompute true active/inactive/unobserved time using matched trace windows,
   explain achieved savings against OPT-125's causal budget, and state residual
   headroom per workload. Do not add component savings or call an exhausted
   candidate list a hardware ceiling. Clarify that OPT-124's
   supports_continuing=false described its exhausted ladder, not proof that
   optimization is impossible.

## Acceptance and artifacts

Deliver `evidence/optimization/opt132-combined-decode/REPORT.md`, machine-
readable identities/interaction verdicts, corrected comparisons and a dated
ledger outcome. Preserve original OPT-115–124 evidence and link corrections.
Report throughput keeps, diagnostic corrections, quality outcomes and
resource-blocked gates separately. Completion requires all applicable gates
or an honest retained-control disposition; no guaranteed win over llama.

## OPT-136 dated correction (2026-09-14)

Derived GPU idle from OPT-133 `cuda_gpu_trace` is **invalid**. The capture used
`--cuda-graph-trace=graph`, but the GPU sum omitted
`CUPTI_ACTIVITY_KIND_GRAPH_TRACE`. Subtracting ordinary kernel/copy union from
the window does not prove hardware idle; graph-internal activity is unknown
until node tracing covers those envelopes. Cross-run
`min(old_unobserved, new_idle)` is **not causal**.

Raw OPT-133 sample arrays and historical admission are preserved. Current
derived idle fields are null. Evidence:
[`evidence/optimization/opt136-graph-accounting/historical-reconciliation.json`](../../evidence/optimization/opt136-graph-accounting/historical-reconciliation.json).

Do not treat graph envelopes as continuous busy time, and do not replace the
old ~188 ms idle claim with a claim of ~194 ms continuous hardware busy time.
