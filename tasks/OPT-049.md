# OPT-049 — Share decode FFN staging and fuse gate/up

Status: done. Dependencies: OPT-046, OPT-047, OPT-048. Coupled IDs: none.
Primary target: D2048. Read [the shared protocol](PERFORMANCE-RECOVERY-2026-09-10.md).

## Change

Optimize `cuda/full_scheduler.cu::execute_ffn` in two independently measured
steps. First stage normalized input once and call gate/up prequant launchers;
today both calls to `launch_quant_mmv` stage the same vector again. Second add
a paired gate/up Q4_K launcher sharing activation loads and computing SwiGLU
in its epilogue. Both projections have independent accumulators and scales.
Keep the residual/norm and down leg on their already accepted kernels.

Use the same admitted SwiGLU function and preserve the single BF16 rounding
before down activation quantization. A later epilogue may directly produce the
down Q8_1 stage only if it first reproduces this BF16 rounding, then performs
each 32-value maximum/sum with correct ownership. No storing a partial group
before every value is available. Compare paired-only against paired-plus-stage
fusion; increased registers can outweigh saved traffic. Do not add a separate
gate/up weight copy merely to concatenate matrices: a two-pointer kernel is
enough for this model.

References: pinned llama `mmvq.cu` optional fusion argument/GLU epilogue;
ds4 `cuda/mmq/ds4_mmq_d2r.cuh` fused gate/up/down-stage pattern. Borrow the
data-flow idea only; ds4 routing weights, sanitization and clamps are not Qwen
semantics. OPT-043 determines whether llama actually uses its fusion here.

## Acceptance

Retain separate-leg selected kernels as the control, and compare all three
projections plus stage/SwiGLU/residual/norm as one complete FFN. Test actual
layer activations, every BF16-to-Q8 boundary, graph/eager equality within each
path, and candidate-state isolation. Recapture the 64 decode FFN graphs after
changing their nodes. Apply OPT-044 and full P/D guards. Add task fixture/pin,
`tests/test_opt049_decode_ffn_fusion.py`, and
`evidence/optimization/opt049-decode-ffn-fusion/`. Run new host/native test,
`tests/test_cuda_fusion.py`, `tests/test_cuda_graph.py`,
`tests/test_cuda_full_scheduler.py`, and production-numerics.

## Run record

### Planning

- Agent/model: coordinator (pre-authored dossier admitted)
- UTC/time/tokens/cost: 2026-09-10T19:30:00Z; telemetry_unavailable
- Outcome: skipped — pre-authored dossier admitted

### Implementation

- Agent/model: cursor-grok-4.6
- UTC/time/tokens/cost: 2026-09-10T21:00:00Z; telemetry_unavailable
- A/B winner: `paired_staged` (weighted complete FFN **0.239974757** ms vs separate **0.26093938** ms, ~1.09×). Occupancy 5, 48 regs, `local_bytes` 0. Shared-stage **0.259414752** ms (bit-exact, tiny). Paired-only BF16 **0.257577956** ms but max_abs ~0.02–0.03 vs Q8 staging (not installed). Graph/eager equal on every path.
- Keep/reject: **keep** (`reverted` false, `keep_sitting_skipped` false, production `kSelectedFfnDecodePath = "paired_staged"`). Separate-leg packed Q4_K remains the control and the `execute_ffn` else-branch. Down/residual/norm stay on accepted kernels. First exclusive sitting wrote `rejected` because `_keep_predicates` required `status=="measured"` before the sitting set that field; the P/D numbers already satisfied every keep floor. Predicate fixed; same sitting reused (oracles not re-run).
- tok/s sitting: ran under the `paired_staged` pin. Quartz P **2130.79614**, D128 **37.2543182**, D2048 **35.4072151** versus OPT-048 keep P **2129.85938**, D128 **35.9651642**, D2048 **34.3371162**. D128 p95 **27.0801449** / run-mean p95 **26.9719276**; D2048 p95 **28.3656578** / run-mean p95 **28.2625351** (all inside 105% of OPT-045). Speedup vs OPT-048: P ~1.0004×, D128 ~1.036×, D2048 ~1.031×. D2048 strictly beats **34.3371162**.
- Quality: shared-stage and paired-staged match separate on layers 0/3/31 (max_abs 0). Admitted SwiGLU `gate / (1 + expf(-gate)) * up` then one BF16 round. Two-pointer kernel (no concatenated weight copy). OPT-046 packed Q4, OPT-047 DP4A Q8, OPT-048 integer Q6 stay.
- Blockers / discovered extra work: none beyond the circular keep-predicate. `test_full_scheduler_matches_scalar_taps_logits_and_greedy` not used as a keep gate (pre-existing packed NaN on HEAD, same as OPT-046–048).
- Changes:
  - `cuda/ffn_decode_path.cuh` — production pin `paired_staged`, legal paths, A/B override
  - `cuda/quant_mmv.cu` / `.h` — two-pointer `quant_mmv_q4k_gate_up_swiglu`, admitted SwiGLU epilogue, staged-Q8 and BF16 launchers
  - `cuda/full_scheduler.cu::execute_ffn` — shared Q8 stage; fused gate/up/SwiGLU; separate-leg control retained
  - `cuda/opt049_decode_ffn_fusion_ab_test.cu` — four-candidate complete-FFN A/B; not added to the Makefile; fail-closed on missing `QW38_CUDA_TEST_TIER`
  - `Makefile` — `quant_mmv` / `full_scheduler` depend on `ffn_decode_path.cuh`
  - `pins/opt049_decode_ffn_fusion_contract.json`, `fixtures/opt049_decode_ffn_fusion.json`, `tests/test_opt049_decode_ffn_fusion.py`
  - `evidence/optimization/opt049-decode-ffn-fusion/` — `REPORT.md`, `decode-ffn-fusion-ab-raw.txt`, quartz P/D128/D2048 JSON (no `REJECTION.md`)
- Commands:
  - fail-closed: `./build/qw38-cuda-opt049-decode-ffn-fusion-ab-test` without `QW38_CUDA_TEST_TIER` → `QW38_CUDA_TEST_TIER must be set`
  - `QW38_CUDA_TEST_TIER=smoke` → `status=passed` (probe only)
  - `QW38_CUDA_TEST_TIER=correctness` → `status=passed` (3 samples; layers 0/3/31)
  - `QW38_CUDA_TEST_TIER=acceptance` → `status=passed` winner `paired_staged`; separate **0.26093938** ms; winner **0.239974757** ms; measurement_utc 2026-09-10T20:52:08Z
  - paired_staged pin sitting: Quartz P **2130.79614** (≥ 95% of OPT-048 **2129.85938**)
  - decode 128 → Quartz D128 **37.2543182**, p95 **27.0801449** / run-mean p95 **26.9719276**
  - decode 2048 → Quartz D2048 **35.4072151**, p95 **28.3656578** / run-mean p95 **28.2625351**
  - `uv run ruff format tests/test_opt049_decode_ffn_fusion.py && uv run ruff check tests/test_opt049_decode_ffn_fusion.py` → exit 0
  - `uv run pytest -q tests/test_opt049_decode_ffn_fusion.py tests/test_production_numerics.py -k "not exclusive and not native and not sitting"` → **12 passed, 1 deselected**
  - `QW38_RUN_CUDA_TESTS=1 QW38_CUDA_TEST_TIER=smoke uv run pytest -q tests/test_opt049_decode_ffn_fusion.py tests/test_cuda_fusion.py tests/test_cuda_graph.py tests/test_cuda_full_scheduler.py tests/test_production_numerics.py -k "not matches_scalar"` → **18 passed, 1 deselected** (exclusive sitting reused the keep fixture; `matches_scalar_taps` not re-run as a keep gate)
- Notes: Do not vendor ggml headers. `--fmad=false` stays. No `nsys`/`ncu`. No `plan.md` edit. OPT-046 packed Q4, OPT-047 DP4A Q8, and OPT-048 integer Q6 remain. This increment does not substitute for the 2K llama.cpp parity gate. Bounded decode-FFN win (~0.021 ms complete FFN, 64 layers), not the remaining decode recovery.

### Verification

- Attempt: 1
- Agent/model: composer-2.5 (integration verifier)
- Diff review: Matches dossier. Shared Q8 staging and two-pointer paired Q4_K gate/up/SwiGLU in `cuda/quant_mmv.{cu,h}`; production pin `kSelectedFfnDecodePath = "paired_staged"` in `cuda/ffn_decode_path.cuh`; `cuda/full_scheduler.cu::execute_ffn` routes fused path with separate-leg control retained; contract, fixture (`status=measured`, `reverted=false`, `keep_sitting_skipped=false`), report, A/B raw sidecars, and host validators present. OPT-048 keep denominators frozen in contract.
- Commands:
  1. `uv run ruff format . && uv run ruff check .` → exit 0; `125 files left unchanged`; `All checks passed!`
  2. `QW38_RUN_CUDA_TESTS=1 QW38_CUDA_TEST_TIER=smoke uv run pytest -q tests/test_opt049_decode_ffn_fusion.py tests/test_cuda_fusion.py tests/test_cuda_graph.py tests/test_cuda_full_scheduler.py tests/test_production_numerics.py -k "not matches_scalar"` → exit 0; **18 passed, 1 deselected**
- Criteria checked:
  - Production pin `paired_staged`: pass (`selected_ffn_decode_path=paired_staged`, `reverted=false`)
  - A/B winner `paired_staged` faster on weighted complete FFN: pass (**0.239974757** ms vs separate **0.26093938** ms, ~1.09×)
  - OPT-044 production-numerics budgets: pass (layers 0/3/31 max_abs 0; graph/eager equal within each path)
  - OPT-048 keep P/D128 floors and strict D2048 improvement: pass (P **2130.79614** ≥ 95%×**2129.85938**; D128 **37.2543182** ≥ 95%×**35.9651642**; D2048 **35.4072151** > **34.3371162**; p95 inside 105% of OPT-045)
  - OPT-046 packed Q4_K, OPT-047 DP4A Q8, and OPT-048 integer Q6 unchanged: pass
- Verdict: **pass**
- UTC/time/tokens/cost: 2026-09-10T21:05:17Z; telemetry_unavailable

### Retries and escalation

none

### Delivery

- Agent/model: composer-2.5 (delivery subagent)
- UTC/time/tokens/cost: 2026-09-10T21:05:17Z; telemetry_unavailable
- Outcome: verification pass 2026-09-10T21:05:17Z confirmed. Marked OPT-049 `done`. Final audit row published. Commit and push authorized.

### Final outcome

- Status: `done` (keep)
- Acceptance evidence: verification pass 2026-09-10T21:05:17Z — live fixture [`fixtures/opt049_decode_ffn_fusion.json`](../fixtures/opt049_decode_ffn_fusion.json) (`measurement_utc` 2026-09-10T20:52:08Z), contract [`pins/opt049_decode_ffn_fusion_contract.json`](../pins/opt049_decode_ffn_fusion_contract.json), report [`evidence/optimization/opt049-decode-ffn-fusion/REPORT.md`](../evidence/optimization/opt049-decode-ffn-fusion/REPORT.md), A/B harness, host validators, and exclusive RTX 5090 native sitting. Production pin `paired_staged`.
- Throughput delta: baseline OPT-048 P **2129.86**, D128 **35.97**, D2048 **34.34** tok/s → post OPT-049 P **2130.80** (+0.94, 1.0004×), D128 **37.25** (+1.29, 1.036×), D2048 **35.41** (+1.07, 1.031×). Weighted complete FFN: separate **0.26093938** ms → paired_staged **0.239974757** ms (~1.09×).
- Commit: `6173a7b` on `main`
- Push: pending
- First-pass acceptance: **yes** (verification)
- Total elapsed/tokens/cost: telemetry_unavailable
- Remaining risk or recovery condition: GPU exclusivity remains mandatory for the native sitting. `test_full_scheduler_matches_scalar_taps_logits_and_greedy` not used as a keep gate (pre-existing packed-MMV NaN on HEAD, same as OPT-046–048). Do not treat this keep as the 2K llama.cpp parity gate. OPT-016 remains the blocked 2K parity owner. Next eligible recovery task: OPT-050.
- Agent/model for delivery: composer-2.5; telemetry_unavailable
