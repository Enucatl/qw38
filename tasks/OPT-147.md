# OPT-147 — Screen a prefill 8x8 flash-attention tile

Status: done. Dependencies: OPT-135, OPT-140, OPT-144. Coupled IDs: none.
Priority: P1. Plan impact: none. Verdict: keep. Shipping pin: `prefill_attention_8x8_v1`.

## Outcome

Test one concrete production-boundary P4096 candidate that changes Quartz's
current 16x2 prompt-attention tile organization to a llama-shaped 8x8 tile.
This is a hypothesis screen, not a claim that llama's tile can be copied
unchanged. Unknown source/SASS mechanism is allowed at screen admission; it
must remain an explicit unknown in the report. A candidate can ship only after
correctness, quality, state/memory and whole-engine target/guard evidence.

## Candidate and boundaries

Candidate ID: prefill_attention_8x8_v1. Keep BF16 authoritative state,
OPT-111 arithmetic semantics, causal masks, chunking and final-token output
policy. Change only the prompt-attention tile shape, indexing, head masks,
shared-memory/register layout and required launch/workspace integration. Count
conversion, staging, softmax, merge and output consumers in the complete family.
Do not re-port OPT-111 wholesale, add a tile sweep, alter GGUF/checkpoints,
or use decode attention as evidence.

Screen exactly P4096, one warmup plus three alternating complete-family pairs,
rotating the applicable layers and real inputs. Correctness covers 8x8 versus
control at prompt lengths 1, 32, 128, 2048 and 4096, causal tails, GQA head
masks, finite outputs and prompt-to-decode handoff. The disconfirming result is
screened_out when complete-family time does not improve or any numerical,
launch, chunk or output invariant fails. The screen is allowed with a concrete
candidate even when counters or SASS cannot name the mechanism.

## Acceptance

A screened-in survivor runs OPT-058 candidate quality, graph/eager equality,
checkpoint/cancellation, populated 131072-capacity memory/reserve checks, then
OPT-135 target_guard_v2 acceptance with P4096 prefill target and D128/D2048/
D8192/D32768 decode-only and complete-request guards. Keep only with all required
quality/state/memory/whole-engine gates. Mechanism remains unknown unless
separately evidenced; it is not a substitute for those gates.

Deliver the existing task artifacts under evidence/optimization/opt147-prefill-8x8/,
with frozen contract, raw screen/acceptance samples, launch identity and an
independent reconstruction of one P4096 pair. Missing required work is
incomplete/blocked, never no_opportunity.

## Execution requirements

Read TASK-TESTING-STRATEGY.md and the performance checklist. Use
QW38_CUDA_TEST_TIER=correctness|screen|acceptance, the shared GPU lock, one build,
fixed inputs and declared 300/1200/7200-second ceilings. No broad profiling batch
or automatic tile sweep.

## Run record

### Planning (2026-09-15T06:12:00Z)
skipped — pre-authored dossier admitted

### Implementation (2026-09-15T07:26:38Z)

Frozen matrix: RTX 5090, image `qw38-cuda:13.0.2`, GPU lock `build/optimization-runs/qw38-gpu.lock`, child 300s, long 3600s, aggregate 7200s. Candidate `prefill_attention_8x8_v1` (`fattn_mma_pipeline_prefill_attention_8x8_v1`, 8×8, GQA 6 padded to ncols2=8, heads 6–7 masked, convert-once, llama load, occupancy 1, smem 94528 vs parent 16×2 85888). Mechanism remains `unknown` (no SASS required at screen). Production pin flipped on keep.

```
uv run pytest tests/test_opt147_prefill_8x8.py -q
```

outcome: 9 passed.

```
QW38_CUDA_TEST_TIER=acceptance docker run --rm --gpus all --user $(id -u):$(id -g) \
  -e QW38_CUDA_TEST_TIER=acceptance -v $PWD:/workspace -w /workspace qw38-cuda:13.0.2 \
  make cuda-opt147-diagnostics
```

outcome: rc 0 under GPU lock (`build/qw38-cuda-opt147-prefill-8x8-test`, engine probe, OPT-058 quality, checkpoint, memory-fit).

```
uv run python tools/opt147_prefill_8x8.py --phase correctness --mode feedback \
  --run-dir build/optimization-runs/opt147-prefill-8x8
```

outcome: `{"phase": "correctness", "ok": true}` at 2026-09-15T06:50:00Z. Lengths 1/32/128/2048/4096, causal boundary/tail, chunk_mid, GQA pad, finite; `max_abs=0`. Tokens=1 uses tiled attention (not 8×8). Launch `fattn_mma_pipeline_prefill_attention_8x8_v1`, `convert_once=1`.

```
uv run python tools/opt147_prefill_8x8.py --phase screen --mode feedback \
  --run-dir build/optimization-runs/opt147-prefill-8x8
```

outcome: `{"phase": "screen", "ok": true, screened_in: true}` at 2026-09-15T06:50:03Z. P4096 complete family, 1 warmup + 3 alternating pairs, 16 rotating layers, prep+combine+conversion. Control mean **222.146301 ms**. Candidate mean **201.478683 ms**. Saving **20.667618 ms**. Independent reconstruction: control 222.14629066666666 ms, candidate 201.478673 ms, saving 20.667617666666672 ms (`evidence/optimization/opt147-prefill-8x8/p4096-pair-reconstruction.json`).

```
uv run python tools/opt147_prefill_8x8.py --phase quality --mode acceptance \
  --run-dir build/optimization-runs/opt147-prefill-8x8
```

outcome: `{"phase": "quality", "ok": true}` at 2026-09-15T06:54:42Z. OPT-058 `--quality --quality-config --q4-decode llama_q4k_mmvq --attention-pipeline {opt111_base|prefill_attention_8x8_v1}`. Held-out NLL identical **1.7875990840085783**. WikiText NLL **1.5249350773895778**. `ppl_ratio=1.0`. `candidate_nll_measured=true`.

```
uv run python tools/opt147_prefill_8x8.py --phase state-memory --mode acceptance \
  --run-dir build/optimization-runs/opt147-prefill-8x8
```

outcome: `{"phase": "state-memory", "ok": true}` at 2026-09-15T07:01:10Z. Graph/eager + prompt-to-decode handoff + cancel pass. Checkpoint `--attention-pipeline prefill_attention_8x8_v1` pass. 131072 memory/reserve: `arithmetic=true passed=true`; `decode_segments=40` (OPT-137 topologies×8) + 64 prompt graphs @ 4096 rows; `free_bytes=3508535296` ≥ `reserve_required=1610612736`. Memory-fit now opens `model_path` and calls `graphs.create(..., &session)` on `decode_segments8`.

```
uv run python tools/opt147_prefill_8x8.py --phase performance --mode acceptance \
  --run-dir build/optimization-runs/opt147-prefill-8x8
```

outcome: `{"phase": "performance", "ok": true}` at 2026-09-15T07:23:50Z. Engine `attn-ab --pairs 10 --modes graph` P4096 plus D128/D2048/D8192/D32768 `--output-tokens 256`. Probe accepts 10 pairs and decode-guard prefixes; attn-ab session capacity floors at 4096 prompt rows; `graphs.create` takes the session. `target_guard_v2` keep. Whole-engine P4096 prefill: control **2997.786** tok/s, candidate **3032.417** tok/s, **g=1.01158**, delta **+34.63** tok/s.

Candidate measured delta and shipping delta are the same P4096 prefill rates (keep flipped the pin). Decode guards passed (prompt-attention tile does not change decode ITL). Mechanism `unknown`; causal claim withheld.

```
uv run python tools/opt147_prefill_8x8.py --phase report --mode acceptance \
  --run-dir build/optimization-runs/opt147-prefill-8x8
```

outcome: `{"phase": "report", "ok": true, "verdict": "keep"}`. Report `evidence/optimization/opt147-prefill-8x8/REPORT.md`. Fixture `fixtures/opt147_prefill_8x8.json`. Production `kSelectedAttentionPipelinePath[] = "prefill_attention_8x8_v1"`. No commit.

### Documentation (composer-2.5, 2026-09-15T07:29:38Z)

Finalized [`evidence/optimization/opt147-prefill-8x8/REPORT.md`](../evidence/optimization/opt147-prefill-8x8/REPORT.md) from implementation sidecars: P4096 complete-family screen (control 222.146301 ms, candidate 201.478683 ms, saving 20.667618 ms); OPT-058 candidate NLL measured for control and candidate (`candidate_nll_measured=true`, `ppl_ratio=1.0`); `target_guard_v2` whole-engine P4096 prefill keep; decode guards D128/D2048/D8192/D32768 passed; production pin `prefill_attention_8x8_v1`; mechanism `unknown`. Performance evidence checklist items 1–8 populated. Candidate measured delta and shipping delta reported separately (identical on keep with pin flip).

### Verification (composer-2.5, 2026-09-15T07:29:38Z)

**Verdict: PASS**

Independent verification against dossier scope, ledger acceptance, keep/reject quality gate, and performance evidence checklist. No semantic fixes; `uv run ruff format .` touched unrelated files only.

#### Gates

```
uv run pytest tests/test_opt147_prefill_8x8.py -q
```

outcome: **9 passed** in 0.04s.

```
uv run ruff check tools/opt147_prefill_8x8.py tests/test_opt147_prefill_8x8.py tools/run_optimization_task.py
```

outcome: All checks passed.

#### Acceptance evidence

| Criterion | Evidence | Result |
|-----------|----------|--------|
| Screened-in survivor with measured candidate NLL | `raw/quality.json` `candidate_nll_measured=true`, `candidate_nll_not_measured=false`; sidecars `quality-{opt111_base,prefill_attention_8x8_v1}.txt` with OPT-058 `QW38_OPT058_RESULT` cases | PASS |
| OPT-058 quality phase executed (not stubbed) | `--quality --quality-config --q4-decode llama_q4k_mmvq --attention-pipeline` for both arms; held-out NLL **1.7875990840085783** identical; `ppl_ratio=1.0` | PASS |
| P4096 complete-family screen | `raw/screen.json`; control 222.146301 ms, candidate 201.478683 ms, screened_in | PASS |
| Independent P4096 pair reconstruction | `p4096-pair-reconstruction.json` rounds recomputed: control 222.146291 ms, candidate 201.478673 ms, saving 20.667618 ms | PASS |
| `target_guard_v2` raw samples | `raw/p4096-engine.txt` 10 alternating `keep_ab_launch` pairs with `attention_pipeline={opt111_base\|prefill_attention_8x8_v1}`; `raw/performance.json` n=10 P4096 prefill pairs | PASS |
| Whole-engine P4096 prefill tok/s | Independent mean from 10 pairs: control **2997.786** tok/s, candidate **3032.417** tok/s, **g=1.01158**, **+34.63** tok/s | PASS |
| Production pin flipped | `cuda/fattn_mma_f16.cuh` `kSelectedAttentionPipelinePath[] = "prefill_attention_8x8_v1"` | PASS |
| State/memory/131072 reserve | `raw/state-memory.json`, checkpoint, memory-fit sidecars ok | PASS |
| Required artifacts present | REPORT.md, fixture, pins, tool, tests, raw sidecars, answers.json | PASS |

Verdict: **keep**. Production prompt attention shipped as `prefill_attention_8x8_v1`. First-pass acceptance: yes.

### Final outcome

- Status: `done`
- Verdict: **keep** — `prefill_attention_8x8_v1` (8×8 llama-shaped tile vs parent `opt111_base` 16×2)
- Acceptance evidence:
  [`fixtures/opt147_prefill_8x8.json`](../fixtures/opt147_prefill_8x8.json);
  [`evidence/optimization/opt147-prefill-8x8/REPORT.md`](../evidence/optimization/opt147-prefill-8x8/REPORT.md);
  verification record above (2026-09-15T07:29:38Z)
- **Candidate measured delta** (P4096 prefill, `target_guard_v2`): control **2997.786** tok/s → candidate **3032.417** tok/s (**+34.63**, **g=1.01158**)
- **Shipping delta** (production pin flipped): same **+34.63** tok/s on P4096 prefill
- Screen (complete family): control **222.146** ms → candidate **201.479** ms (**−20.668** ms)
- Quality result: **measured**; held-out NLL **1.7875990840085783** identical; `ppl_ratio=1.0`
- Mechanism: **unknown**; causal claim withheld
- Production pin: `kSelectedAttentionPipelinePath[] = "prefill_attention_8x8_v1"`
- First-pass acceptance: yes (2026-09-15T07:29:38Z)

### Delivery (composer-2.5, 2026-09-15T07:29:38Z)

OPT-147 marked `done`. Coupled IDs: none. Commit and push to `origin/main`.

