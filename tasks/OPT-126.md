# OPT-126 — Make decode graph inputs stable across token commits

Status: done. Dependencies: OPT-125. Coupled IDs: none.
Priority: P1.

## Run record

### Planning (2026-09-13T22:15:00Z)

skipped — pre-authored dossier admitted (coordinator)

### Implementation (cursor-grok-4.6-high, 2026-09-13T23:04:19Z)

Foundation/correctness only. Shipping selector stays `ffn_only`.
`claims_throughput=false`. Did not commit. GDN design
`stable_pingpong_committed_slot` (not two parity graph variants).
Dynamic scalars live in session-owned device `DecodeLaunchState`
(56 bytes) and upload in stream order. Segment graphs are 8×2=16.
`apply_segment_launch_params` does not call `patch_segment_kernel_params`.
Recapture only on ping-pong base or KV identity change, not frontier/slot.
Topology 0 = warp_query (capture 0); topology 1 = vec128_online (capture
1024, only while position ∈ [1024, 4096]).

Did not keep as a production speed change. OPT-127 owns replay admission.

**Files created**
- `cuda/decode_launch_state.cuh`
- `cuda/opt126_stable_graph_inputs_test.cu`
- `pins/opt126_stable_graph_inputs_contract.json`
- `pins/opt126_iteration_contract.json`
- `tools/opt126_stable_graph_inputs.py`
- `tests/test_opt126_stable_graph_inputs.py`
- `fixtures/opt126_stable_graph_inputs.json`
- `evidence/optimization/opt126-stable-graph-inputs/INVENTORY.md`
- `evidence/optimization/opt126-stable-graph-inputs/REPORT.md`

**Files modified**
- `cuda/full_scheduler.cu`, `cuda/full_scheduler.h` (launch-state, GDN
  slot, 16 segment graphs, bind/upload/commit)
- `cuda/attention_decode.cu`, `cuda/attention_decode.h`,
  `cuda/attention_decode_path.cuh`
- `cuda/gdn_step.cu`, `cuda/gdn_step.h`
- `cuda/checkpoint.cu` (restore into slot 0, bump generation)
- `cuda/opt096_decode_graphs_test.cu`, `cuda/opt114_sitting_launch_test.cu`,
  `cuda/opt117_decode_segment_graphs_test.cu` (16-graph count)
- `tests/test_opt122_gdn_state_precision.py`
- `Makefile` (`cuda-opt126-diagnostics`)
- `tools/run_optimization_task.py` (`QW38_OPT126_*` prefixes)
- `tasks/OPT-126.md` (this run record)

**Host validation**
```
uv run ruff format tools/opt126_stable_graph_inputs.py tests/test_opt126_stable_graph_inputs.py
# 2 files left unchanged
uv run ruff check tools/opt126_stable_graph_inputs.py tests/test_opt126_stable_graph_inputs.py
# All checks passed!
uv run pytest tests/test_opt126_stable_graph_inputs.py -q
# 4 passed in 0.03s
```

**GPU phases** (device NVIDIA GeForce RTX 5090, image `qw38-cuda:13.0.2`,
GGUF present). Sidecars in `build/optimization-runs/opt126/`.

```
docker run --rm --gpus all --user "$(id -u):$(id -g)" -v /home/user/qw38:/workspace -w /workspace qw38-cuda:13.0.2 make build/qw38-cuda-opt126-stable-graph-inputs-test
# ok

uv run python tools/opt126_stable_graph_inputs.py --phase preflight --mode feedback --run-dir build/optimization-runs/opt126
# {"phase": "preflight", "ok": true}

uv run python tools/opt126_stable_graph_inputs.py --phase inventory --mode feedback --run-dir build/optimization-runs/opt126
# ok; selector_unchanged; gdn_parity_design=stable_pingpong_committed_slot; launch_state_bytes=56; topology_count=2

uv run python tools/opt126_stable_graph_inputs.py --phase isolation --mode feedback --run-dir build/optimization-runs/opt126
# ok; cancel/retry/failure; candidate_isolated=true

uv run python tools/opt126_stable_graph_inputs.py --phase restore --mode feedback --run-dir build/optimization-runs/opt126
# ok; generation_bumped; stale_descriptors_rejected; divergent_prefix_invalidated; restored_runs

uv run python tools/opt126_stable_graph_inputs.py --phase same-math --mode feedback --run-dir build/optimization-runs/opt126
# ok; exact=true; matched_tokens=8; prefix=128; topology_recaptures=0

uv run python tools/opt126_stable_graph_inputs.py --phase costs --mode feedback --run-dir build/optimization-runs/opt126
# ok; create_ms≈14.47; graph_bytes=14680064; launch_state_upload_ms≈0.0036; topology_recaptures=0; 16 graphs

uv run python tools/opt126_stable_graph_inputs.py --phase positions --mode feedback --run-dir build/optimization-runs/opt126
# ok; 0/127/1023 topology 0; 1024/2047 topology 1; 131071 launch-param-only topology 0 (past verified_max 4096); odd/even exact; no recapture

uv run python tools/opt126_stable_graph_inputs.py --phase report --mode acceptance --run-dir build/optimization-runs/opt126
# {"phase": "report", "ok": true} verdict=prerequisite_verified
```

Same-math compares sequential eager (`ffn_only` nullptr graphs) then
`decode_segments8` so two full sessions are never live together.
Positions also sequentializes eager, `ffn_only` graphs, and
`decode_segments8`. Live `state_equals` across concurrent sessions is
not used; token logits/hidden match is the recorded `state_equals`.
Stale-graph checks require the capture session to remain live (pointer
identity); restored decode uses a fresh session after the capture
session and graphs are destroyed together.

**Blockers:** none for this prerequisite. Shipping selector unchanged.

### Verification (integration-verifier, 2026-09-13T23:06:00Z)

Independent integration verification against dossier acceptance and
`build/optimization-runs/opt126/` sidecars. Diagnostics only; no semantic
fixes. **Verdict: PASS** (`prerequisite_verified`).

**Scope check**
- Deliverables present: `evidence/optimization/opt126-stable-graph-inputs/INVENTORY.md`,
  `evidence/optimization/opt126-stable-graph-inputs/REPORT.md`,
  `fixtures/opt126_stable_graph_inputs.json`,
  `build/optimization-runs/opt126/*`.
- `claims_throughput=false`, `claims_performance_improvement=false`,
  `production_kept=false` in contract, fixture, REPORT, and all GPU sidecars
  (`keep=false`).
- Shipping selector unchanged: `kSelectedExecutionGraphPath="ffn_only"` in
  `cuda/execution_graph_path.cuh`; inventory/preflight `selector_unchanged=true`.
- Foundation prerequisite only; not a production speed keep (OPT-127 owns replay
  admission).

**Gates**
```
uv run ruff format --check tools/opt126_stable_graph_inputs.py tests/test_opt126_stable_graph_inputs.py
# 2 files already formatted

uv run ruff check tools/opt126_stable_graph_inputs.py tests/test_opt126_stable_graph_inputs.py
# All checks passed!

uv run pytest tests/test_opt126_stable_graph_inputs.py -q
# 4 passed in 0.03s

test -x build/qw38-cuda-opt126-stable-graph-inputs-test && echo binary exists
# binary exists

uv run python tools/opt126_stable_graph_inputs.py --phase report --mode acceptance --run-dir build/optimization-runs/opt126
# {"phase": "report", "ok": true}

uv run python tools/opt126_stable_graph_inputs.py --phase same-math --mode feedback --run-dir build/optimization-runs/opt126
# {"phase": "same-math", "ok": true}
```

Did not rebuild CUDA binary: existing artifact matches implementation run;
same-math GPU phase re-run succeeded with fresh native stdout markers.

**Acceptance claims → artifacts**
| Claim | Artifact |
|---|---|
| Argument/ownership inventory | `INVENTORY.md`, `inventory.json` (7 inputs, ownership §) |
| Measured costs + REPORT | `REPORT.md`, `costs.json`, `report.json` |
| Prerequisite (not speed keep) | `verdict=prerequisite_verified`, `production_kept=false`, `keep=false` |
| Identical logits/state (same-math) | `same-math.json`: `exact=true`, `state_equals=true`, `matched_tokens=8` |
| Positions 0/127/1023/1024/2047/near-cap | `positions.json`: all `ok=true`, `odd_even_exact=true`, `no_recapture=true` |
| Candidate isolation (cancel/retry/failure) | `isolation.json`: `candidate_isolated=true`, hashes distinguish buffers |
| Checkpoint restore + stale descriptors | `restore.json`: `generation_bumped`, `stale_descriptors_rejected`, `divergent_prefix_invalidated`, `restored_runs` |
| Both `ffn_only` and `decode_segments8` paths | `same-math.json`: `ffn_only_path=true`, `decode_segments8_path=true` |
| Shipping selector unchanged | `inventory.json`/`preflight.json`: `selector_unchanged=true`, `shipping_execution_graphs=ffn_only` |
| Launch-state design | `decode_launch_state.cuh`: 56-byte `DecodeLaunchState`, `stable_pingpong_committed_slot` |
| No opaque byte patching on segment path | `apply_segment_launch_params` uses `upload_decode_launch_state`; no `patch_segment_kernel_params` call |
| 16 segment graphs (8×2 topologies) | `costs.json`: `decode_segment_graph_count=16`, `topology_count=2` |

Cross-check: REPORT metrics match sidecars (`graph_bytes=14680064`, `create_ms≈14.47`,
`launch_state_upload_ms≈0.0036`, `topology_recaptures=0`). Fixture keys align with
`report.json` and contract `required_fixture_keys`.

**Findings:** none. All acceptance criteria satisfied.

### Delivery (2026-09-13T23:07:00Z)

model: composer-2.5, telemetry_unavailable

OPT-126 marked `done`. Coupled IDs: none. Shipping selector unchanged
(`ffn_only`). Next eligible pending task: **OPT-127**.

### Final outcome

- Status: `done`
- Acceptance evidence: [`fixtures/opt126_stable_graph_inputs.json`](../fixtures/opt126_stable_graph_inputs.json); [`evidence/optimization/opt126-stable-graph-inputs/INVENTORY.md`](../evidence/optimization/opt126-stable-graph-inputs/INVENTORY.md); [`evidence/optimization/opt126-stable-graph-inputs/REPORT.md`](../evidence/optimization/opt126-stable-graph-inputs/REPORT.md)
- Throughput delta: baseline unchanged (foundation prerequisite only). Verdict `prerequisite_verified`; `production_kept=false`; `claims_throughput=false`. Shipping selector `ffn_only` unchanged. 56-byte `DecodeLaunchState`; GDN `stable_pingpong_committed_slot`; 16 segment graphs (8×2 topologies); topology_recaptures=0.
- First-pass acceptance: yes
- Remaining risk: OPT-127 owns full replay admission; segment path exercised diagnostically only

## Outcome

Provide the smallest correct prerequisite for replay without per-token
recapture. OPT-117 repaired capture but lost because frontier and GDN pointer
changes rebuild all eight graphs. This is a new causal design, not another
segmentation sweep. Read OPT-125's shared protocol and OPT-117's rejection.

## Implementation

1. Inventory every changing input captured by
   `cuda/full_scheduler.cu::capture_decode_segment_graph` and
   `SchedulerGraphs::update_launch_params`: frontier/RoPE position, visible
   KV length, split count/crossover, GDN committed/candidate pointers, token,
   session identity and grouped-Q8 descriptor lifetime. Name every consumer
   and whether it takes a value, pointer or changes topology.
2. Implement a session-owned device launch-state record for dynamic values
   where the actual consumer can read it without changing arithmetic. Keep
   pointer ownership and lifetime explicit. Update it in stream order once
   per token; prohibit captured pointers to temporary host storage. Do not
   patch opaque captured argument bytes (OPT-117 did not establish safety).
3. Resolve GDN parity using stable physical ping-pong storage with an explicit
   committed slot, or two bounded parity graph variants. Select one design
   from the inventory before implementation; allow a second only on evidenced
   infeasibility. Do not copy the entire recurrent state every token to retain
   addresses. Preserve candidate isolation and commit only on success.
4. Separate true topology boundaries from ordinary frontier updates. Include
   the actual attention dispatch transition(s) and session replacement.
   Keep topology-dependent graph variants bounded; no graph per position.
   Account for parameter upload and extra device indirection in component cost.
5. Keep the shipping graph selector unchanged in this foundation task.
   Exercise dynamic-input consumers through eager and diagnostic graph paths,
   without switching kernels to historical graph-compatible alternatives.

## Verification and acceptance

Prove identical same-path logits/state over consecutive odd/even commits,
positions 0/127/1023/1024/2047 and near capacity; test every actual split
transition, cancel before commit, retry, failure, divergent prefix,
checkpoint restore and session destruction/replacement. State hashes and
frontiers must distinguish candidate and committed buffers. Check that a
restored/new session cannot replay stale descriptors. Validate both existing
`ffn_only` and experimental segment paths.

Deliver the argument/ownership inventory and
`evidence/optimization/opt126-stable-graph-inputs/REPORT.md`, with measured
upload/indirection/memory costs. Completion is a verified prerequisite, not
a production speed keep; OPT-127 owns full replay admission. If infeasible,
retain production and document the exact consumer or semantics blocker.

