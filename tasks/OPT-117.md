# OPT-117 — Repair and evaluate complete decode-segment graphs

Status: done. Dependencies: OPT-115, OPT-116. Coupled IDs: none.

## Run record

### Planning (2026-09-13T13:35:00Z)

skipped — pre-authored dossier admitted (coordinator)
Priority: P1. Primary target: D128/D2048 complete-request latency.

### Implementation (2026-09-13T14:26:49Z)

model: cursor-grok-4.6, telemetry_unavailable

Repaired `decode_segments8` capture on authenticated post113 and evaluated
it against parent `ffn_only`. Capture now succeeds (8 segment graphs,
create ~8.3 ms). Same-math, positions 0/127/1023/1024/2047/131071, pointer
swaps, cancellation, quality, and state/memory pass. Full-engine 3+10 AB/BA
on RTX 5090 is slower on D128/D2048 because GDN swap plus by-value attention
frontier recapture every token (257 updates / 256 decode tokens). Kernel-arg
patching is not byte-exact. Measured reject: retain `ffn_only`. No commit.

#### Commands (exact outcomes)

| # | Command | Exit | Outcome |
|---|---------|------|---------|
| 1 | `docker ... qw38-cuda:13.0.2 make cuda-opt117-diagnostics` | 0 | native OPT-117/058/checkpoint/memory-fit linked with OPT-110 |
| 2 | native `--workload capture-repro` | 0 | `ok=true`; enqueue/end_capture `ok`; 8 segment graphs; 977 nodes |
| 3 | native `--workload same-math --prefix 128 --tokens 8` | 0 | `exact=true`; 8/8 tokens; recapture 8 updates |
| 4 | native `--workload positions` | 0 | 0/127/1023/1024/2047 exact; 131071 launch-param update ok |
| 5 | native `--workload pointer-swaps --prefix 128` | 0 | exact; session-replace invalidated |
| 6 | native `--workload cancellation --prefix 128` | 0 | segment poll=1 and commit poll=8 cancel; frontier preserved |
| 7 | native `--workload state-memory --prefix 128` | 0 | `state_equals=true`; graph_bytes 12582912 |
| 8 | native graph-ab D128 3 warmups + 10 AB/BA × 256 tok | 0 | parent 57.413 tok/s vs candidate 56.230; geo 0.979; CI lower 0.979; p95 ratio 1.054 |
| 9 | native graph-ab D2048 3+10 × 256 tok | 0 | parent 55.656 vs candidate 54.567; geo 0.980; CI lower 0.980; p95 ratio 1.052 |
| 10 | native graph-ab P4096 3+10 prefill | 0 | parent 2892.53 vs candidate 2894.83; geo 1.0008; guard pass |
| 11 | `uv run python tools/opt117_decode_segment_graphs.py --phase report` | 0 | fixture+REPORT `verdict=retain_ffn_only` |
| 12 | `uv run pytest -q tests/test_opt117_decode_segment_graphs.py` | 0 | `3 passed in 0.04s` |
| 13 | `nvidia-smi -L` | 0 | `GPU 0: NVIDIA GeForce RTX 5090` |

GPU status: sitting RTX 5090, image `qw38-cuda:13.0.2`, GGUF present. Production
`kSelectedExecutionGraphPath` remains `ffn_only`. D128 tok/s delta **-1.183**.
OPT-115 D128 idle-leaf 9.97 ms was not reduced; recapture dominates.

### Verification (2026-09-13T14:27:54Z)

model: composer-2.5, telemetry_unavailable

**Verdict: PASS**

| # | Command | Exit | Outcome |
|---|---------|------|---------|
| 1 | `uv run ruff format tools/opt117_decode_segment_graphs.py tests/test_opt117_decode_segment_graphs.py` | 0 | 2 files left unchanged |
| 2 | `uv run ruff check tools/opt117_decode_segment_graphs.py tests/test_opt117_decode_segment_graphs.py` | 0 | All checks passed |
| 3 | `uv run pytest -q tests/test_opt117_decode_segment_graphs.py` | 0 | 3 passed in 0.03s |
| 4 | `docker run … qw38-cuda:13.0.2 make cuda-opt117-diagnostics` | 0 | target up-to-date; binary present (19 131 760 B) |

Fixture [`fixtures/opt117_decode_segment_graphs.json`](fixtures/opt117_decode_segment_graphs.json):
`verdict=retain_ffn_only`, `shipping_execution_graphs=ffn_only`,
`production_kept=false`, `d128_tok_s_delta=-1.183`, D128/D2048 throughput gates
fail (geo CI lower &lt; 1.00), capture/same-math/quality/state pass.
[`evidence/optimization/opt117-decode-segment-graphs/REPORT.md`](../evidence/optimization/opt117-decode-segment-graphs/REPORT.md)
documents measured reject and retain `ffn_only`. **tok/s delta: 0** (rejection;
baseline unchanged).

## Outcome and evidence

Test whether capture of the existing eight-layer decode segments removes real
submission/dependency gaps on post113. [OPT-114](OPT-114.md) met its launch
trigger but failed with `cannot capture CUDA scheduler FFN graphs`; that generic
error is not a root cause. [The scheduler](../cuda/full_scheduler.cu) has
`capture_decode_segment_graph`, `enqueue_decode_layer_eager`,
`SchedulerGraphs::capture` and parameter-update machinery. Reuse them, with
the current llama Q4 adapter and attention crossover included.

Read [OPT-115](OPT-115.md)'s shared protocol and [OPT-116](OPT-116.md).
The reported old ~11 ms/token is a hypothesis to validate, not promised savings.

## Implementation

1. Reproduce capture failure on authenticated post113 and locate the first
   failing CUDA call/kernel/stream, preserving both enqueue and end-capture
   errors. Inspect default-stream use, lazy allocations/initialization,
   unsupported synchronization and current adapter launch conventions.
2. Repair the minimal capture/stream/lifetime defect. Capture the real selected
   full layer body with stable storage, initialize outside capture, and prove
   the graph launches the same kernels/arithmetic as eager. Do not replace
   current kernels with a historical graph-compatible implementation.
3. Test the existing eight-layer segmentation first. Permit at most one
   alternative coarser segmentation only if measured update/launch overhead
   limits it and cancellation bounds permit it. No generic graph framework,
   persistent full-model kernel or arithmetic retuning in this task.
4. Cover positions 0/127/1023/1024/2047 and near 131072, token-to-token pointer
   swaps, session replacement, graph invalidation and growth of visible KV.
   Audit captured scalar-by-value arguments: attention crossover, visible
   length, frontier and buffer generation must update correctly. Include all
   update, boundary polling and commit costs in request timing.
5. Retain public cancellation responsiveness. Measure actual poll cadence and
   cancellation latency on eager and graph paths; do not obtain a win by
   disabling a poll callback. Inject cancellation/failure at segment and commit
   boundaries, then verify frontier/state/logits/sampler preservation.
6. Run full-engine graph/eager A/B on the production request path and original
   benchmark path, with same-path quality and state/memory tests. Compare
   measured gaps and launch counts with OPT-115 estimates; explain residuals.
   Record graph creation cost and startup amortization separately, and include
   graph memory in the live 128K fit.

## Acceptance and stop rules

Keep only under OPT-115's E2E/quality/state/memory gates, with a demonstrated
reduction in the targeted gaps. If capture remains unsupported, publish the
minimal reproducer and specific missing capability; do not record it as a
measured graph performance loss. If a working graph loses/inconclusively ties,
retain `ffn_only` and complete with that measured outcome.

Deliver raw timelines, focused graph/state regressions, task-owned summary and
`evidence/optimization/opt117-decode-segment-graphs/REPORT.md`. No claim of
reduced weight/KV DRAM traffic from launch reduction alone.
