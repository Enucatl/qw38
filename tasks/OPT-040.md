# OPT-040 — Hoist prompt GDN inverse normalization

## Control

- Primary ID: OPT-040
- Coupled IDs: none
- Dependencies: OPT-038 (explicit prerequisite), OPT-019 done
- Status: pending
- Ledger acceptance: A/B shared per-token/key-head inverse norms against repeated warp-column normalization; keep only with byte-equal quality outputs/state, frozen GDN gates, lower GDN time, improved P, 95% decode throughput floors, and 105% p95 ceilings; otherwise reject.

## Goal and boundaries

Compute Q/K inverse norms once per token/key head. Keep the FP32 recurrence,
convolution, decay, beta, state layout, decode path, cancellation, allocation,
and all GDN-002/OPT-013 limits. This is not OPT-029 fusion and does not
transpose persistent state. Classification: CUDA/numeric; plan impact none.

## Repository evidence

- `cuda/gdn_fused_quality.cuh:100-175` — repeated `query_squares`/`key_squares`.
- `cuda/gdn_step.cu:694-850` — production launch and retained paths.
- `.cache/authorities/llama.cpp/src/models/qwen35.cpp:267-291` — Q/K norms precede recurrence.
- `.cache/authorities/llama.cpp/ggml/src/ggml-cuda/gated_delta_net.cu` — recurrence consumes normalized Q/K.

## Implementation decisions

Add a shared-inverse diagnostic branch for Hk=16,Hv=48,D=128. One warp per
token/key head computes the existing ordered sums and exact `sqrtf` equations,
stores two FP32 values in aliased prompt scratch, and the recurrence loads them.
Do not use `rsqrtf`, change association, or alter the recurrence loop. Retain
the repeated branch and verify scratch lifetime/capacity before dispatch.

## Acceptance and validation

Frozen denominators are P 1869.84412, D128 25.3816128, D2048 20.169548 and
the OPT-034 p95s. Test token counts 1,2,3,4,63,64,65,512,2048,4096, nonzero
state/history, and layers 0/1/62. Require byte-equal inverse values, output,
state, convolution candidate, max abs 5e-8, RMS 5e-9, zero nonfinites, and
prepare/cancel/graph equality. A/B complete 4096-token GDN chain: 3 warmups,
30 alternating CUDA-event pairs. End-to-end follows unchanged OPT-021/032,
requiring P improvement and cross-workload guards. Focused:
`uv run pytest -q tests/test_opt040_gdn_shared_inverse.py tests/test_documentation.py`;
hardware adds `QW38_RUN_CUDA_TESTS=1`. Rebuild GDN/scheduler consumers and
update GDN/full-prefill docs. Keep or retain REJECTION.md.

## Run record

Planning proposed 2026-09-10; implementation and verification not run.
