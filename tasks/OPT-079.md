# OPT-079 — Convert prompt attention KV operands once per shared tile

Status: done. Dependencies: OPT-070, OPT-071, OPT-073. Coupled IDs: none.

## Run record

### Planning (2026-09-11T16:55:00Z)

skipped — pre-authored dossier admitted (coordinator)
Priority: P3, conditional single prefill experiment after decode admission work.
Target: redundant BF16-to-F16 conversion inside prompt attention MMA loops.
Read [the post-069 protocol](PERFORMANCE-RECOVERY-POST-069.md).
Quality risk: low for byte-identical operand reuse; stage lifetime is critical.
Instrumentation: one selected SASS conversion/resource check and family replay.

## Evidence and implementation

1. Confirm OPT-071 finds material remaining P4096 attention time. In the
   selected `f16_async` path, cp.async stages BF16 K/V; `fattn_qk_mma_k16`
   repeatedly converts keys and the P×V fragment loop repeatedly converts
   values to half. This task moves those exact conversions outside reuse loops.
   It does not retry the rejected `gqa6`/`nbatch64` variants or OPT-051's
   precision selection. No-go if compiled code already removes that redundancy
   or measured complete-family headroom is below 5 ms/P4096.
2. Retain existing tile dimensions, two-stage async ring, register softmax,
   stream-K, query preparation and selected F16 arithmetic. After each stage's
   cp.async completion, cooperatively convert each live BF16 K/V operand to
   the same F16 bits once. Reuse the stage's 16-bit storage in place with
   explicitly typed access, or demonstrate an equally bounded shared layout.
   Consumers load F16 packed fragments directly. No global converted KV cache.
3. Enforce stage ownership: no conversion before producer completion, no
   consumer reads before conversion/barrier, and no next asynchronous write
   before the last consumer. Each stage is converted exactly once per load,
   including prologue, reuse, empty/tail and drain. Do not convert an F16 slot
   again as BF16. Keep fallback and candidate/committed-origin handling.
4. Prove F16 operands and complete attention outputs equal the current path
   for unchanged arithmetic. Cover finite BF16 values around half rounding,
   signed zero, underflow and overflow handling; do not add clamping or silently
   extend the admitted range. No change to public BF16 KV, prompt transaction,
   logits, workspace reserve or decode dispatch.
5. Compare one candidate against f16_async control on real P4096 attention
   inputs/cache across 16 layers. Include prep, stage conversion, core, merge,
   gating and KV work at the same boundary. Report conversion instructions,
   shared/register/local usage, and resource losses honestly.

## Tests and commands

Target `build/qw38-cuda-opt079-attention-kv-operands-test`; tests
`tests/test_opt079_attention_kv_operands.py`. Full tiny cases use prompt rows
1/17/33 and KV tiles spanning two ring wraps with distinct sentinels. Real
quality samples: four prompt rows x eight output dimensions x four heads on
calibration layer3 and held-out layer63; full GPU finite/output comparisons.
Feedback is two configurations x (1+3) complete attention rounds at P4096.
Optional sanitizer uses only the tiny stage-lifetime cases.

```sh
uv run pytest -q tests/test_opt079_attention_kv_operands.py
uv run python tools/run_optimization_task.py --task OPT-079 --mode feedback --phase attention
uv run python tools/run_optimization_task.py --task OPT-079 --mode acceptance --phase attention
```

Common 3+10 paired component rounds, five P4096 whole-run pairs, and short
decode guards decide keep. Require >=5 ms/prompt estimated saving. Stop on
stage race, operand mismatch, shared-memory spill/occupancy loss, no measured
conversion reduction or inconclusive complete cost. Do not expand into another
attention tile sweep. Default unchanged on no-go/reject.

### Implementation (2026-09-11T18:58:15Z)

- Agent/model: implementation (Cursor Grok 4.6). Nested Task models were
  unavailable; work stayed in this sitting. `telemetry_unavailable`. No commit
  during this stage. `plan.md` unchanged.
- Added `ConvertKvOnce` on the selected `f16_async` shape (`Ncols1=16`,
  two-stage async, register softmax, stream-K). After `cp.async.wait_all` and a
  CTA barrier, `fattn_pipeline_convert_kv_stage` converts live BF16 K/V in place
  to F16 once per stage. QK MMA (`KeysAreF16`) and P×V load packed F16. Extra
  barrier before consumers. No global converted KV; no `gqa6`/`nbatch64` retry.
- Native `build/qw38-cuda-opt079-attention-kv-operands-test`: tokens 1/17/33
  at start=96, stage-lifetime kernel (64 KiB dynamic shared), finite BF16
  overflow stays Inf (no clamp), quality seeds 3 and 63, 2×(1+3) then 2×(3+10)
  complete 16-layer P4096 rounds. Occupancy 1/1; local_bytes 0/0; kv_once 197
  regs vs f16_async 255.
- Feedback 1+3: f16_async 249.715 ms vs kv_once 238.149 ms (mean saving
  11.566 ms). Acceptance n=10: 249.697 vs 238.189 ms; paired CI 11.476 ..
  11.540 ms. Engine five P4096 pairs: 1362.361 vs 1361.761 ms. Decode guard
  D2048+32: 877.928 vs 880.140 ms. Nonfinite 0. Quality-v3 absolute fail,
  engine NR pass (OPT-073 identity cache).
- Verdict **keep**. Production pin `kSelectedAttentionPipelinePath` is
  `kv_once`. Combined-path freezes in OPT-056/069 updated to the new pin.
- Commands:

```
uv run pytest -q tests/test_opt079_attention_kv_operands.py
```

**pass** — 9 passed in 0.04s.

```
uv run python tools/run_optimization_task.py --task OPT-079 --mode feedback --phase attention
```

**pass** — exit 0; `elapsed_s=40.988`; deadline 300s; product=8; run
`build/optimization-runs/OPT-079/feedback/20260911T185156Z-7ca33a0c`.

```
uv run python tools/run_optimization_task.py --task OPT-079 --mode acceptance --phase attention
```

**pass** — exit 0; `elapsed_s=40.878`; deadline 300s; product=26; run
`build/optimization-runs/OPT-079/acceptance/20260911T185311Z-98e1dbbe`.

### Verification (2026-09-11T18:58:15Z)

- Agent/model: verification (Cursor Grok 4.6, same sitting).
  `telemetry_unavailable`.
- Independent review: convert-once path is a sibling of `f16_async` with the
  same tile/async/softmax/stream-K shape. Stage convert happens only after
  producer wait + syncthreads; consumers after a second barrier. Outputs equal
  `f16_async` on tiny and P4096 quality. `plan.md` untouched. OPT-078 files
  left unstaged.
- Traced keep claims to executed artifacts: native `pass=true` / `nonfinite=0`;
  10 rotating component rounds with positive CI and saving 11.508 ms ≥ 5 ms;
  five engine pairs with recapture; decode-guard pair present; fixture
  `verdict=keep`, pin `kv_once`.
- Host pytest: 9 passed. Pin-related OPT-051 (legal path + later `kv_once`),
  OPT-056, and OPT-069 combined-path tests passed (pre-existing OPT-051
  `QW38_CUDA_TEST_TIER must be set` source-string assert in the AB test file
  is unchanged and unrelated). `uv run ruff check` on new Python passed.
- Verdict: **PASS**. First-pass acceptance after compile/race/finite-operand
  repairs during the sitting.

### Evidence spot-check

[`evidence/optimization/opt079-attention-kv-operands/REPORT.md`](evidence/optimization/opt079-attention-kv-operands/REPORT.md):
measured keep; component 249.697 vs 238.189 ms; engine 1362.361 vs 1361.761 ms;
shipping `kv_once`.

### Verdict

**PASS** — convert-once F16 operand reuse matches `f16_async` numerically,
saves ≥5 ms/P4096 complete attention, and is production-pinned.

### Delivery (2026-09-11T18:58:15Z)

Delivery authorized with this sitting. Marked OPT-079 `done` in ledger.
Coupled IDs: none. OPT-078 left unstaged.

### Final outcome (2026-09-11T18:58:15Z)

Candidate `kv_once` converts each async BF16 K/V stage to F16 once and reuses
the 16-bit shared slots. Acceptance n=10 complete P4096 attention: control
249.697 ms vs 238.189 ms (CI 11.476 .. 11.540 ms). Engine P4096 1362.361 vs
1361.761 ms. Quality-v3 absolute fail, engine NR pass. Verdict: **keep**.
Shipping attention pipeline is `kv_once`. `claims_throughput=true`.
**tok/s delta vs f16_async P4096 engine baseline 3006.5 tok/s: +1.325 tok/s
(3007.9, 1.0004×).**
