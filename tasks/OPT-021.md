# OPT-021 — Pin cold 4K prefill oracle protocol and baseline

## Control

- Primary ID: `OPT-021`
- Coupled IDs: `none`
- Dependencies: `OPT-020`; done at planning admission
- Status: `done`
- Ledger acceptance: Frozen protocol and checked-in report compare cold exact-4096 Quartz `sync_tokens` (3 replicates, attribution null, graphs created, production path) to same-sitting llama.cpp `llama-bench -p 4096 -n 0 --no-warmup -r 3 -ngl 99` on the pinned GGUF and RTX 5090; baseline Quartz/llama tok/s are retained as the OPT-022+ keep/reject oracle; does not substitute for OPT-016

## Goal and boundaries

Freeze one exclusive-RTX-5090 sitting protocol for cold exact-4096-token prefill and retain the live Quartz mean tok/s and same-sitting llama.cpp `avg_ts` as the starting keep/reject oracle for OPT-022 through OPT-026. Quartz uses production `sync_tokens` with `PrefillAttribution* = nullptr`, graphs created, and the production fused GDN path. llama.cpp uses pinned `cc83d7b` `llama-bench` with the ledger arguments. This increment does not change production kernels, does not require Quartz tok/s ≥ llama.cpp, and does not own or re-assert the OPT-016 2K parity gate.

- Constraints: keep C++17, CUDA 13.0.2, `sm_120`, Makefile `NVCCFLAGS --fmad=false` and host `-ffp-contract=off` unchanged, the pinned Qwen3.8-27B Q4_K_M GGUF SHA-256 `31629f53165ab6a7dad8c9847dcfd1fdf55829dac1e6e748f4a68581b0033d34`, BF16 activation/KV storage, FP32 residual and GDN recurrence, production `GdnConfig{16, 48, 128, 128, 4}`, GDN-002 `kScanWindow = 64`, OPT-008 `kPromptChunkRows = 4096` and `prompt_chunk_rows_ = min(4096, capacity)`, workspace byte formula `160,380,416 + 96*C + 404,992*R`, OPT-011 fused overlapped production plus retained unfused serial path, OPT-012 prompt FFN graphs (replay when `token_count == 4096`), OPT-013 parallel associative overlay fallback, OPT-017 mixer Q8_0 MMA with visible OPT-009 byte-exact reference, OPT-018 FFN MMA, OPT-019 warp-column GDN and fattn-mma attention, SES-002 candidate-versus-committed isolation, Engine session capacity `131072`, and all frozen SCH-002/OPT-008/OPT-009/OPT-011/OPT-012/OPT-013/OPT-014/OPT-017/OPT-018/OPT-019/CUD-002/GDN-002/ATN-002/OPT-005 numeric and exact-state gates. Mirror the OPT-016 measurement pattern (docker images `qw38-cuda:13.0.2` and `qw38-llama-authority:cuda-13.0.2`, pinned GGUF, 3 cold replicates, attribution null, graphs created, production path, llama.cpp first then Quartz) at 4096 tokens. Exclusive RTX 5090 work stays serialized. Do not add a persistent session or workspace `cudaMalloc`. Do not edit `Makefile`; build the diagnostic with the same explicit pinned `nvcc` command pattern as `tests/test_opt016_parity.py`. Do not run `nsys` or `ncu`. Do not edit `plan.md`. The plan.md Q8 association exception is already present for OPT-022 and later; this increment must not exercise it or treat it as in-scope production. Do not change public Engine, CLI, server, or `qw38-bench` JSON schemas. Scout sitting 2026-09-09 outputs under `/tmp/oracle4k/` are informational only and must not be copied in as the retained fixture.
- Non-goals: the OPT-016 2K tok/s ≥ llama.cpp gate; requiring Quartz 4K ≥ llama.cpp 4K; OPT-022 mixer Q8_0 quality MMQ / shared residual Y; OPT-023 skinny-M mixer dispatch; OPT-024 Blackwell Q8 D2R; OPT-025 dense FFN shared-Y / SwiGLU-into-down; OPT-026 fattn stream-K; changing production kernels or dispatch; 4K attributed split (attribution stays null on the oracle wall); 8K/32K/128K throughput; QLT-001 128K quality; CMP-002/CMP-003; loosening any numeric or exact-state envelope; applying the plan.md Q8 association rule; extra `cudaMalloc`; public API or BEN-001 schema changes; overwriting OPT-016/017/018/019/020 pins, fixtures, or evidence; a new EDU task; editing `plan.md`.
- Plan impact: `none`
- Affected interfaces: no public Engine, CLI, server, model, or BEN-001 JSON API changes. No CUDA-internal production dispatch change. New schema-1 OPT-021 contract, fixture, diagnostic, host test, and 4K evidence files are task evidence, not a public protocol.
- Implementation classification: not mechanical. This designs new acceptance evidence: a frozen exact-4096 keep/reject oracle, a CUDA diagnostic distinct from the OPT-016 2K gate, and live same-sitting llama.cpp comparison without claiming 2K parity. Use the CUDA/numeric implementation agent.
- Documentation stage: required. Update the existing optimization, benchmark-harness, and full-prefill handbook chapters plus `docs/sources.md`, `docs/README.md`, and one non-final then final OPT-021 audit row. Implementation writes the measured oracle report from the live JSON so host validators can pass; documentation links that report from the handbook and must not rewrite live numbers. Do not add an EDU task; EDU-046 and EDU-047 already cover chapters 61 and 62. The audit-table row must not contain other task IDs.

## Repository evidence

- `implementation_ledger.md:90` — OPT-021 is pending after done OPT-020; acceptance is a frozen protocol and checked-in report comparing cold exact-4096 Quartz `sync_tokens` (3 replicates, attribution null, graphs created, production path) to same-sitting llama.cpp `llama-bench -p 4096 -n 0 --no-warmup -r 3 -ngl 99` on the pinned GGUF and RTX 5090; baseline Quartz/llama tok/s are retained as the OPT-022+ keep/reject oracle; this does not substitute for OPT-016. OPT-021 occurs exactly once in the ledger.
- `implementation_ledger.md:89` — dependency OPT-020 is `done`.
- `implementation_ledger.md:85` — OPT-016 is `blocked` and remains the sole ≥ llama.cpp **2K** gate; do not couple it, do not re-assert that gate here, and do not treat a 4K comparison as 2K parity. The ledger already records that OPT-021+ may use a 4K steering oracle without substituting for OPT-016.
- `implementation_ledger.md:86-88` — OPT-017, OPT-018, and OPT-019 are `done` and remain the current production mixer/FFN/GDN/attention paths. This increment does not reopen them.
- `implementation_ledger.md:91-95` — OPT-022 through OPT-026 are pending idea-ladder tasks that depend on this oracle (OPT-022/025/026 list OPT-021; OPT-023/024 depend on OPT-022). Do not couple them. Do not start mixer quality, skinny-M, D2R, FFN shared-Y, or stream-K here.
- `implementation_ledger.md:118` — QLT-001 is `blocked` on OPT-016; it is not pending and must not be coupled.
- `implementation_ledger.md:119-121` — CMP-001/002/003 remain later comparison work; do not start the 30-sample matrix here.
- `implementation_ledger.md:168-169` — EDU-046 and EDU-047 are `done` and already own chapters 61 and 62; do not open a new EDU task.
- `implementation_ledger.md:4288-4310` — OPT-021–OPT-026 were admitted after a scout cold exact-4096 remasurement: Quartz mean **965.204895** tok/s versus same-sitting llama.cpp `llama-bench -p 4096` **3182.476587** tok/s. Each OPT-022–OPT-026 must revert and retain a rejection if the 4K oracle does not strictly improve. Ladder stop condition (owned by later tasks, recorded here) is Quartz cold 4K mean tok/s ≥ same-protocol llama.cpp. Next eligible pending by row order is OPT-021.
- `plan.md:134-144` — Q8_0 prompt MMQ quality paths may use the ds4 Q8 association rule for **OPT-022 and later** while the OPT-009 byte-exact reference stays visible. That exception already exists; this increment must not change `plan.md` and must not land a quality Q8 path.
- `plan.md:63-65` — restricted C++17, simple Makefile, no generic tensor framework.
- `plan.md:104-107` and `plan.md:122-123` — optimized fused paths keep a visible unfused reference; an optimization cannot loosen the tolerance used to admit it. OPT-021 does not admit a new kernel.
- `plan.md:186-198` — comparison protocol already names 2K prefill as a first-class workload and uses the identical Q4_K_M GGUF. The 4K oracle is an operational steering protocol for OPT-022+, not a plan change and not the 30-sample CMP matrix. Plan benchmark lengths remain 128/2K/8K; 4K is not added as a plan.md primary comparison length.
- `pins/artifacts.lock.json` and `pins/llama_authority_contract.json:37-46` — production GGUF `Qwen3.8-27B-Q4_K_M.gguf` SHA-256 `31629f53165ab6a7dad8c9847dcfd1fdf55829dac1e6e748f4a68581b0033d34`; llama.cpp revision `cc83d7b4824f73cfdda4dfbb47ee39804f71b328` is MIT and is the independent same-GGUF oracle and baseline.
- `tasks/OPT-016.md:57-77` — frozen 2K protocol: images `qw38-cuda:13.0.2` and `qw38-llama-authority:cuda-13.0.2`, exact token count, token IDs `(42 + index * 997) % kVocabularySize`, unperturbed `sync_tokens` with attribution null, capacity `131072`, graphs created, production GDN path, cache_policy disabled, ResidentModel may persist, fresh session/workspace/graphs per replicate, 0 warm-ups, 3 timed replicates, tok/s `tokens * 1000 / wall_ms`, arithmetic mean of three tok/s values, llama.cpp `llama-bench -p N -n 0 --no-warmup -r 3 -ngl 99` in the same sitting with llama first then Quartz, Nsight `not_used`. OPT-021 copies that pattern with `N = 4096`.
- `cuda/prefill_2k_parity_test.cu:14-16,45-48,62-84` — 2K diagnostic times only `sync_tokens` (session/workspace/graphs created before `cudaEventRecord`); graphs.create is outside the timed region; `PrefillAttribution*` is omitted (defaults to nullptr); production `GdnScanPath` default is `kFusedTokenLoop`.
- `cuda/full_scheduler.h:22,473-481` — `kPromptChunkRows = 4096`; `sync_tokens` defaults `graphs = nullptr`, `gdn_scan = GdnScanPath::kFusedTokenLoop`, `attribution = nullptr`. The oracle must pass a created `SchedulerGraphs*` so production FFN graphs can replay.
- `cuda/full_scheduler.cu:2259-2267` — prompt FFN graphs replay only when `graphs != nullptr`, `prompt_graph_count() == 64`, and `token_count == prompt_graph_rows()`. Exact-4096 is therefore the production graph-replay length; exact-2048 is not. The 4K oracle is the keep/reject bar that actually exercises captured 4096-row FFN graphs.
- `tests/test_opt016_parity.py:12-19,136-175,245-305` — host/CUDA pattern: docker `--gpus all --user uid:gid -v $PWD:/workspace`, llama-bench `-o json` parsed for `n_prompt`, Quartz nvcc line with `--fmad=false` and existing diagnostic objects, native test writes fixture. OPT-016 native test asserts `gate_passed`; OPT-021 native test must not.
- `fixtures/opt016_parity.json` — historical 2K comparison (`measurement_utc` 2026-09-08T12:59:39Z; Quartz mean 208.758591 tok/s; llama `avg_ts` 3183.528255; `gate_passed` false). Do not overwrite. Do not reuse `gate_passed` as this task's acceptance bit.
- `Makefile:6` — production `nvcc` keeps `--fmad=false`. Do not flip this flag. Do not add a Makefile target for the 4K diagnostic.
- `tests/test_documentation.py:57-97` — a done non-EDU task needs a final audit row; an in-progress row must be visibly non-final. `_audit_rows` keys every task-ID token found in a row, so the OPT-021 audit-table row must not contain other task IDs (including `OPT-016`, which already appears in the OPT-017 row).
- Scout sitting 2026-09-09, informational only, must not be checked in as the fixture:
  - Quartz mean_tok_s 965.204895; walls 4239.26953, 4244.69238, 4247.021 ms; tok/s 966.204224, 964.969788, 964.440735; device NVIDIA GeForce RTX 5090; compute 12.0; `measurement_utc` 2026-09-09T00:42:04Z
  - llama.cpp `avg_ts` 3182.476587; `avg_ns` 1287105922; `n_prompt` 4096; `n_batch` 2048; `n_ubatch` 512; `flash_attn` -1; `build_commit` cc83d7b; `test_time` 2026-09-09T00:42:27Z
  - Scout binary printed `QW38_PREFILL_2K_PARITY_RESULT` and `"task":"OPT-016"` with `prompt_tokens` 4096. The in-tree diagnostic must use a distinct prefix and `task` `OPT-021`. Do not copy `/tmp/oracle4k/` into the repository.

## Implementation decisions

Pin the 4K protocol, land a dedicated diagnostic and host validator, re-run the exclusive sitting live, and retain Quartz and llama.cpp tok/s as the OPT-022+ keep/reject baseline. Do not change production CUDA kernels. Do not treat Quartz ≥ llama as this task's pass condition.

### Frozen 4K protocol (the keep/reject oracle)

Use this protocol for every OPT-021 speed number and for every later OPT-022+ keep/reject comparison until a later done task publishes a replacement baseline under the same protocol. Do not introduce 2K, 8K, 32K, 128K, or chat-rendered samples as this oracle.

| Field | Frozen value |
|---|---|
| GGUF | `models/Qwen3.8-27B-Q4_K_M.gguf` SHA-256 `31629f53165ab6a7dad8c9847dcfd1fdf55829dac1e6e748f4a68581b0033d34` |
| GPU | NVIDIA GeForce RTX 5090, compute capability 12.0, exclusive |
| Quartz image | `qw38-cuda:13.0.2` |
| llama.cpp image | `qw38-llama-authority:cuda-13.0.2` |
| llama.cpp revision | `cc83d7b4824f73cfdda4dfbb47ee39804f71b328` |
| Tokens | exact 4096, not chat-rendered |
| Quartz token IDs | `(42 + index * 997) % kVocabularySize` for `index in 0..4095` (same generator as `cuda/prefill_2k_parity_test.cu:46-48`, longer) |
| Quartz path | production `sync_tokens` with `PrefillAttribution* = nullptr` (unperturbed wall). Capacity `131072`. `SchedulerGraphs::create` succeeds and the graphs pointer is passed in. Default production GDN scan path `GdnScanPath::kFusedTokenLoop`. Default fused overlapped pipeline. `cache_policy` disabled. Cold: each replicate creates a fresh `SchedulerSession`, `SchedulerWorkspace`, and `SchedulerGraphs` (frontier 0, no prefix). `ResidentModel` upload may persist across replicates. 0 warm-ups. 3 timed replicates. Graph capture (`graphs.create`) stays **outside** the timed region, matching OPT-016. Timed region is `sync_tokens` only. |
| Quartz tok/s | `4096 * 1000 / wall_ms` per replicate; oracle baseline is the arithmetic mean of the three tok/s values (not tok/s of the mean wall) |
| llama.cpp command | in `qw38-llama-authority:cuda-13.0.2`, from `/workspace`: `.cache/authorities/llama-build/bin/llama-bench -m models/Qwen3.8-27B-Q4_K_M.gguf -p 4096 -n 0 --no-warmup -r 3 -ngl 99` with `-o json`. Do not add `-p 2048`, `-p 8192`, `-p 32768`, or `-b 4096`. Expected live defaults matching the scout JSON shape: `n_batch` 2048, `n_ubatch` 512, `flash_attn` -1. Record whatever the live object reports; do not fail the oracle because llama.cpp keeps `n_batch` 2048 at a 4096-token prompt. If `llama-bench` is missing, cmake-build only that target in the existing authority build dir with the same flags as `tools/build_llama_authority.sh` (`-DCMAKE_CUDA_ARCHITECTURES=120 -DGGML_CUDA=ON -DGGML_NATIVE=OFF`). Do not rebuild the Quartz authority adapters. |
| llama.cpp tok/s | live `avg_ts` from that JSON object (`n_prompt` 4096) |
| Same session | run live `llama-bench` and the three Quartz replicates in one exclusive 5090 sitting, llama.cpp first then Quartz, so clocks are comparable |
| Nsight | `not_used` |
| Attribution | null on every timed replicate; not collected as an optional fourth run in this increment |
| Scout sitting | Quartz 965.204895 tok/s and llama 3182.476587 tok/s on 2026-09-09 are recorded on the contract as `scout_*` for transparency. They are **not** a second bar and **not** the retained fixture. A failed live `llama-bench` fails the task. Copying `/tmp/oracle4k/` fails the task. |
| Keep/reject rule for OPT-022+ | a later production change is kept only if its cold exact-4096 mean tok/s is **strictly greater** than the retained OPT-021 `quartz.mean_tok_s` (or the then-current successor baseline named by a later done task). Equality is a reject. This increment does not implement that comparison for a new kernel; it publishes the initial denominator. |
| Ladder stop (not this gate) | later tasks may stop the idea ladder when Quartz cold 4K mean tok/s ≥ same-protocol llama.cpp `avg_ts`. OPT-021 records `quartz_meets_llama` as that informational comparison and **must not fail** when it is false. |
| 2K parity | `owns_opt016_parity_gate` false; `substitutes_for_opt016` false |

### Diagnostic

New file `cuda/prefill_4k_oracle_test.cu`, compiled to `build/qw38-cuda-prefill-4k-oracle-test`. Do not add a Makefile target. Do not compile or check in `/tmp/oracle4k/prefill_4k_parity_test.cu`.

Mirror `cuda/prefill_2k_parity_test.cu` with these differences:

- `kPromptTokens = 4096`
- After `graphs.create`, assert `graphs.prompt_graph_rows() == kPromptChunkRows` (4096) and `graphs.prompt_graph_count() == 64`
- After each `sync_tokens`, assert `session.frontier() == 4096`
- Print prefix `QW38_PREFILL_4K_ORACLE_RESULT=` (not the 2K prefix)
- JSON `task` is `OPT-021`
- Include `"graphs_created":true` and `"prompt_graph_rows":4096` in the printed object
- Usage remains `qw38-cuda-prefill-4k-oracle-test MODEL.gguf`

Call `sync_tokens` with the graphs pointer and default GDN/attribution (nullptr attribution). Do not pass a non-null `PrefillAttribution`. Do not pass `GdnScanPath::kSequentialWindows`.

nvcc (inside `qw38-cuda:13.0.2`), matching OPT-016:

`nvcc -std=c++17 -O2 -arch=sm_120 --expt-relaxed-constexpr --fmad=false -Xcompiler=-Wall,-Wextra,-Werror,-fno-exceptions,-fno-rtti,-ffp-contract=off,-pthread -Iinclude -Isrc -Ithird_party/utf8proc -Icuda -DQW38_DIAGNOSTIC_TRACE cuda/prefill_4k_oracle_test.cu` plus the same diagnostic objects as `tests/test_opt016_parity.py` (`build/full_scheduler.trace.cuda.o` through `build/utf8proc.o`) `-o build/qw38-cuda-prefill-4k-oracle-test`.

First `make build/qw38-cuda-timing-test` inside `qw38-cuda:13.0.2` so CUDA objects are current. Rebuild products when sources would otherwise be stale: `docker run --rm --gpus all --user "$(id -u):$(id -g)" -v "$(pwd):/workspace" qw38-cuda:13.0.2 make cuda-products cuda-native` is allowed before the diagnostic but is not required to change objects if no kernel file changed.

### Contract, fixture, evidence, and host tests

`pins/opt021_oracle_contract.json` schema 1, `task` `OPT-021`, including:

- `primary_tokens`: 4096
- `yardstick`: `4k_keep_reject_oracle`
- `llama_revision`: `cc83d7b4824f73cfdda4dfbb47ee39804f71b328`
- `gguf_sha256`: `31629f53165ab6a7dad8c9847dcfd1fdf55829dac1e6e748f4a68581b0033d34`
- `device_substring`: `RTX 5090`
- `quartz_replicates`: 3
- `quartz_warmups`: 0
- `quartz_attribution`: `null_unperturbed`
- `quartz_graphs`: `created`
- `llama_bench_args`: `["-p", "4096", "-n", "0", "--no-warmup", "-r", "3", "-ngl", "99"]`
- `scout_quartz_mean_tok_s`: 965.204895
- `scout_llama_avg_ts`: 3182.476587
- `scout_is_retained_fixture`: false
- `keep_reject_rule`: `strictly_greater_mean_tok_s`
- `owns_opt016_parity_gate`: false
- `substitutes_for_opt016`: false
- `envelopes_unloosened`: true
- `q8_0_production`: `opt017_mma_with_opt009_reference`
- `required_fixture_keys` matching the fixture below
- `proof_limit` required substrings: `4K keep/reject oracle`, `envelopes unloosened`, `does not substitute for the 2K llama.cpp parity gate`, `attribution null`, `graphs created`, `scout sitting is not the retained fixture`
- `report_path`: `evidence/optimization/opt021-4k-oracle/REPORT.md`

`fixtures/opt021_oracle.json` is the retained live claim. Keys:

```text
schema_version, task, status, measurement_utc, device, compute_capability,
llama_revision, gguf_sha256, quartz, llama_cpp, scout_quartz_mean_tok_s,
scout_llama_avg_ts, quartz_meets_llama, owns_opt016_parity_gate,
substitutes_for_opt016, nsight_systems, nsight_compute, proof_limit,
report_path
```

- `status` is `measured` (this task times a new run). Reject `scout`, `source_inspected`, and any path under `/tmp/oracle4k`.
- `quartz` holds `prompt_tokens` 4096, `replicates` 3, `wall_ms` array, `tok_s` array, `mean_tok_s`, `cold` true, `cache_policy` `disabled`, `attribution` null, `graphs_created` true, `prompt_graph_rows` 4096.
- `llama_cpp` holds live `avg_ts`, `avg_ns`, `n_prompt` 4096, `n_batch`, `n_ubatch`, `flash_attn`, `build_commit`, `test_time`.
- `quartz_meets_llama` is true iff `quartz.mean_tok_s >= llama_cpp.avg_ts`. Host validation checks that equality; the native test does **not** require it to be true.
- `owns_opt016_parity_gate` and `substitutes_for_opt016` are false.
- `nsight_*` are `not_used`.
- `scout_*` copy the contract scout numbers for transparency and must not equal a requirement that live `mean_tok_s` / `avg_ts` match them.
- `report_path` is `evidence/optimization/opt021-4k-oracle/REPORT.md`.

Evidence files written by the native sitting:

- `evidence/optimization/opt021-4k-oracle/llama-bench-4k.json` — full live llama-bench JSON array
- `evidence/optimization/opt021-4k-oracle/quartz-4k.json` — parsed Quartz diagnostic object
- `evidence/optimization/opt021-4k-oracle/REPORT.md` — claim-labeled 4K oracle report with live numbers, protocol table, scout-versus-retained distinction, keep/reject rule, and the proof-limit phrases. Implementation writes this from the live JSON. Documentation must not change the live numbers.

`tests/test_opt021_oracle.py` host tests always run:

- Contract/fixture connected: 4096 tokens, 4K keep/reject yardstick, 3 replicates, 0 warm-ups, attribution null, graphs created, llama args exactly as above, device contains `RTX 5090`, `compute_capability` `12.0`, `mean_tok_s` matches the arithmetic mean of `tok_s`, `quartz_meets_llama` equals `mean_tok_s >= avg_ts`, `n_prompt` 4096, `owns_opt016_parity_gate` false, `substitutes_for_opt016` false, `scout_is_retained_fixture` false, proof-limit phrases present in fixture and report, report file exists, report_path does not contain `/tmp`.
- Validator rejects mutations: `prompt_tokens` 2048; `prompt_tokens` 2052; `llama_cpp.n_prompt` 2048; `attribution` non-null; `replicates` 1; `graphs_created` false; `substitutes_for_opt016` true; `owns_opt016_parity_gate` true; `quartz_meets_llama` true while `mean_tok_s` is below `avg_ts`; `nsight_systems` a capture path; `status` `source_inspected`; `status` `scout`; `report_path` under `/tmp/oracle4k`; proof-limit missing `does not substitute for the 2K llama.cpp parity gate`; `task` `OPT-016`.

CUDA native (`QW38_RUN_CUDA_TESTS=1`): exclusive RTX 5090; llama-bench under the frozen 4096 args first; build and run `cuda/prefill_4k_oracle_test.cu` three cold replicates; write the evidence JSON files and `fixtures/opt021_oracle.json`; write `REPORT.md` with live numbers and proof-limit phrases; `validate_result`; do **not** assert `quartz_meets_llama`. Do not invoke `tests/test_opt016_parity.py` native 2K gate in this sitting.

### Files to change

Implementation (code, tests, fixtures, live evidence):

| Path | Role |
|---|---|
| `cuda/prefill_4k_oracle_test.cu` | new; unperturbed 3× cold exact-4096 `sync_tokens` JSON |
| `tests/test_opt021_oracle.py` | host contract/fixture validator, mutation rejections, CUDA oracle sitting (llama-bench + Quartz) |
| `pins/opt021_oracle_contract.json` | schema 1 protocol, scout numbers, keep/reject rule, required keys |
| `fixtures/opt021_oracle.json` | retained live 4K comparison (`status` `measured`) |
| `evidence/optimization/opt021-4k-oracle/REPORT.md` | claim-labeled 4K oracle report |
| `evidence/optimization/opt021-4k-oracle/llama-bench-4k.json` | live llama-bench 4096 object |
| `evidence/optimization/opt021-4k-oracle/quartz-4k.json` | live Quartz 3-replicate record |

Documentation stage (prose, links, mechanical index):

| Path | Role |
|---|---|
| `docs/61-benchmark-harness.md` | OPT-021 frozen 4K oracle versus BEN-001, versus the OPT-016 2K yardstick, versus the 2026-09-08 scaling citation; scout sitting is not retained |
| `docs/62-cuda-full-prefill.md` | 4K oracle, graphs created and replayed at 4096, keep/reject baseline, proof boundary (not 2K parity) |
| `docs/06-system-optimization.md` | 4K keep/reject oracle as Measured protocol/baseline; not a throughput gate versus llama.cpp |
| `docs/sources.md` | local-derivation OPT-021 entry (pinned llama.cpp `llama-bench` already an External authority; no new implementation source) |
| `docs/README.md` | one-line updates for chapters 6/61/62 as needed |
| `docs/65-documentation-audit.md` | non-final OPT-021 row while `in_progress`, then final row at delivery; row body must not contain other task IDs |

Do not edit: `plan.md`, `Makefile` `NVCCFLAGS`, `include/qw38/engine.h`, `src/engine.cpp`, `src/bench.cpp`, `pins/benchmark_contract.json`, `pins/artifacts.lock.json`, `pins/opt016_parity_contract.json`, `fixtures/opt016_parity.json`, `tests/test_opt016_parity.py`, `cuda/prefill_2k_parity_test.cu`, any OPT-017/018/019/020 pin, fixture, CUDA kernel, or evidence file, `pins/cuda_prompt_mmq_contract.json`, `pins/cuda_mmq_contract.json` numeric fields.

### Makefile and public API

Do not add a `Makefile` target. Do not change `include/qw38/engine.h`, `src/engine.cpp`, or `src/bench.cpp`. Production `Session::sync` remains the unattributed path.

### Invariants

- Live cold exact-4096 Quartz mean tok/s and live llama.cpp `llama-bench` 4K `avg_ts` are retained from one exclusive sitting.
- OPT-022+ keep/reject uses strictly greater Quartz mean tok/s than this retained baseline (or a later successor published under the same protocol).
- This increment does not claim Quartz ≥ llama.cpp and does not own the OPT-016 2K gate.
- Timed Quartz wall is unperturbed `sync_tokens` with attribution null and graphs created; 4096-row prompt FFN graphs are the production replay path.
- CUD-002, OPT-009 Q8_0 byte equality on the visible reference, OPT-013/OPT-019 GDN envelopes, OPT-005/OPT-019 attention envelopes, SCH-002/OPT-008 exact-state, SES-002 isolation, and the workspace byte formula are unloosened.
- No extra persistent `cudaMalloc`. No Makefile `--fmad=false` change. No `plan.md` change.
- Scout `/tmp/oracle4k/` numbers are not the retained fixture.
- QLT-001 stays blocked until OPT-016 is done; do not run 128K quality.

### Rejected alternatives

- Checking in `/tmp/oracle4k/` scout JSON as the fixture — the ledger and this dossier require a live re-run under the frozen in-tree protocol; the scout binary labeled the result `OPT-016` / `QW38_PREFILL_2K_PARITY_RESULT`.
- Treating Quartz ≥ llama.cpp as OPT-021 acceptance — the ledger asks to pin a protocol and retain a baseline, not to pass 4K parity.
- Reusing `cuda/prefill_2k_parity_test.cu` or `tests/test_opt016_parity.py` with a 4096 override — would mix 2K gate ownership, the 2K prefix, and `gate_passed` into this oracle.
- Using chat-rendered prompts, BEN-001 smoke, or `qw38-bench` as the oracle — would expand the BEN-001 schema and would not match exact-4096 `sync_tokens`.
- Timing `graphs.create` inside the wall — OPT-016 times only `sync_tokens`; capture cost is not the oracle denominator.
- Passing a non-null `PrefillAttribution` on the timed replicates — attribution perturbs the wall; the ledger says attribution null.
- Forcing llama.cpp `-b 4096` — the frozen command is the ledger argv; live `n_batch` 2048 is an expected llama.cpp default.
- Coupling OPT-016, OPT-022–OPT-026, QLT-001, or a new EDU task — none is pending-eligible inseparable documentation for this increment.
- Applying the plan.md Q8 association exception here — that exception is for OPT-022+; this task is protocol/evidence only.
- Editing `plan.md` to add 4K as a primary comparison length — 4K is a steering oracle; plan.md already has the Q8 association exception for OPT-022+.
- Running Nsight, ds4, or 8K/32K/128K prefills as this gate.

### Discovered ledger work

none

### Unresolved decisions

none

## Acceptance and validation

- Acceptance conditions:
  - On the pinned GGUF and RTX 5090, one exclusive sitting records three cold exact-4096 Quartz `sync_tokens` tok/s (attribution null, graphs created, production path) and live pinned llama.cpp `llama-bench` `-p 4096 -n 0 --no-warmup -r 3 -ngl 99` `avg_ts`.
  - Checked-in contract, fixture, report, and JSON evidence retain both means as the OPT-022+ keep/reject oracle under the frozen protocol.
  - Host validators prove the protocol (4096, 3 replicates, attribution null, graphs created, llama args, device, unloosened envelopes, does not substitute for the 2K parity gate) and reject scout/`/tmp` mutations.
  - Native test does not require `quartz_meets_llama`.
  - Production kernels, `plan.md`, Makefile `NVCCFLAGS`, and OPT-016/017/018/019/020 evidence files are unchanged.
  - Coupled IDs: none.
- Tests/fixtures to add or change:
  - `cuda/prefill_4k_oracle_test.cu`
  - `tests/test_opt021_oracle.py`
  - `pins/opt021_oracle_contract.json`
  - `fixtures/opt021_oracle.json`
  - `evidence/optimization/opt021-4k-oracle/REPORT.md`
  - `evidence/optimization/opt021-4k-oracle/llama-bench-4k.json`
  - `evidence/optimization/opt021-4k-oracle/quartz-4k.json`
- Focused commands:
  - `uv run pytest -q tests/test_opt021_oracle.py`
  - After documentation: `uv run pytest -q tests/test_opt021_oracle.py tests/test_documentation.py`
  - `QW38_RUN_CUDA_TESTS=1 uv run pytest -q tests/test_opt021_oracle.py` on exclusive RTX 5090 after `docker run --rm --gpus all --user "$(id -u):$(id -g)" -v "$(pwd):/workspace" qw38-cuda:13.0.2 make build/qw38-cuda-timing-test`
- Repository-wide commands:
  - `uv run ruff format .`
  - `uv run ruff check .`
  - `uv run pytest -q`
- Native/CUDA/hardware gates: required. Exclusive RTX 5090. The named hardware gate is `QW38_RUN_CUDA_TESTS=1 uv run pytest -q tests/test_opt021_oracle.py` (llama-bench 4096 in `qw38-llama-authority:cuda-13.0.2` then Quartz diagnostic in `qw38-cuda:13.0.2`). Do not run `nsys` or `ncu`. Do not run ds4. Do not run 8K/32K/128K prefills as gates. Do not run `tests/test_opt016_parity.py` native 2K gate as this task's acceptance (that gate remains blocked and would fail independently).
- Documentation/evidence updates: the documentation-stage table above; non-final audit row while `in_progress`, final row at delivery (claim class **Measured** for the live 4K comparison; **External** for llama.cpp `llama-bench` provenance). Audit row must not contain other task IDs. Proof boundary: 4K keep/reject oracle, not 2K parity, not Quartz ≥ llama.cpp.
- Definition of done: checked-in live 4K evidence shows the frozen protocol, retains Quartz mean tok/s and llama.cpp `avg_ts` from the same sitting as the OPT-022+ keep/reject oracle, scout `/tmp` numbers are not the fixture, envelopes and production kernels are untouched, `plan.md` is unchanged, and OPT-016 remains the blocked 2K parity owner.

## Run record

### Planning

- Agent/model: cursor-grok-4.6-high
- UTC/time/tokens/cost: telemetry_unavailable (Cursor session)
- Outcome: Decision-complete dossier created at `tasks/OPT-021.md`. Coupled IDs `none`. Plan impact `none`. Frozen 4K protocol mirrors OPT-016 measurement mechanics at exact-4096 with graphs created and attribution null. Live re-run is required; scout 965.204895 / 3182.476587 tok/s are contract transparency only. Keep/reject is strictly greater Quartz mean tok/s; Quartz ≥ llama is informational and not this gate. OPT-021 marked `in_progress` in `implementation_ledger.md`.

### Implementation

- Agent/model: cursor-grok-4.6-high; telemetry_unavailable
- UTC/time/tokens/cost: telemetry_unavailable (Cursor session)
- Outcome: Landed dedicated 4K diagnostic, schema-1 contract, host validator, live exclusive-RTX-5090 sitting, and retained fixture/report. Production kernels, `plan.md`, Makefile, and OPT-016/017/018/019/020 files were not edited. Handbook prose left to documentation. Native test did not require `quartz_meets_llama`. Scout `/tmp/oracle4k/` was not copied.
- Changes:
  - `cuda/prefill_4k_oracle_test.cu` — new; 3× cold exact-4096 `sync_tokens` with graphs created outside the timed region, attribution omitted, prefix `QW38_PREFILL_4K_ORACLE_RESULT=`, `task` `OPT-021`
  - `tests/test_opt021_oracle.py` — host contract/fixture validator, mutation rejections, CUDA sitting (llama-bench 4096 first, then Quartz)
  - `pins/opt021_oracle_contract.json` — schema 1 protocol, scout transparency numbers, keep/reject rule
  - `fixtures/opt021_oracle.json` — retained live 4K comparison (`status` `measured`)
  - `evidence/optimization/opt021-4k-oracle/REPORT.md` — claim-labeled report from live JSON
  - `evidence/optimization/opt021-4k-oracle/llama-bench-4k.json` — live llama-bench JSON array
  - `evidence/optimization/opt021-4k-oracle/quartz-4k.json` — live Quartz diagnostic object
- Commands:
  - `uv run ruff format tests/test_opt021_oracle.py` — pass (1 file left unchanged)
  - `uv run ruff check tests/test_opt021_oracle.py` — pass
  - `uv run pytest -q tests/test_opt021_oracle.py` (host, before live overwrite) — 2 passed, 1 skipped
  - `QW38_RUN_CUDA_TESTS=1 uv run pytest -q tests/test_opt021_oracle.py -s` — 3 passed in 23.26s; exclusive RTX 5090 sitting; llama.cpp first (`test_time` 2026-09-09T00:55:32Z) then Quartz (`measurement_utc` 2026-09-09T00:55:40Z)
  - `uv run pytest -q tests/test_opt021_oracle.py` (host, against live fixture) — 2 passed, 1 skipped
- Live fixture produced: yes
  - Device: NVIDIA GeForce RTX 5090, compute 12.0
  - Quartz mean_tok_s: 966.039062 (walls 4236.88428, 4241.2334, 4241.86768 ms; tok/s 966.748169, 965.756775, 965.612366); prompt_tokens 4096; graphs_created true; prompt_graph_rows 4096; attribution null; cold; cache_policy disabled
  - llama.cpp avg_ts: 3224.522433; avg_ns 1270323993; n_prompt 4096; n_batch 2048; n_ubatch 512; flash_attn -1; build_commit cc83d7b
  - quartz_meets_llama: false (informational; native test did not fail)
  - Scout 965.204895 / 3182.476587 remain contract transparency only
- Blockers: none

### Documentation

- Agent/model: cursor-grok-4.6-high
- UTC/time/tokens/cost: telemetry_unavailable (Cursor session)
- Outcome: Linked the implementation-written 4K oracle report from handbook chapters 6, 61, and 62 without rewriting live numbers. Added a local-derivation sources entry (pinned llama.cpp `llama-bench` remains External; no new implementation source). Indexed chapters 6/61/62. Inserted a non-final OPT-021 audit row (`Measured; External; in progress`) whose body contains no other task IDs. Did not edit `plan.md`, `REPORT.md` live numbers, or mark the ledger done.
- Changes and evidence:
  - `docs/06-system-optimization.md` — Measured 4K keep/reject protocol/baseline; not a throughput gate versus llama.cpp; links report
  - `docs/61-benchmark-harness.md` — frozen 4K oracle versus BEN-001, versus the 2K yardstick, versus the 2026-09-08 scaling citation; scout sitting is not retained
  - `docs/62-cuda-full-prefill.md` — 4K oracle, graphs created/replayed at 4096, keep/reject baseline, proof boundary (not 2K parity)
  - `docs/sources.md` — local-derivation OPT-021 entry plus SCH-002 chain mention
  - `docs/README.md` — one-line updates for chapters 6/61/62
  - `docs/65-documentation-audit.md` — non-final OPT-021 row
- Commands:
  - `uv run pytest -q tests/test_opt021_oracle.py tests/test_documentation.py` — 6 passed, 1 skipped
- Blockers: none

### Verification

- Attempt: 1
- Agent/model: cursor-grok-4.6-high
- Diff review: complete uncommitted tree versus dossier, ledger acceptance, and `plan.md`. Tracked edits: handbook files (`docs/06-system-optimization.md`, `docs/61-benchmark-harness.md`, `docs/62-cuda-full-prefill.md`, `docs/65-documentation-audit.md`, `docs/README.md`, `docs/sources.md`) and `implementation_ledger.md` (`pending` → `in_progress` plus planning log). Untracked: `cuda/prefill_4k_oracle_test.cu`, `tests/test_opt021_oracle.py`, `pins/opt021_oracle_contract.json`, `fixtures/opt021_oracle.json`, `evidence/optimization/opt021-4k-oracle/` (`REPORT.md`, `llama-bench-4k.json`, `quartz-4k.json`), `tasks/OPT-021.md`. Scope matches the dossier allowlist plus ledger/dossier bookkeeping. `plan.md` hash `389bae52ef135b08d665c8fa6b7a89485d78b74b` equals `HEAD:plan.md`. Empty diffs for `Makefile`, `include/qw38/engine.h`, `src/engine.cpp`, `src/bench.cpp`, `pins/artifacts.lock.json`, `pins/opt016_parity_contract.json`, `fixtures/opt016_parity.json`, `tests/test_opt016_parity.py`, `cuda/prefill_2k_parity_test.cu`, and OPT-017/018/019/020 pins/fixtures/kernels. No Makefile target for the 4K diagnostic. Production `sync_tokens` call in the new diagnostic is `control=nullptr`, `graphs=&graphs`, default `GdnScanPath::kFusedTokenLoop`, default `PrefillAttribution*=nullptr`; `graphs.create` is before `cudaEventRecord`; `sync_tokens` production chunk path is `PromptPipelinePath::kFusedOverlapped`. Native test records `quartz_meets_llama` and checks consistency with `mean_tok_s >= avg_ts`; it does not require that bit to be true (no `assert gate_passed`). Audit row body contains only task-ID token `OPT-021` and is marked `in progress`. Scout `/tmp/oracle4k/llama-bench-4k.json` SHA prefix `019776c129502ac2` differs from retained `a9eca2a7d1cd2a0f`; scout `avg_ts` 3182.476587 / `test_time` 2026-09-09T00:42:27Z is not the retained object.
- Commands:
  - `uv run ruff format .` — pass (`94 files left unchanged`)
  - `uv run ruff check .` — pass (`All checks passed!`)
  - `uv run pytest -q tests/test_opt021_oracle.py tests/test_documentation.py` — pass (`6 passed, 1 skipped` in 0.05s); native CUDA skipped without `QW38_RUN_CUDA_TESTS=1`
  - `QW38_RUN_CUDA_TESTS=1 uv run pytest -q tests/test_opt021_oracle.py` — pass (`3 passed` in 23.24s) on exclusive RTX 5090 (idle 0% / 34 MiB, no compute apps at start); docker images `qw38-llama-authority:cuda-13.0.2` then `qw38-cuda:13.0.2`; llama-bench `-p 4096 -n 0 --no-warmup -r 3 -ngl 99 -o json` first (`test_time` 2026-09-09T01:03:41Z) then Quartz diagnostic (`measurement_utc` 2026-09-09T01:03:49Z). Native writer overwrote `fixtures/opt021_oracle.json`, `evidence/optimization/opt021-4k-oracle/REPORT.md`, `llama-bench-4k.json`, and `quartz-4k.json`.
  - Independent arithmetic on that retained fixture (not `status=measured` alone): `prompt_tokens` 4096; `replicates` 3; `wall_ms` 4227.67725, 4236.86816, 4239.29785; recorded `tok_s` 968.853516, 966.751831, 966.197754; arithmetic mean of `tok_s` 967.26770033 versus recorded `mean_tok_s` 967.267761 (float32); `attribution` null; `graphs_created` true; `prompt_graph_rows` 4096; `cold` true; `cache_policy` `disabled`. Live llama-bench JSON array independently parsed: `n_prompt` 4096, `n_gen` 0, `n_gpu_layers` 99, `n_batch` 2048, `n_ubatch` 512, `flash_attn` -1, `build_commit` cc83d7b, `avg_ts` 3243.626016, `avg_ns` 1262836272, three `samples_ns`, `gpu_info` NVIDIA GeForce RTX 5090. `quartz_meets_llama` recorded false equals `967.267761 >= 3243.626016` false. `owns_opt016_parity_gate` false; `substitutes_for_opt016` false; `nsight_*` `not_used`. GGUF SHA-256 `sha256sum` `31629f53165ab6a7dad8c9847dcfd1fdf55829dac1e6e748f4a68581b0033d34`. Scout means 965.204895 / 3182.476587 are not the live means. Report file contains every contract `proof_limit` phrase and does not contain `/tmp/oracle4k`.
  - Focused pytest rerun after fixture regen — pass (`6 passed, 1 skipped` in 0.05s)
  - `uv run pytest -q` — pass (`202 passed, 34 skipped` in 159.51s); CUDA natives skipped without `QW38_RUN_CUDA_TESTS=1`; OPT-016 native 2K gate not run
  - Independent live artifact (fixture not overwritten): `docker run --rm --gpus all --user "$(id -u):$(id -g)" -v /home/user/qw38:/workspace -w /workspace qw38-cuda:13.0.2 ./build/qw38-cuda-prefill-4k-oracle-test models/Qwen3.8-27B-Q4_K_M.gguf` — pass; one `QW38_PREFILL_4K_ORACLE_RESULT=` JSON line; no `QW38_PREFILL_2K_PARITY_RESULT`; `status=passed`; parsed `task` `OPT-021`, `prompt_tokens` 4096, `replicates` 3, `graphs_created` true, `prompt_graph_rows` 4096, `attribution` null, device NVIDIA GeForce RTX 5090 / 12.0. Retained fixture UTC remains 2026-09-09T01:03:49Z. No `nsys`/`ncu` invoked.
  - `git diff --check` — pass
- Formatting changed files: none
- Verdict: `pass`. Frozen cold exact-4096 protocol and same-sitting llama.cpp `llama-bench -p 4096 -n 0 --no-warmup -r 3 -ngl 99` are retained as the OPT-022+ keep/reject oracle. Native test does not require `quartz_meets_llama`. Scout `/tmp/oracle4k/` is not the fixture. Production kernels, `plan.md`, and Makefile `NVCCFLAGS` are unchanged. OPT-016 remains the blocked 2K parity owner. Ledger left `in_progress`; no commit or push.
- UTC/time/tokens/cost: telemetry_unavailable (Cursor session)

### Retries and escalation

none

### Delivery

- Agent/model: cursor-grok-4.6-high; telemetry_unavailable (Cursor session)
- UTC: 2026-09-09T01:11:06Z
- Scope: OPT-021 only; coupled IDs `none`. OPT-016 left `blocked`. QLT-001 left `blocked`.
- Delivery re-check: `uv run pytest -q tests/test_documentation.py tests/test_opt021_oracle.py` — pass (`6 passed, 1 skipped`)
- Ledger Gates: OPT-021 `done`. Final UTC History entry added. Audit row is final Measured; External (no other `XXX-NNN` tokens).

### Final outcome

- Status: `done`
- Acceptance evidence: verification Attempt 1 passed. Retained live 4K oracle is [`fixtures/opt021_oracle.json`](../fixtures/opt021_oracle.json) (`measurement_utc` 2026-09-09T01:03:49Z) plus [`evidence/optimization/opt021-4k-oracle/REPORT.md`](../evidence/optimization/opt021-4k-oracle/REPORT.md), [`llama-bench-4k.json`](../evidence/optimization/opt021-4k-oracle/llama-bench-4k.json), and [`quartz-4k.json`](../evidence/optimization/opt021-4k-oracle/quartz-4k.json). Quartz mean **967.267761** tok/s versus llama.cpp **3243.626016** tok/s; `quartz_meets_llama` false (informational). Native test does not require Quartz ≥ llama.cpp. Scout `/tmp/oracle4k/` is not the fixture. Ledger [`implementation_ledger.md`](../implementation_ledger.md); audit [`docs/65-documentation-audit.md`](../docs/65-documentation-audit.md).
- Commit: authorized delivery on `main`; hash recorded after the commit
- Push: ordinary push to configured `origin/main`; result recorded after push
- First-pass acceptance: yes (verification Attempt 1 `pass`)
- Total elapsed/tokens/cost: telemetry_unavailable (Cursor session)
- Remaining risk or recovery condition: handbook chapters 6 and 62 still quote the earlier implementation-stage sample (Quartz 966.039062 / llama 3224.522433 tok/s) rather than the verifier-regenerated fixture numbers; that copy is not a host assertion. Do not claim Quartz ≥ llama.cpp. Do not treat this oracle as the 2K llama.cpp parity gate. Do not edit `plan.md`.
- Agent/model for delivery: cursor-grok-4.6-high; telemetry_unavailable (Cursor session)
