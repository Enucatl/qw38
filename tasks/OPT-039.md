# OPT-039 — Warp-owned vector decode attention

## Control

- Primary ID: OPT-039
- Coupled IDs: none
- Dependencies: OPT-038 (explicit prerequisite), OPT-036 done
- Status: pending
- Ledger acceptance: A/B warp-owned query-head attention against accepted 16-partition decode attention; keep only with frozen attention envelopes, exact candidate KV and state isolation, lower D2048 component time, improved D2048, 95% throughput floors, and 105% p95 ceilings; otherwise reject.

## Goal and boundaries

Replace only the serial QK fold in one-token decode. Keep 16 contiguous KV
partitions, existing BF16 KV, normalized-query scratch, partial-statistic layout,
ascending FP32 merge, state commit, public APIs, allocations, and all frozen
ATN-001 limits. No partition sweep, prompt change, KV format change, fast math,
or tolerance relaxation. Classification: CUDA/numeric.

## Repository evidence

- `cuda/attention_decode.cu:693-850` — current CTA-wide scratch and serial fold.
- `cuda/attention_decode.cu:907-1000` — retained tiled attention reference.
- `cuda/decode_kv_partition_ab_test.cu` — current production A/B scaffold.
- `.../llama.cpp/.../fattn-vec.cuh` — register Q/KQ/VKQ and warp reductions.

## Implementation decisions

Candidate `warp_query` uses grid `(24,16)`, block `(32)`. One warp owns one
query-head/partition pair; each lane owns eight dimensions. Q is normalized once
with the existing ordered FP32 reduction. Each KV row performs lane-local eight
products, a `__shfl_down_sync` reduction, online FP32 max/denominator/value
update, and register accumulators. No CTA barrier occurs in the row loop.
Write existing `partial_vkq`/`meta`; invoke the unchanged merge. Empty parts
emit `-inf,0,0`. Keep baseline branch for A/B and all non-production shapes.

## Acceptance and validation

Use P=1869.84412, D128=25.3816128, D2048=20.169548 and p95s from
`fixtures/opt034_packed_mmv.json` until a later keep moves them. Component A/B:
3 warmups, 30 alternating CUDA-event pairs at positions 128 and 2048, complete
prepare+attention+merge, plus correctness positions 0,1,15,16,31,32,127,128,
2047,2048,2303,131071. Require max abs 5e-5, RMS 5e-6, zero nonfinites, exact
candidate KV and state isolation. End-to-end uses unchanged OPT-021/032 runs;
strict D2048 improvement, P/D128/D2048 >=95%, both D p95s <=105%. Focused:
`uv run pytest -q tests/test_opt039_decode_warp_attention.py tests/test_documentation.py`;
hardware adds `QW38_RUN_CUDA_TESTS=1`. Rebuild pinned CUDA consumers; update
attention handbook/audit/source docs. Keep or retain REJECTION.md.

## Run record

Planning proposed 2026-09-10; implementation and verification not run.
