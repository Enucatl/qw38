# OPT-038 matched-component experiment specifications

These blocks specify later A/B experiments. OPT-038 does **not** run them.
Nsight Systems and Nsight Compute are not used. Kernel-level llama.cpp
traces that cannot be recovered from pinned public sources are labeled
`unavailable`. `llama_perf_context` is not a component stopwatch. ds4 D2R
is rejected and is not reopened.

Pinned llama.cpp revision: `cc83d7b4824f73cfdda4dfbb47ee39804f71b328`.

## Shared unavailable facts

- unavailable: nsys/ncu kernel traces
- unavailable: llama.cpp GLU fused-epilogue decode dispatch is not confirmed
  from pinned `mmvq.cu` graph construction for Qwen3.5 decode FFN at sitting
  time (`qwen35.cpp` does not select a fused GLU epilogue; `mmvq.cu`
  contains optional GLU fusion code that this sitting does not prove is
  dispatched)
- unavailable: ds4 D2R (rejected; do not reopen)
- unavailable: `llama_perf_context` as a component stopwatch

## q4k_ffn_decode_mmv

- quartz_entry: `cuda/full_scheduler.cu` `execute_ffn` three `matrix_vector`
  launches (gate, up, down) plus SwiGLU; packed OPT-034 path in
  `cuda/quant_mmv.cu`
- llama_entry: `.cache/authorities/llama.cpp/ggml/src/ggml-cuda/mmvq.cu` and
  `vecdotq.cuh` Q4_K integer dots (`vec_dot_q4_K_q8_1`)
- real_inputs: production batch-1 FFN gate/up `17408×5120` and down
  `5120×17408` plus CUD-001 synthetic shapes; activations captured from one
  D2048 decode token or generated by the later diagnostic
- envelope: CUD-001 fixed limits; Q4_K association `abs > 0.20*sqrt(K)`
  **and** `rel > 0.05`
- unavailable: nsys/ncu kernel traces; llama.cpp GLU fused-epilogue decode
  dispatch; ds4 D2R; `llama_perf_context` as a component stopwatch
- maps_to: OPT-042

## q8_0_mixer_decode_mmv

- quartz_entry: mixer-block `matrix_vector` for GDN packed_qkv / value_gate /
  alpha / beta / output and attention query_gate / key / value / output
- llama_entry: `mmvq.cu` Q8_0 vec-dot (`vec_dot_q8_0_q8_1`)
- real_inputs: production mixer rows from the admitted GGUF on one D2048
  token
- envelope: Q8 association `abs > 0.05*sqrt(K)` **and** `rel > 0.05`;
  OPT-009 byte-exact Q8 reference stays visible
- unavailable: nsys/ncu kernel traces; ds4 D2R; `llama_perf_context` as a
  component stopwatch
- maps_to: OPT-042 (informational mixer sibling; no separate ledger ID)

## q6k_output_mmv

- quartz_entry: logits vocabulary `matrix_vector` on `output.weight`
- llama_entry: `mmvq.cu` / `vecdotq.cuh` Q6_K (`vec_dot_q6_K_q8_1`)
- real_inputs: production `248320×5120` Q6_K row on one decode token
- envelope: CUD-001 Q6_K limits; association `abs > 0.20*sqrt(K)` **and**
  `rel > 0.05`
- unavailable: nsys/ncu kernel traces; ds4 D2R; `llama_perf_context` as a
  component stopwatch
- maps_to: OPT-042 (logits sibling)

## decode_attention_vec

- quartz_entry: `partitioned_grouped_decode_attention` +
  `merge_decode_kv_parts` at accepted 16/16 partitions
  (`cuda/attention_decode.cu`)
- llama_entry: `fattn-vec.cuh` warp-owned Q/KQ/VKQ
- real_inputs: production one-token Q/K/V at prefixes 128 and 2048
- envelope: frozen attention envelopes max-abs `5e-5` / RMS `5e-6`;
  SES-002 candidate/committed isolation
- unavailable: nsys/ncu kernel traces; ds4 D2R; `llama_perf_context` as a
  component stopwatch
- maps_to: OPT-039

## prefill_qk_microtiles

- quartz_entry: `fattn_mma_f16.cuh` shared `cparts` reduction across warps
- llama_entry: `fattn-mma-f16.cuh` warp-owned KQ MMA fragments
- real_inputs: production 4096-row Q/K, dual-F16 Q, register VKQ, P×V MMA
  retained
- envelope: frozen attention envelopes; byte-equal quality outputs versus
  current production
- unavailable: nsys/ncu kernel traces; ds4 D2R; `llama_perf_context` as a
  component stopwatch
- maps_to: OPT-041

## prompt_gdn_inverse_norm

- quartz_entry: `prepare_recurrence_fused_warp_column` per-warp Q/K inverse
  (`cuda/gdn_fused_quality.cuh`)
- llama_entry: `src/models/qwen35.cpp` pre-loop Q/K norms (`ggml_l2_norm` on
  `q_conv` / `k_conv` before the recurrent operator);
  `gated_delta_net.cu` register-held state loop
- real_inputs: production 4096-row GDN `GdnConfig{16, 48, 128, 128, 4}`
- envelope: GDN max-abs `5e-8` / RMS `5e-9`; sequential windows remain the
  reference; not OPT-029 fusion
- unavailable: nsys/ncu kernel traces; ds4 D2R; `llama_perf_context` as a
  component stopwatch
- maps_to: OPT-040
