[project]
name = "qw38-tests"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "pytest==8.4.2",
  "ruff==0.12.11",
  "tokenizers==0.22.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88
