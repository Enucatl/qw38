interface:
  display_name: "Run Ledger Task (Codex)"
  short_description: "Execute one ledger task through gated agents"
  default_prompt: "Use $run-ledger-task-codex to execute the next eligible ledger task through verification and delivery."
