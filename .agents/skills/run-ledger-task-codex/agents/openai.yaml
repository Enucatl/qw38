interface:
  display_name: "Run Ledger Task (Codex)"
  short_description: "Execute one ledger task through gated agents"
  default_prompt: "Use $run-ledger-task-codex to execute the next eligible task in docs/architecture/task_ledger.md through verification, one commit, and a successful push."
